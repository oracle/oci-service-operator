/*
  Copyright (c) 2021, Oracle and/or its affiliates. All rights reserved.
  Licensed under the Universal Permissive License v 1.0 as shown at http://oss.oracle.com/licenses/upl.
*/

// Code generated by generator. DO NOT EDIT.

package v1beta1

import (
	shared "github.com/oracle/oci-service-operator/pkg/shared"
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
)

// SecurityRuleSpec defines the desired state of SecurityRule.
type SecurityRuleSpec struct {
	// Name for the Security rule, must be unique within the policy.
	// +kubebuilder:validation:Required
	Name string `json:"name"`
	// +kubebuilder:validation:Required
	Condition SecurityRuleCondition `json:"condition"`
	// Types of Action on the Traffic flow.
	//   * ALLOW - Allows the traffic.
	//   * DROP - Silently drops the traffic, e.g. without sending a TCP reset.
	//   * REJECT - Rejects the traffic, sending a TCP reset to client and/or server as applicable.
	//   * INSPECT - Inspects traffic for vulnerability as specified in `inspection`, which may result in rejection.
	// +kubebuilder:validation:Required
	Action string `json:"action"`
	// Type of inspection to affect the Traffic flow. This is only applicable if action is INSPECT.
	//   * INTRUSION_DETECTION - Intrusion Detection.
	//   * INTRUSION_PREVENTION - Intrusion Detection and Prevention. Traffic classified as potentially malicious will be rejected as described in `type`.
	// +kubebuilder:validation:Optional
	Inspection string `json:"inspection,omitempty"`
	// +kubebuilder:validation:Optional
	Position SecurityRulePosition `json:"position,omitempty"`
	// The description of the security rule. This field can be used to add additional info.
	// +kubebuilder:validation:Optional
	Description string `json:"description,omitempty"`
}

// SecurityRuleCondition defines nested fields for SecurityRule.Condition.
type SecurityRuleCondition struct {
	// An array of IP address list names to be evaluated against the traffic source address.
	// +kubebuilder:validation:Optional
	SourceAddress []string `json:"sourceAddress,omitempty"`
	// An array of IP address list names to be evaluated against the traffic destination address.
	// +kubebuilder:validation:Optional
	DestinationAddress []string `json:"destinationAddress,omitempty"`
	// An array of application list names to be evaluated against the traffic protocol and protocol-specific parameters.
	// +kubebuilder:validation:Optional
	Application []string `json:"application,omitempty"`
	// An array of service list names to be evaluated against the traffic protocol and protocol-specific parameters.
	// +kubebuilder:validation:Optional
	Service []string `json:"service,omitempty"`
	// An array of URL pattern list names to be evaluated against the HTTP(S) request target.
	// +kubebuilder:validation:Optional
	Url []string `json:"url,omitempty"`
}

// SecurityRulePosition defines nested fields for SecurityRule.Position.
type SecurityRulePosition struct {
	// Identifier for rule before which this rule lies.
	// +kubebuilder:validation:Optional
	BeforeRule string `json:"beforeRule,omitempty"`
	// Identifier for rule after which this rule lies.
	// +kubebuilder:validation:Optional
	AfterRule string `json:"afterRule,omitempty"`
}

// SecurityRuleStatus defines the observed state of SecurityRule.
type SecurityRuleStatus struct {
	OsokStatus shared.OSOKStatus `json:"status"`
	// Name for the Security rule, must be unique within the policy.
	Name      string                `json:"name,omitempty"`
	Condition SecurityRuleCondition `json:"condition,omitempty"`
	// Types of Action on the Traffic flow.
	//   * ALLOW - Allows the traffic.
	//   * DROP - Silently drops the traffic, e.g. without sending a TCP reset.
	//   * REJECT - Rejects the traffic, sending a TCP reset to client and/or server as applicable.
	//   * INSPECT - Inspects traffic for vulnerability as specified in `inspection`, which may result in rejection.
	Action string `json:"action,omitempty"`
	// OCID of the Network Firewall Policy this security rule belongs to.
	ParentResourceId string `json:"parentResourceId,omitempty"`
	// Type of inspection to affect the Traffic flow. This is only applicable if action is INSPECT.
	//   * INTRUSION_DETECTION - Intrusion Detection.
	//   * INTRUSION_PREVENTION - Intrusion Detection and Prevention. Traffic classified as potentially malicious will be rejected as described in `type`.
	Inspection string               `json:"inspection,omitempty"`
	Position   SecurityRulePosition `json:"position,omitempty"`
	// The description of the security rule. This field can be used to add additional info.
	Description string `json:"description,omitempty"`
	// The priority order in which this rule should be evaluated.
	PriorityOrder int64 `json:"priorityOrder,omitempty"`
}

// +kubebuilder:object:root=true
// +kubebuilder:subresource:status
// +kubebuilder:printcolumn:name="Name",type="string",JSONPath=".spec.name",priority=1
// +kubebuilder:printcolumn:name="Status",type="string",JSONPath=".status.status.conditions[-1].type",description="status of the SecurityRule",priority=0
// +kubebuilder:printcolumn:name="Ocid",type="string",JSONPath=".status.status.ocid",description="Ocid of the SecurityRule",priority=1
// +kubebuilder:printcolumn:name="Age",type="date",JSONPath=".metadata.creationTimestamp",priority=0
// SecurityRule is the Schema for the securityrules API.
type SecurityRule struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   SecurityRuleSpec   `json:"spec,omitempty"`
	Status SecurityRuleStatus `json:"status,omitempty"`
}

// +kubebuilder:object:root=true
// SecurityRuleList contains a list of SecurityRule.
type SecurityRuleList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []SecurityRule `json:"items"`
}

func init() {
	SchemeBuilder.Register(&SecurityRule{}, &SecurityRuleList{})
}
