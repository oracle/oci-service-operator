schemaVersion: 1
surface: repo-authored-semantics
service: filestorage
slug: outboundconnector
kind: OutboundConnector
archetype: generated-service-manager
states:
- provisioning
- active
- updating
- terminating
repoAuthored:
  operations:
    create:
    - CreateOutboundConnector
    update:
    - UpdateOutboundConnector
    delete:
    - DeleteOutboundConnector
  providerLifecycle:
    createPending:
    - CREATING
    updatePending: []
    deletePending:
    - DELETING
  listLookup:
    filters:
    - availabilityDomain
    - compartmentId
    - displayName
    - id
    - state
    matchRule: exact declared filters match within the resource scope; duplicate matches
      fail
  mutation:
    mutable:
    - compartmentId
    - definedTags
    - displayName
    - freeformTags
    - isLockOverride
    forceNew:
    - availabilityDomain
    - bindDistinguishedName
    - connectorType
    - endpoints
    - endpoints.hostname
    - endpoints.port
    - locks
    - locks.message
    - locks.relatedResourceId
    - locks.timeCreated
    - locks.type
    - passwordSecretId
    - passwordSecretVersion
  hooks:
    create:
    - helper: tfresource.CreateResource
    update:
    - helper: tfresource.UpdateResource
    - helper: tfresource.WaitForUpdatedState
    delete:
    - helper: tfresource.DeleteResource
  followUp:
    create: read-after-write
    update: read-after-write
    delete: confirm-delete
notes:
- The generated service manager uses the reviewed CRUD request, response, lifecycle,
  and status projection contract.
- CRUD responses use lifecycle/read-after-write convergence without a work-request
  tracker.
- Secret side effects are none and finalizer ownership is retained until confirmed
  deletion.
