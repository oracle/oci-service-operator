@startuml
skinparam shadowing false
skinparam backgroundColor #fcfcfd
skinparam defaultFontName Helvetica
skinparam defaultFontSize 14
skinparam arrowColor #0f172a
skinparam noteBackgroundColor #fff7d6
skinparam noteBorderColor #b45309
skinparam noteFontColor #334155
skinparam roundcorner 16
title Activity - dataintegration/DisApplicationDetailedDescription
skinparam activity {
  backgroundColor #f8fafc
  borderColor #0f172a
  fontColor #0f172a
  diamondBackgroundColor #e2e8f0
  diamondBorderColor #1d4ed8
  diamondFontColor #0f172a
  startColor #1d4ed8
  endColor #0f172a
  barColor #1d4ed8
}
start
partition "Observe and Bind" {
:Start from shared flow\nshared/diagrams/shared-reconcile-activity.svg;
:Read desired DisApplicationDetailedDescription\nspec, tracked status, and delete intent;
if ("Tracked or explicit OCI identity present?") then (yes)
:Get the current OCI resource via\nGetDisApplicationDetailedDescription;
else (no)
:Resolve an existing OCI resource through\nListDisApplicationDetailedDescription using\nfilters compartment_id, state;
:Create the OCI resource via\nCreateDisApplicationDetailedDescription when\nno reusable match is found;
:Persist the resolved or created OCI identity\ninto status;
endif
}
if ("Delete requested?") then (yes)
partition "Delete" {
:Delete path uses\nDeleteDisApplicationDetailedDescription with\nrequired via\nGetDisApplicationDetailedDescription or\nListDisApplicationDetailedDescription to\nDELETED confirmation;
:Confirm deletion via\nGetDisApplicationDetailedDescription or\nListDisApplicationDetailedDescription until\nOCI reaches DELETED;
if ("Delete cleanup and finalizer release succeed?") then (yes)
:Remove the finalizer after delete completion;
else (no)
:Stay blocked until cleanup or finalizer\nrelease succeeds;
endif
}
stop
else (no)
partition "Lifecycle Classification" {
if ("OCI state in retryable set?") then (yes)
:Request requeue while OCI remains in\nPROVISIONING, UPDATING, DELETING;
stop
endif
if ("OCI state is outside success targets?") then (yes)
:Return an unsuccessful reconcile result until\nOCI reaches ACTIVE;
stop
endif
}
partition "Ready and Drift Handling" {
:Compare live OCI state against the\nrepo-authored mutation surface;
if ("Force-new or conflicting drift detected?") then (yes)
:Reject drift before OCI mutation for force-new\nfields workspaceId, applicationKey;
stop
endif
if ("Supported mutable drift detected?") then (yes)
:Apply UpdateDisApplicationDetailedDescription\nonly for mutable fields logo,\ndetailedDescription;
else (no)
:Skip the no-op mutation path;
endif
:Run provider helper hooks\ncreate=tfresource.CreateResource,\ntfresource.WaitForWorkRequestWithErrorHandling\nentity=template action=CREATED;\nupdate=tfresource.UpdateResource;\ndelete=tfresource.DeleteResource when the\nmatching field-driven path executes;
:Apply explicit follow-up semantics\ncreate=read-after-write;\nupdate=read-after-write; delete=confirm-delete;
:Return success for the ACTIVE success state;
:Project required status and finalizer policy\nretain-until-confirmed-delete;
:Requeue on terminating;
}
endif
stop
note right
shared diagram:
shared/diagrams/shared-reconcile-activity.svg
Start from the shared reconcile activity and
specialize provider-backed CRUD, wait, and
status-projection details here.
Controller-local activity diagrams add
list-lookup hints, secret policies, delete
handling, and open formal gaps.
archetype batch: Generated service-manager
controllers specialize the shared reconcile path
with formal runtime ownership.
service: dataintegration
slug: disapplicationdetaileddescription
kind: DisApplicationDetailedDescription
archetype: generated-service-manager
status/finalizer: required /
retain-until-confirmed-delete
requeue: terminating
provider states: create PROVISIONING; update
UPDATING; delete DELETING
follow-up: create=read-after-write;
update=read-after-write; delete=confirm-delete
supported drift: logo, detailedDescription
reject before mutate: force-new fields
workspaceId, applicationKey
create-only fields: none
list lookup:
scaffold_dataintegration_disapplicationdetaileddescription
filters compartment_id, state
conflicts: none
open gaps: none
The vendored SDK response has no lifecycle state
or required work-request flow; successful writes
settle from the response and configured
follow-up.
Parent identifiers scope composite OCI request
paths and remain replacement-only unless the SDK
update details explicitly expose them.
Create, get, list where available, update, and
delete request shapes are exercised by the
package-local typed OCI mock integration test.
end note
@enduml
