@startuml
skinparam shadowing false
skinparam backgroundColor #fcfcfd
skinparam defaultFontName Helvetica
skinparam defaultFontSize 14
skinparam arrowColor #0f172a
skinparam noteBackgroundColor #fff7d6
skinparam noteBorderColor #b45309
skinparam noteFontColor #334155
skinparam roundcorner 16
title Activity - opsi/OperationsInsightsWarehouse
skinparam activity {
  backgroundColor #f8fafc
  borderColor #0f172a
  fontColor #0f172a
  diamondBackgroundColor #e2e8f0
  diamondBorderColor #1d4ed8
  diamondFontColor #0f172a
  startColor #1d4ed8
  endColor #0f172a
  barColor #1d4ed8
}
start
partition "Observe and Bind" {
:Start from shared flow\nshared/diagrams/shared-reconcile-activity.svg;
:Read desired OperationsInsightsWarehouse spec,\ntracked status, and delete intent;
if ("Tracked or explicit OCI identity present?") then (yes)
:Get the current OCI resource via\nGetOperationsInsightsWarehouse;
else (no)
:Resolve an existing OCI resource through\nListOperationsInsightsWarehouses using filters\ncompartmentId, displayName, id;
:Create the OCI resource via\nCreateOperationsInsightsWarehouse when no\nreusable match is found;
:Persist the resolved or created OCI identity\ninto status;
endif
}
if ("Delete requested?") then (yes)
partition "Delete" {
:Delete path uses\nDeleteOperationsInsightsWarehouse with\nrequired via GetOperationsInsightsWarehouse or\nListOperationsInsightsWarehouses to DELETED\nconfirmation;
:Confirm deletion via\nGetOperationsInsightsWarehouse or\nListOperationsInsightsWarehouses until OCI\nreaches DELETED;
if ("Delete cleanup and finalizer release succeed?") then (yes)
:Remove the finalizer after delete completion;
else (no)
:Stay blocked until cleanup or finalizer\nrelease succeeds;
endif
}
stop
else (no)
partition "Lifecycle Classification" {
if ("OCI state in retryable set?") then (yes)
:Request requeue while OCI remains in CREATING,\nUPDATING, DELETING;
stop
endif
if ("OCI state is outside success targets?") then (yes)
:Return an unsuccessful reconcile result until\nOCI reaches ACTIVE;
stop
endif
}
partition "Ready and Drift Handling" {
:Compare live OCI state against the\nrepo-authored mutation surface;
if ("Force-new or conflicting drift detected?") then (yes)
:Reject drift before OCI mutation for force-new\nfields compartmentId;
stop
endif
if ("Supported mutable drift detected?") then (yes)
:Apply UpdateOperationsInsightsWarehouse only\nfor mutable fields displayName, cpuAllocated,\ncomputeModel, storageAllocatedInGBs (+2 more);
else (no)
:Skip the no-op mutation path;
endif
:Run provider helper hooks\ncreate=tfresource.CreateResource,\ntfresource.WaitForWorkRequestWithErrorHandling\nentity=operationsInsightsWarehouse\naction=CREATE_OPSI_WAREHOUSE;\nupdate=tfresource.UpdateResource,\ntfresource.WaitForWorkRequestWithErrorHandling\nentity=operationsInsightsWarehouse\naction=UPDATE_OPSI_WAREHOUSE;\ndelete=tfresource.DeleteResource,\ntfresource.WaitForWorkRequestWithErrorHandling\nentity=operationsInsightsWarehouse\naction=DELETE_OPSI_WAREHOUSE when the matching\nfield-driven path executes;
:Apply explicit follow-up semantics\ncreate=GetWorkRequest ->\nGetOperationsInsightsWarehouse;\nupdate=GetWorkRequest ->\nGetOperationsInsightsWarehouse;\ndelete=confirm-delete;
:Return success for the ACTIVE success state;
:Project required status and finalizer policy\nretain-until-confirmed-delete;
:Requeue on provisioning, updating, terminating;
}
endif
stop
note right
shared diagram:
shared/diagrams/shared-reconcile-activity.svg
Start from the shared reconcile activity and
specialize provider-backed CRUD, wait, and
status-projection details here.
Controller-local activity diagrams add
list-lookup hints, secret policies, delete
handling, and open formal gaps.
archetype batch: Generated service-manager
controllers specialize the shared reconcile path
with formal runtime ownership.
service: opsi
slug: operationsinsightswarehouse
kind: OperationsInsightsWarehouse
archetype: generated-service-manager
status/finalizer: required /
retain-until-confirmed-delete
requeue: provisioning, updating, terminating
provider states: create CREATING; update
UPDATING; delete DELETING
follow-up: create=GetWorkRequest ->
GetOperationsInsightsWarehouse;
update=GetWorkRequest ->
GetOperationsInsightsWarehouse;
delete=confirm-delete
supported drift: displayName, cpuAllocated,
computeModel, storageAllocatedInGBs (+2 more)
reject before mutate: force-new fields
compartmentId
create-only fields: none
list lookup:
oci_opsi_operations_insights_warehouses filters
compartmentId, displayName, id; exact
compartmentId, displayName, id match within the
resource scope; duplicate matches fail
conflicts: none
open gaps: none
The production runtime uses the declared
work-request phases and service-local async
adapter.
Lifecycle success and pending states mirror the
production service-manager semantics.
Status projection is required, Secret side
effects are none, and the finalizer policy is
retain-until-confirmed-delete.
Delete ownership is retained until a terminal
lifecycle state or confirmed absence.
end note
@enduml
