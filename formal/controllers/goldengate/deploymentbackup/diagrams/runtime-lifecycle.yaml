schemaVersion: 1
surface: repo-authored-semantics
service: goldengate
slug: deploymentbackup
kind: DeploymentBackup
archetype: generated-service-manager
states:
- provisioning
- active
- updating
- terminating
repoAuthored:
  operations:
    create:
    - CreateDeploymentBackup
    update:
    - UpdateDeploymentBackup
    delete:
    - DeleteDeploymentBackup
  providerLifecycle:
    createPending:
    - CREATING
    updatePending:
    - UPDATING
    deletePending:
    - DELETING
  listLookup:
    filters:
    - compartmentId
    - deploymentId
    - lifecycleState
    - displayName
    - opc-request-id
    matchRule: exact declared filters match within the resource scope; duplicate matches
      fail
  mutation:
    mutable:
    - freeformTags
    - definedTags
    forceNew:
    - displayName
    - compartmentId
    - deploymentId
    - namespaceName
    - bucketName
    - objectName
    - isMetadataOnly
    - locks
    - jsonData
    - type
  hooks:
    create:
    - helper: tfresource.CreateResource
    - helper: tfresource.WaitForWorkRequestWithErrorHandling
      entityType: DeploymentBackup
      action: CreateDeploymentBackup
    update:
    - helper: tfresource.UpdateResource
    delete:
    - helper: tfresource.DeleteResource
    - helper: tfresource.WaitForWorkRequestWithErrorHandling
      entityType: DeploymentBackup
      action: DeleteDeploymentBackup
  followUp:
    create: GetWorkRequest -> read-after-write
    update: read-after-write
    delete: GetWorkRequest -> confirm-delete
notes:
- The generated service manager uses the reviewed CRUD request, response, lifecycle,
  and status projection contract.
- Work-request phases are declared per operation from the vendored SDK response surface.
- Secret side effects are none and finalizer ownership is retained until confirmed
  deletion.
