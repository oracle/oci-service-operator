schemaVersion: 1
surface: repo-authored-semantics
service: dbmulticloud
slug: oracledbawsidentityconnector
kind: OracleDbAwsIdentityConnector
archetype: generated-service-manager
states:
- provisioning
- active
- updating
- terminating
repoAuthored:
  operations:
    create:
    - CreateOracleDbAwsIdentityConnector
    update:
    - UpdateOracleDbAwsIdentityConnector
    delete:
    - DeleteOracleDbAwsIdentityConnector
  providerLifecycle:
    createPending:
    - CREATING
    updatePending:
    - UPDATING
    deletePending:
    - DELETING
  listLookup:
    filters:
    - compartmentId
    - displayName
    - lifecycleState
    - resourceId
    - opc-request-id
    matchRule: exact declared filters match within the resource scope; duplicate matches
      fail
  mutation:
    mutable:
    - displayName
    - resourceId
    - oidcScope
    - issuerUrl
    - serviceRoleDetails
    - awsLocation
    - awsStsPrivateEndpoint
    - awsAccountId
    - freeformTags
    - definedTags
    forceNew:
    - compartmentId
  hooks:
    create:
    - helper: tfresource.CreateResource
    - helper: tfresource.WaitForWorkRequestWithErrorHandling
      entityType: OracleDbAwsIdentityConnector
      action: CreateOracleDbAwsIdentityConnector
    update:
    - helper: tfresource.UpdateResource
    - helper: tfresource.WaitForWorkRequestWithErrorHandling
      entityType: OracleDbAwsIdentityConnector
      action: UpdateOracleDbAwsIdentityConnector
    delete:
    - helper: tfresource.DeleteResource
    - helper: tfresource.WaitForWorkRequestWithErrorHandling
      entityType: OracleDbAwsIdentityConnector
      action: DeleteOracleDbAwsIdentityConnector
  followUp:
    create: GetWorkRequest -> read-after-write
    update: GetWorkRequest -> read-after-write
    delete: GetWorkRequest -> confirm-delete
notes:
- The generated service manager uses the reviewed CRUD request, response, lifecycle,
  and status projection contract.
- Work-request phases are declared per operation from the vendored SDK response surface.
- Secret side effects are none and finalizer ownership is retained until confirmed
  deletion.
