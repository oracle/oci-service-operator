@startuml
skinparam shadowing false
skinparam backgroundColor #fcfcfd
skinparam defaultFontName Helvetica
skinparam defaultFontSize 14
skinparam arrowColor #0f172a
skinparam noteBackgroundColor #fff7d6
skinparam noteBorderColor #b45309
skinparam noteFontColor #334155
skinparam roundcorner 16
title Sequence - wlms/ManagedInstance
hide footbox
skinparam sequence {
  actorBorderColor #0f172a
  actorBackgroundColor #e2e8f0
  participantBorderColor #0f172a
  participantBackgroundColor #e2e8f0
  participantFontColor #0f172a
  lifeLineBorderColor #94a3b8
  groupBorderColor #cbd5e1
  groupBackgroundColor #f8fafc
}
actor Kubernetes
participant Controller
participant BaseReconciler
participant "OSOKServiceManager" as ServiceManager
participant OCI
note over BaseReconciler,ServiceManager
shared resolution:
shared/diagrams/shared-resolution-sequence.svg
shared delete:
shared/diagrams/shared-delete-sequence.svg
end note
Kubernetes -> Controller: enqueue ManagedInstance reconcile
Controller -> BaseReconciler: Reconcile(request)
BaseReconciler -> ServiceManager: reconcile desired ManagedInstance
group Lookup and bind
alt tracked or explicit OCI identity already exists
ServiceManager -> OCI: GetManagedInstance
else tracked identity is missing
ServiceManager -> OCI: ListManagedInstances(filters:\ncompartmentId, displayName, id,\npluginStatus)
ServiceManager --> BaseReconciler: persist resolved OCI identity in status
end
alt delete requested
group Delete
ServiceManager -> OCI: none
ServiceManager -> OCI: confirm delete via\nGetManagedInstance or\nListManagedInstances
ServiceManager --> BaseReconciler: best-effort delete completion observed
end
else live state requires field-aware\ndrift evaluation
group Drift handling
note over ServiceManager,OCI
Supported update surface: configuration
Reject before mutate: force-new fields
compartmentId, displayName, id,
pluginStatus
Lookup filters: compartmentId,
displayName, id, pluginStatus
Lookup matching: exact id or exact
compartmentId plus displayName plus
optional pluginStatus unique match;
duplicate matches fail
end note
opt force-new or conflicting drift is detected
ServiceManager --> BaseReconciler: reject before OCI mutation
end
opt supported mutable drift is detected
ServiceManager -> OCI: Apply UpdateManagedInstance only for\nmutable fields configuration
end
ServiceManager -> OCI: run helper hooks\nupdate=tfresource.UpdateResource
ServiceManager -> OCI: apply follow-up strategy\ncreate=none; update=next reconcile\nGetManagedInstance confirmation;\ndelete=none
end
ServiceManager --> BaseReconciler: status=required, finalizer=none
end
BaseReconciler --> Controller: requeue on updating
note right of ServiceManager
shared diagram:
shared/diagrams/shared-resolution-sequence.svg
shared delete diagram:
shared/diagrams/shared-delete-sequence.svg
Start from the shared resolution sequence for
OCID binding, datasource pagination, and
create-or-update dispatch.
Layer the shared delete sequence when finalizer
retention, delete confirmation, work-request
polling, or Secret cleanup apply.
archetype batch: Generated service-manager
controllers follow the shared resolution and
delete sequences with imported waits and
controller-local policy.
service: wlms
slug: managedinstance
kind: ManagedInstance
archetype: generated-service-manager
status/finalizer: required / none
requeue: updating
provider states: create none; update none;
delete none
follow-up: create=none; update=next reconcile
GetManagedInstance confirmation; delete=none
supported drift: configuration
reject before mutate: force-new fields
compartmentId, displayName, id, pluginStatus
create-only fields: none
list lookup: oci_wlms_managed_instances filters
compartmentId, displayName, id, pluginStatus;
exact id or exact compartmentId plus displayName
plus optional pluginStatus unique match;
duplicate matches fail
conflicts: none
open gaps: none
WLMS does not expose CreateManagedInstance or
DeleteManagedInstance in v65.110.0, so the
published runtime is bind/manage-existing only.
Successful UpdateManagedInstance responses
already return a ManagedInstance body. The
runtime uses that body plus one bounded
GetManagedInstance confirmation pass on the next
reconcile instead of inventing lifecycle or
work-request polling.
Delete is Kubernetes-local unbind only because
the SDK exposes no delete helper and no
cloud-side delete lifecycle for this family.
Plugin status remains a projected observed-state
field and optional bind filter, not a
controller-owned async lifecycle.
end note
@enduml
