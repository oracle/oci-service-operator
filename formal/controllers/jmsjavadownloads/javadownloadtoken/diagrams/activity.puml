@startuml
skinparam shadowing false
skinparam backgroundColor #fcfcfd
skinparam defaultFontName Helvetica
skinparam defaultFontSize 14
skinparam arrowColor #0f172a
skinparam noteBackgroundColor #fff7d6
skinparam noteBorderColor #b45309
skinparam noteFontColor #334155
skinparam roundcorner 16
title Activity - jmsjavadownloads/JavaDownloadToken
skinparam activity {
  backgroundColor #f8fafc
  borderColor #0f172a
  fontColor #0f172a
  diamondBackgroundColor #e2e8f0
  diamondBorderColor #1d4ed8
  diamondFontColor #0f172a
  startColor #1d4ed8
  endColor #0f172a
  barColor #1d4ed8
}
start
partition "Observe and Bind" {
:Start from shared flow\nshared/diagrams/shared-reconcile-activity.svg;
:Read desired JavaDownloadToken spec, tracked\nstatus, and delete intent;
if ("Tracked or explicit OCI identity present?") then (yes)
:Get the current OCI resource via\nGetJavaDownloadToken;
else (no)
:Resolve an existing OCI resource through\nListJavaDownloadTokens using filters\ncompartmentId, displayName, id;
:Create the OCI resource via\nCreateJavaDownloadToken when no reusable match\nis found;
:Persist the resolved or created OCI identity\ninto status;
endif
}
if ("Delete requested?") then (yes)
partition "Delete" {
:Delete path uses DeleteJavaDownloadToken with\nrequired via GetJavaDownloadToken or\nListJavaDownloadTokens to DELETED, NOT_FOUND\nconfirmation;
:Confirm deletion via GetJavaDownloadToken or\nListJavaDownloadTokens until OCI reaches\nDELETED, NOT_FOUND;
if ("Delete cleanup and finalizer release succeed?") then (yes)
:Remove the finalizer after delete completion;
else (no)
:Stay blocked until cleanup or finalizer\nrelease succeeds;
endif
}
stop
else (no)
partition "Lifecycle Classification" {
if ("OCI state in retryable set?") then (yes)
:Request requeue while OCI remains in CREATING,\nUPDATING, DELETING;
stop
endif
if ("OCI state is outside success targets?") then (yes)
:Return an unsuccessful reconcile result until\nOCI reaches ACTIVE;
stop
endif
}
partition "Ready and Drift Handling" {
:Compare live OCI state against the\nrepo-authored mutation surface;
if ("Force-new or conflicting drift detected?") then (yes)
:Reject drift before OCI mutation for force-new\nfields compartmentId, javaVersion;
stop
endif
if ("Supported mutable drift detected?") then (yes)
:Apply UpdateJavaDownloadToken only for mutable\nfields definedTags, description, displayName,\nfreeformTags (+3 more);
else (no)
:Skip the no-op mutation path;
endif
:Run provider helper hooks\ncreate=tfresource.CreateResource,\ntfresource.WaitForWorkRequestWithErrorHandling\nentity=javadownloadtoken action=CREATED;\nupdate=tfresource.UpdateResource,\ntfresource.WaitForWorkRequestWithErrorHandling\nentity=javadownloadtoken action=UPDATED;\ndelete=tfresource.DeleteResource,\ntfresource.WaitForWorkRequestWithErrorHandling\nentity=javadownloadtoken action=DELETED when\nthe matching field-driven path executes;
:Apply explicit follow-up semantics\ncreate=GetWorkRequest -> GetJavaDownloadToken;\nupdate=GetWorkRequest -> GetJavaDownloadToken;\ndelete=GetWorkRequest ->\nGetJavaDownloadToken/ListJavaDownloadTokens\nconfirm-delete;
:Return success for the ACTIVE success state;
:Project required status and finalizer policy\nretain-until-confirmed-delete;
:Requeue on provisioning, updating, terminating;
}
endif
stop
note right
shared diagram:
shared/diagrams/shared-reconcile-activity.svg
Start from the shared reconcile activity and
specialize provider-backed CRUD, wait, and
status-projection details here.
Controller-local activity diagrams add
list-lookup hints, secret policies, delete
handling, and open formal gaps.
archetype batch: Generated service-manager
controllers specialize the shared reconcile path
with formal runtime ownership.
service: jmsjavadownloads
slug: javadownloadtoken
kind: JavaDownloadToken
archetype: generated-service-manager
status/finalizer: required /
retain-until-confirmed-delete
requeue: provisioning, updating, terminating
provider states: create CREATING; update
UPDATING; delete DELETING
follow-up: create=GetWorkRequest ->
GetJavaDownloadToken; update=GetWorkRequest ->
GetJavaDownloadToken; delete=GetWorkRequest ->
GetJavaDownloadToken/ListJavaDownloadTokens
confirm-delete
supported drift: definedTags, description,
displayName, freeformTags (+3 more)
reject before mutate: force-new fields
compartmentId, javaVersion
create-only fields: none
list lookup:
oci_jmsjavadownloads_java_download_tokens
filters compartmentId, displayName, id; exact
compartmentId plus displayName unique match in
ACTIVE, CREATING, or UPDATING; duplicate matches
fail
conflicts: none
open gaps: none
Create returns a JavaDownloadToken body plus
opc-work-request-id, while update and delete
return work-request headers only. The reviewed
runtime resumes all three phases through
GetWorkRequest before rereading
GetJavaDownloadToken.
The token value authorizes downloads and is
intentionally excluded from published status,
generated docs, and generic bind/parity matching
in this story. No Kubernetes Secret side effects
are emitted.
Pre-create reuse stays bounded to exact
compartmentId plus displayName matching and does
not use the sensitive value filter even though
ListJavaDownloadTokens supports it.
Delete keeps the finalizer until the delete work
request is terminal and GetJavaDownloadToken or
fallback ListJavaDownloadTokens confirms the
token is gone.
end note
@enduml
