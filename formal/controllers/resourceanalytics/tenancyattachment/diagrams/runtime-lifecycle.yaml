schemaVersion: 1
surface: repo-authored-semantics
service: resourceanalytics
slug: tenancyattachment
kind: TenancyAttachment
archetype: generated-service-manager
states:
  - provisioning
  - active
  - updating
  - terminating
repoAuthored:
  providerLifecycle:
    createPending: ["CREATING"]
    updatePending: ["UPDATING"]
    deletePending: ["DELETING"]
  listLookup:
    filters: ["id","resourceAnalyticsInstanceId","tenancyId"]
    matchRule: exact resource identity match; duplicate matches fail
  mutation:
    mutable: ["description"]
    forceNew: ["resourceAnalyticsInstanceId","tenancyId"]
  followUp:
    create: read-after-write
    update: read-after-write
    delete: confirm-delete
notes:
  - "The runtime uses its declared lifecycle-state contract for TenancyAttachment reconciliation."
  - "The credential-free mock exercises SDK request serialization, response projection, mutation when supported, and confirmed deletion."
