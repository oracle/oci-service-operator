schemaVersion: 1
surface: repo-authored-semantics
service: devops
slug: project
kind: Project
archetype: generated-service-manager
states:
    - provisioning
    - active
    - updating
    - terminating
repoAuthored:
    operations:
        update:
            - UpdateProject
    providerLifecycle:
        createPending:
            - CREATING
        updatePending:
            - UPDATING
        deletePending:
            - DELETING
    listLookup:
        filters:
            - compartmentId
            - name
            - id
        matchRule: exact compartmentId, name, id match within the resource scope; duplicate matches fail
    mutation:
        mutable:
            - description
            - notificationConfig
            - freeformTags
            - definedTags
        forceNew:
            - compartmentId
            - name
    hooks:
        create:
            - helper: tfresource.CreateResource
            - helper: tfresource.WaitForWorkRequestWithErrorHandling
              entityType: project
              action: CREATED
        update:
            - helper: tfresource.UpdateResource
            - helper: tfresource.WaitForWorkRequestWithErrorHandling
              entityType: project
              action: UPDATED
        delete:
            - helper: tfresource.DeleteResource
            - helper: tfresource.WaitForWorkRequestWithErrorHandling
              entityType: project
              action: DELETED
    followUp:
        create: GetWorkRequest -> GetProject
        update: GetWorkRequest -> GetProject
        delete: confirm-delete
notes:
    - The production runtime uses generatedruntime/workrequest async handling for create, update, delete work-request phases.
    - Lifecycle success states are ACTIVE; pending create, update, and delete states requeue until the corresponding follow-up converges.
    - Status projection is required, Secret side effects are none, and the finalizer policy is retain-until-confirmed-delete.
    - Delete policy required retains ownership until DELETED or confirmed absence.
