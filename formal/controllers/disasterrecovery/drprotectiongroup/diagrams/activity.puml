@startuml
skinparam shadowing false
skinparam backgroundColor #fcfcfd
skinparam defaultFontName Helvetica
skinparam defaultFontSize 14
skinparam arrowColor #0f172a
skinparam noteBackgroundColor #fff7d6
skinparam noteBorderColor #b45309
skinparam noteFontColor #334155
skinparam roundcorner 16
title Activity - disasterrecovery/DrProtectionGroup
skinparam activity {
  backgroundColor #f8fafc
  borderColor #0f172a
  fontColor #0f172a
  diamondBackgroundColor #e2e8f0
  diamondBorderColor #1d4ed8
  diamondFontColor #0f172a
  startColor #1d4ed8
  endColor #0f172a
  barColor #1d4ed8
}
start
partition "Observe and Bind" {
:Start from shared flow\nshared/diagrams/shared-reconcile-activity.svg;
:Read desired DrProtectionGroup spec, tracked\nstatus, and delete intent;
if ("Tracked or explicit OCI identity present?") then (yes)
:Get the current OCI resource via\nGetDrProtectionGroup;
else (no)
:Resolve an existing OCI resource through\nListDrProtectionGroups using filters\ncompartmentId, displayName, lifecycleState,\nrole (+1 more);
:Create the OCI resource via\nCreateDrProtectionGroup when no reusable match\nis found;
:Persist the resolved or created OCI identity\ninto status;
endif
}
if ("Delete requested?") then (yes)
partition "Delete" {
:Delete path uses DeleteDrProtectionGroup with\nrequired via GetDrProtectionGroup or\nListDrProtectionGroups to DELETED confirmation;
:Confirm deletion via GetDrProtectionGroup or\nListDrProtectionGroups until OCI reaches\nDELETED;
if ("Delete cleanup and finalizer release succeed?") then (yes)
:Remove the finalizer after delete completion;
else (no)
:Stay blocked until cleanup or finalizer\nrelease succeeds;
endif
}
stop
else (no)
partition "Lifecycle Classification" {
if ("OCI state in retryable set?") then (yes)
:Request requeue while OCI remains in CREATING,\nUPDATING, DELETING;
stop
endif
if ("OCI state is outside success targets?") then (yes)
:Return an unsuccessful reconcile result until\nOCI reaches ACTIVE, INACTIVE;
stop
endif
}
partition "Ready and Drift Handling" {
:Compare live OCI state against the\nrepo-authored mutation surface;
if ("Force-new or conflicting drift detected?") then (yes)
:Reject drift before OCI mutation for force-new\nfields association, compartmentId;
stop
endif
if ("Supported mutable drift detected?") then (yes)
:Apply UpdateDrProtectionGroup only for mutable\nfields definedTags, displayName, freeformTags,\nlogLocation.bucket (+2 more);
else (no)
:Skip the no-op mutation path;
endif
:Run provider helper hooks\ncreate=tfresource.CreateResource,\ntfresource.WaitForWorkRequestWithErrorHandling\nentity=drprotectiongroup action=CREATED;\nupdate=tfresource.UpdateResource,\ntfresource.WaitForWorkRequestWithErrorHandling\nentity=drprotectiongroup action=UPDATED;\ndelete=tfresource.DeleteResource,\ntfresource.WaitForWorkRequestWithErrorHandling\nentity=drprotectiongroup action=DELETED when\nthe matching field-driven path executes;
:Apply explicit follow-up semantics\ncreate=GetWorkRequest -> GetDrProtectionGroup;\nupdate=GetWorkRequest -> GetDrProtectionGroup;\ndelete=GetWorkRequest ->\nGetDrProtectionGroup/ListDrProtectionGroups\nconfirm-delete;
:Return success for the ACTIVE, INACTIVE\nsuccess state;
:Project required status and finalizer policy\nretain-until-confirmed-delete;
:Requeue on provisioning, updating, terminating;
}
endif
stop
note right
shared diagram:
shared/diagrams/shared-reconcile-activity.svg
Start from the shared reconcile activity and
specialize provider-backed CRUD, wait, and
status-projection details here.
Controller-local activity diagrams add
list-lookup hints, secret policies, delete
handling, and open formal gaps.
archetype batch: Generated service-manager
controllers specialize the shared reconcile path
with formal runtime ownership.
service: disasterrecovery
slug: drprotectiongroup
kind: DrProtectionGroup
archetype: generated-service-manager
status/finalizer: required /
retain-until-confirmed-delete
requeue: provisioning, updating, terminating
provider states: create CREATING; update
UPDATING; delete DELETING
follow-up: create=GetWorkRequest ->
GetDrProtectionGroup; update=GetWorkRequest ->
GetDrProtectionGroup; delete=GetWorkRequest ->
GetDrProtectionGroup/ListDrProtectionGroups
confirm-delete
supported drift: definedTags, displayName,
freeformTags, logLocation.bucket (+2 more)
reject before mutate: force-new fields
association, compartmentId
create-only fields: none
list lookup:
oci_disaster_recovery_dr_protection_groups
filters compartmentId, displayName,
lifecycleState, role (+1 more); exact
compartmentId plus displayName unique match,
optionally narrowed by lifecycleState, role, and
lifecycleSubState; duplicate matches fail
conflicts: none
open gaps: none
DrProtectionGroup create, update, and delete are
work-request-backed and resume through
status.async.current as the canonical in-flight
tracker.
List responses expose DrProtectionGroupSummary
items only, so the runtime rereads
GetDrProtectionGroup for authoritative
logLocation and members projection instead of
treating summaries as complete observed state.
The published runtime keeps role,
lifecycleState, lifecycleSubState, peer fields,
and polymorphic members truthful in status while
limiting in-place writes to
UpdateDrProtectionGroupDetails.
Associate/Disassociate role changes, compartment
moves, DR plans, and the broader member-action
families remain out of scope for this first
controller-backed disasterrecovery rollout.
end note
@enduml
