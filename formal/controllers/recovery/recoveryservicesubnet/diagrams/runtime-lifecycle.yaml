schemaVersion: 1
surface: repo-authored-semantics
service: recovery
slug: recoveryservicesubnet
kind: RecoveryServiceSubnet
archetype: generated-service-manager
states:
    - provisioning
    - active
    - updating
    - terminating
repoAuthored:
    operations:
        update:
            - UpdateRecoveryServiceSubnet
    providerLifecycle:
        createPending:
            - CREATING
        updatePending:
            - UPDATING
        deletePending:
            - DELETE_SCHEDULED
            - DELETING
    listLookup:
        filters:
            - compartmentId
            - vcnId
            - id
        matchRule: exact compartmentId, vcnId, id match within the resource scope; duplicate matches fail
    mutation:
        mutable:
            - compartmentId
            - displayName
            - subnets
            - nsgIds
            - freeformTags
            - definedTags
        forceNew:
            - vcnId
            - subnetId
    hooks:
        create:
            - helper: tfresource.CreateResource
            - helper: tfresource.WaitForWorkRequestWithErrorHandling
              entityType: recoveryServiceSubnet
              action: CREATED
        update:
            - helper: tfresource.UpdateResource
            - helper: ChangeRecoveryServiceSubnetCompartment
            - helper: tfresource.WaitForWorkRequestWithErrorHandling
              entityType: recoveryServiceSubnet
              action: UPDATED
        delete:
            - helper: tfresource.DeleteResource
            - helper: tfresource.WaitForWorkRequestWithErrorHandling
              entityType: recoveryServiceSubnet
              action: DELETED
    followUp:
        create: read-after-write
        update: read-after-write
        delete: confirm-delete
notes:
    - The production runtime uses generatedruntime/workrequest async handling for create, update, delete work-request phases.
    - Lifecycle success states are ACTIVE; pending create, update, and delete states requeue until the corresponding follow-up converges.
    - Status projection is required, Secret side effects are none, and the finalizer policy is retain-until-confirmed-delete.
    - Delete policy required retains ownership until DELETED or confirmed absence.
