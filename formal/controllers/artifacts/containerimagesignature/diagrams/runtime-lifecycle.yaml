schemaVersion: 1
surface: repo-authored-semantics
service: artifacts
slug: containerimagesignature
kind: ContainerImageSignature
archetype: generated-service-manager
states:
  - active
  - terminating
repoAuthored:
  providerLifecycle:
    createPending: []
    updatePending: []
    deletePending: ["DELETING"]
  listLookup:
    filters: ["compartmentId","imageId","kmsKeyId","kmsKeyVersionId","message","signature","signingAlgorithm"]
    matchRule: exact resource identity match; duplicate matches fail
  mutation:
    mutable: ["definedTags","freeformTags"]
    forceNew: ["compartmentId","imageId","kmsKeyId","kmsKeyVersionId","message","signature","signingAlgorithm"]
  followUp:
    create: read-after-write
    update: read-after-write
    delete: confirm-delete
notes:
  - "The runtime uses its declared lifecycle-state contract for ContainerImageSignature reconciliation."
  - "The credential-free mock exercises SDK request serialization, response projection, mutation when supported, and confirmed deletion."
