@startuml
skinparam shadowing false
skinparam backgroundColor #fcfcfd
skinparam defaultFontName Helvetica
skinparam defaultFontSize 14
skinparam arrowColor #0f172a
skinparam noteBackgroundColor #fff7d6
skinparam noteBorderColor #b45309
skinparam noteFontColor #334155
skinparam roundcorner 16
title Activity - psql/DbSystem
skinparam activity {
  backgroundColor #f8fafc
  borderColor #0f172a
  fontColor #0f172a
  diamondBackgroundColor #e2e8f0
  diamondBorderColor #1d4ed8
  diamondFontColor #0f172a
  startColor #1d4ed8
  endColor #0f172a
  barColor #1d4ed8
}
start
partition "Observe and Bind" {
:Start from shared flow\nshared/diagrams/shared-reconcile-activity.svg;
:Read desired DbSystem spec, tracked status,\nand delete intent;
if ("Tracked or explicit OCI identity present?") then (yes)
:Get the current OCI resource via GetDbSystem;
else (no)
:Resolve an existing OCI resource through\nListDbSystems using filters compartmentId,\ndisplayName;
:Create the OCI resource via CreateDbSystem\nwhen no reusable match is found;
:Persist the resolved or created OCI identity\ninto status;
endif
}
if ("Delete requested?") then (yes)
partition "Delete" {
:Delete path uses DeleteDbSystem with required\nvia GetDbSystem or ListDbSystems to DELETED\nconfirmation;
:Confirm deletion via GetDbSystem or\nListDbSystems until OCI reaches DELETED;
if ("Delete cleanup and finalizer release succeed?") then (yes)
:Remove the finalizer after delete completion;
else (no)
:Stay blocked until cleanup or finalizer\nrelease succeeds;
endif
}
stop
else (no)
partition "Lifecycle Classification" {
if ("OCI state in retryable set?") then (yes)
:Request requeue while OCI remains in CREATING,\nUPDATING, DELETING;
stop
endif
if ("OCI state is outside success targets?") then (yes)
:Return an unsuccessful reconcile result until\nOCI reaches ACTIVE, NEEDS_ATTENTION;
stop
endif
}
partition "Ready and Drift Handling" {
:Compare live OCI state against the\nrepo-authored mutation surface;
if ("Force-new or conflicting drift detected?") then (yes)
:Reject drift before OCI mutation for force-new\nfields compartmentId, dbVersion,\ninstanceCount, instanceMemorySizeInGBs (+7\nmore);
stop
endif
if ("Supported mutable drift detected?") then (yes)
:Apply ChangeDbSystemCompartment,\nPatchDbSystem, ResetMasterUserPassword (+1\nmore) only for mutable fields\ndbConfigurationParams.applyConfig,\ndbConfigurationParams.configId, definedTags,\ndescription (+4 more);
else (no)
:Skip the no-op mutation path;
endif
:Run provider helper hooks\ncreate=tfresource.CreateResource;\nupdate=tfresource.UpdateResource,\ntfresource.WaitForUpdatedState;\ndelete=tfresource.DeleteResource when the\nmatching field-driven path executes;
:Return success for the ACTIVE, NEEDS_ATTENTION\nsuccess state;
:Project required status and finalizer policy\nretain-until-confirmed-delete;
:Requeue on provisioning, updating, terminating;
}
endif
stop
note right
shared diagram:
shared/diagrams/shared-reconcile-activity.svg
Start from the shared reconcile activity and
specialize provider-backed CRUD, wait, and
status-projection details here.
Controller-local activity diagrams add
list-lookup hints, secret policies, delete
handling, and open formal gaps.
archetype batch: Generated service-manager
controllers specialize the shared reconcile path
with formal runtime ownership.
service: psql
slug: dbsystem
kind: DbSystem
archetype: generated-service-manager
status/finalizer: required /
retain-until-confirmed-delete
requeue: provisioning, updating, terminating
provider states: create CREATING; update
UPDATING; delete DELETING
supported drift:
dbConfigurationParams.applyConfig,
dbConfigurationParams.configId, definedTags,
description (+4 more)
reject before mutate: force-new fields
compartmentId, dbVersion, instanceCount,
instanceMemorySizeInGBs (+7 more)
create-only fields:
adminPassword.secret.secretName,
adminUsername.secret.secretName, configId,
credentials (+2 more)
list lookup: oci_psql_db_systems filters
compartmentId, displayName; tracked OCI ID via
GetDbSystem wins; otherwise exact compartmentId
plus displayName unique match in ACTIVE,
CREATING, UPDATING, INACTIVE, or
NEEDS_ATTENTION; duplicate matches fail
conflicts: none
open gaps: none
Handwritten DbSystem bind first checks the
tracked OCID with GetDbSystem, then searches
ListDbSystems for an exact compartment plus
displayName match, rereads a single candidate
through GetDbSystem, and fails on ambiguous
duplicates instead of guessing.
Create resolves inline credentials or
same-namespace admin secret refs, stamps only
non-empty secret refs into status, and does not
write follow-up Kubernetes secrets.
Create and update do not poll work requests
directly; provider-backed completion is modeled
through GetDbSystem lifecycle projection with
one-minute requeues while OCI reports CREATING,
UPDATING, or DELETING.
ACTIVE, INACTIVE, and NEEDS_ATTENTION settle as
Active unless the reconcile is already in update
or delete follow-up, and delete retains the
finalizer until GetDbSystem or ListDbSystems
confirms the DbSystem is deleted or not found.
end note
@enduml
