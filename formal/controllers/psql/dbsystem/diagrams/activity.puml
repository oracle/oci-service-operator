@startuml
skinparam shadowing false
skinparam backgroundColor #fcfcfd
skinparam defaultFontName Helvetica
skinparam defaultFontSize 14
skinparam arrowColor #0f172a
skinparam noteBackgroundColor #fff7d6
skinparam noteBorderColor #b45309
skinparam noteFontColor #334155
skinparam roundcorner 16
title Activity - psql/DbSystem
skinparam activity {
  backgroundColor #f8fafc
  borderColor #0f172a
  fontColor #0f172a
  diamondBackgroundColor #e2e8f0
  diamondBorderColor #1d4ed8
  diamondFontColor #0f172a
  startColor #1d4ed8
  endColor #0f172a
  barColor #1d4ed8
}
start
:Start from shared flow\nshared/diagrams/shared-reconcile-activity.svg;
:Load desired DbSystem spec and status from\nKubernetes;
:Resolve OCI state via GetDbSystem with\nListDbSystems fallback;
:Use oci_psql_db_systems filters\ncompartment_id, display_name, id, state when\nOCID lookup or bind-versus-create needs list\nresolution;
if (Resource exists?) then (yes)
:Update path calls ChangeDbSystemCompartment,\nPatchDbSystem, ResetMasterUserPassword (+1\nmore);
:Guard mutable fields apply_config,\ncompartment_id, config_id,\ncredentials.password_details.password (+29\nmore) and force-new fields credentials,\ncredentials.username, db_version,\ninstances_details (+13 more);
else (no)
:Create path calls CreateDbSystem;
endif
:Run provider helper hooks\ncreate=tfresource.CreateResource;\nupdate=tfresource.UpdateResource,\ntfresource.WaitForUpdatedState;\ndelete=tfresource.DeleteResource;
:Apply repo-authored secret policy none;
:Delete path uses DeleteDbSystem with required\nvia GetDbSystem or ListDbSystems to DELETED\nconfirmation;
:Project required status and finalizer policy\nretain-until-confirmed-delete;
:Requeue on provisioning, updating, terminating;
:Keep promotion blocked on bind-versus-create,\nlist-lookup, mutation-policy,\nwaiter-work-request;
stop
note right
shared diagram:
shared/diagrams/shared-reconcile-activity.svg
Start from the shared reconcile activity and
specialize provider-backed CRUD, wait, and
status-projection details here.
Controller-local activity diagrams add
list-lookup hints, secret policies, delete
handling, and open formal gaps.
archetype batch: Generated service-manager
controllers specialize the shared reconcile path
with formal runtime ownership.
service: psql
slug: dbsystem
kind: DbSystem
archetype: generated-service-manager
status/finalizer: required /
retain-until-confirmed-delete
requeue: provisioning, updating, terminating
provider states: create CREATING; update none;
delete DELETING
mutable/forceNew: apply_config, compartment_id,
config_id, credentials.password_details.password
(+29 more) / credentials, credentials.username,
db_version, instances_details (+13 more)
open gaps: bind-versus-create, list-lookup,
mutation-policy, waiter-work-request
Scaffold metadata only; replace with
controller-specific runtime states before
promotion.
end note
@enduml
