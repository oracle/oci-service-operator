@startuml
skinparam shadowing false
skinparam backgroundColor #fcfcfd
skinparam defaultFontName Helvetica
skinparam defaultFontSize 14
skinparam arrowColor #0f172a
skinparam noteBackgroundColor #fff7d6
skinparam noteBorderColor #b45309
skinparam noteFontColor #334155
skinparam roundcorner 16
title Sequence - bds/BdsInstance
hide footbox
skinparam sequence {
  actorBorderColor #0f172a
  actorBackgroundColor #e2e8f0
  participantBorderColor #0f172a
  participantBackgroundColor #e2e8f0
  participantFontColor #0f172a
  lifeLineBorderColor #94a3b8
  groupBorderColor #cbd5e1
  groupBackgroundColor #f8fafc
}
actor Kubernetes
participant Controller
participant BaseReconciler
participant "OSOKServiceManager" as ServiceManager
participant OCI
note over BaseReconciler,ServiceManager
shared resolution:
shared/diagrams/shared-resolution-sequence.svg
shared delete:
shared/diagrams/shared-delete-sequence.svg
end note
Kubernetes -> Controller: enqueue BdsInstance reconcile
Controller -> BaseReconciler: Reconcile(request)
BaseReconciler -> ServiceManager: reconcile desired BdsInstance
group Lookup and bind
alt tracked or explicit OCI identity already exists
ServiceManager -> OCI: GetBdsInstance
else tracked identity is missing
ServiceManager -> OCI: ListBdsInstances(filters:\ncompartment_id, display_name, state)
alt reusable OCI resource is not found
ServiceManager -> OCI: AddKafka, AddWorkerNodes,\nCreateBdsInstance
loop create pending CREATING
ServiceManager -> OCI: GetBdsInstance
end
else reusable OCI resource already exists
ServiceManager -> OCI: GetBdsInstance
end
ServiceManager --> BaseReconciler: persist resolved OCI identity in status
end
alt delete requested
group Delete
ServiceManager -> OCI: DeleteBdsInstance
ServiceManager -> OCI: confirm delete via GetBdsInstance or\nListBdsInstances
loop delete pending DELETING
ServiceManager -> OCI: GetBdsInstance or ListBdsInstances
end
alt cleanup or finalizer release is blocked
ServiceManager --> BaseReconciler: retain delete state and retry
else delete completion observed
ServiceManager --> BaseReconciler: release finalizer after delete completion
end
end
else OCI state is retryable
ServiceManager --> BaseReconciler: requeue while OCI remains in\nCREATING, RESUMING, SUSPENDING,\nUPDATING (+1 more)
else live state requires field-aware\ndrift evaluation
group Drift handling
note over ServiceManager,OCI
Supported update surface:
bootstrap_script_url, defined_tags,
display_name, freeform_tags (+1 more)
Reject before mutate: force-new fields
cluster_profile, cluster_version,
compartment_id, is_high_availability (+3
more)
Lookup filters: compartment_id,
display_name, state
end note
opt force-new or conflicting drift is detected
ServiceManager --> BaseReconciler: reject before OCI mutation
end
opt supported mutable drift is detected
ServiceManager -> OCI: Apply AddBlockStorage, AddKafka,\nAddMasterNodes (+7 more) only for\nmutable fields bootstrap_script_url,\ndefined_tags, display_name,\nfreeform_tags (+1 more)
loop update pending RESUMING, SUSPENDING,\nUPDATING
ServiceManager -> OCI: GetBdsInstance
end
end
ServiceManager -> OCI: run helper hooks\ncreate=tfresource.CreateResource;\nupdate=tfresource.UpdateResource;\ndelete=tfresource.DeleteResource
ServiceManager -> OCI: apply follow-up strategy\ncreate=read-after-write;\nupdate=read-after-write;\ndelete=confirm-delete
end
ServiceManager --> BaseReconciler: status=required,\nfinalizer=retain-until-confirmed-delete
end
BaseReconciler --> Controller: requeue on provisioning, updating,\nterminating
note right of ServiceManager
shared diagram:
shared/diagrams/shared-resolution-sequence.svg
shared delete diagram:
shared/diagrams/shared-delete-sequence.svg
Start from the shared resolution sequence for
OCID binding, datasource pagination, and
create-or-update dispatch.
Layer the shared delete sequence when finalizer
retention, delete confirmation, work-request
polling, or Secret cleanup apply.
archetype batch: Generated service-manager
controllers follow the shared resolution and
delete sequences with imported waits and
controller-local policy.
service: bds
slug: bdsinstance
kind: BdsInstance
archetype: generated-service-manager
status/finalizer: required /
retain-until-confirmed-delete
requeue: provisioning, updating, terminating
provider states: create CREATING; update
RESUMING, SUSPENDING, UPDATING; delete DELETING
follow-up: create=read-after-write;
update=read-after-write; delete=confirm-delete
supported drift: bootstrap_script_url,
defined_tags, display_name, freeform_tags (+1
more)
reject before mutate: force-new fields
cluster_profile, cluster_version,
compartment_id, is_high_availability (+3 more)
create-only fields: none
list lookup: oci_bds_bds_instances filters
compartment_id, display_name, state
conflicts: none
open gaps: none
Generated runtime uses read-after-write
follow-up for create and update plus
confirm-delete for delete; `CREATING` maps to
provisioning, `UPDATING`/`SUSPENDING`/`RESUMING`
to updating, `ACTIVE`/`INACTIVE`/`SUSPENDED` to
success, `DELETING` to terminating, and `FAILED`
to the terminal failed bucket.
Pre-create lookup queries `ListBdsInstances`
with `compartmentId` and `displayName`; delete
fallback can re-resolve the tracked cluster
through `ListBdsInstances` when `GetBdsInstance`
stops finding the resource.
Only `displayName`, `bootstrapScriptUrl`,
`freeformTags`, `definedTags`, and `kmsKeyId`
reconcile in place. `compartmentId`,
`clusterVersion`, `isHighAvailability`,
`isSecure`, `clusterProfile`, `networkConfig`,
and `nodes` remain replacement-only drift, while
the runtime has no secret side effects.
end note
@enduml
