schemaVersion: 1
surface: repo-authored-semantics
service: identity
slug: user
kind: User
archetype: generated-service-manager
states:
  - provisioning
  - active
  - updating
  - terminating
  - failed
notes:
  - Blank/default-active runs no longer check in the generated User runtime, but explicit backlog regeneration still preserves this row.
  - The preserved generated runtime reads back the OCI user after create or update and projects the response into status.
  - Finalizer removal still waits for delete confirmation through Get or list fallback, and no secret side effects or legacy adapter seams are involved.
