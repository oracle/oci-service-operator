schemaVersion: 1
surface: repo-authored-semantics
service: datasafe
slug: sdmmaskingpolicydifference
kind: SdmMaskingPolicyDifference
archetype: generated-service-manager
states:
  - provisioning
  - active
  - updating
  - terminating
repoAuthored:
  providerLifecycle:
    createPending: ["CREATING"]
    updatePending: ["UPDATING"]
    deletePending: ["DELETING"]
  listLookup:
    filters: ["compartmentId","differenceType","displayName","id","maskingPolicyId"]
    matchRule: exact resource identity match; duplicate matches fail
  mutation:
    mutable: ["definedTags","displayName","freeformTags"]
    forceNew: ["compartmentId","differenceType","maskingPolicyId"]
  followUp:
    create: read-after-write
    update: read-after-write
    delete: confirm-delete
notes:
  - "The runtime uses its declared lifecycle-state contract for SdmMaskingPolicyDifference reconciliation."
  - "The credential-free mock exercises SDK request serialization, response projection, mutation when supported, and confirmed deletion."
