schemaVersion: 1
surface: repo-authored-semantics
service: mysql
slug: dbsystem
kind: DbSystem
archetype: generated-service-manager
states:
  - provisioning
  - active
  - updating
  - terminating
notes:
  - Scaffold metadata only; replace with controller-specific runtime states before promotion.
  - Create and update resolve admin credential inputs from same-namespace Kubernetes Secrets before request projection.
  - Status mirrors secret references only; no Secret create, update, or delete side effects are modeled.
