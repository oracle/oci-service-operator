---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: fastconnectproviderservices.core.oracle.com
spec:
  group: core.oracle.com
  names:
    kind: FastConnectProviderService
    listKind: FastConnectProviderServiceList
    plural: fastconnectproviderservices
    singular: fastconnectproviderservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the FastConnectProviderService
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the FastConnectProviderService
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: FastConnectProviderService is the Schema for the fastconnectproviderservices
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FastConnectProviderServiceSpec defines the desired state
              of FastConnectProviderService.
            type: object
          status:
            description: FastConnectProviderServiceStatus defines the observed state
              of FastConnectProviderService.
            properties:
              bandwithShapeManagement:
                description: Who is responsible for managing the virtual circuit bandwidth.
                type: string
              customerAsnManagement:
                description: |-
                  Who is responsible for managing the ASN information for the network at the other end
                  of the connection from Oracle.
                type: string
              description:
                description: |-
                  The location of the provider's website or portal. This portal is where you can get information
                  about the provider service, create a virtual circuit connection from the provider to Oracle
                  Cloud Infrastructure, and retrieve your provider service key for that virtual circuit connection.
                  Example: `https://example.com`
                type: string
              id:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the service offered by the provider.
                type: string
              privatePeeringBgpManagement:
                description: Who is responsible for managing the private peering BGP
                  information.
                type: string
              providerName:
                description: The name of the provider.
                type: string
              providerServiceKeyManagement:
                description: Who is responsible for managing the provider service
                  key.
                type: string
              providerServiceName:
                description: The name of the service offered by the provider.
                type: string
              publicPeeringBgpManagement:
                description: Who is responsible for managing the public peering BGP
                  information.
                type: string
              requiredTotalCrossConnects:
                description: Total number of cross-connect or cross-connect groups
                  required for the virtual circuit.
                type: integer
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              supportedVirtualCircuitTypes:
                description: An array of virtual circuit types supported by this service.
                items:
                  type: string
                type: array
              type:
                description: Provider service type.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
