---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: securityrules.networkfirewall.oracle.com
spec:
  group: networkfirewall.oracle.com
  names:
    kind: SecurityRule
    listKind: SecurityRuleList
    plural: securityrules
    singular: securityrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      priority: 1
      type: string
    - description: status of the SecurityRule
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the SecurityRule
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: SecurityRule is the Schema for the securityrules API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityRuleSpec defines the desired state of SecurityRule.
            properties:
              action:
                description: |-
                  Types of Action on the Traffic flow.
                    * ALLOW - Allows the traffic.
                    * DROP - Silently drops the traffic, e.g. without sending a TCP reset.
                    * REJECT - Rejects the traffic, sending a TCP reset to client and/or server as applicable.
                    * INSPECT - Inspects traffic for vulnerability as specified in `inspection`, which may result in rejection.
                type: string
              condition:
                description: SecurityRuleCondition defines nested fields for SecurityRule.Condition.
                properties:
                  application:
                    description: An array of application list names to be evaluated
                      against the traffic protocol and protocol-specific parameters.
                    items:
                      type: string
                    type: array
                  destinationAddress:
                    description: An array of IP address list names to be evaluated
                      against the traffic destination address.
                    items:
                      type: string
                    type: array
                  service:
                    description: An array of service list names to be evaluated against
                      the traffic protocol and protocol-specific parameters.
                    items:
                      type: string
                    type: array
                  sourceAddress:
                    description: An array of IP address list names to be evaluated
                      against the traffic source address.
                    items:
                      type: string
                    type: array
                  url:
                    description: An array of URL pattern list names to be evaluated
                      against the HTTP(S) request target.
                    items:
                      type: string
                    type: array
                type: object
              description:
                description: The description of the security rule. This field can
                  be used to add additional info.
                type: string
              inspection:
                description: |-
                  Type of inspection to affect the Traffic flow. This is only applicable if action is INSPECT.
                    * INTRUSION_DETECTION - Intrusion Detection.
                    * INTRUSION_PREVENTION - Intrusion Detection and Prevention. Traffic classified as potentially malicious will be rejected as described in `type`.
                type: string
              name:
                description: Name for the Security rule, must be unique within the
                  policy.
                type: string
              position:
                description: SecurityRulePosition defines nested fields for SecurityRule.Position.
                properties:
                  afterRule:
                    description: Identifier for rule after which this rule lies.
                    type: string
                  beforeRule:
                    description: Identifier for rule before which this rule lies.
                    type: string
                type: object
            required:
            - action
            - condition
            - name
            type: object
          status:
            description: SecurityRuleStatus defines the observed state of SecurityRule.
            properties:
              action:
                description: |-
                  Types of Action on the Traffic flow.
                    * ALLOW - Allows the traffic.
                    * DROP - Silently drops the traffic, e.g. without sending a TCP reset.
                    * REJECT - Rejects the traffic, sending a TCP reset to client and/or server as applicable.
                    * INSPECT - Inspects traffic for vulnerability as specified in `inspection`, which may result in rejection.
                type: string
              condition:
                description: SecurityRuleCondition defines nested fields for SecurityRule.Condition.
                properties:
                  application:
                    description: An array of application list names to be evaluated
                      against the traffic protocol and protocol-specific parameters.
                    items:
                      type: string
                    type: array
                  destinationAddress:
                    description: An array of IP address list names to be evaluated
                      against the traffic destination address.
                    items:
                      type: string
                    type: array
                  service:
                    description: An array of service list names to be evaluated against
                      the traffic protocol and protocol-specific parameters.
                    items:
                      type: string
                    type: array
                  sourceAddress:
                    description: An array of IP address list names to be evaluated
                      against the traffic source address.
                    items:
                      type: string
                    type: array
                  url:
                    description: An array of URL pattern list names to be evaluated
                      against the HTTP(S) request target.
                    items:
                      type: string
                    type: array
                type: object
              description:
                description: The description of the security rule. This field can
                  be used to add additional info.
                type: string
              inspection:
                description: |-
                  Type of inspection to affect the Traffic flow. This is only applicable if action is INSPECT.
                    * INTRUSION_DETECTION - Intrusion Detection.
                    * INTRUSION_PREVENTION - Intrusion Detection and Prevention. Traffic classified as potentially malicious will be rejected as described in `type`.
                type: string
              name:
                description: Name for the Security rule, must be unique within the
                  policy.
                type: string
              parentResourceId:
                description: OCID of the Network Firewall Policy this security rule
                  belongs to.
                type: string
              position:
                description: SecurityRulePosition defines nested fields for SecurityRule.Position.
                properties:
                  afterRule:
                    description: Identifier for rule after which this rule lies.
                    type: string
                  beforeRule:
                    description: Identifier for rule before which this rule lies.
                    type: string
                type: object
              priorityOrder:
                description: The priority order in which this rule should be evaluated.
                format: int64
                type: integer
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
