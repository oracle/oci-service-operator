---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: librarymaskingformats.datasafe.oracle.com
spec:
  group: datasafe.oracle.com
  names:
    kind: LibraryMaskingFormat
    listKind: LibraryMaskingFormatList
    plural: librarymaskingformats
    singular: librarymaskingformat
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the LibraryMaskingFormat
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the LibraryMaskingFormat
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: LibraryMaskingFormat is the Schema for the librarymaskingformats
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LibraryMaskingFormatSpec defines the desired state of LibraryMaskingFormat.
            properties:
              compartmentId:
                description: The OCID of the compartment where the library masking
                  format should be created.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm)
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              description:
                description: The description of the library masking format.
                type: string
              displayName:
                description: The display name of the library masking format. The name
                  does not have to be unique, and it's changeable.
                type: string
              formatEntries:
                description: An array of format entries. The combined output of all
                  the format entries is used for masking.
                items:
                  description: LibraryMaskingFormatFormatEntry defines nested fields
                    for LibraryMaskingFormat.FormatEntry.
                  properties:
                    columnName:
                      description: The name of the substitution column.
                      type: string
                    description:
                      description: The description of the format entry.
                      type: string
                    endDate:
                      description: |-
                        The upper bound of the range within which all the original column values fall.
                        The end date must be greater than or equal to the start date.
                      type: string
                    endLength:
                      description: |-
                        The maximum number of characters the generated strings should have. It can
                        be any integer greater than zero, but it must be greater than or equal to
                        the start length.
                      type: integer
                    endValue:
                      description: |-
                        The upper bound of the range within which random decimal numbers should be
                        generated. It must be greater than or equal to the start value. It supports
                        input of double type.
                      type: number
                    fixedNumber:
                      description: The constant number to be used for masking.
                      type: number
                    fixedString:
                      description: The constant string to be used for masking.
                      type: string
                    groupingColumns:
                      description: |-
                        One or more reference columns to be used to group column values so that
                        they can be shuffled within their own group. The grouping columns and
                        the column to be masked must belong to the same table.
                      items:
                        type: string
                      type: array
                    jsonData:
                      type: string
                    length:
                      description: |-
                        The number of characters that should be there in the substring. It should
                        be an integer and greater than zero.
                      type: integer
                    libraryMaskingFormatId:
                      description: The OCID of the library masking format.
                      type: string
                    pattern:
                      description: The pattern that should be used to mask data.
                      type: string
                    postProcessingFunction:
                      description: |-
                        The post processing function in SCHEMA_NAME.PACKAGE_NAME.FUNCTION_NAME
                        format. It can be a standalone or packaged function, so PACKAGE_NAME
                        is optional.
                      type: string
                    randomList:
                      description: |-
                        A comma-separated list of values to be used to replace column values.
                        The list can be of strings, numbers, or dates. The data type of each
                        value in the list must be compatible with the data type of the column.
                        The number of entries in the list cannot be more than 999.
                      items:
                        type: string
                      type: array
                    regularExpression:
                      description: |-
                        The regular expression to be used for masking. For data with characters in the
                        ASCII character set, providing a regular expression is optional. However, it
                        is required if the data contains multi-byte characters. If not provided, an
                        error is returned when a multi-byte character is found.
                        In the case of ASCII characters, if a regular expression is not provided,
                        Deterministic Encryption can encrypt variable-length column values while
                        preserving their original format.
                        If a regular expression is provided, the column values in all the rows must match
                        the regular expression. Deterministic Encryption supports a subset of the regular
                        expression language. It supports encryption of fixed-length strings, and does not
                        support * or + syntax of regular expressions. The encrypted values also match the
                        regular expression, which helps to ensure that the original format is preserved.
                        If an original value does not match the regular expression, Deterministic Encryption
                        might not produce a one-to-one mapping. All non-confirming values are mapped to a
                        single encrypted value, thereby producing a many-to-one mapping.
                      type: string
                    replaceWith:
                      description: |-
                        The value that should be used to replace the data matching the regular
                        expression. It can be a fixed string, fixed number or null value.
                      type: string
                    schemaName:
                      description: The name of the schema that contains the substitution
                        column.
                      type: string
                    sqlExpression:
                      description: |-
                        The SQL expression to be used to generate the masked values. It can
                        consist of one or more values, operators, and SQL functions that
                        evaluate to a value. It can also contain substitution columns from
                        the same table. Specify the substitution columns within percent (%)
                        symbols.
                      type: string
                    startDate:
                      description: |-
                        The lower bound of the range within which all the original column values fall.
                        The start date must be less than or equal to the end date.
                      type: string
                    startLength:
                      description: |-
                        The minimum number of characters the generated strings should have. It can
                        be any integer greater than zero, but it must be less than or equal to the
                        end length.
                      type: integer
                    startPosition:
                      description: |-
                        The starting position in the original string from where the substring
                        should be extracted. It can be either a positive or a negative integer.
                        If It's negative, the counting starts from the end of the string.
                      type: integer
                    startValue:
                      description: |-
                        The lower bound of the range within which random decimal numbers should
                        be generated. It must be less than or equal to the end value. It supports
                        input of double type.
                      type: number
                    tableName:
                      description: The name of the table that contains the substitution
                        column.
                      type: string
                    type:
                      type: string
                    userDefinedFunction:
                      description: |-
                        The user-defined function in SCHEMA_NAME.PACKAGE_NAME.FUNCTION_NAME format.
                        It can be a standalone or packaged function, so PACKAGE_NAME is optional.
                      type: string
                  type: object
                type: array
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm)
                  Example: `{"Department": "Finance"}`
                type: object
              sensitiveTypeIds:
                description: An array of OCIDs of the sensitive types compatible with
                  the library masking format. It helps track the sensitive types for
                  which the library masking format is being created.
                items:
                  type: string
                type: array
            required:
            - compartmentId
            - formatEntries
            type: object
          status:
            description: LibraryMaskingFormatStatus defines the observed state of
              LibraryMaskingFormat.
            properties:
              compartmentId:
                description: The OCID of the compartment that contains the library
                  masking format.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm)
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              description:
                description: The description of the library masking format.
                type: string
              displayName:
                description: The display name of the library masking format.
                type: string
              formatEntries:
                description: An array of format entries. The combined output of all
                  the format entries is used for masking.
                items:
                  description: LibraryMaskingFormatFormatEntry defines nested fields
                    for LibraryMaskingFormat.FormatEntry.
                  properties:
                    columnName:
                      description: The name of the substitution column.
                      type: string
                    description:
                      description: The description of the format entry.
                      type: string
                    endDate:
                      description: |-
                        The upper bound of the range within which all the original column values fall.
                        The end date must be greater than or equal to the start date.
                      type: string
                    endLength:
                      description: |-
                        The maximum number of characters the generated strings should have. It can
                        be any integer greater than zero, but it must be greater than or equal to
                        the start length.
                      type: integer
                    endValue:
                      description: |-
                        The upper bound of the range within which random decimal numbers should be
                        generated. It must be greater than or equal to the start value. It supports
                        input of double type.
                      type: number
                    fixedNumber:
                      description: The constant number to be used for masking.
                      type: number
                    fixedString:
                      description: The constant string to be used for masking.
                      type: string
                    groupingColumns:
                      description: |-
                        One or more reference columns to be used to group column values so that
                        they can be shuffled within their own group. The grouping columns and
                        the column to be masked must belong to the same table.
                      items:
                        type: string
                      type: array
                    jsonData:
                      type: string
                    length:
                      description: |-
                        The number of characters that should be there in the substring. It should
                        be an integer and greater than zero.
                      type: integer
                    libraryMaskingFormatId:
                      description: The OCID of the library masking format.
                      type: string
                    pattern:
                      description: The pattern that should be used to mask data.
                      type: string
                    postProcessingFunction:
                      description: |-
                        The post processing function in SCHEMA_NAME.PACKAGE_NAME.FUNCTION_NAME
                        format. It can be a standalone or packaged function, so PACKAGE_NAME
                        is optional.
                      type: string
                    randomList:
                      description: |-
                        A comma-separated list of values to be used to replace column values.
                        The list can be of strings, numbers, or dates. The data type of each
                        value in the list must be compatible with the data type of the column.
                        The number of entries in the list cannot be more than 999.
                      items:
                        type: string
                      type: array
                    regularExpression:
                      description: |-
                        The regular expression to be used for masking. For data with characters in the
                        ASCII character set, providing a regular expression is optional. However, it
                        is required if the data contains multi-byte characters. If not provided, an
                        error is returned when a multi-byte character is found.
                        In the case of ASCII characters, if a regular expression is not provided,
                        Deterministic Encryption can encrypt variable-length column values while
                        preserving their original format.
                        If a regular expression is provided, the column values in all the rows must match
                        the regular expression. Deterministic Encryption supports a subset of the regular
                        expression language. It supports encryption of fixed-length strings, and does not
                        support * or + syntax of regular expressions. The encrypted values also match the
                        regular expression, which helps to ensure that the original format is preserved.
                        If an original value does not match the regular expression, Deterministic Encryption
                        might not produce a one-to-one mapping. All non-confirming values are mapped to a
                        single encrypted value, thereby producing a many-to-one mapping.
                      type: string
                    replaceWith:
                      description: |-
                        The value that should be used to replace the data matching the regular
                        expression. It can be a fixed string, fixed number or null value.
                      type: string
                    schemaName:
                      description: The name of the schema that contains the substitution
                        column.
                      type: string
                    sqlExpression:
                      description: |-
                        The SQL expression to be used to generate the masked values. It can
                        consist of one or more values, operators, and SQL functions that
                        evaluate to a value. It can also contain substitution columns from
                        the same table. Specify the substitution columns within percent (%)
                        symbols.
                      type: string
                    startDate:
                      description: |-
                        The lower bound of the range within which all the original column values fall.
                        The start date must be less than or equal to the end date.
                      type: string
                    startLength:
                      description: |-
                        The minimum number of characters the generated strings should have. It can
                        be any integer greater than zero, but it must be less than or equal to the
                        end length.
                      type: integer
                    startPosition:
                      description: |-
                        The starting position in the original string from where the substring
                        should be extracted. It can be either a positive or a negative integer.
                        If It's negative, the counting starts from the end of the string.
                      type: integer
                    startValue:
                      description: |-
                        The lower bound of the range within which random decimal numbers should
                        be generated. It must be less than or equal to the end value. It supports
                        input of double type.
                      type: number
                    tableName:
                      description: The name of the table that contains the substitution
                        column.
                      type: string
                    type:
                      type: string
                    userDefinedFunction:
                      description: |-
                        The user-defined function in SCHEMA_NAME.PACKAGE_NAME.FUNCTION_NAME format.
                        It can be a standalone or packaged function, so PACKAGE_NAME is optional.
                      type: string
                  type: object
                type: array
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm)
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The OCID of the library masking format.
                type: string
              lifecycleState:
                description: The current state of the library masking format.
                type: string
              sensitiveTypeIds:
                description: An array of OCIDs of the sensitive types compatible with
                  the library masking format.
                items:
                  type: string
                type: array
              source:
                description: Specifies whether the library masking format is user-defined
                  or predefined.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: The date and time the library masking format was created,
                  in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339)
                type: string
              timeUpdated:
                description: The date and time the library masking format was updated,
                  in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339)
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
