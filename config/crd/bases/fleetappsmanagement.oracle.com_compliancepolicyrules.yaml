---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: compliancepolicyrules.fleetappsmanagement.oracle.com
spec:
  group: fleetappsmanagement.oracle.com
  names:
    kind: CompliancePolicyRule
    listKind: CompliancePolicyRuleList
    plural: compliancepolicyrules
    singular: compliancepolicyrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the CompliancePolicyRule
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the CompliancePolicyRule
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: CompliancePolicyRule is the Schema for the compliancepolicyrules
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CompliancePolicyRuleSpec defines the desired state of CompliancePolicyRule.
            properties:
              compliancePolicyId:
                description: Unique OCID of the CompliancePolicy this CompliancePolicyRule
                  belongs to.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                  Example: `My new resource`
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              gracePeriod:
                description: |-
                  Grace period in days,weeks,months or years the exemption is applicable for the rule.
                  This enables a grace period when Fleet Application Management doesn't report the product as noncompliant when patch is not applied.
                type: string
              patchSelection:
                description: CompliancePolicyRulePatchSelection defines nested fields
                  for CompliancePolicyRule.PatchSelection.
                properties:
                  daysSinceRelease:
                    description: Days passed since patch release.
                    type: integer
                  jsonData:
                    type: string
                  patchLevel:
                    description: Patch Name.
                    type: string
                  patchName:
                    description: Patch Name.
                    type: string
                  selectionType:
                    type: string
                type: object
              patchTypeId:
                description: PlatformConfiguration OCID for the patch type to which
                  this CompliancePolicyRule applies.
                items:
                  type: string
                type: array
              productVersion:
                description: CompliancePolicyRuleProductVersion defines nested fields
                  for CompliancePolicyRule.ProductVersion.
                properties:
                  isApplicableForAllHigherVersions:
                    description: Is rule applicable to all higher versions also
                    type: boolean
                  version:
                    description: Product version the rule is applicable.
                    type: string
                required:
                - version
                type: object
              severity:
                description: Severity to which this CompliancePolicyRule applies.
                items:
                  type: string
                type: array
            required:
            - compliancePolicyId
            - displayName
            - patchSelection
            - patchTypeId
            - productVersion
            type: object
          status:
            description: CompliancePolicyRuleStatus defines the observed state of
              CompliancePolicyRule.
            properties:
              compartmentId:
                description: The OCID of the compartment the CompliancePolicyRule
                  belongs to.
                type: string
              compliancePolicyId:
                description: Unique OCID of the CompliancePolicy.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                  Example: `My new resource`
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              gracePeriod:
                description: |-
                  Grace period in days,weeks,months or years the exemption is applicable for the rule.
                  This enables a grace period when Fleet Application Management doesn't report the product as noncompliant when patch is not applied.
                type: string
              id:
                description: Unique OCID of the CompliancePolicyRule.
                type: string
              lifecycleDetails:
                description: |-
                  A message that describes the current state of the CompliancePolicyRule in more detail. For example,
                  can be used to provide actionable information for a resource in the Failed state.
                type: string
              lifecycleState:
                description: The current state of the CompliancePolicyRule.
                type: string
              patchSelection:
                description: CompliancePolicyRulePatchSelection defines nested fields
                  for CompliancePolicyRule.PatchSelection.
                properties:
                  daysSinceRelease:
                    description: Days passed since patch release.
                    type: integer
                  jsonData:
                    type: string
                  patchLevel:
                    description: Patch Name.
                    type: string
                  patchName:
                    description: Patch Name.
                    type: string
                  selectionType:
                    type: string
                type: object
              patchTypeId:
                description: PlatformConfiguration OCID for the patch type to which
                  this CompliancePolicyRule applies.
                items:
                  type: string
                type: array
              productVersion:
                description: CompliancePolicyRuleProductVersion defines nested fields
                  for CompliancePolicyRule.ProductVersion.
                properties:
                  isApplicableForAllHigherVersions:
                    description: Is rule applicable to all higher versions also
                    type: boolean
                  version:
                    description: Product version the rule is applicable.
                    type: string
                required:
                - version
                type: object
              severity:
                description: Severity to which this CompliancePolicyRule applies.
                items:
                  type: string
                type: array
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: |-
                  The date and time the CompliancePolicyRule was created, in the format defined by RFC 3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              timeUpdated:
                description: |-
                  The date and time the CompliancePolicyRule was updated, in the format defined by RFC 3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
