---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: managedinstances.wlms.oracle.com
spec:
  group: wlms.oracle.com
  names:
    kind: ManagedInstance
    listKind: ManagedInstanceList
    plural: managedinstances
    singular: managedinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the ManagedInstance
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ManagedInstance
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ManagedInstance is the Schema for the managedinstances API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ManagedInstanceSpec defines the desired state of ManagedInstance.
            properties:
              compartmentId:
                description: |-
                  The compartment containing the existing managed instance when binding by list lookup.
                  Provide this together with `displayName` when `id` is not set.
                type: string
              configuration:
                additionalProperties:
                  description: JSONValue preserves arbitrary JSON values inside generated
                    CRD fields.
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: |-
                  Desired in-place WLMS managed-instance configuration for an existing bound managed instance.
                  Use the WLMS update field names such as `discoveryInterval` and `domainSearchPaths`.
                  Omit this field to leave the current OCI configuration unchanged.
                type: object
              displayName:
                description: |-
                  The display name of the existing managed instance when binding by list lookup.
                  Provide this together with `compartmentId` when `id` is not set.
                type: string
              id:
                description: The OCI identifier of the existing managed instance to
                  bind directly.
                type: string
              pluginStatus:
                description: |-
                  Optional exact-match filter used to narrow list-based binding to an existing managed instance.
                  Supported values are `ACTIVE` and `INACTIVE`.
                type: string
            type: object
          status:
            description: ManagedInstanceStatus defines the observed state of ManagedInstance.
            properties:
              compartmentId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment.
                type: string
              configuration:
                description: ManagedInstanceConfiguration defines nested fields for
                  ManagedInstance.Configuration.
                properties:
                  discoveryInterval:
                    description: Frequency of domain discovery to be run on the managed
                      instance. The unit is in hours.
                    type: integer
                  domainSearchPaths:
                    description: The whitelisted paths which domain discovery are
                      run against.
                    items:
                      type: string
                    type: array
                type: object
              displayName:
                description: A user-friendly name that does not have to be unique
                  and is changeable.
                type: string
              hostName:
                description: The FQDN of the managed instance.
                type: string
              id:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the managed instance.
                type: string
              osArch:
                description: The operating system architecture on the managed instance.
                type: string
              osName:
                description: The operating system name on the managed instance.
                type: string
              pluginStatus:
                description: The plugin status of the managed instance.
                type: string
              serverCount:
                description: The number of servers running in the managed instance.
                type: integer
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time the managed instance was first reported (in RFC 3339 (https://tools.ietf.org/rfc/rfc3339) format).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              timeUpdated:
                description: |-
                  The date and time the managed instance was last report (in RFC 3339 (https://tools.ietf.org/rfc/rfc3339) format).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
