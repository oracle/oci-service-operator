---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: odainstanceattachments.oda.oracle.com
spec:
  group: oda.oracle.com
  names:
    kind: OdaInstanceAttachment
    listKind: OdaInstanceAttachmentList
    plural: odainstanceattachments
    singular: odainstanceattachment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the OdaInstanceAttachment
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the OdaInstanceAttachment
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: OdaInstanceAttachment is the Schema for the odainstanceattachments
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OdaInstanceAttachmentSpec defines the desired state of OdaInstanceAttachment.
            properties:
              attachToId:
                description: The OCID of the target instance (which could be any other
                  OCI PaaS/SaaS resource), to which this ODA instance is being attached.
                type: string
              attachmentMetadata:
                description: Attachment specific metadata. Defined by the target service.
                type: string
              attachmentType:
                description: The type of target instance which this ODA instance is
                  being attached.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Usage of predefined tag keys. These predefined keys are scoped to namespaces.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type, or scope.
                  Example: `{"bar-key": "value"}`
                type: object
              owner:
                description: OdaInstanceAttachmentOwner defines nested fields for
                  OdaInstanceAttachment.Owner.
                properties:
                  ownerServiceName:
                    description: Name of the owner service principal
                    type: string
                  ownerServiceTenancy:
                    description: Tenancy OCID of the owner service principal
                    type: string
                required:
                - ownerServiceName
                - ownerServiceTenancy
                type: object
              restrictedOperations:
                description: List of operations that are restricted while this instance
                  is attached.
                items:
                  type: string
                type: array
            required:
            - attachToId
            - attachmentType
            - owner
            type: object
          status:
            description: OdaInstanceAttachmentStatus defines the observed state of
              OdaInstanceAttachment.
            properties:
              attachToId:
                description: The OCID of the target instance (which could be any other
                  OCI PaaS/SaaS resource), to which the ODA instance is or is being
                  attached.
                type: string
              attachmentMetadata:
                description: Attachment-specific metadata, defined by the target service.
                type: string
              attachmentType:
                description: The type of attachment defined as an enum.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Usage of predefined tag keys. These predefined keys are scoped to namespaces.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type, or scope.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: Unique immutable identifier that was assigned when the
                  ODA instance attachment was created.
                type: string
              instanceId:
                description: The OCID of the ODA instance to which the attachment
                  applies.
                type: string
              lifecycleState:
                description: The current state of the attachment.
                type: string
              owner:
                description: OdaInstanceAttachmentOwner defines nested fields for
                  OdaInstanceAttachment.Owner.
                properties:
                  ownerServiceName:
                    description: Name of the owner service principal
                    type: string
                  ownerServiceTenancy:
                    description: Tenancy OCID of the owner service principal
                    type: string
                required:
                - ownerServiceName
                - ownerServiceTenancy
                type: object
              restrictedOperations:
                description: List of operation names that are restricted while this
                  ODA instance is attached.
                items:
                  type: string
                type: array
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: The time the attachment was created. An RFC3339 formatted
                  datetime string
                type: string
              timeLastUpdate:
                description: The time the attachment was last modified. An RFC3339
                  formatted datetime string
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
