---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: bastions.bastion.oracle.com
spec:
  group: bastion.oracle.com
  names:
    kind: Bastion
    listKind: BastionList
    plural: bastions
    singular: bastion
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      priority: 1
      type: string
    - description: status of the Bastion
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Bastion
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Bastion is the Schema for the bastions API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BastionSpec defines the desired state of Bastion.
            properties:
              bastionType:
                description: The type of bastion. Use `standard`.
                type: string
              clientCidrBlockAllowList:
                description: A list of address ranges in CIDR notation that you want
                  to allow to connect to sessions hosted by this bastion.
                items:
                  type: string
                type: array
              compartmentId:
                description: The unique identifier (OCID) of the compartment where
                  the bastion is located.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              dnsProxyStatus:
                description: The desired dns proxy status of the bastion.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              maxSessionTtlInSeconds:
                description: The maximum amount of time that any session on the bastion
                  can remain active.
                type: integer
              name:
                description: The name of the bastion, which can't be changed after
                  creation.
                type: string
              phoneBookEntry:
                description: The phonebook entry of the customer's team, which can't
                  be changed after creation. Not applicable to `standard` bastions.
                type: string
              securityAttributes:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Security attributes for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Oracle-ZPR": {"MaxEgressCount": {"value": "42", "mode": "enforce"}}}`
                type: object
              staticJumpHostIpAddresses:
                description: A list of IP addresses of the hosts that the bastion
                  has access to. Not applicable to `standard` bastions.
                items:
                  type: string
                type: array
              targetSubnetId:
                description: The unique identifier (OCID) of the subnet that the bastion
                  connects to.
                type: string
            required:
            - bastionType
            - compartmentId
            - targetSubnetId
            type: object
          status:
            description: BastionStatus defines the observed state of Bastion.
            properties:
              bastionType:
                description: The type of bastion.
                type: string
              clientCidrBlockAllowList:
                description: A list of address ranges in CIDR notation that you want
                  to allow to connect to sessions hosted by this bastion.
                items:
                  type: string
                type: array
              compartmentId:
                description: The unique identifier (OCID) of the compartment where
                  the bastion is located.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              dnsProxyStatus:
                description: The current dns proxy status of the bastion.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: The unique identifier (OCID) of the bastion, which can't
                  be changed after creation.
                type: string
              lifecycleDetails:
                description: A message describing the current state in more detail.
                type: string
              lifecycleState:
                description: The current state of the bastion.
                type: string
              maxSessionTtlInSeconds:
                description: The maximum amount of time that any session on the bastion
                  can remain active.
                type: integer
              maxSessionsAllowed:
                description: The maximum number of active sessions allowed on the
                  bastion.
                type: integer
              name:
                description: The name of the bastion, which can't be changed after
                  creation.
                type: string
              phoneBookEntry:
                description: The phonebook entry of the customer's team, which can't
                  be changed after creation. Not applicable to `standard` bastions.
                type: string
              privateEndpointIpAddress:
                description: The private IP address of the created private endpoint.
                type: string
              securityAttributes:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Security attributes for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Oracle-ZPR": {"MaxEgressCount": {"value": "42", "mode": "enforce"}}}`
                type: object
              staticJumpHostIpAddresses:
                description: A list of IP addresses of the hosts that the bastion
                  has access to. Not applicable to `standard` bastions.
                items:
                  type: string
                type: array
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Usage of system tag keys. These predefined keys are scoped to namespaces.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              targetSubnetId:
                description: The unique identifier (OCID) of the subnet that the bastion
                  connects to.
                type: string
              targetVcnId:
                description: The unique identifier (OCID) of the virtual cloud network
                  (VCN) that the bastion connects to.
                type: string
              timeCreated:
                description: |-
                  The time the bastion was created. Format is defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2020-01-25T21:10:29.600Z`
                type: string
              timeUpdated:
                description: |-
                  The time the bastion was updated. Format is defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2020-01-25T21:10:29.600Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
