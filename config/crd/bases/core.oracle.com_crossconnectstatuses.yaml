---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: crossconnectstatuses.core.oracle.com
spec:
  group: core.oracle.com
  names:
    kind: CrossConnectStatus
    listKind: CrossConnectStatusList
    plural: crossconnectstatuses
    singular: crossconnectstatus
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the CrossConnectStatus
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the CrossConnectStatus
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: CrossConnectStatus is the Schema for the crossconnectstatuses
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CrossConnectStatusSpec defines the desired state of CrossConnectStatus.
            type: object
          status:
            description: CrossConnectStatusObservedState defines the observed state
              of CrossConnectStatus.
            properties:
              crossConnectId:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the cross-connect.
                type: string
              encryptionStatus:
                description: |-
                  Encryption status of this cross connect.
                  Possible values:
                  * **UP:** Traffic is encrypted over this cross-connect
                  * **DOWN:** Traffic is not encrypted over this cross-connect
                  * **CIPHER_MISMATCH:** The MACsec encryption cipher doesn't match the cipher on the CPE
                  * **CKN_MISMATCH:** The MACsec Connectivity association Key Name (CKN) doesn't match the CKN on the CPE
                  * **CAK_MISMATCH:** The MACsec Connectivity Association Key (CAK) doesn't match the CAK on the CPE
                type: string
              interfaceState:
                description: Indicates whether Oracle's side of the interface is up
                  or down.
                type: string
              lightLevelIndBm:
                description: |-
                  The light level of the cross-connect (in dBm).
                  Example: `14.0`
                type: number
              lightLevelIndicator:
                description: |-
                  Status indicator corresponding to the light level.
                    * **NO_LIGHT:** No measurable light
                    * **LOW_WARN:** There's measurable light but it's too low
                    * **HIGH_WARN:** Light level is too high
                    * **BAD:** There's measurable light but the signal-to-noise ratio is bad
                    * **GOOD:** Good light level
                type: string
              lightLevelsInDBm:
                description: |-
                  The light levels of the cross-connect (in dBm).
                  Example: `[14.0, -14.0, 2.1, -10.1]`
                items:
                  type: number
                type: array
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
