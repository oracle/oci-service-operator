---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: deployartifacts.devops.oracle.com
spec:
  group: devops.oracle.com
  names:
    kind: DeployArtifact
    listKind: DeployArtifactList
    plural: deployartifacts
    singular: deployartifact
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the DeployArtifact
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the DeployArtifact
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: DeployArtifact is the Schema for the deployartifacts API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DeployArtifactSpec defines the desired state of DeployArtifact.
            properties:
              argumentSubstitutionMode:
                description: Mode for artifact parameter substitution.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: 'Defined tags for this resource. Each key is predefined
                  and scoped to a namespace. See Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"foo-namespace": {"bar-key": "value"}}`'
                type: object
              deployArtifactSource:
                description: DeployArtifactSource defines nested fields for DeployArtifact.DeployArtifactSource.
                properties:
                  base64EncodedContent:
                    description: The Helm commands to be executed, base 64 encoded
                    type: string
                  chartUrl:
                    description: The URL of an OCIR repository.
                    type: string
                  deployArtifactPath:
                    description: Specifies the artifact path in the repository.
                    type: string
                  deployArtifactSourceType:
                    type: string
                  deployArtifactVersion:
                    description: Users can set this as a placeholder value that refers
                      to a pipeline parameter.
                    type: string
                  helmArtifactSourceType:
                    description: Specifies types of artifact sources.
                    type: string
                  helmVerificationKeySource:
                    description: DeployArtifactSourceHelmVerificationKeySource defines
                      nested fields for DeployArtifact.DeployArtifactSource.HelmVerificationKeySource.
                    properties:
                      currentPublicKey:
                        description: Current version of Base64 encoding of the public
                          key which is in binary GPG exported format.
                        type: string
                      jsonData:
                        type: string
                      previousPublicKey:
                        description: Previous version of Base64 encoding of the public
                          key which is in binary GPG exported format. This would be
                          used for key rotation scenarios.
                        type: string
                      vaultSecretId:
                        description: The OCID of the Vault Secret containing the verification
                          key versions.
                        type: string
                      verificationKeySourceType:
                        type: string
                    type: object
                  imageDigest:
                    description: Specifies image digest for the version of the image.
                    type: string
                  imageUri:
                    description: Specifies OCIR image path - optionally include tag.
                    type: string
                  jsonData:
                    type: string
                  repositoryId:
                    description: The OCID of a repository.
                    type: string
                type: object
              deployArtifactType:
                description: Type of the deployment artifact.
                type: string
              description:
                description: Optional description about the deployment artifact.
                type: string
              displayName:
                description: Deployment artifact display name. Avoid entering confidential
                  information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: 'Simple key-value pair that is applied without any predefined
                  name, type or scope. Exists for cross-compatibility only.  See Resource
                  Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"bar-key": "value"}`'
                type: object
              projectId:
                description: The OCID of a project.
                type: string
            required:
            - argumentSubstitutionMode
            - deployArtifactSource
            - deployArtifactType
            - projectId
            type: object
          status:
            description: DeployArtifactStatus defines the observed state of DeployArtifact.
            properties:
              argumentSubstitutionMode:
                description: Mode for artifact parameter substitution.
                type: string
              compartmentId:
                description: The OCID of a compartment.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: 'Defined tags for this resource. Each key is predefined
                  and scoped to a namespace. See Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"foo-namespace": {"bar-key": "value"}}`'
                type: object
              deployArtifactSource:
                description: DeployArtifactSource defines nested fields for DeployArtifact.DeployArtifactSource.
                properties:
                  base64EncodedContent:
                    description: The Helm commands to be executed, base 64 encoded
                    type: string
                  chartUrl:
                    description: The URL of an OCIR repository.
                    type: string
                  deployArtifactPath:
                    description: Specifies the artifact path in the repository.
                    type: string
                  deployArtifactSourceType:
                    type: string
                  deployArtifactVersion:
                    description: Users can set this as a placeholder value that refers
                      to a pipeline parameter.
                    type: string
                  helmArtifactSourceType:
                    description: Specifies types of artifact sources.
                    type: string
                  helmVerificationKeySource:
                    description: DeployArtifactSourceHelmVerificationKeySource defines
                      nested fields for DeployArtifact.DeployArtifactSource.HelmVerificationKeySource.
                    properties:
                      currentPublicKey:
                        description: Current version of Base64 encoding of the public
                          key which is in binary GPG exported format.
                        type: string
                      jsonData:
                        type: string
                      previousPublicKey:
                        description: Previous version of Base64 encoding of the public
                          key which is in binary GPG exported format. This would be
                          used for key rotation scenarios.
                        type: string
                      vaultSecretId:
                        description: The OCID of the Vault Secret containing the verification
                          key versions.
                        type: string
                      verificationKeySourceType:
                        type: string
                    type: object
                  imageDigest:
                    description: Specifies image digest for the version of the image.
                    type: string
                  imageUri:
                    description: Specifies OCIR image path - optionally include tag.
                    type: string
                  jsonData:
                    type: string
                  repositoryId:
                    description: The OCID of a repository.
                    type: string
                type: object
              deployArtifactType:
                description: Type of the deployment artifact.
                type: string
              description:
                description: Optional description about the artifact to be deployed.
                type: string
              displayName:
                description: Deployment artifact identifier, which can be renamed
                  and is not necessarily unique. Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: 'Simple key-value pair that is applied without any predefined
                  name, type or scope. Exists for cross-compatibility only.  See Resource
                  Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"bar-key": "value"}`'
                type: object
              id:
                description: Unique identifier that is immutable on creation.
                type: string
              lifecycleDetails:
                description: A detailed message describing the current state. For
                  example, can be used to provide actionable information for a resource
                  in Failed state.
                type: string
              lifecycleState:
                description: Current state of the deployment artifact.
                type: string
              projectId:
                description: The OCID of a project.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: 'Usage of system tag keys. These predefined keys are
                  scoped to namespaces. See Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`'
                type: object
              timeCreated:
                description: Time the deployment artifact was created. Format defined
                  by RFC3339 (https://datatracker.ietf.org/doc/html/rfc3339).
                type: string
              timeUpdated:
                description: Time the deployment artifact was updated. Format defined
                  by RFC3339 (https://datatracker.ietf.org/doc/html/rfc3339).
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
