---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: provisions.fleetappsmanagement.oracle.com
spec:
  group: fleetappsmanagement.oracle.com
  names:
    kind: Provision
    listKind: ProvisionList
    plural: provisions
    singular: provision
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the Provision
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Provision
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Provision is the Schema for the provisions API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProvisionSpec defines the desired state of Provision.
            properties:
              compartmentId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment to create the FamProvision in.
                type: string
              configCatalogItemId:
                description: A OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the Catalog Item to a file with key/value pairs to set up variables
                  for createStack API.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              displayName:
                description: A user-friendly name. Does not have to be unique, and
                  it's changeable. Avoid entering confidential information.
                type: string
              fleetId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the Fleet.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              packageCatalogItemId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the Catalog Item.
                type: string
              provisionDescription:
                description: A description of the provision.
                type: string
              tfVariableCompartmentId:
                description: An optional variable added to a list of RMS variables
                  for createStack API. Overrides the one supplied in configuration
                  file.
                type: string
              tfVariableCurrentUserId:
                description: An optional variable added to a list of RMS variables
                  for createStack API. Overrides the one supplied in configuration
                  file.
                type: string
              tfVariableRegionId:
                description: A mandatory variable added to a list of RMS variables
                  for createStack API. Overrides the one supplied in configuration
                  file.
                type: string
              tfVariableTenancyId:
                description: A mandatory variable added to a list of RMS variables
                  for createStack API. Overrides the one supplied in configuration
                  file.
                type: string
            required:
            - compartmentId
            - configCatalogItemId
            - fleetId
            - packageCatalogItemId
            - tfVariableRegionId
            - tfVariableTenancyId
            type: object
          status:
            description: ProvisionStatus defines the observed state of Provision.
            properties:
              compartmentId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment.
                type: string
              configCatalogItemDisplayName:
                description: A display Name of the Catalog Item in the Catalog.
                type: string
              configCatalogItemId:
                description: A OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the Catalog Item to a file with key/value pairs to set up variables
                  for createStack API.
                type: string
              configCatalogItemListingId:
                description: A listing ID of the Catalog Item in the Catalog.
                type: string
              configCatalogItemListingVersion:
                description: A listing version of the Catalog Item in the Catalog.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              deployedResources:
                description: The deployed resources and their summary
                items:
                  description: ProvisionDeployedResource defines nested fields for
                    Provision.DeployedResource.
                  properties:
                    mode:
                      description: 'The mode of the resource. Example: "managed"'
                      type: string
                    resourceInstanceList:
                      description: Collection of InstanceSummary
                      items:
                        description: ProvisionDeployedResourceResourceInstanceList
                          defines nested fields for Provision.DeployedResource.ResourceInstanceList.
                        properties:
                          compartmentId:
                            description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                              of the compartment in which instance is deployed.
                            type: string
                          id:
                            description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                              of the instance.
                            type: string
                          state:
                            description: Instance lifecycle state
                            type: string
                        type: object
                      type: array
                    resourceName:
                      description: The name of the resource
                      type: string
                    resourceProvider:
                      description: The name of the Provider
                      type: string
                    resourceType:
                      description: |-
                        The provider resource type. Must be supported by the Oracle Cloud Infrastructure provider (https://registry.terraform.io/providers/oracle/oci/latest/docs).
                        Example: oci_core_instance
                      type: string
                  type: object
                type: array
              displayName:
                description: A user-friendly name. Does not have to be unique, and
                  it's changeable.
                type: string
              fleetId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the Fleet.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the FamProvision.
                type: string
              lifecycleDetails:
                description: |-
                  A message that describes the current state of the FamProvision in more detail. For example,
                  can be used to provide actionable information for a resource in the Failed state.
                type: string
              lifecycleState:
                description: The current state of the FamProvision.
                type: string
              packageCatalogItemDisplayName:
                description: A display Name of the Catalog Item in the Catalog.
                type: string
              packageCatalogItemId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the Catalog Item.
                type: string
              packageCatalogItemListingId:
                description: A listing ID of the Catalog Item in the Catalog.
                type: string
              packageCatalogItemListingVersion:
                description: A listing version of the Catalog Item in the Catalog.
                type: string
              provisionDescription:
                description: A description of the provision.
                type: string
              rmsApplyJobId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the RMS APPLY Job.
                type: string
              stackId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the RMS Stack.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              tfOutputs:
                description: Outputs from the Terraform Apply job
                items:
                  description: ProvisionTfOutput defines nested fields for Provision.TfOutput.
                  properties:
                    isSensitive:
                      description: The indicator if the data for this parameter is
                        sensitive (e.g. should the data be hidden in UI, encrypted
                        if stored, etc.)
                      type: boolean
                    outputDescription:
                      description: The output description
                      type: string
                    outputName:
                      description: The output name
                      type: string
                    outputType:
                      description: The output type
                      type: string
                    outputValue:
                      description: The output value
                      type: string
                  type: object
                type: array
              tfVariableCompartmentId:
                description: An optional variable added to a list of RMS variables
                  for createStack API. Overrides the one supplied in configuration
                  file.
                type: string
              tfVariableCurrentUserId:
                description: An optional variable added to a list of RMS variables
                  for createStack API. Overrides the one supplied in configuration
                  file.
                type: string
              tfVariableRegionId:
                description: A mandatory variable added to a list of RMS variables
                  for createStack API. Overrides the one supplied in configuration
                  file.
                type: string
              tfVariableTenancyId:
                description: A mandatory variable added to a list of RMS variables
                  for createStack API. Overrides the one supplied in configuration
                  file.
                type: string
              timeCreated:
                description: |-
                  The date and time the FamProvision was created, in the format defined by RFC 3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              timeUpdated:
                description: |-
                  The date and time the FamProvision was updated, in the format defined by RFC 3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
