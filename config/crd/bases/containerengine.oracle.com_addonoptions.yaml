---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: addonoptions.containerengine.oracle.com
spec:
  group: containerengine.oracle.com
  names:
    kind: AddonOption
    listKind: AddonOptionList
    plural: addonoptions
    singular: addonoption
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the AddonOption
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the AddonOption
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: AddonOption is the Schema for the addonoptions API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AddonOptionSpec defines the desired state of AddonOption.
            type: object
          status:
            description: AddonOptionStatus defines the observed state of AddonOption.
            properties:
              addonGroup:
                description: Addon group info, a namespace concept that groups addons
                  with similar functionalities.
                type: string
              addonSchemaVersion:
                description: Addon definition schema version to validate addon.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              description:
                description: Description on the addon.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              isEssential:
                description: Is it an essential addon for cluster operation or not.
                type: boolean
              lifecycleState:
                description: The life cycle state of the addon.
                type: string
              name:
                description: Name of the addon and it would be unique.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Usage of system tag keys. These predefined keys are scoped to namespaces.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: The time the work request was created.
                type: string
              versions:
                description: The resources this work request affects.
                items:
                  description: AddonOptionVersion defines nested fields for AddonOption.Version.
                  properties:
                    configurations:
                      description: Addon version configuration details.
                      items:
                        description: AddonOptionVersionConfiguration defines nested
                          fields for AddonOption.Version.Configuration.
                        properties:
                          description:
                            description: Information about the addon version configuration.
                            type: string
                          displayName:
                            description: Display name of addon version.
                            type: string
                          isRequired:
                            description: If the the configuration is required or not.
                            type: boolean
                          key:
                            description: Addon configuration key
                            type: string
                          value:
                            description: Addon configuration value
                            type: string
                        type: object
                      type: array
                    description:
                      description: Information about the addon version.
                      type: string
                    kubernetesVersionFilters:
                      description: The range of kubernetes versions an addon can be
                        configured.
                      properties:
                        exactKubernetesVersions:
                          description: The exact version of kubernetes that are compatible.
                          items:
                            type: string
                          type: array
                        maximumVersion:
                          description: The latest kubernetes version.
                          type: string
                        minimalVersion:
                          description: The earliest kubernetes version.
                          type: string
                      type: object
                    status:
                      description: Current state of the addon, only active will be
                        visible to customer, visibility of versions in other status
                        will be filtered  based on limits property.
                      type: string
                    versionNumber:
                      description: Version number, need be comparable within an addon.
                      type: string
                  type: object
                type: array
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
