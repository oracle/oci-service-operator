---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  name: apigateways.apigateway.oracle.com
spec:
  group: apigateway.oracle.com
  names:
    kind: ApiGateway
    listKind: ApiGatewayList
    plural: apigateways
    singular: apigateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - jsonPath: .spec.endpointType
      name: EndpointType
      priority: 1
      type: string
    - description: status of the ApiGateway
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ApiGateway
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ApiGateway is the Schema for the apigateways API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ApiGatewaySpec defines the desired state of ApiGateway.
            properties:
              certificateId:
                description: CertificateId is the OCID of a certificate resource to
                  use for HTTPS.
                maxLength: 255
                minLength: 1
                type: string
              compartmentId:
                description: CompartmentId is the OCID of the compartment in which
                  to create the gateway.
                maxLength: 255
                minLength: 1
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                type: object
              displayName:
                description: DisplayName is a user-friendly name for the gateway.
                type: string
              endpointType:
                description: EndpointType is the gateway endpoint type.
                enum:
                - PUBLIC
                - PRIVATE
                type: string
                x-kubernetes-validations:
                - message: endpointType is immutable
                  rule: self == oldSelf
              freeformTags:
                additionalProperties:
                  type: string
                type: object
              id:
                description: The OCID of an existing ApiGateway to bind to.
                maxLength: 255
                minLength: 1
                type: string
              networkSecurityGroupIds:
                description: NetworkSecurityGroupIds is an optional list of NSG OCIDs
                  associated with the gateway.
                items:
                  type: string
                type: array
              subnetId:
                description: SubnetId is the OCID of the subnet in which the gateway
                  is created.
                maxLength: 255
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: subnetId is immutable
                  rule: self == oldSelf
            required:
            - compartmentId
            - endpointType
            - subnetId
            type: object
          status:
            description: ApiGatewayStatus defines the observed state of ApiGateway.
            properties:
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
