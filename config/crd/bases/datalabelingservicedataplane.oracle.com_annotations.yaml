---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: annotations.datalabelingservicedataplane.oracle.com
spec:
  group: datalabelingservicedataplane.oracle.com
  names:
    kind: Annotation
    listKind: AnnotationList
    plural: annotations
    singular: annotation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the Annotation
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Annotation
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Annotation is the Schema for the annotations API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AnnotationSpec defines the desired state of Annotation.
            properties:
              compartmentId:
                description: The OCID of the compartment for the annotation.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  The defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              entities:
                description: The entity types are validated against the dataset to
                  ensure consistency.
                items:
                  description: AnnotationEntity defines nested fields for Annotation.Entity.
                  properties:
                    boundingPolygon:
                      description: AnnotationEntityBoundingPolygon defines nested
                        fields for Annotation.Entity.BoundingPolygon.
                      properties:
                        normalizedVertices:
                          description: The normalized vertices that make up the polygon.  They
                            are in the order of the segments they connect.
                          items:
                            description: AnnotationEntityBoundingPolygonNormalizedVertice
                              defines nested fields for Annotation.Entity.BoundingPolygon.NormalizedVertice.
                            properties:
                              x:
                                description: The X axis coordinate.
                                type: number
                              "y":
                                description: The Y axis coordinate.
                                type: number
                            required:
                            - x
                            - "y"
                            type: object
                          type: array
                      required:
                      - normalizedVertices
                      type: object
                    confidence:
                      description: float value, score from OCR.
                      type: number
                    documentEntityMetadata:
                      description: AnnotationEntityDocumentEntityMetadata defines
                        nested fields for Annotation.Entity.DocumentEntityMetadata.
                      properties:
                        pageNumber:
                          description: This stores page number of document.
                          type: number
                      required:
                      - pageNumber
                      type: object
                    entityType:
                      type: string
                    extendedMetadata:
                      additionalProperties:
                        type: string
                      description: |-
                        A simple key-value pair that is applied without any predefined name, type, or scope. It exists for cross-compatibility only.
                        For example: `{"bar-key": "value"}`
                      type: object
                    jsonData:
                      type: string
                    labels:
                      description: A collection of label entities.
                      items:
                        description: AnnotationEntityLabel defines nested fields for
                          Annotation.Entity.Label.
                        properties:
                          label:
                            description: The label provided by the annotator.
                            type: string
                        required:
                        - label
                        type: object
                      type: array
                    pageNumber:
                      description: Integer value.
                      type: number
                    rotation:
                      description: Integer value.
                      type: number
                    text:
                      description: Entity Name.
                      type: string
                    textSpan:
                      description: AnnotationEntityTextSpan defines nested fields
                        for Annotation.Entity.TextSpan.
                      properties:
                        length:
                          description: The length of the selected text.
                          type: number
                        offset:
                          description: The offset of the selected text within the
                            entire text.
                          type: number
                      type: object
                  type: object
                type: array
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  A simple key-value pair that is applied without any predefined name, type, or scope. It exists for cross-compatibility only.
                  For example: `{"bar-key": "value"}`
                type: object
              recordId:
                description: The OCID of the record annotated.
                type: string
            required:
            - compartmentId
            - entities
            - recordId
            type: object
          status:
            description: AnnotationStatus defines the observed state of Annotation.
            properties:
              compartmentId:
                description: The OCID of the compartment for the annotation. This
                  is tied to the dataset. It is not changeable on the record itself.
                type: string
              createdBy:
                description: The OCID of the principal which created the annotation.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  The defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              entities:
                description: The entity types are validated against the dataset to
                  ensure consistency.
                items:
                  description: AnnotationEntity defines nested fields for Annotation.Entity.
                  properties:
                    boundingPolygon:
                      description: AnnotationEntityBoundingPolygon defines nested
                        fields for Annotation.Entity.BoundingPolygon.
                      properties:
                        normalizedVertices:
                          description: The normalized vertices that make up the polygon.  They
                            are in the order of the segments they connect.
                          items:
                            description: AnnotationEntityBoundingPolygonNormalizedVertice
                              defines nested fields for Annotation.Entity.BoundingPolygon.NormalizedVertice.
                            properties:
                              x:
                                description: The X axis coordinate.
                                type: number
                              "y":
                                description: The Y axis coordinate.
                                type: number
                            required:
                            - x
                            - "y"
                            type: object
                          type: array
                      required:
                      - normalizedVertices
                      type: object
                    confidence:
                      description: float value, score from OCR.
                      type: number
                    documentEntityMetadata:
                      description: AnnotationEntityDocumentEntityMetadata defines
                        nested fields for Annotation.Entity.DocumentEntityMetadata.
                      properties:
                        pageNumber:
                          description: This stores page number of document.
                          type: number
                      required:
                      - pageNumber
                      type: object
                    entityType:
                      type: string
                    extendedMetadata:
                      additionalProperties:
                        type: string
                      description: |-
                        A simple key-value pair that is applied without any predefined name, type, or scope. It exists for cross-compatibility only.
                        For example: `{"bar-key": "value"}`
                      type: object
                    jsonData:
                      type: string
                    labels:
                      description: A collection of label entities.
                      items:
                        description: AnnotationEntityLabel defines nested fields for
                          Annotation.Entity.Label.
                        properties:
                          label:
                            description: The label provided by the annotator.
                            type: string
                        required:
                        - label
                        type: object
                      type: array
                    pageNumber:
                      description: Integer value.
                      type: number
                    rotation:
                      description: Integer value.
                      type: number
                    text:
                      description: Entity Name.
                      type: string
                    textSpan:
                      description: AnnotationEntityTextSpan defines nested fields
                        for Annotation.Entity.TextSpan.
                      properties:
                        length:
                          description: The length of the selected text.
                          type: number
                        offset:
                          description: The offset of the selected text within the
                            entire text.
                          type: number
                      type: object
                  type: object
                type: array
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  A simple key-value pair that is applied without any predefined name, type, or scope. It exists for cross-compatibility only.
                  For example: `{"bar-key": "value"}`
                type: object
              id:
                description: The OCID of the annotation.
                type: string
              lifecycleState:
                description: |-
                  The lifecycle state of an annotation.
                  ACTIVE - The annotation is active to be used for labeling.
                  INACTIVE - The annotation has been marked as inactive and should not be used for labeling.
                  DELETED - Tha annotation been deleted and no longer available for labeling.
                type: string
              recordId:
                description: The OCID of the record annotated.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: The date and time the annotation was created, in the
                  timestamp format defined by RFC3339.
                type: string
              timeUpdated:
                description: The date and time the resource was updated, in the timestamp
                  format defined by RFC3339.
                type: string
              updatedBy:
                description: The OCID of the principal which updated the annotation.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
