---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: peertargetdatabases.datasafe.oracle.com
spec:
  group: datasafe.oracle.com
  names:
    kind: PeerTargetDatabase
    listKind: PeerTargetDatabaseList
    plural: peertargetdatabases
    singular: peertargetdatabase
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the PeerTargetDatabase
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the PeerTargetDatabase
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: PeerTargetDatabase is the Schema for the peertargetdatabases
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PeerTargetDatabaseSpec defines the desired state of PeerTargetDatabase.
            properties:
              databaseDetails:
                description: PeerTargetDatabaseDatabaseDetails defines nested fields
                  for PeerTargetDatabase.DatabaseDetails.
                properties:
                  autonomousDatabaseId:
                    description: The OCID of the Autonomous Database registered as
                      a target database in Data Safe.
                    type: string
                  databaseType:
                    type: string
                  dbSystemId:
                    description: The OCID of the cloud database registered as a target
                      database in Data Safe.
                    type: string
                  infrastructureType:
                    description: The infrastructure type the database is running on.
                    type: string
                  instanceId:
                    description: The OCID of the compute instance on which the database
                      is running.
                    type: string
                  ipAddresses:
                    description: The list of database host IP Addresses. Fully qualified
                      domain names can be used if connectionType is 'ONPREM_CONNECTOR'.
                    items:
                      type: string
                    type: array
                  jsonData:
                    type: string
                  listenerPort:
                    description: The port number of the database listener.
                    type: integer
                  pluggableDatabaseId:
                    description: The OCID of the pluggable database registered as
                      a target database in Data Safe.
                    type: string
                  serviceName:
                    description: The service name of the database registered as target
                      database.
                    type: string
                  vmClusterId:
                    description: The OCID of the VM cluster in which the database
                      is running.
                    type: string
                required:
                - infrastructureType
                type: object
              dataguardAssociationId:
                description: The OCID of the Data Guard Association resource in which
                  the database being registered is considered as peer database to
                  the primary database.
                type: string
              description:
                description: The description of the peer target database in Data Safe.
                type: string
              displayName:
                description: The display name of the peer target database in Data
                  Safe. The name is modifiable and does not need to be unique.
                type: string
              tlsConfig:
                description: PeerTargetDatabaseTlsConfig defines nested fields for
                  PeerTargetDatabase.TlsConfig.
                properties:
                  certificateStoreType:
                    description: The format of the certificate store.
                    type: string
                  keyStoreContent:
                    description: Base64 encoded string of key store file content.
                    type: string
                  status:
                    description: Status to represent whether the database connection
                      is TLS enabled or not.
                    type: string
                  storePassword:
                    description: The password to read the trust store and key store
                      files, if they are password protected.
                    type: string
                  trustStoreContent:
                    description: Base64 encoded string of trust store file content.
                    type: string
                required:
                - status
                type: object
            required:
            - databaseDetails
            type: object
          status:
            description: PeerTargetDatabaseStatus defines the observed state of PeerTargetDatabase.
            properties:
              databaseDetails:
                description: PeerTargetDatabaseDatabaseDetails defines nested fields
                  for PeerTargetDatabase.DatabaseDetails.
                properties:
                  autonomousDatabaseId:
                    description: The OCID of the Autonomous Database registered as
                      a target database in Data Safe.
                    type: string
                  databaseType:
                    type: string
                  dbSystemId:
                    description: The OCID of the cloud database registered as a target
                      database in Data Safe.
                    type: string
                  infrastructureType:
                    description: The infrastructure type the database is running on.
                    type: string
                  instanceId:
                    description: The OCID of the compute instance on which the database
                      is running.
                    type: string
                  ipAddresses:
                    description: The list of database host IP Addresses. Fully qualified
                      domain names can be used if connectionType is 'ONPREM_CONNECTOR'.
                    items:
                      type: string
                    type: array
                  jsonData:
                    type: string
                  listenerPort:
                    description: The port number of the database listener.
                    type: integer
                  pluggableDatabaseId:
                    description: The OCID of the pluggable database registered as
                      a target database in Data Safe.
                    type: string
                  serviceName:
                    description: The service name of the database registered as target
                      database.
                    type: string
                  vmClusterId:
                    description: The OCID of the VM cluster in which the database
                      is running.
                    type: string
                required:
                - infrastructureType
                type: object
              databaseUniqueName:
                description: Unique name of the database associated to the peer target
                  database.
                type: string
              dataguardAssociationId:
                description: The OCID of the Data Guard Association resource in which
                  the database associated to the peer target database is considered
                  as peer database to the primary database.
                type: string
              description:
                description: The description of the peer target database in Data Safe.
                type: string
              displayName:
                description: The display name of the peer target database in Data
                  Safe.
                type: string
              key:
                description: The secondary key assigned for the peer target database
                  in Data Safe.
                type: integer
              lifecycleDetails:
                description: Details about the current state of the peer target database
                  in Data Safe.
                type: string
              lifecycleState:
                description: The current state of the peer target database in Data
                  Safe.
                type: string
              role:
                description: Role of the database associated to the peer target database.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: The date and time of the peer target database registration
                  in Data Safe.
                type: string
              tlsConfig:
                description: PeerTargetDatabaseTlsConfig defines nested fields for
                  PeerTargetDatabase.TlsConfig.
                properties:
                  certificateStoreType:
                    description: The format of the certificate store.
                    type: string
                  keyStoreContent:
                    description: Base64 encoded string of key store file content.
                    type: string
                  status:
                    description: Status to represent whether the database connection
                      is TLS enabled or not.
                    type: string
                  storePassword:
                    description: The password to read the trust store and key store
                      files, if they are password protected.
                    type: string
                  trustStoreContent:
                    description: Base64 encoded string of trust store file content.
                    type: string
                required:
                - status
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
