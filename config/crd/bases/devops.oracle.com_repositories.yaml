---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: repositories.devops.oracle.com
spec:
  group: devops.oracle.com
  names:
    kind: Repository
    listKind: RepositoryList
    plural: repositories
    singular: repository
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      priority: 1
      type: string
    - description: status of the Repository
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Repository
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Repository is the Schema for the repositories API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RepositorySpec defines the desired state of Repository.
            properties:
              defaultBranch:
                description: The default branch of the repository.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: 'Defined tags for this resource. Each key is predefined
                  and scoped to a namespace. See Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"foo-namespace": {"bar-key": "value"}}`'
                type: object
              description:
                description: Details of the repository. Avoid entering confidential
                  information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: 'Simple key-value pair that is applied without any predefined
                  name, type or scope. Exists for cross-compatibility only.  See Resource
                  Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"bar-key": "value"}`'
                type: object
              mirrorRepositoryConfig:
                description: RepositoryMirrorRepositoryConfig defines nested fields
                  for Repository.MirrorRepositoryConfig.
                properties:
                  connectorId:
                    description: Upstream git repository connection identifier.
                    type: string
                  repositoryUrl:
                    description: URL of external repository you want to mirror.
                    type: string
                  triggerSchedule:
                    description: RepositoryMirrorRepositoryConfigTriggerSchedule defines
                      nested fields for Repository.MirrorRepositoryConfig.TriggerSchedule.
                    properties:
                      customSchedule:
                        description: |-
                          Valid if type is CUSTOM. Following RFC 5545 recurrence rules, we can specify starting time, occurrence frequency, and interval size.
                          Example for frequency could be DAILY/WEEKLY/HOURLY or any RFC 5545 supported frequency, which is followed by start time of this window.
                          You can control the start time with BYHOUR, BYMINUTE and BYSECONDS. It is followed by the interval size.
                        type: string
                      scheduleType:
                        description: |-
                          Different types of trigger schedule:
                          NONE - No automated synchronization schedule.
                          DEFAULT - Trigger schedule is every 30 minutes.
                          CUSTOM - Custom triggering schedule.
                        type: string
                    required:
                    - scheduleType
                    type: object
                type: object
              name:
                description: Name of the repository. Should be unique within the project.
                type: string
              parentRepositoryId:
                description: The OCID of the parent repository.
                type: string
              projectId:
                description: The OCID of the DevOps project containing the repository.
                type: string
              repositoryType:
                description: |-
                  Type of repository. Allowed values:
                  `MIRRORED`
                  `HOSTED`
                  `FORKED`
                type: string
            required:
            - name
            - projectId
            - repositoryType
            type: object
          status:
            description: RepositoryStatus defines the observed state of Repository.
            properties:
              branchCount:
                description: The count of the branches present in the repository.
                type: integer
              commitCount:
                description: The count of the commits present in the repository.
                type: integer
              compartmentId:
                description: The OCID of the repository's compartment.
                type: string
              defaultBranch:
                description: The default branch of the repository.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: 'Defined tags for this resource. Each key is predefined
                  and scoped to a namespace. See Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"foo-namespace": {"bar-key": "value"}}`'
                type: object
              description:
                description: Details of the repository. Avoid entering confidential
                  information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: 'Simple key-value pair that is applied without any predefined
                  name, type or scope. Exists for cross-compatibility only.  See Resource
                  Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"bar-key": "value"}`'
                type: object
              httpUrl:
                description: HTTP URL that you use to git clone, pull and push.
                type: string
              id:
                description: The OCID of the repository. This value is unique and
                  immutable.
                type: string
              lifecycleDetails:
                description: A message describing the current state in more detail.
                  For example, can be used to provide actionable information for a
                  resource in Failed state.
                type: string
              lifecycleState:
                description: The current state of the repository.
                type: string
              lifecyleDetails:
                description: A message describing the current state in more detail.
                  For example, can be used to provide actionable information for a
                  resource in Failed state.
                type: string
              mirrorRepositoryConfig:
                description: RepositoryMirrorRepositoryConfig defines nested fields
                  for Repository.MirrorRepositoryConfig.
                properties:
                  connectorId:
                    description: Upstream git repository connection identifier.
                    type: string
                  repositoryUrl:
                    description: URL of external repository you want to mirror.
                    type: string
                  triggerSchedule:
                    description: RepositoryMirrorRepositoryConfigTriggerSchedule defines
                      nested fields for Repository.MirrorRepositoryConfig.TriggerSchedule.
                    properties:
                      customSchedule:
                        description: |-
                          Valid if type is CUSTOM. Following RFC 5545 recurrence rules, we can specify starting time, occurrence frequency, and interval size.
                          Example for frequency could be DAILY/WEEKLY/HOURLY or any RFC 5545 supported frequency, which is followed by start time of this window.
                          You can control the start time with BYHOUR, BYMINUTE and BYSECONDS. It is followed by the interval size.
                        type: string
                      scheduleType:
                        description: |-
                          Different types of trigger schedule:
                          NONE - No automated synchronization schedule.
                          DEFAULT - Trigger schedule is every 30 minutes.
                          CUSTOM - Custom triggering schedule.
                        type: string
                    required:
                    - scheduleType
                    type: object
                type: object
              name:
                description: Name of the repository. Should be unique within the project.
                  This value is mutable.
                type: string
              namespace:
                description: Tenancy unique namespace.
                type: string
              parentRepositoryId:
                description: The OCID of the parent repository.
                type: string
              projectId:
                description: The OCID of the DevOps project containing the repository.
                type: string
              projectName:
                description: Unique project name in a namespace.
                type: string
              repositoryType:
                description: |-
                  Type of repository:
                  MIRRORED - Repository created by mirroring an existing repository.
                  HOSTED - Repository created and hosted using OCI DevOps code repository.
                  FORKED - Repository created by forking an existing repository.
                type: string
              sizeInBytes:
                description: The size of the repository in bytes.
                format: int64
                type: integer
              sshUrl:
                description: SSH URL that you use to git clone, pull and push.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: 'Usage of system tag keys. These predefined keys are
                  scoped to namespaces. See Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`'
                type: object
              timeCreated:
                description: The time the repository was created. Format defined by
                  RFC3339 (https://datatracker.ietf.org/doc/html/rfc3339).
                type: string
              timeUpdated:
                description: The time the repository was updated. Format defined by
                  RFC3339 (https://datatracker.ietf.org/doc/html/rfc3339).
                type: string
              triggerBuildEvents:
                description: |-
                  Trigger build events supported for this repository:
                  PUSH - Build is triggered when a push event occurs.
                  PULL_REQUEST_CREATED - Build is triggered when a pull request is created in the repository.
                  PULL_REQUEST_UPDATED - Build is triggered when a push is made to a branch with an open pull request.
                  COMMIT_UPDATES - Build is triggered when new commits are mirrored into a repository.
                items:
                  type: string
                type: array
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
