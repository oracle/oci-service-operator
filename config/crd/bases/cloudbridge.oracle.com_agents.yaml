---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: agents.cloudbridge.oracle.com
spec:
  group: cloudbridge.oracle.com
  names:
    kind: Agent
    listKind: AgentList
    plural: agents
    singular: agent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the Agent
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Agent
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Agent is the Schema for the agents API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgentSpec defines the desired state of Agent.
            properties:
              agentType:
                description: Agent identifier.
                type: string
              agentVersion:
                description: Agent identifier.
                type: string
              compartmentId:
                description: Compartment identifier.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  The defined tags associated with this resource, if any. Each key is predefined and scoped to namespaces.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: Agent identifier.
                type: string
              environmentId:
                description: Environment identifier.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  The freeform tags associated with this resource, if any. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace/scope. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              osVersion:
                description: OS version.
                type: string
            required:
            - agentType
            - agentVersion
            - compartmentId
            - displayName
            - environmentId
            - osVersion
            type: object
          status:
            description: AgentStatus defines the observed state of Agent.
            properties:
              agentPubKey:
                description: Resource principal public key.
                type: string
              agentType:
                description: Type of the Agent.
                type: string
              agentVersion:
                description: Agent identifier.
                type: string
              compartmentId:
                description: Compartment identifier.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  The defined tags associated with this resource, if any. Each key is predefined and scoped to namespaces.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: Agent identifier, can be renamed.
                type: string
              environmentId:
                description: Environment identifier.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  The freeform tags associated with this resource, if any. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace/scope. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              heartBeatStatus:
                description: The current heartbeat status of the Agent based on its
                  timeLastSyncReceived value.
                type: string
              id:
                description: Unique identifier that is immutable on creation.
                type: string
              lifecycleDetails:
                description: A message describing the current state of the Agent in
                  more detail. For example, it can be used to provide actionable information
                  for a resource in Failed state.
                type: string
              lifecycleState:
                description: The current state of the Agent.
                type: string
              osVersion:
                description: OS version.
                type: string
              pluginList:
                description: List of plugins associated with the agent.
                items:
                  description: AgentPluginList defines nested fields for Agent.PluginList.
                  properties:
                    agentId:
                      description: Agent identifier.
                      type: string
                    definedTags:
                      additionalProperties:
                        additionalProperties:
                          type: string
                        type: object
                      description: |-
                        The defined tags associated with this resource, if any. Each key is predefined and scoped to namespaces.
                        For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                        Example: `{"Operations": {"CostCenter": "42"}}`
                      type: object
                    freeformTags:
                      additionalProperties:
                        type: string
                      description: |-
                        The freeform tags associated with this resource, if any. Each tag is a simple key-value pair with no
                        predefined name, type, or namespace/scope. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                        Example: `{"Department": "Finance"}`
                      type: object
                    lifecycleDetails:
                      description: A message describing the current state in more
                        detail. For example, it can be used to provide actionable
                        information for a resource in Failed state.
                      type: string
                    lifecycleState:
                      description: The current state of the plugin.
                      type: string
                    name:
                      description: Plugin identifier, which can be renamed.
                      type: string
                    pluginVersion:
                      description: Plugin version.
                      type: string
                    timeCreated:
                      description: The time when the plugin was created. An RFC3339
                        formatted datetime string.
                      type: string
                    timeUpdated:
                      description: The time when the plugin was updated. An RFC3339
                        formatted datetime string.
                      type: string
                  type: object
                type: array
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  The system tags associated with this resource, if any. The system tags are set by Oracle cloud infrastructure services. Each key is predefined and scoped to namespaces.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{orcl-cloud: {free-tier-retain: true}}`
                type: object
              timeCreated:
                description: The time when the Agent was created. An RFC3339 formatted
                  datetime string.
                type: string
              timeExpireAgentKeyInMs:
                description: The time since epoch for when the public key will expire.
                  An RFC3339 formatted datetime string.
                type: string
              timeLastSyncReceived:
                description: The time when the last heartbeat of the Agent was noted.
                  An RFC3339 formatted datetime string.
                type: string
              timeUpdated:
                description: The time when the Agent was updated. An RFC3339 formatted
                  datetime string.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
