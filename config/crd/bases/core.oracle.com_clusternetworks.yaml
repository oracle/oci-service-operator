---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: clusternetworks.core.oracle.com
spec:
  group: core.oracle.com
  names:
    kind: ClusterNetwork
    listKind: ClusterNetworkList
    plural: clusternetworks
    singular: clusternetwork
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the ClusterNetwork
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ClusterNetwork
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ClusterNetwork is the Schema for the clusternetworks API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterNetworkSpec defines the desired state of ClusterNetwork.
            properties:
              clusterConfiguration:
                description: ClusterNetworkClusterConfiguration defines nested fields
                  for ClusterNetwork.ClusterConfiguration.
                properties:
                  hpcIslandId:
                    description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the HPC island.
                    type: string
                  networkBlockIds:
                    description: The list of network block OCIDs.
                    items:
                      type: string
                    type: array
                required:
                - hpcIslandId
                type: object
              compartmentId:
                description: |-
                  The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the compartment
                  containing the cluster network.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              instancePools:
                description: |-
                  The data to create the instance pools in the cluster network.
                  Each cluster network can have one instance pool.
                items:
                  description: ClusterNetworkInstancePool defines nested fields for
                    ClusterNetwork.InstancePool.
                  properties:
                    definedTags:
                      additionalProperties:
                        additionalProperties:
                          type: string
                        type: object
                      description: |-
                        Defined tags for this resource. Each key is predefined and scoped to a
                        namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                        Example: `{"Operations": {"CostCenter": "42"}}`
                      type: object
                    displayName:
                      description: |-
                        A user-friendly name. Does not have to be unique, and it's changeable.
                        Avoid entering confidential information.
                      type: string
                    freeformTags:
                      additionalProperties:
                        type: string
                      description: |-
                        Free-form tags for this resource. Each tag is a simple key-value pair with no
                        predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                        Example: `{"Department": "Finance"}`
                      type: object
                    instanceConfigurationId:
                      description: |-
                        The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the instance configuration
                        associated with the instance pool.
                      type: string
                    size:
                      description: The number of instances that should be in the instance
                        pool.
                      type: integer
                  required:
                  - instanceConfigurationId
                  - size
                  type: object
                type: array
              placementConfiguration:
                description: ClusterNetworkPlacementConfiguration defines nested fields
                  for ClusterNetwork.PlacementConfiguration.
                properties:
                  availabilityDomain:
                    description: |-
                      The availability domain to place instances.
                      Example: `Uocm:PHX-AD-1`
                    type: string
                  placementConstraint:
                    description: The placement constraint when reserving hosts.
                    type: string
                  primarySubnetId:
                    description: |-
                      The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the primary subnet to place instances. This field is deprecated.
                      Use `primaryVnicSubnets` instead to set VNIC data for instances in the pool.
                    type: string
                  primaryVnicSubnets:
                    description: ClusterNetworkPlacementConfigurationPrimaryVnicSubnets
                      defines nested fields for ClusterNetwork.PlacementConfiguration.PrimaryVnicSubnets.
                    properties:
                      ipv6AddressIpv6SubnetCidrPairDetails:
                        description: |-
                          A list of IPv6 prefix ranges from which the VNIC should be assigned an IPv6 address.
                          You can provide only the prefix ranges and OCI will select an available
                          address from the range. You can optionally choose to leave the prefix range empty
                          and instead provide the specific IPv6 address that should be used from within that range.
                        items:
                          description: ClusterNetworkPlacementConfigurationPrimaryVnicSubnetsIpv6AddressIpv6SubnetCidrPairDetail
                            defines nested fields for ClusterNetwork.PlacementConfiguration.PrimaryVnicSubnets.Ipv6AddressIpv6SubnetCidrPairDetail.
                          properties:
                            ipv6SubnetCidr:
                              description: Optional. Used to disambiguate which subnet
                                prefix should be used to create an IPv6 allocation.
                              type: string
                          type: object
                        type: array
                      isAssignIpv6Ip:
                        description: |-
                          Whether to allocate an IPv6 address at instance and VNIC creation from an IPv6 enabled
                          subnet. Default: False. When provided you may optionally provide an IPv6 prefix
                          (`ipv6SubnetCidr`) of your choice to assign the IPv6 address from. If `ipv6SubnetCidr`
                          is not provided then an IPv6 prefix is chosen
                          for you.
                        type: boolean
                      subnetId:
                        description: The subnet OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                          for the secondary VNIC.
                        type: string
                    required:
                    - subnetId
                    type: object
                  secondaryVnicSubnets:
                    description: The set of secondary VNIC data for instances in the
                      pool.
                    items:
                      description: ClusterNetworkPlacementConfigurationSecondaryVnicSubnet
                        defines nested fields for ClusterNetwork.PlacementConfiguration.SecondaryVnicSubnet.
                      properties:
                        displayName:
                          description: |-
                            The display name of the VNIC. This is also used to match against the instance configuration defined
                            secondary VNIC.
                          type: string
                        ipv6AddressIpv6SubnetCidrPairDetails:
                          description: |-
                            A list of IPv6 prefix ranges from which the VNIC should be assigned an IPv6 address.
                            You can provide only the prefix ranges and OCI will select an available
                            address from the range. You can optionally choose to leave the prefix range empty
                            and instead provide the specific IPv6 address that should be used from within that range.
                          items:
                            description: ClusterNetworkPlacementConfigurationSecondaryVnicSubnetIpv6AddressIpv6SubnetCidrPairDetail
                              defines nested fields for ClusterNetwork.PlacementConfiguration.SecondaryVnicSubnet.Ipv6AddressIpv6SubnetCidrPairDetail.
                            properties:
                              ipv6SubnetCidr:
                                description: Optional. Used to disambiguate which
                                  subnet prefix should be used to create an IPv6 allocation.
                                type: string
                            type: object
                          type: array
                        isAssignIpv6Ip:
                          description: |-
                            Whether to allocate an IPv6 address at instance and VNIC creation from an IPv6 enabled
                            subnet. Default: False. When provided you may optionally provide an IPv6 prefix
                            (`ipv6SubnetCidr`) of your choice to assign the IPv6 address from. If `ipv6SubnetCidr`
                            is not provided then an IPv6 prefix is chosen
                            for you.
                          type: boolean
                        subnetId:
                          description: The subnet OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                            for the secondary VNIC.
                          type: string
                      required:
                      - subnetId
                      type: object
                    type: array
                required:
                - availabilityDomain
                type: object
            required:
            - compartmentId
            - instancePools
            - placementConfiguration
            type: object
          status:
            description: ClusterNetworkStatus defines the observed state of ClusterNetwork.
            properties:
              compartmentId:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment containing the cluster network.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              hpcIslandId:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the HPC island used by the cluster network.
                type: string
              id:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the cluster network.
                type: string
              instancePools:
                description: |-
                  The instance pools in the cluster network.
                  Each cluster network can have one instance pool.
                items:
                  description: ClusterNetworkInstancePool defines nested fields for
                    ClusterNetwork.InstancePool.
                  properties:
                    definedTags:
                      additionalProperties:
                        additionalProperties:
                          type: string
                        type: object
                      description: |-
                        Defined tags for this resource. Each key is predefined and scoped to a
                        namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                        Example: `{"Operations": {"CostCenter": "42"}}`
                      type: object
                    displayName:
                      description: |-
                        A user-friendly name. Does not have to be unique, and it's changeable.
                        Avoid entering confidential information.
                      type: string
                    freeformTags:
                      additionalProperties:
                        type: string
                      description: |-
                        Free-form tags for this resource. Each tag is a simple key-value pair with no
                        predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                        Example: `{"Department": "Finance"}`
                      type: object
                    instanceConfigurationId:
                      description: |-
                        The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the instance configuration
                        associated with the instance pool.
                      type: string
                    size:
                      description: The number of instances that should be in the instance
                        pool.
                      type: integer
                  required:
                  - instanceConfigurationId
                  - size
                  type: object
                type: array
              lifecycleState:
                description: The current state of the cluster network.
                type: string
              networkBlockIds:
                description: The list of network block OCIDs of the HPC island.
                items:
                  type: string
                type: array
              placementConfiguration:
                description: ClusterNetworkPlacementConfiguration defines nested fields
                  for ClusterNetwork.PlacementConfiguration.
                properties:
                  availabilityDomain:
                    description: |-
                      The availability domain to place instances.
                      Example: `Uocm:PHX-AD-1`
                    type: string
                  placementConstraint:
                    description: The placement constraint when reserving hosts.
                    type: string
                  primarySubnetId:
                    description: |-
                      The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the primary subnet to place instances. This field is deprecated.
                      Use `primaryVnicSubnets` instead to set VNIC data for instances in the pool.
                    type: string
                  primaryVnicSubnets:
                    description: ClusterNetworkPlacementConfigurationPrimaryVnicSubnets
                      defines nested fields for ClusterNetwork.PlacementConfiguration.PrimaryVnicSubnets.
                    properties:
                      ipv6AddressIpv6SubnetCidrPairDetails:
                        description: |-
                          A list of IPv6 prefix ranges from which the VNIC should be assigned an IPv6 address.
                          You can provide only the prefix ranges and OCI will select an available
                          address from the range. You can optionally choose to leave the prefix range empty
                          and instead provide the specific IPv6 address that should be used from within that range.
                        items:
                          description: ClusterNetworkPlacementConfigurationPrimaryVnicSubnetsIpv6AddressIpv6SubnetCidrPairDetail
                            defines nested fields for ClusterNetwork.PlacementConfiguration.PrimaryVnicSubnets.Ipv6AddressIpv6SubnetCidrPairDetail.
                          properties:
                            ipv6SubnetCidr:
                              description: Optional. Used to disambiguate which subnet
                                prefix should be used to create an IPv6 allocation.
                              type: string
                          type: object
                        type: array
                      isAssignIpv6Ip:
                        description: |-
                          Whether to allocate an IPv6 address at instance and VNIC creation from an IPv6 enabled
                          subnet. Default: False. When provided you may optionally provide an IPv6 prefix
                          (`ipv6SubnetCidr`) of your choice to assign the IPv6 address from. If `ipv6SubnetCidr`
                          is not provided then an IPv6 prefix is chosen
                          for you.
                        type: boolean
                      subnetId:
                        description: The subnet OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                          for the secondary VNIC.
                        type: string
                    required:
                    - subnetId
                    type: object
                  secondaryVnicSubnets:
                    description: The set of secondary VNIC data for instances in the
                      pool.
                    items:
                      description: ClusterNetworkPlacementConfigurationSecondaryVnicSubnet
                        defines nested fields for ClusterNetwork.PlacementConfiguration.SecondaryVnicSubnet.
                      properties:
                        displayName:
                          description: |-
                            The display name of the VNIC. This is also used to match against the instance configuration defined
                            secondary VNIC.
                          type: string
                        ipv6AddressIpv6SubnetCidrPairDetails:
                          description: |-
                            A list of IPv6 prefix ranges from which the VNIC should be assigned an IPv6 address.
                            You can provide only the prefix ranges and OCI will select an available
                            address from the range. You can optionally choose to leave the prefix range empty
                            and instead provide the specific IPv6 address that should be used from within that range.
                          items:
                            description: ClusterNetworkPlacementConfigurationSecondaryVnicSubnetIpv6AddressIpv6SubnetCidrPairDetail
                              defines nested fields for ClusterNetwork.PlacementConfiguration.SecondaryVnicSubnet.Ipv6AddressIpv6SubnetCidrPairDetail.
                            properties:
                              ipv6SubnetCidr:
                                description: Optional. Used to disambiguate which
                                  subnet prefix should be used to create an IPv6 allocation.
                                type: string
                            type: object
                          type: array
                        isAssignIpv6Ip:
                          description: |-
                            Whether to allocate an IPv6 address at instance and VNIC creation from an IPv6 enabled
                            subnet. Default: False. When provided you may optionally provide an IPv6 prefix
                            (`ipv6SubnetCidr`) of your choice to assign the IPv6 address from. If `ipv6SubnetCidr`
                            is not provided then an IPv6 prefix is chosen
                            for you.
                          type: boolean
                        subnetId:
                          description: The subnet OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                            for the secondary VNIC.
                          type: string
                      required:
                      - subnetId
                      type: object
                    type: array
                required:
                - availabilityDomain
                type: object
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time the resource was created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              timeUpdated:
                description: |-
                  The date and time the resource was updated, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
