---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: tableusages.nosql.oracle.com
spec:
  group: nosql.oracle.com
  names:
    kind: TableUsage
    listKind: TableUsageList
    plural: tableusages
    singular: tableusage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the TableUsage
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the TableUsage
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: TableUsage is the Schema for the tableusages API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TableUsageSpec defines the desired state of TableUsage.
            type: object
          status:
            description: TableUsageStatus defines the observed state of TableUsage.
            properties:
              maxShardSizeUsageInPercent:
                description: The percentage of allowed per-shard usage for the table
                  shard with the highest usage.
                type: integer
              readThrottleCount:
                description: |-
                  The number of times reads were throttled due to exceeding
                  the read throughput limit.
                type: integer
              readUnits:
                description: Read throughput during the sampling period.
                type: integer
              secondsInPeriod:
                description: The length of the sampling period.
                type: integer
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              storageInGBs:
                description: The size of the table, in GB.
                type: integer
              storageThrottleCount:
                description: |-
                  The number of times writes were throttled because the table
                  exceeded its size limit.
                type: integer
              timeStarted:
                description: The time stamp of this usage record.
                type: string
              writeThrottleCount:
                description: |-
                  The number of times writes were throttled due to exceeding
                  the write throughput limit.
                type: integer
              writeUnits:
                description: Write throughput during the sampling period.
                type: integer
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
