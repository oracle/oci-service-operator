---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: replicationpolicies.objectstorage.oracle.com
spec:
  group: objectstorage.oracle.com
  names:
    kind: ReplicationPolicy
    listKind: ReplicationPolicyList
    plural: replicationpolicies
    singular: replicationpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      priority: 1
      type: string
    - description: status of the ReplicationPolicy
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ReplicationPolicy
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ReplicationPolicy is the Schema for the replicationpolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ReplicationPolicySpec defines the desired state of ReplicationPolicy.
            properties:
              destinationBucketName:
                description: |-
                  The bucket to replicate to in the destination region. Replication policy creation does not automatically
                  create a destination bucket. Create the destination bucket before creating the policy.
                type: string
              destinationRegionName:
                description: The destination region to replicate to, for example "us-ashburn-1".
                type: string
              name:
                description: The name of the policy. Avoid entering confidential information.
                type: string
            required:
            - destinationBucketName
            - destinationRegionName
            - name
            type: object
          status:
            description: ReplicationPolicyStatus defines the observed state of ReplicationPolicy.
            properties:
              destinationBucketName:
                description: |-
                  The bucket to replicate to in the destination region. Replication policy creation does not automatically
                  create a destination bucket. Create the destination bucket before creating the policy.
                type: string
              destinationRegionName:
                description: The destination region to replicate to, for example "us-ashburn-1".
                type: string
              id:
                description: The id of the replication policy.
                type: string
              name:
                description: The name of the policy.
                type: string
              sdkStatus:
                description: |-
                  The replication status of the policy. If the status is CLIENT_ERROR, once the user fixes the issue
                  described in the status message, the status will become ACTIVE.
                  This uses a distinct JSON name so it can coexist with the OSOK status envelope.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              statusMessage:
                description: A human-readable description of the status.
                type: string
              timeCreated:
                description: The date when the replication policy was created as per
                  RFC 3339 (https://tools.ietf.org/html/rfc3339).
                type: string
              timeLastSync:
                description: Changes made to the source bucket before this time has
                  been replicated.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
