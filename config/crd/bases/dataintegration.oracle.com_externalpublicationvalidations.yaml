---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: externalpublicationvalidations.dataintegration.oracle.com
spec:
  group: dataintegration.oracle.com
  names:
    kind: ExternalPublicationValidation
    listKind: ExternalPublicationValidationList
    plural: externalpublicationvalidations
    singular: externalpublicationvalidation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the ExternalPublicationValidation
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ExternalPublicationValidation
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ExternalPublicationValidation is the Schema for the externalpublicationvalidations
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ExternalPublicationValidationSpec defines the desired state
              of ExternalPublicationValidation.
            properties:
              key:
                description: Generated key that can be used in API calls to identify
                  the task. On scenarios where reference to the task is needed, a
                  value can be passed in the create operation.
                type: string
            type: object
          status:
            description: ExternalPublicationValidationStatus defines the observed
              state of ExternalPublicationValidation.
            properties:
              errorMessageCount:
                description: Total number of validation error messages.
                type: integer
              infoMessageCount:
                description: Total number of validation information messages.
                type: integer
              key:
                description: Objects use a 36 character key as unique ID. It is system
                  generated and cannot be modified.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              totalMessageCount:
                description: Total number of validation messages.
                type: integer
              validationMessages:
                additionalProperties:
                  items:
                    description: ExternalPublicationValidationValidationMessages defines
                      nested fields for ExternalPublicationValidation.ValidationMessages.
                    properties:
                      level:
                        description: The total number of validation messages.
                        type: string
                      messageKey:
                        description: The validation message key.
                        type: string
                      validationMessage:
                        description: The validation message.
                        type: string
                    type: object
                  type: array
                description: Detailed information of the data flow object validation.
                type: object
              warnMessageCount:
                description: Total number of validation warning messages.
                type: integer
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
