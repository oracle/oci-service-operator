---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: senders.email.oracle.com
spec:
  group: email.oracle.com
  names:
    kind: Sender
    listKind: SenderList
    plural: senders
    singular: sender
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the Sender
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Sender
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Sender is the Schema for the senders API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SenderSpec defines the desired state of Sender.
            properties:
              compartmentId:
                description: The OCID of the compartment that contains the sender.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              emailAddress:
                description: The email address of the sender.
                type: string
              emailIpPoolId:
                description: An optional field. The IpPool OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  used to submit an email by Email Delivery when sent from this sender.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
            required:
            - compartmentId
            - emailAddress
            type: object
          status:
            description: SenderStatus defines the observed state of Sender.
            properties:
              compartmentId:
                description: The OCID for the compartment.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              emailAddress:
                description: Email address of the sender.
                type: string
              emailDomainId:
                description: The email domain used to assert responsibility for emails
                  sent from this sender.
                type: string
              emailIpPoolId:
                description: The IpPool OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  used to submit an email by Email Delivery when sent from this sender.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The unique OCID of the sender.
                type: string
              isSpf:
                description: |-
                  Value of the SPF field. For more information about SPF, please see
                  SPF Authentication (https://docs.oracle.com/iaas/Content/Email/Concepts/overview.htm#components).
                type: boolean
              lifecycleState:
                description: The sender's current lifecycle state.
                type: string
              locks:
                description: Locks associated with this resource.
                items:
                  description: SenderLock defines nested fields for Sender.Lock.
                  properties:
                    compartmentId:
                      description: The lock compartment ID.
                      type: string
                    message:
                      description: |-
                        A message added by the lock creator. The message typically gives an
                        indication of why the resource is locked.
                      type: string
                    relatedResourceId:
                      description: The resource ID that is locking this resource.
                        Indicates that deleting this resource removes the lock.
                      type: string
                    timeCreated:
                      description: Indicates when the lock was created, in the format
                        defined by RFC 3339.
                      type: string
                    type:
                      description: Lock type.
                      type: string
                  type: object
                type: array
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Usage of system tag keys. These predefined keys are scoped to namespaces.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: |-
                  The date and time the approved sender was added in "YYYY-MM-ddThh:mmZ"
                  format with a Z offset, as defined by RFC 3339 (https://tools.ietf.org/html/rfc3339).
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
