---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: resolverendpoints.dns.oracle.com
spec:
  group: dns.oracle.com
  names:
    kind: ResolverEndpoint
    listKind: ResolverEndpointList
    plural: resolverendpoints
    singular: resolverendpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      priority: 1
      type: string
    - description: status of the ResolverEndpoint
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ResolverEndpoint
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ResolverEndpoint is the Schema for the resolverendpoints API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ResolverEndpointSpec defines the desired state of ResolverEndpoint.
            properties:
              endpointType:
                type: string
              forwardingAddress:
                description: |-
                  An IP address from which forwarded queries may be sent. For VNIC endpoints, this IP address must be part
                  of the subnet and will be assigned by the system if unspecified when isForwarding is true.
                type: string
              isForwarding:
                description: A Boolean flag indicating whether or not the resolver
                  endpoint is for forwarding.
                type: boolean
              isListening:
                description: A Boolean flag indicating whether or not the resolver
                  endpoint is for listening.
                type: boolean
              jsonData:
                type: string
              listeningAddress:
                description: |-
                  An IP address to listen to queries on. For VNIC endpoints this IP address must be part of the
                  subnet and will be assigned by the system if unspecified when isListening is true.
                type: string
              name:
                description: The name of the resolver endpoint. Must be unique, case-insensitive,
                  within the resolver.
                type: string
              nsgIds:
                description: |-
                  An array of network security group OCIDs for the resolver endpoint. These must be part of the VCN that the
                  resolver endpoint is a part of.
                items:
                  type: string
                type: array
              subnetId:
                description: The OCID of a subnet. Must be part of the VCN that the
                  resolver is attached to.
                type: string
            required:
            - isForwarding
            - isListening
            - name
            - subnetId
            type: object
          status:
            description: ResolverEndpointStatus defines the observed state of ResolverEndpoint.
            properties:
              compartmentId:
                description: |-
                  The OCID of the owning compartment. This will match the resolver that the resolver endpoint is under
                  and will be updated if the resolver's compartment is changed.
                type: string
              endpointType:
                type: string
              forwardingAddress:
                description: |-
                  An IP address from which forwarded queries may be sent. For VNIC endpoints, this IP address must be part
                  of the subnet and will be assigned by the system if unspecified when isForwarding is true.
                type: string
              isForwarding:
                description: A Boolean flag indicating whether or not the resolver
                  endpoint is for forwarding.
                type: boolean
              isListening:
                description: A Boolean flag indicating whether or not the resolver
                  endpoint is for listening.
                type: boolean
              jsonData:
                type: string
              lifecycleState:
                description: The current state of the resource.
                type: string
              listeningAddress:
                description: |-
                  An IP address to listen to queries on. For VNIC endpoints this IP address must be part of the
                  subnet and will be assigned by the system if unspecified when isListening is true.
                type: string
              name:
                description: The name of the resolver endpoint. Must be unique, case-insensitive,
                  within the resolver.
                type: string
              nsgIds:
                description: |-
                  An array of network security group OCIDs for the resolver endpoint. These must be part of the VCN that the
                  resolver endpoint is a part of.
                items:
                  type: string
                type: array
              self:
                description: The canonical absolute URL of the resource.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              subnetId:
                description: The OCID of a subnet. Must be part of the VCN that the
                  resolver is attached to.
                type: string
              timeCreated:
                description: |-
                  The date and time the resource was created in "YYYY-MM-ddThh:mm:ssZ" format
                  with a Z offset, as defined by RFC 3339.
                  **Example:** `2016-07-22T17:23:59:60Z`
                type: string
              timeUpdated:
                description: |-
                  The date and time the resource was last updated in "YYYY-MM-ddThh:mm:ssZ"
                  format with a Z offset, as defined by RFC 3339.
                  **Example:** `2016-07-22T17:23:59:60Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
