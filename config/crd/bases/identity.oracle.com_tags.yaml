---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: tags.identity.oracle.com
spec:
  group: identity.oracle.com
  names:
    kind: Tag
    listKind: TagList
    plural: tags
    singular: tag
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      priority: 1
      type: string
    - description: status of the Tag
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Tag
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Tag is the Schema for the tags API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TagSpec defines the desired state of Tag.
            properties:
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              description:
                description: The description you assign to the tag during creation.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              isCostTracking:
                description: Indicates whether the tag is enabled for cost tracking.
                type: boolean
              isRetired:
                description: |-
                  Whether the tag is retired.
                  See Retiring Key Definitions and Namespace Definitions (https://docs.cloud.oracle.com/Content/Tagging/Tasks/managingtagsandtagnamespaces.htm#retiringkeys).
                type: boolean
              name:
                description: |-
                  The name you assign to the tag during creation. This is the tag key definition.
                  The name must be unique within the tag namespace and cannot be changed.
                type: string
              validator:
                description: TagValidator defines nested fields for Tag.Validator.
                properties:
                  jsonData:
                    type: string
                  validatorType:
                    type: string
                  values:
                    description: The list of allowed values for a definedTag value.
                    items:
                      type: string
                    type: array
                type: object
            required:
            - description
            - name
            type: object
          status:
            description: TagStatus defines the observed state of Tag.
            properties:
              compartmentId:
                description: The OCID of the compartment that contains the tag definition.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              description:
                description: The description you assign to the tag.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The OCID of the tag definition.
                type: string
              isCostTracking:
                description: Indicates whether the tag is enabled for cost tracking.
                type: boolean
              isRetired:
                description: |-
                  Indicates whether the tag is retired.
                  See Retiring Key Definitions and Namespace Definitions (https://docs.cloud.oracle.com/Content/Tagging/Tasks/managingtagsandtagnamespaces.htm#retiringkeys).
                type: boolean
              lifecycleState:
                description: The tag's current state. After creating a tag, make sure
                  its `lifecycleState` is ACTIVE before using it. After retiring a
                  tag, make sure its `lifecycleState` is INACTIVE before using it.
                  If you delete a tag, you cannot delete another tag until the deleted
                  tag's `lifecycleState` changes from DELETING to DELETED.
                type: string
              name:
                description: |-
                  The name assigned to the tag during creation. This is the tag key definition.
                  The name must be unique within the tag namespace and cannot be changed.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              tagNamespaceId:
                description: The OCID of the namespace that contains the tag definition.
                type: string
              tagNamespaceName:
                description: The name of the tag namespace that contains the tag definition.
                type: string
              timeCreated:
                description: |-
                  Date and time the tag was created, in the format defined by RFC3339.
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              validator:
                description: TagValidator defines nested fields for Tag.Validator.
                properties:
                  jsonData:
                    type: string
                  validatorType:
                    type: string
                  values:
                    description: The list of allowed values for a definedTag value.
                    items:
                      type: string
                    type: array
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
