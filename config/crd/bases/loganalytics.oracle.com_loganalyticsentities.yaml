---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: loganalyticsentities.loganalytics.oracle.com
spec:
  group: loganalytics.oracle.com
  names:
    kind: LogAnalyticsEntity
    listKind: LogAnalyticsEntityList
    plural: loganalyticsentities
    singular: loganalyticsentity
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      priority: 1
      type: string
    - description: status of the LogAnalyticsEntity
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the LogAnalyticsEntity
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: LogAnalyticsEntity is the Schema for the loganalyticsentities
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LogAnalyticsEntitySpec defines the desired state of LogAnalyticsEntity.
            properties:
              cloudResourceId:
                description: |-
                  The OCID of the Cloud resource which this entity is a representation of. This may be blank when the entity
                  represents a non-cloud resource that the customer may have on their premises.
                type: string
              compartmentId:
                description: Compartment Identifier OCID  (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm).
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              entityTypeName:
                description: Log analytics entity type name.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              hostname:
                description: |-
                  The hostname where the entity represented here is actually present. This would be the output one would get if
                  they run `echo $HOSTNAME` on Linux or an equivalent OS command. This may be different from
                  management agents host since logs may be collected remotely.
                type: string
              managementAgentId:
                description: The OCID of the Management Agent.
                type: string
              metadata:
                description: LogAnalyticsEntityMetadata defines nested fields for
                  LogAnalyticsEntity.Metadata.
                properties:
                  items:
                    description: An array of entity metadata details.
                    items:
                      description: LogAnalyticsEntityMetadataItem defines nested fields
                        for LogAnalyticsEntity.Metadata.Item.
                      properties:
                        name:
                          description: The metadata name.
                          type: string
                        type:
                          description: The metadata type.
                          type: string
                        value:
                          description: The metadata value.
                          type: string
                      type: object
                    type: array
                type: object
              name:
                description: Log analytics entity name.
                type: string
              properties:
                additionalProperties:
                  type: string
                description: The name/value pairs for parameter values to be used
                  in file patterns specified in log sources.
                type: object
              sourceId:
                description: This indicates the type of source. It is primarily for
                  Enterprise Manager Repository ID.
                type: string
              timeLastDiscovered:
                description: The date and time the resource was last discovered, in
                  the format defined by RFC3339.
                type: string
              timezoneRegion:
                description: The timezone region of the log analytics entity.
                type: string
            required:
            - compartmentId
            - entityTypeName
            - name
            type: object
          status:
            description: LogAnalyticsEntityStatus defines the observed state of LogAnalyticsEntity.
            properties:
              areLogsCollected:
                description: The Boolean flag to indicate if logs are collected for
                  an entity for log analytics usage.
                type: boolean
              associatedSourcesCount:
                description: The count of associated log sources for a given log analytics
                  entity.
                type: integer
              cloudResourceId:
                description: |-
                  The OCID of the Cloud resource which this entity is a representation of. This may be blank when the entity
                  represents a non-cloud resource that the customer may have on their premises.
                type: string
              compartmentId:
                description: Compartment Identifier OCID  (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm).
                type: string
              creationSource:
                description: LogAnalyticsEntityCreationSource defines nested fields
                  for LogAnalyticsEntity.CreationSource.
                properties:
                  details:
                    description: |-
                      This will provide additional details for source of auto-creation. For example, if entity is auto-created
                      by enterprise manager bridge, this field provides additional detail on enterprise manager that contributed
                      to the entity auto-creation.
                    type: string
                  type:
                    description: Source that auto-created the entity.
                    type: string
                type: object
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              entityTypeInternalName:
                description: Internal name for the log analytics entity type.
                type: string
              entityTypeName:
                description: Log analytics entity type name.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              hostname:
                description: |-
                  The hostname where the entity represented here is actually present. This would be the output one would get if
                  they run `echo $HOSTNAME` on Linux or an equivalent OS command. This may be different from
                  management agents host since logs may be collected remotely.
                type: string
              id:
                description: |-
                  The log analytics entity OCID. This ID is a reference used by log analytics features and it represents
                  a resource that is provisioned and managed by the customer on their premises or on the cloud.
                type: string
              lifecycleDetails:
                description: lifecycleDetails has additional information regarding
                  substeps such as management agent plugin deployment.
                type: string
              lifecycleState:
                description: The current state of the log analytics entity.
                type: string
              managementAgentCompartmentId:
                description: Management agent (management-agents resource kind) compartment
                  OCID
                type: string
              managementAgentDisplayName:
                description: Management agent (management-agents resource kind) display
                  name
                type: string
              managementAgentId:
                description: The OCID of the Management Agent.
                type: string
              metadata:
                description: LogAnalyticsEntityMetadata defines nested fields for
                  LogAnalyticsEntity.Metadata.
                properties:
                  items:
                    description: An array of entity metadata details.
                    items:
                      description: LogAnalyticsEntityMetadataItem defines nested fields
                        for LogAnalyticsEntity.Metadata.Item.
                      properties:
                        name:
                          description: The metadata name.
                          type: string
                        type:
                          description: The metadata type.
                          type: string
                        value:
                          description: The metadata value.
                          type: string
                      type: object
                    type: array
                type: object
              name:
                description: Log analytics entity name.
                type: string
              properties:
                additionalProperties:
                  type: string
                description: The name/value pairs for parameter values to be used
                  in file patterns specified in log sources.
                type: object
              sourceId:
                description: This indicates the type of source. It is primarily for
                  Enterprise Manager Repository ID.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: The date and time the resource was created, in the format
                  defined by RFC3339.
                type: string
              timeLastDiscovered:
                description: The date and time the resource was last discovered, in
                  the format defined by RFC3339.
                type: string
              timeUpdated:
                description: The date and time the resource was last updated, in the
                  format defined by RFC3339.
                type: string
              timezoneRegion:
                description: The timezone region of the log analytics entity.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
