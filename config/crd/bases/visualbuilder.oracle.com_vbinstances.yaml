---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: vbinstances.visualbuilder.oracle.com
spec:
  group: visualbuilder.oracle.com
  names:
    kind: VbInstance
    listKind: VbInstanceList
    plural: vbinstances
    singular: vbinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the VbInstance
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the VbInstance
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: VbInstance is the Schema for the vbinstances API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VbInstanceSpec defines the desired state of VbInstance.
            properties:
              alternateCustomEndpoints:
                description: |-
                  A list of alternate custom endpoints to be used for the vb instance URL
                  (contact Oracle for alternateCustomEndpoints availability for a specific instance).
                items:
                  description: VbInstanceAlternateCustomEndpoint defines nested fields
                    for VbInstance.AlternateCustomEndpoint.
                  properties:
                    certificateSecretId:
                      description: |-
                        Optional OCID of a vault/secret containing a private SSL certificate bundle to be used for the custom hostname.
                        All certificates should be stored in a single base64 encoded secret
                        Note the update will fail if this is not a valid certificate.
                      type: string
                    hostname:
                      description: A custom hostname to be used for the vb instance
                        URL, in FQDN format.
                      type: string
                  required:
                  - hostname
                  type: object
                type: array
              compartmentId:
                description: Compartment Identifier.
                type: string
              consumptionModel:
                description: Optional parameter specifying which entitlement to use
                  for billing purposes. Only required if the account possesses more
                  than one entitlement.
                type: string
              customEndpoint:
                description: VbInstanceCustomEndpoint defines nested fields for VbInstance.CustomEndpoint.
                properties:
                  certificateSecretId:
                    description: |-
                      Optional OCID of a vault/secret containing a private SSL certificate bundle to be used for the custom hostname.
                      All certificates should be stored in a single base64 encoded secret
                      Note the update will fail if this is not a valid certificate.
                    type: string
                  hostname:
                    description: A custom hostname to be used for the vb instance
                      URL, in FQDN format.
                    type: string
                required:
                - hostname
                type: object
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Usage of predefined tag keys. These predefined keys are scoped to
                  namespaces.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              displayName:
                description: Vb Instance Identifier.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name,
                  type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              idcsOpenId:
                description: Encrypted IDCS Open ID token. This is required for pre-UCPIS
                  cloud accounts, but not UCPIS, hence not a required parameter
                type: string
              isVisualBuilderEnabled:
                description: Visual Builder is enabled or not.
                type: boolean
              networkEndpointDetails:
                description: VbInstanceNetworkEndpointDetails defines nested fields
                  for VbInstance.NetworkEndpointDetails.
                properties:
                  allowlistedHttpIps:
                    description: |-
                      Source IP addresses or IP address ranges ingress rules. (ex: "168.122.59.5/32", "10.20.30.0/26")
                      An invalid IP or CIDR block will result in a 400 response.
                    items:
                      type: string
                    type: array
                  allowlistedHttpVcns:
                    description: Virtual Cloud Networks allowed to access this network
                      endpoint.
                    items:
                      description: VbInstanceNetworkEndpointDetailsAllowlistedHttpVcn
                        defines nested fields for VbInstance.NetworkEndpointDetails.AllowlistedHttpVcn.
                      properties:
                        allowlistedIpCidrs:
                          description: |-
                            Source IP addresses or IP address ranges ingress rules. (ex: "168.122.59.5/32", "10.20.30.0/26")
                            An invalid IP or CIDR block will result in a 400 response.
                          items:
                            type: string
                          type: array
                        id:
                          description: The Virtual Cloud Network OCID.
                          type: string
                      required:
                      - id
                      type: object
                    type: array
                  jsonData:
                    type: string
                  networkEndpointType:
                    type: string
                  networkSecurityGroupIds:
                    description: Network Security Group OCIDs for the Private Endpoint.
                    items:
                      type: string
                    type: array
                  privateEndpointIp:
                    description: The IP address to be assigned to Private Endpoint
                    type: string
                  subnetId:
                    description: The subnet OCID for the private endpoint.
                    type: string
                type: object
              nodeCount:
                description: The number of Nodes
                type: integer
            required:
            - compartmentId
            - displayName
            - nodeCount
            type: object
          status:
            description: VbInstanceStatus defines the observed state of VbInstance.
            properties:
              alternateCustomEndpoints:
                description: A list of alternate custom endpoints used for the vb
                  instance URL.
                items:
                  description: VbInstanceAlternateCustomEndpoint defines nested fields
                    for VbInstance.AlternateCustomEndpoint.
                  properties:
                    certificateSecretId:
                      description: |-
                        Optional OCID of a vault/secret containing a private SSL certificate bundle to be used for the custom hostname.
                        All certificates should be stored in a single base64 encoded secret
                        Note the update will fail if this is not a valid certificate.
                      type: string
                    hostname:
                      description: A custom hostname to be used for the vb instance
                        URL, in FQDN format.
                      type: string
                  required:
                  - hostname
                  type: object
                type: array
              compartmentId:
                description: Compartment Identifier.
                type: string
              consumptionModel:
                description: The entitlement used for billing purposes.
                type: string
              customEndpoint:
                description: VbInstanceCustomEndpoint defines nested fields for VbInstance.CustomEndpoint.
                properties:
                  certificateSecretId:
                    description: |-
                      Optional OCID of a vault/secret containing a private SSL certificate bundle to be used for the custom hostname.
                      All certificates should be stored in a single base64 encoded secret
                      Note the update will fail if this is not a valid certificate.
                    type: string
                  hostname:
                    description: A custom hostname to be used for the vb instance
                      URL, in FQDN format.
                    type: string
                required:
                - hostname
                type: object
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              displayName:
                description: Vb Instance Identifier, can be renamed.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: Unique identifier that is immutable on creation.
                type: string
              instanceUrl:
                description: The Vb Instance URL.
                type: string
              isVisualBuilderEnabled:
                description: Visual Builder is enabled or not.
                type: boolean
              lifecycleState:
                description: The current state of the vb instance.
                type: string
              managementNatGatewayIp:
                description: The NAT gateway IP address for the VB management VCN
                type: string
              managementVcnId:
                description: The Oracle Cloud ID (OCID) of the Visual Builder management
                  VCN
                type: string
              networkEndpointDetails:
                description: VbInstanceNetworkEndpointDetails defines nested fields
                  for VbInstance.NetworkEndpointDetails.
                properties:
                  allowlistedHttpIps:
                    description: |-
                      Source IP addresses or IP address ranges ingress rules. (ex: "168.122.59.5/32", "10.20.30.0/26")
                      An invalid IP or CIDR block will result in a 400 response.
                    items:
                      type: string
                    type: array
                  allowlistedHttpVcns:
                    description: Virtual Cloud Networks allowed to access this network
                      endpoint.
                    items:
                      description: VbInstanceNetworkEndpointDetailsAllowlistedHttpVcn
                        defines nested fields for VbInstance.NetworkEndpointDetails.AllowlistedHttpVcn.
                      properties:
                        allowlistedIpCidrs:
                          description: |-
                            Source IP addresses or IP address ranges ingress rules. (ex: "168.122.59.5/32", "10.20.30.0/26")
                            An invalid IP or CIDR block will result in a 400 response.
                          items:
                            type: string
                          type: array
                        id:
                          description: The Virtual Cloud Network OCID.
                          type: string
                      required:
                      - id
                      type: object
                    type: array
                  jsonData:
                    type: string
                  networkEndpointType:
                    type: string
                  networkSecurityGroupIds:
                    description: Network Security Group OCIDs for the Private Endpoint.
                    items:
                      type: string
                    type: array
                  privateEndpointIp:
                    description: The IP address to be assigned to Private Endpoint
                    type: string
                  subnetId:
                    description: The subnet OCID for the private endpoint.
                    type: string
                type: object
              nodeCount:
                description: The number of Nodes
                type: integer
              serviceNatGatewayIp:
                description: The NAT gateway IP address for the VB service VCN
                type: string
              serviceVcnId:
                description: The Oracle Cloud ID (OCID) of the Visual Builder service
                  VCN
                type: string
              stateMessage:
                description: An message describing the current state in more detail.
                  For example, can be used to provide actionable information for a
                  resource in Failed state.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Usage of system tag keys. These predefined keys are scoped to namespaces.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: The time the the VbInstance was created. An RFC3339 formatted
                  datetime string.
                type: string
              timeUpdated:
                description: The time the VbInstance was updated. An RFC3339 formatted
                  datetime string.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
