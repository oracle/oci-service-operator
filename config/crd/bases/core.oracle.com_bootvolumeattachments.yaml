---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: bootvolumeattachments.core.oracle.com
spec:
  group: core.oracle.com
  names:
    kind: BootVolumeAttachment
    listKind: BootVolumeAttachmentList
    plural: bootvolumeattachments
    singular: bootvolumeattachment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the BootVolumeAttachment
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the BootVolumeAttachment
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: BootVolumeAttachment is the Schema for the bootvolumeattachments
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BootVolumeAttachmentSpec defines the desired state of BootVolumeAttachment.
            type: object
          status:
            description: BootVolumeAttachmentStatus defines the observed state of
              BootVolumeAttachment.
            properties:
              availabilityDomain:
                description: |-
                  The availability domain of an instance.
                  Example: `Uocm:PHX-AD-1`
                type: string
              bootVolumeId:
                description: The OCID of the boot volume.
                type: string
              compartmentId:
                description: The OCID of the compartment.
                type: string
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              encryptionInTransitType:
                description: |-
                  Refer the top-level definition of encryptionInTransitType.
                  The default value is NONE.
                type: string
              id:
                description: The OCID of the boot volume attachment.
                type: string
              instanceId:
                description: The OCID of the instance the boot volume is attached
                  to.
                type: string
              isPvEncryptionInTransitEnabled:
                description: Whether in-transit encryption for the boot volume's paravirtualized
                  attachment is enabled or not.
                type: boolean
              lifecycleState:
                description: The current state of the boot volume attachment.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time the boot volume was created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
