---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: agentdependencies.cloudbridge.oracle.com
spec:
  group: cloudbridge.oracle.com
  names:
    kind: AgentDependency
    listKind: AgentDependencyList
    plural: agentdependencies
    singular: agentdependency
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the AgentDependency
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the AgentDependency
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: AgentDependency is the Schema for the agentdependencies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgentDependencySpec defines the desired state of AgentDependency.
            properties:
              bucket:
                description: Object storage bucket where the dependency is uploaded.
                type: string
              compartmentId:
                description: Compartment identifier.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  The defined tags associated with this resource, if any. Each key is predefined and scoped to namespaces.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              dependencyName:
                description: Name of the dependency type. This should match the whitelisted
                  enum of dependency names.
                type: string
              dependencyVersion:
                description: Version of the Agent dependency.
                type: string
              description:
                description: Description about the Agent dependency.
                type: string
              displayName:
                description: Display name of the Agent dependency.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  The freeform tags associated with this resource, if any. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace/scope. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              namespace:
                description: Object storage namespace associated with the customer's
                  tenancy.
                type: string
              objectName:
                description: Name of the dependency object uploaded by the customer.
                type: string
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  The system tags associated with this resource, if any. The system tags are set by Oracle cloud infrastructure services. Each key is predefined and scoped to namespaces.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{orcl-cloud: {free-tier-retain: true}}`
                type: object
            required:
            - bucket
            - compartmentId
            - dependencyName
            - displayName
            - namespace
            - objectName
            type: object
          status:
            description: AgentDependencyStatus defines the observed state of AgentDependency.
            properties:
              bucket:
                description: Object storage bucket where the Agent dependency is uploaded.
                type: string
              checksum:
                description: The checksum associated with the dependency object returned
                  by Object Storage.
                type: string
              compartmentId:
                description: Compartment identifier
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  The defined tags associated with this resource, if any. Each key is predefined and scoped to namespaces.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              dependencyName:
                description: Name of the dependency type. This should match the whitelisted
                  enum of dependency names.
                type: string
              dependencyVersion:
                description: Version of the Agent dependency.
                type: string
              description:
                description: Description about the Agent dependency.
                type: string
              displayName:
                description: Display name of the Agent dependency.
                type: string
              eTag:
                description: The eTag associated with the dependency object returned
                  by Object Storage.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  The freeform tags associated with this resource, if any. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace/scope. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: Unique identifier that is immutable on creation.
                type: string
              lifecycleDetails:
                description: A message describing the current state in more detail.
                  For example, it can be used to provide actionable information for
                  a resource in Failed state.
                type: string
              lifecycleState:
                description: The current state of AgentDependency.
                type: string
              namespace:
                description: Object storage namespace associated with the customer's
                  tenancy.
                type: string
              objectName:
                description: Name of the dependency object uploaded by the customer.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  The system tags associated with this resource, if any. The system tags are set by Oracle cloud infrastructure services. Each key is predefined and scoped to namespaces.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{orcl-cloud: {free-tier-retain: true}}`
                type: object
              timeCreated:
                description: The time when the AgentDependency was created. An RFC3339
                  formatted datetime string.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
