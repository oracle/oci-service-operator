---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: securityattributes.securityattribute.oracle.com
spec:
  group: securityattribute.oracle.com
  names:
    kind: SecurityAttribute
    listKind: SecurityAttributeList
    plural: securityattributes
    singular: securityattribute
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      priority: 1
      type: string
    - description: status of the SecurityAttribute
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the SecurityAttribute
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: SecurityAttribute is the Schema for the securityattributes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityAttributeSpec defines the desired state of SecurityAttribute.
            properties:
              description:
                description: The description you assign to the security attribute
                  during creation.
                type: string
              isRetired:
                description: |-
                  Whether the security attribute is retired.
                  See Managing Security Attributes (https://docs.oracle.com/iaas/Content/zero-trust-packet-routing/managing-security-attributes.htm).
                type: boolean
              name:
                description: |-
                  The name you assign to the security attribute during creation. This is the security attribute key.
                  The name must be unique within the namespace and cannot be changed.
                type: string
              validator:
                description: SecurityAttributeValidator defines nested fields for
                  SecurityAttribute.Validator.
                properties:
                  jsonData:
                    type: string
                  validatorType:
                    type: string
                  values:
                    description: The list of allowed values for a security attribute
                      value.
                    items:
                      type: string
                    type: array
                type: object
            required:
            - description
            - name
            type: object
          status:
            description: SecurityAttributeStatus defines the observed state of SecurityAttribute.
            properties:
              compartmentId:
                description: The OCID of the compartment that contains the security
                  attribute definition.
                type: string
              description:
                description: The description of the security attribute.
                type: string
              id:
                description: The OCID of the security attribute definition.
                type: string
              isRetired:
                description: |-
                  Indicates whether the security attribute is retired.
                  See Managing Security Attribute Namespaces (https://docs.oracle.com/iaas/Content/zero-trust-packet-routing/managing-security-attribute-namespaces.htm).
                type: boolean
              lifecycleState:
                description: The security attribute's current state. After creating
                  a security attribute, make sure its `lifecycleState` is ACTIVE before
                  using it. After retiring a security attribute, make sure its `lifecycleState`
                  is INACTIVE before using it. If you delete a security attribute,
                  you cannot delete another security attribute until the deleted tag's
                  `lifecycleState` changes from DELETING to DELETED.
                type: string
              name:
                description: |-
                  The name assigned to the security attribute during creation. This is the security attribute key.
                  The name must be unique within the security attribute namespace and cannot be changed.
                type: string
              securityAttributeNamespaceId:
                description: The OCID of the security attribute namespace that contains
                  the security attribute definition.
                type: string
              securityAttributeNamespaceName:
                description: The name of the security attribute namespace that contains
                  the security attribute.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  Date and time the security attribute was created, in the format defined by RFC3339.
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              type:
                description: The data type of the security attribute.
                type: string
              validator:
                description: SecurityAttributeValidator defines nested fields for
                  SecurityAttribute.Validator.
                properties:
                  jsonData:
                    type: string
                  validatorType:
                    type: string
                  values:
                    description: The list of allowed values for a security attribute
                      value.
                    items:
                      type: string
                    type: array
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
