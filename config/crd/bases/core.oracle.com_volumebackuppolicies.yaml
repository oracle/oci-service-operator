---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: volumebackuppolicies.core.oracle.com
spec:
  group: core.oracle.com
  names:
    kind: VolumeBackupPolicy
    listKind: VolumeBackupPolicyList
    plural: volumebackuppolicies
    singular: volumebackuppolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the VolumeBackupPolicy
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the VolumeBackupPolicy
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: VolumeBackupPolicy is the Schema for the volumebackuppolicies
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VolumeBackupPolicySpec defines the desired state of VolumeBackupPolicy.
            properties:
              compartmentId:
                description: The OCID of the compartment.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              destinationRegion:
                description: |-
                  The paired destination region for copying scheduled backups to. Example: `us-ashburn-1`.
                  See Region Pairs (https://docs.cloud.oracle.com/iaas/Content/Block/Tasks/schedulingvolumebackups.htm#RegionPairs) for details about paired regions.
                type: string
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              schedules:
                description: |-
                  The collection of schedules for the volume backup policy. See
                  see Schedules (https://docs.cloud.oracle.com/iaas/Content/Block/Tasks/schedulingvolumebackups.htm#schedules) in
                  Policy-Based Backups (https://docs.cloud.oracle.com/iaas/Content/Block/Tasks/schedulingvolumebackups.htm) for more information.
                items:
                  description: VolumeBackupPolicySchedule defines nested fields for
                    VolumeBackupPolicy.Schedule.
                  properties:
                    backupType:
                      description: The type of volume backup to create.
                      type: string
                    dayOfMonth:
                      description: The day of the month to schedule the volume backup.
                      type: integer
                    dayOfWeek:
                      description: The day of the week to schedule the volume backup.
                      type: string
                    hourOfDay:
                      description: The hour of the day to schedule the volume backup.
                      type: integer
                    month:
                      description: The month of the year to schedule the volume backup.
                      type: string
                    offsetSeconds:
                      description: |-
                        The number of seconds that the volume backup start
                        time should be shifted from the default interval boundaries specified by
                        the period. The volume backup start time is the frequency start time plus the offset.
                      type: integer
                    offsetType:
                      description: |-
                        Indicates how the offset is defined. If value is `STRUCTURED`,
                        then `hourOfDay`, `dayOfWeek`, `dayOfMonth`, and `month` fields are used
                        and `offsetSeconds` will be ignored in requests and users should ignore its
                        value from the responses.
                        `hourOfDay` is applicable for periods `ONE_DAY`,
                        `ONE_WEEK`, `ONE_MONTH` and `ONE_YEAR`.
                        `dayOfWeek` is applicable for period
                        `ONE_WEEK`.
                        `dayOfMonth` is applicable for periods `ONE_MONTH` and `ONE_YEAR`.
                        'month' is applicable for period 'ONE_YEAR'.
                        They will be ignored in the requests for inapplicable periods.
                        If value is `NUMERIC_SECONDS`, then `offsetSeconds`
                        will be used for both requests and responses and the structured fields will be
                        ignored in the requests and users should ignore their values from the responses.
                        For clients using older versions of Apis and not sending `offsetType` in their
                        requests, the behaviour is just like `NUMERIC_SECONDS`.
                      type: string
                    period:
                      description: The volume backup frequency.
                      type: string
                    retentionSeconds:
                      description: How long, in seconds, to keep the volume backups
                        created by this schedule.
                      type: integer
                    timeZone:
                      description: Specifies what time zone is the schedule in
                      type: string
                  required:
                  - backupType
                  - period
                  - retentionSeconds
                  type: object
                type: array
            required:
            - compartmentId
            type: object
          status:
            description: VolumeBackupPolicyStatus defines the observed state of VolumeBackupPolicy.
            properties:
              id:
                description: The OCID of the volume backup policy.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: The date and time the volume backup policy was created.
                  Format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
