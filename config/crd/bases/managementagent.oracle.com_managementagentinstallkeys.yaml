---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: managementagentinstallkeys.managementagent.oracle.com
spec:
  group: managementagent.oracle.com
  names:
    kind: ManagementAgentInstallKey
    listKind: ManagementAgentInstallKeyList
    plural: managementagentinstallkeys
    singular: managementagentinstallkey
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the ManagementAgentInstallKey
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ManagementAgentInstallKey
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ManagementAgentInstallKey is the Schema for the managementagentinstallkeys
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ManagementAgentInstallKeySpec defines the desired state of
              ManagementAgentInstallKey.
            properties:
              allowedKeyInstallCount:
                description: Total number of install for this keys
                type: integer
              compartmentId:
                description: Compartment Identifier
                type: string
              displayName:
                description: Management Agent install Key Name
                type: string
              isKeyActive:
                description: if set to true the install key state would be set to
                  Active and if false to Inactive
                type: boolean
              isUnlimited:
                description: If set to true, the install key has no expiration date
                  or usage limit. Defaults to false
                type: boolean
              timeExpires:
                description: date after which key would expire after creation
                type: string
            required:
            - compartmentId
            - displayName
            type: object
          status:
            description: ManagementAgentInstallKeyStatus defines the observed state
              of ManagementAgentInstallKey.
            properties:
              allowedKeyInstallCount:
                description: Total number of install for this keys
                type: integer
              compartmentId:
                description: Compartment Identifier
                type: string
              createdByPrincipalId:
                description: Principal id of user who created the Agent Install key
                type: string
              currentKeyInstallCount:
                description: Total number of install for this keys
                type: integer
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              displayName:
                description: Management Agent Install Key Name
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: Agent install Key identifier
                type: string
              isUnlimited:
                description: If set to true, the install key has no expiration date
                  or usage limit. Defaults to false
                type: boolean
              key:
                description: Management Agent Install Key
                type: string
              lifecycleDetails:
                description: A message describing the current state in more detail.
                  For example, can be used to provide actionable information for a
                  resource in Failed state.
                type: string
              lifecycleState:
                description: Status of Key
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Usage of system tag keys. These predefined keys are scoped to namespaces.
                  Example: `{ "orcl-cloud": { "free-tier-retained": "true" } }`
                type: object
              timeCreated:
                description: The time when Management Agent install Key was created.
                  An RFC3339 formatted date time string
                type: string
              timeExpires:
                description: date after which key would expire after creation
                type: string
              timeUpdated:
                description: The time when Management Agent install Key was updated.
                  An RFC3339 formatted date time string
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
