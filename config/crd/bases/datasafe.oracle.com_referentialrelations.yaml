---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: referentialrelations.datasafe.oracle.com
spec:
  group: datasafe.oracle.com
  names:
    kind: ReferentialRelation
    listKind: ReferentialRelationList
    plural: referentialrelations
    singular: referentialrelation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the ReferentialRelation
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ReferentialRelation
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ReferentialRelation is the Schema for the referentialrelations
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ReferentialRelationSpec defines the desired state of ReferentialRelation.
            properties:
              child:
                description: ReferentialRelationChild defines nested fields for ReferentialRelation.Child.
                properties:
                  appName:
                    description: The application name.
                    type: string
                  columnGroup:
                    description: Group of columns in referential relation. Order needs
                      to be maintained in the elements of the parent/child array listing.
                    items:
                      type: string
                    type: array
                  objectName:
                    description: The database object that contains the columns.
                    type: string
                  objectType:
                    description: The type of the database object that contains the
                      sensitive column.
                    type: string
                  schemaName:
                    description: The schema name.
                    type: string
                  sensitiveTypeIds:
                    description: |-
                      Sensitive type ocids of each column groups. Order needs to be maintained with the parent column group.
                      For the DB defined referential relations identified during SDM creation, we cannot add sensitive types.
                      Instead use the sensitiveColumn POST API to mark the columns sensitive.
                    items:
                      type: string
                    type: array
                required:
                - appName
                - columnGroup
                - objectName
                - objectType
                - schemaName
                type: object
              isSensitive:
                description: |-
                  Add to sensitive data model if passed true. If false is passed, then the
                  columns will not be added in the sensitive data model as sensitive columns and
                  if sensitive type OCIDs are assigned to the columns, then the sensitive type
                  OCIDs will not be retained.
                type: boolean
              parent:
                description: ReferentialRelationParent defines nested fields for ReferentialRelation.Parent.
                properties:
                  appName:
                    description: The application name.
                    type: string
                  columnGroup:
                    description: Group of columns in referential relation. Order needs
                      to be maintained in the elements of the parent/child array listing.
                    items:
                      type: string
                    type: array
                  objectName:
                    description: The database object that contains the columns.
                    type: string
                  objectType:
                    description: The type of the database object that contains the
                      sensitive column.
                    type: string
                  schemaName:
                    description: The schema name.
                    type: string
                  sensitiveTypeIds:
                    description: |-
                      Sensitive type ocids of each column groups. Order needs to be maintained with the parent column group.
                      For the DB defined referential relations identified during SDM creation, we cannot add sensitive types.
                      Instead use the sensitiveColumn POST API to mark the columns sensitive.
                    items:
                      type: string
                    type: array
                required:
                - appName
                - columnGroup
                - objectName
                - objectType
                - schemaName
                type: object
              relationType:
                description: |-
                  The type of referential relationship the sensitive column has with its parent.
                  DB_DEFINED indicates that the relationship is defined in the database dictionary.
                  APP_DEFINED indicates that the relationship is defined at the application level and not in the database dictionary.
                type: string
            required:
            - child
            - parent
            - relationType
            type: object
          status:
            description: ReferentialRelationStatus defines the observed state of ReferentialRelation.
            properties:
              child:
                description: ReferentialRelationChild defines nested fields for ReferentialRelation.Child.
                properties:
                  appName:
                    description: The application name.
                    type: string
                  columnGroup:
                    description: Group of columns in referential relation. Order needs
                      to be maintained in the elements of the parent/child array listing.
                    items:
                      type: string
                    type: array
                  objectName:
                    description: The database object that contains the columns.
                    type: string
                  objectType:
                    description: The type of the database object that contains the
                      sensitive column.
                    type: string
                  schemaName:
                    description: The schema name.
                    type: string
                  sensitiveTypeIds:
                    description: |-
                      Sensitive type ocids of each column groups. Order needs to be maintained with the parent column group.
                      For the DB defined referential relations identified during SDM creation, we cannot add sensitive types.
                      Instead use the sensitiveColumn POST API to mark the columns sensitive.
                    items:
                      type: string
                    type: array
                required:
                - appName
                - columnGroup
                - objectName
                - objectType
                - schemaName
                type: object
              isSensitive:
                description: Determines if the columns present in the referential
                  relation is present in the sensitive data model
                type: boolean
              key:
                description: The unique key that identifies the referential relation.
                  It's numeric and unique within a sensitive data model.
                type: string
              lifecycleState:
                description: The current state of the referential relation.
                type: string
              parent:
                description: ReferentialRelationParent defines nested fields for ReferentialRelation.Parent.
                properties:
                  appName:
                    description: The application name.
                    type: string
                  columnGroup:
                    description: Group of columns in referential relation. Order needs
                      to be maintained in the elements of the parent/child array listing.
                    items:
                      type: string
                    type: array
                  objectName:
                    description: The database object that contains the columns.
                    type: string
                  objectType:
                    description: The type of the database object that contains the
                      sensitive column.
                    type: string
                  schemaName:
                    description: The schema name.
                    type: string
                  sensitiveTypeIds:
                    description: |-
                      Sensitive type ocids of each column groups. Order needs to be maintained with the parent column group.
                      For the DB defined referential relations identified during SDM creation, we cannot add sensitive types.
                      Instead use the sensitiveColumn POST API to mark the columns sensitive.
                    items:
                      type: string
                    type: array
                required:
                - appName
                - columnGroup
                - objectName
                - objectType
                - schemaName
                type: object
              relationType:
                description: |-
                  The type of referential relationship the sensitive column has with its parent. NONE indicates that the
                  sensitive column does not have a parent. DB_DEFINED indicates that the relationship is defined in the database
                  dictionary. APP_DEFINED indicates that the relationship is defined at the application level and not in the database dictionary.
                type: string
              sensitiveDataModelId:
                description: The OCID of the sensitive data model that contains the
                  sensitive column.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
