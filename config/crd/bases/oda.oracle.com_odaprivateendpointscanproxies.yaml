---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: odaprivateendpointscanproxies.oda.oracle.com
spec:
  group: oda.oracle.com
  names:
    kind: OdaPrivateEndpointScanProxy
    listKind: OdaPrivateEndpointScanProxyList
    plural: odaprivateendpointscanproxies
    singular: odaprivateendpointscanproxy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the OdaPrivateEndpointScanProxy
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the OdaPrivateEndpointScanProxy
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: OdaPrivateEndpointScanProxy is the Schema for the odaprivateendpointscanproxies
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OdaPrivateEndpointScanProxySpec defines the desired state
              of OdaPrivateEndpointScanProxy.
            properties:
              protocol:
                description: The protocol used for communication between client, scanProxy
                  and RAC's scan listeners
                type: string
              scanListenerInfos:
                description: The FQDN/IPs and port information of customer's Real
                  Application Cluster (RAC)'s SCAN listeners.
                items:
                  description: OdaPrivateEndpointScanProxyScanListenerInfo defines
                    nested fields for OdaPrivateEndpointScanProxy.ScanListenerInfo.
                  properties:
                    scanListenerFqdn:
                      description: FQDN of the customer's Real Application Cluster
                        (RAC)'s SCAN listeners.
                      type: string
                    scanListenerIp:
                      description: A SCAN listener's IP of the customer's Real Application
                        Cluster (RAC).
                      type: string
                    scanListenerPort:
                      description: The port that customer's Real Application Cluster
                        (RAC)'s SCAN listeners are listening on.
                      type: integer
                  type: object
                type: array
              scanListenerType:
                description: Type indicating whether Scan listener is specified by
                  its FQDN or list of IPs
                type: string
            required:
            - protocol
            - scanListenerInfos
            - scanListenerType
            type: object
          status:
            description: OdaPrivateEndpointScanProxyStatus defines the observed state
              of OdaPrivateEndpointScanProxy.
            properties:
              id:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the ODA Private Endpoint Scan Proxy.
                type: string
              lifecycleState:
                description: The current state of the ODA Private Endpoint Scan Proxy.
                type: string
              protocol:
                description: The protocol used for communication between client, scanProxy
                  and RAC's scan listeners
                type: string
              scanListenerInfos:
                description: The FQDN/IPs and port information of customer's Real
                  Application Cluster (RAC)'s SCAN listeners.
                items:
                  description: OdaPrivateEndpointScanProxyScanListenerInfo defines
                    nested fields for OdaPrivateEndpointScanProxy.ScanListenerInfo.
                  properties:
                    scanListenerFqdn:
                      description: FQDN of the customer's Real Application Cluster
                        (RAC)'s SCAN listeners.
                      type: string
                    scanListenerIp:
                      description: A SCAN listener's IP of the customer's Real Application
                        Cluster (RAC).
                      type: string
                    scanListenerPort:
                      description: The port that customer's Real Application Cluster
                        (RAC)'s SCAN listeners are listening on.
                      type: integer
                  type: object
                type: array
              scanListenerType:
                description: Type indicating whether Scan listener is specified by
                  its FQDN or list of IPs
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: When the resource was created. A date-time string as
                  described in RFC 3339 (https://tools.ietf.org/rfc/rfc3339), section
                  14.29.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
