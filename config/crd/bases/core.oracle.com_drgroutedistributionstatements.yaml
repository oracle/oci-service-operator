---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: drgroutedistributionstatements.core.oracle.com
spec:
  group: core.oracle.com
  names:
    kind: DrgRouteDistributionStatement
    listKind: DrgRouteDistributionStatementList
    plural: drgroutedistributionstatements
    singular: drgroutedistributionstatement
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the DrgRouteDistributionStatement
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the DrgRouteDistributionStatement
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: DrgRouteDistributionStatement is the Schema for the drgroutedistributionstatements
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DrgRouteDistributionStatementSpec defines the desired state
              of DrgRouteDistributionStatement.
            properties:
              id:
                description: The Oracle-assigned ID of each route distribution statement
                  to be updated.
                type: string
              matchCriteria:
                description: The action is applied only if all of the match criteria
                  is met.
                items:
                  description: DrgRouteDistributionStatementMatchCriteria defines
                    nested fields for DrgRouteDistributionStatement.MatchCriteria.
                  properties:
                    attachmentType:
                      description: |-
                        The type of the network resource to be included in this match. A match for a network type implies that all
                        DRG attachments of that type insert routes into the table.
                      type: string
                    drgAttachmentId:
                      description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                        of the DRG attachment.
                      type: string
                    jsonData:
                      type: string
                    matchType:
                      type: string
                  required:
                  - attachmentType
                  - drgAttachmentId
                  type: object
                type: array
              priority:
                description: The priority of the statement you'd like to update.
                type: integer
              statements:
                description: The route distribution statements to update, and the
                  details to be updated.
                items:
                  description: DrgRouteDistributionStatementStatement defines nested
                    fields for DrgRouteDistributionStatement.Statement.
                  properties:
                    id:
                      description: The Oracle-assigned ID of each route distribution
                        statement to be updated.
                      type: string
                    matchCriteria:
                      description: The action is applied only if all of the match
                        criteria is met.
                      items:
                        description: DrgRouteDistributionStatementStatementMatchCriteria
                          defines nested fields for DrgRouteDistributionStatement.Statement.MatchCriteria.
                        properties:
                          attachmentType:
                            description: |-
                              The type of the network resource to be included in this match. A match for a network type implies that all
                              DRG attachments of that type insert routes into the table.
                            type: string
                          drgAttachmentId:
                            description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                              of the DRG attachment.
                            type: string
                          jsonData:
                            type: string
                          matchType:
                            type: string
                        required:
                        - attachmentType
                        - drgAttachmentId
                        type: object
                      type: array
                    priority:
                      description: The priority of the statement you'd like to update.
                      type: integer
                  required:
                  - id
                  type: object
                type: array
            required:
            - id
            - statements
            type: object
          status:
            description: DrgRouteDistributionStatementStatus defines the observed
              state of DrgRouteDistributionStatement.
            properties:
              action:
                description: '`ACCEPT` indicates the route should be imported or exported
                  as-is.'
                type: string
              id:
                description: The Oracle-assigned ID of the route distribution statement.
                type: string
              matchCriteria:
                description: |-
                  The action is applied only if all of the match criteria is met.
                  If there are no match criteria in a statement, any input is considered a match and the action is applied.
                items:
                  description: DrgRouteDistributionStatementMatchCriteria defines
                    nested fields for DrgRouteDistributionStatement.MatchCriteria.
                  properties:
                    attachmentType:
                      description: |-
                        The type of the network resource to be included in this match. A match for a network type implies that all
                        DRG attachments of that type insert routes into the table.
                      type: string
                    drgAttachmentId:
                      description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                        of the DRG attachment.
                      type: string
                    jsonData:
                      type: string
                    matchType:
                      type: string
                  required:
                  - attachmentType
                  - drgAttachmentId
                  type: object
                type: array
              priority:
                description: |-
                  This field specifies the priority of each statement in a route distribution.
                  Priorities must be unique within a particular route distribution.
                  The priority will be represented as a number between 0 and 65535 where a lower number
                  indicates a higher priority. When a route is processed, statements are applied in the order
                  defined by their priority. The first matching rule dictates the action that will be taken
                  on the route.
                type: integer
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
