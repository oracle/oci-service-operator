---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: loganalyticsembridges.loganalytics.oracle.com
spec:
  group: loganalytics.oracle.com
  names:
    kind: LogAnalyticsEmBridge
    listKind: LogAnalyticsEmBridgeList
    plural: loganalyticsembridges
    singular: loganalyticsembridge
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the LogAnalyticsEmBridge
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the LogAnalyticsEmBridge
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: LogAnalyticsEmBridge is the Schema for the loganalyticsembridges
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LogAnalyticsEmBridgeSpec defines the desired state of LogAnalyticsEmBridge.
            properties:
              bucketName:
                description: Object store bucket name where enterprise manager harvested
                  entities will be uploaded.
                type: string
              compartmentId:
                description: Compartment Identifier OCID  (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm).
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              description:
                description: A description for log analytics enterprise manager bridge.
                type: string
              displayName:
                description: Log analytics enterprise manager bridge display name.
                type: string
              emEntitiesCompartmentId:
                description: Compartment for entities created from enterprise manager.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
            required:
            - bucketName
            - compartmentId
            - displayName
            - emEntitiesCompartmentId
            type: object
          status:
            description: LogAnalyticsEmBridgeStatus defines the observed state of
              LogAnalyticsEmBridge.
            properties:
              bucketName:
                description: Object store bucket name where enterprise manager harvested
                  entities will be uploaded.
                type: string
              compartmentId:
                description: Compartment Identifier OCID  (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm).
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              description:
                description: A description for log analytics enterprise manager bridge.
                type: string
              displayName:
                description: Log analytics enterprise manager bridge display name.
                type: string
              emEntitiesCompartmentId:
                description: Compartment for entities created from enterprise manager.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: The enterprise manager bridge OCID.
                type: string
              lastImportProcessingDetails:
                description: |-
                  Processing status details of enterprise manager upload. This provides additional details
                  for failed status
                type: string
              lastImportProcessingStatus:
                description: The status from last processing status of enterprise
                  manager upload.
                type: string
              lifecycleDetails:
                description: lifecycleDetails has additional information regarding
                  substeps such as verifying connection to object store.
                type: string
              lifecycleState:
                description: The current state of the enterprise manager bridge.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: The date and time the resource was created, in the format
                  defined by RFC3339.
                type: string
              timeEmDataLastExtracted:
                description: The timestamp of last enterprise manager upload to OCI
                  Object Store. This is in the format defined by RFC3339
                type: string
              timeImportLastProcessed:
                description: The last time of enterprise manager upload was processed.
                  This is in the format defined by RFC3339
                type: string
              timeUpdated:
                description: The date and time the resource was last updated, in the
                  format defined by RFC3339.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
