---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: routingpolicies.loadbalancer.oracle.com
spec:
  group: loadbalancer.oracle.com
  names:
    kind: RoutingPolicy
    listKind: RoutingPolicyList
    plural: routingpolicies
    singular: routingpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      priority: 1
      type: string
    - description: status of the RoutingPolicy
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the RoutingPolicy
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: RoutingPolicy is the Schema for the routingpolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RoutingPolicySpec defines the desired state of RoutingPolicy.
            properties:
              conditionLanguageVersion:
                description: The version of the language in which `condition` of `rules`
                  are composed.
                type: string
              name:
                description: |-
                  The name for this list of routing rules. It must be unique and it cannot be changed. Avoid entering
                  confidential information.
                  Example: `example_routing_rules`
                type: string
              rules:
                description: The list of routing rules.
                items:
                  description: RoutingPolicyRule defines nested fields for RoutingPolicy.Rule.
                  properties:
                    actions:
                      description: A list of actions to be applied when conditions
                        of the routing rule are met.
                      items:
                        description: RoutingPolicyRuleAction defines nested fields
                          for RoutingPolicy.Rule.Action.
                        properties:
                          backendSetName:
                            description: |-
                              Name of the backend set the listener will forward the traffic to.
                              Example: `backendSetForImages`
                            type: string
                          jsonData:
                            type: string
                          name:
                            type: string
                        type: object
                      type: array
                    condition:
                      description: A routing rule to evaluate defined conditions against
                        the incoming HTTP request and perform an action.
                      type: string
                    name:
                      description: A unique name for the routing policy rule. Avoid
                        entering confidential information.
                      type: string
                  required:
                  - actions
                  - condition
                  - name
                  type: object
                type: array
            required:
            - conditionLanguageVersion
            - name
            - rules
            type: object
          status:
            description: RoutingPolicyStatus defines the observed state of RoutingPolicy.
            properties:
              conditionLanguageVersion:
                description: The version of the language in which `condition` of `rules`
                  are composed.
                type: string
              name:
                description: |-
                  The unique name for this list of routing rules. Avoid entering confidential information.
                  Example: `example_routing_policy`
                type: string
              rules:
                description: The ordered list of routing rules.
                items:
                  description: RoutingPolicyRule defines nested fields for RoutingPolicy.Rule.
                  properties:
                    actions:
                      description: A list of actions to be applied when conditions
                        of the routing rule are met.
                      items:
                        description: RoutingPolicyRuleAction defines nested fields
                          for RoutingPolicy.Rule.Action.
                        properties:
                          backendSetName:
                            description: |-
                              Name of the backend set the listener will forward the traffic to.
                              Example: `backendSetForImages`
                            type: string
                          jsonData:
                            type: string
                          name:
                            type: string
                        type: object
                      type: array
                    condition:
                      description: A routing rule to evaluate defined conditions against
                        the incoming HTTP request and perform an action.
                      type: string
                    name:
                      description: A unique name for the routing policy rule. Avoid
                        entering confidential information.
                      type: string
                  required:
                  - actions
                  - condition
                  - name
                  type: object
                type: array
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
