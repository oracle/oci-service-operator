---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: connections.dataintegration.oracle.com
spec:
  group: dataintegration.oracle.com
  names:
    kind: Connection
    listKind: ConnectionList
    plural: connections
    singular: connection
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      priority: 1
      type: string
    - description: status of the Connection
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Connection
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Connection is the Schema for the connections API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConnectionSpec defines the desired state of Connection.
            properties:
              accessKey:
                description: ConnectionAccessKey defines nested fields for Connection.AccessKey.
                properties:
                  secretConfig:
                    description: ConnectionAccessKeySecretConfig defines nested fields
                      for Connection.AccessKey.SecretConfig.
                    properties:
                      jsonData:
                        type: string
                      modelType:
                        type: string
                      secretId:
                        description: OCID of the OCI vault secret
                        type: string
                    type: object
                  value:
                    description: Attribute to provide sensitive content.
                    type: string
                type: object
              accessTokenUrl:
                description: Specifies the endpoint used to exchange authentication
                  credentials for access tokens
                type: string
              authHeader:
                description: Optional header name if used other than default header(Authorization).
                type: string
              clientId:
                description: Specifies the client ID key for specific application
                type: string
              clientSecret:
                description: ConnectionClientSecret defines nested fields for Connection.ClientSecret.
                properties:
                  secretConfig:
                    description: ConnectionClientSecretSecretConfig defines nested
                      fields for Connection.ClientSecret.SecretConfig.
                    properties:
                      jsonData:
                        type: string
                      modelType:
                        type: string
                      secretId:
                        description: OCID of the OCI vault secret
                        type: string
                    type: object
                  value:
                    description: Attribute to provide sensitive content.
                    type: string
                type: object
              connectionProperties:
                description: The properties for the connection.
                items:
                  description: ConnectionProperty defines nested fields for Connection.ConnectionProperty.
                  properties:
                    name:
                      description: Free form text without any restriction on permitted
                        characters. Name can have letters, numbers, and special characters.
                        The value is editable and is restricted to 1000 characters.
                      type: string
                    value:
                      description: The value for the connection name property.
                      type: string
                  type: object
                type: array
              credentialFileContent:
                description: The credential file content from an Oracle Object Storage
                  wallet.
                type: string
              dataNodePrincipal:
                description: The HDFS Data Node principal.
                type: string
              defaultExternalStorage:
                description: ConnectionDefaultExternalStorage defines nested fields
                  for Connection.DefaultExternalStorage.
                properties:
                  bucket:
                    description: Bucket Name where BICC extracts stores the files
                    type: string
                  host:
                    description: Object Storage host Url. DO not give http/https.
                    type: string
                  namespace:
                    description: Namespace for the OOS bucket
                    type: string
                  storageId:
                    description: Id of the external stoarge configured in BICC console.
                      Usually its numeric.
                    type: string
                  storageName:
                    description: Name of the external storage configured in BICC console
                    type: string
                  tenancyId:
                    description: Tenancy OCID for the OOS bucket
                    type: string
                type: object
              description:
                description: User-defined description for the connection.
                type: string
              fingerPrint:
                description: The fingerprint for the user.
                type: string
              grantType:
                description: Specifies the OAuth2 grant mechanism. Example CLIENT_CREDENTIALS,
                  Implicit Flow etc.
                type: string
              hdfsPrincipal:
                description: The HDFS principal.
                type: string
              identifier:
                description: Value can only contain upper case letters, underscore,
                  and numbers. It should begin with upper case letter or underscore.
                  The value can be modified.
                type: string
              jsonData:
                type: string
              key:
                description: Generated key that can be used in API calls to identify
                  connection. On scenarios where reference to the connection is needed,
                  a value can be passed in create.
                type: string
              keyDistributionCenter:
                description: The HDFS Key Distribution Center.
                type: string
              keyTabContent:
                description: ConnectionKeyTabContent defines nested fields for Connection.KeyTabContent.
                properties:
                  secretConfig:
                    description: ConnectionKeyTabContentSecretConfig defines nested
                      fields for Connection.KeyTabContent.SecretConfig.
                    properties:
                      jsonData:
                        type: string
                      modelType:
                        type: string
                      secretId:
                        description: OCID of the OCI vault secret
                        type: string
                    type: object
                  value:
                    description: Attribute to provide sensitive content.
                    type: string
                type: object
              modelType:
                type: string
              modelVersion:
                description: The model version of an object.
                type: string
              name:
                description: Free form text without any restriction on permitted characters.
                  Name can have letters, numbers, and special characters. The value
                  is editable and is restricted to 1000 characters.
                type: string
              nameNodePrincipal:
                description: The HDFS Name Node principal.
                type: string
              objectStatus:
                description: The status of an object that can be set to value 1 for
                  shallow references across objects, other values reserved.
                type: integer
              objectVersion:
                description: The version of the object that is used to track changes
                  in the object instance.
                type: integer
              parentRef:
                description: ConnectionParentRef defines nested fields for Connection.ParentRef.
                properties:
                  parent:
                    description: Key of the parent object.
                    type: string
                  rootDocId:
                    description: Key of the root document object.
                    type: string
                type: object
              passPhrase:
                description: The passphrase for the connection.
                type: string
              password:
                description: The password for the connection.
                type: string
              passwordSecret:
                description: ConnectionPasswordSecret defines nested fields for Connection.PasswordSecret.
                properties:
                  secretConfig:
                    description: ConnectionPasswordSecretSecretConfig defines nested
                      fields for Connection.PasswordSecret.SecretConfig.
                    properties:
                      jsonData:
                        type: string
                      modelType:
                        type: string
                      secretId:
                        description: OCID of the OCI vault secret
                        type: string
                    type: object
                  value:
                    description: Attribute to provide sensitive content.
                    type: string
                type: object
              realm:
                description: HDFS Realm name.
                type: string
              registryMetadata:
                description: ConnectionRegistryMetadata defines nested fields for
                  Connection.RegistryMetadata.
                properties:
                  aggregatorKey:
                    description: The owning object's key for this object.
                    type: string
                  isFavorite:
                    description: Specifies whether this object is a favorite or not.
                    type: boolean
                  key:
                    description: The identifying key for the object.
                    type: string
                  labels:
                    description: Labels are keywords or labels that you can add to
                      data assets, dataflows etc. You can define your own labels and
                      use them to categorize content.
                    items:
                      type: string
                    type: array
                  registryVersion:
                    description: The registry version.
                    type: integer
                type: object
              scope:
                description: Specifies the OAuth scopes that limit the permissions
                  granted by an access token.
                type: string
              secretKey:
                description: ConnectionSecretKey defines nested fields for Connection.SecretKey.
                properties:
                  secretConfig:
                    description: ConnectionSecretKeySecretConfig defines nested fields
                      for Connection.SecretKey.SecretConfig.
                    properties:
                      jsonData:
                        type: string
                      modelType:
                        type: string
                      secretId:
                        description: OCID of the OCI vault secret
                        type: string
                    type: object
                  value:
                    description: Attribute to provide sensitive content.
                    type: string
                type: object
              tnsAlias:
                description: The Autonomous Transaction Processing instance service
                  name.
                type: string
              tnsNames:
                description: Array of service names that are available for selection
                  in the tnsAlias property.
                items:
                  type: string
                type: array
              userId:
                description: The OCI user OCID for the user to connect to.
                type: string
              username:
                description: The user name for the connection.
                type: string
            required:
            - identifier
            - name
            - objectVersion
            type: object
          status:
            description: ConnectionStatus defines the observed state of Connection.
            properties:
              accessKey:
                description: ConnectionAccessKey defines nested fields for Connection.AccessKey.
                properties:
                  secretConfig:
                    description: ConnectionAccessKeySecretConfig defines nested fields
                      for Connection.AccessKey.SecretConfig.
                    properties:
                      jsonData:
                        type: string
                      modelType:
                        type: string
                      secretId:
                        description: OCID of the OCI vault secret
                        type: string
                    type: object
                  value:
                    description: Attribute to provide sensitive content.
                    type: string
                type: object
              accessTokenUrl:
                description: Specifies the endpoint used to exchange authentication
                  credentials for access tokens
                type: string
              authHeader:
                description: Optional header name if used other than default header(Authorization).
                type: string
              clientId:
                description: Specifies the client ID key for specific application
                type: string
              clientSecret:
                description: ConnectionClientSecret defines nested fields for Connection.ClientSecret.
                properties:
                  secretConfig:
                    description: ConnectionClientSecretSecretConfig defines nested
                      fields for Connection.ClientSecret.SecretConfig.
                    properties:
                      jsonData:
                        type: string
                      modelType:
                        type: string
                      secretId:
                        description: OCID of the OCI vault secret
                        type: string
                    type: object
                  value:
                    description: Attribute to provide sensitive content.
                    type: string
                type: object
              connectionProperties:
                description: The properties for the connection.
                items:
                  description: ConnectionProperty defines nested fields for Connection.ConnectionProperty.
                  properties:
                    name:
                      description: Free form text without any restriction on permitted
                        characters. Name can have letters, numbers, and special characters.
                        The value is editable and is restricted to 1000 characters.
                      type: string
                    value:
                      description: The value for the connection name property.
                      type: string
                  type: object
                type: array
              credentialFileContent:
                description: The credential file content from an Oracle Object Storage
                  wallet.
                type: string
              dataNodePrincipal:
                description: The HDFS Data Node principal.
                type: string
              defaultExternalStorage:
                description: ConnectionDefaultExternalStorage defines nested fields
                  for Connection.DefaultExternalStorage.
                properties:
                  bucket:
                    description: Bucket Name where BICC extracts stores the files
                    type: string
                  host:
                    description: Object Storage host Url. DO not give http/https.
                    type: string
                  namespace:
                    description: Namespace for the OOS bucket
                    type: string
                  storageId:
                    description: Id of the external stoarge configured in BICC console.
                      Usually its numeric.
                    type: string
                  storageName:
                    description: Name of the external storage configured in BICC console
                    type: string
                  tenancyId:
                    description: Tenancy OCID for the OOS bucket
                    type: string
                type: object
              description:
                description: User-defined description for the connection.
                type: string
              fingerPrint:
                description: The fingerprint for the user.
                type: string
              grantType:
                description: Specifies the OAuth2 grant mechanism. Example CLIENT_CREDENTIALS,
                  Implicit Flow etc.
                type: string
              hdfsPrincipal:
                description: The HDFS principal.
                type: string
              identifier:
                description: Value can only contain upper case letters, underscore,
                  and numbers. It should begin with upper case letter or underscore.
                  The value can be modified.
                type: string
              isDefault:
                description: The default property for the connection.
                type: boolean
              jsonData:
                type: string
              key:
                description: Generated key that can be used in API calls to identify
                  connection. On scenarios where reference to the connection is needed,
                  a value can be passed in create.
                type: string
              keyDistributionCenter:
                description: The HDFS Key Distribution Center.
                type: string
              keyMap:
                additionalProperties:
                  type: string
                description: A key map. If provided, key is replaced with generated
                  key. This structure provides mapping between user provided key and
                  generated key.
                type: object
              keyTabContent:
                description: ConnectionKeyTabContent defines nested fields for Connection.KeyTabContent.
                properties:
                  secretConfig:
                    description: ConnectionKeyTabContentSecretConfig defines nested
                      fields for Connection.KeyTabContent.SecretConfig.
                    properties:
                      jsonData:
                        type: string
                      modelType:
                        type: string
                      secretId:
                        description: OCID of the OCI vault secret
                        type: string
                    type: object
                  value:
                    description: Attribute to provide sensitive content.
                    type: string
                type: object
              metadata:
                description: ConnectionMetadata defines nested fields for Connection.Metadata.
                properties:
                  aggregator:
                    description: ConnectionMetadataAggregator defines nested fields
                      for Connection.Metadata.Aggregator.
                    properties:
                      description:
                        description: The description of the aggregator.
                        type: string
                      identifier:
                        description: The identifier of the aggregator.
                        type: string
                      key:
                        description: The key of the aggregator object.
                        type: string
                      name:
                        description: The name of the aggregator.
                        type: string
                      type:
                        description: The type of the aggregator.
                        type: string
                    type: object
                  aggregatorKey:
                    description: The owning object key for this object.
                    type: string
                  countStatistics:
                    description: ConnectionMetadataCountStatistics defines nested
                      fields for Connection.Metadata.CountStatistics.
                    properties:
                      objectTypeCountList:
                        description: The array of statistics.
                        items:
                          description: ConnectionMetadataCountStatisticsObjectTypeCountList
                            defines nested fields for Connection.Metadata.CountStatistics.ObjectTypeCountList.
                          properties:
                            objectCount:
                              description: The value for the count statistic object.
                              format: int64
                              type: integer
                            objectType:
                              description: The type of object for the count statistic
                                object.
                              type: string
                          type: object
                        type: array
                    type: object
                  createdBy:
                    description: The user that created the object.
                    type: string
                  createdByName:
                    description: The user that created the object.
                    type: string
                  identifierPath:
                    description: The full path to identify this object.
                    type: string
                  infoFields:
                    additionalProperties:
                      type: string
                    description: Information property fields.
                    type: object
                  isFavorite:
                    description: Specifies whether this object is a favorite or not.
                    type: boolean
                  labels:
                    description: Labels are keywords or tags that you can add to data
                      assets, dataflows and so on. You can define your own labels
                      and use them to categorize content.
                    items:
                      type: string
                    type: array
                  registryVersion:
                    description: The registry version of the object.
                    type: integer
                  timeCreated:
                    description: The date and time that the object was created.
                    type: string
                  timeUpdated:
                    description: The date and time that the object was updated.
                    type: string
                  updatedBy:
                    description: The user that updated the object.
                    type: string
                  updatedByName:
                    description: The user that updated the object.
                    type: string
                type: object
              modelType:
                type: string
              modelVersion:
                description: The model version of an object.
                type: string
              name:
                description: Free form text without any restriction on permitted characters.
                  Name can have letters, numbers, and special characters. The value
                  is editable and is restricted to 1000 characters.
                type: string
              nameNodePrincipal:
                description: The HDFS Name Node principal.
                type: string
              objectStatus:
                description: The status of an object that can be set to value 1 for
                  shallow references across objects, other values reserved.
                type: integer
              objectVersion:
                description: The version of the object that is used to track changes
                  in the object instance.
                type: integer
              parentRef:
                description: ConnectionParentRef defines nested fields for Connection.ParentRef.
                properties:
                  parent:
                    description: Key of the parent object.
                    type: string
                  rootDocId:
                    description: Key of the root document object.
                    type: string
                type: object
              passPhrase:
                description: The passphrase for the connection.
                type: string
              passwordSecret:
                description: ConnectionPasswordSecret defines nested fields for Connection.PasswordSecret.
                properties:
                  secretConfig:
                    description: ConnectionPasswordSecretSecretConfig defines nested
                      fields for Connection.PasswordSecret.SecretConfig.
                    properties:
                      jsonData:
                        type: string
                      modelType:
                        type: string
                      secretId:
                        description: OCID of the OCI vault secret
                        type: string
                    type: object
                  value:
                    description: Attribute to provide sensitive content.
                    type: string
                type: object
              primarySchema:
                description: ConnectionPrimarySchema defines nested fields for Connection.PrimarySchema.
                properties:
                  defaultConnection:
                    description: The default connection key.
                    type: string
                  description:
                    description: User-defined description for the schema.
                    type: string
                  externalKey:
                    description: The external key for the object.
                    type: string
                  identifier:
                    description: Value can only contain upper case letters, underscore,
                      and numbers. It should begin with upper case letter or underscore.
                      The value can be modified.
                    type: string
                  isHasContainers:
                    description: Specifies whether the schema has containers.
                    type: boolean
                  key:
                    description: The object key.
                    type: string
                  metadata:
                    description: ConnectionPrimarySchemaMetadata defines nested fields
                      for Connection.PrimarySchema.Metadata.
                    properties:
                      aggregator:
                        description: ConnectionPrimarySchemaMetadataAggregator defines
                          nested fields for Connection.PrimarySchema.Metadata.Aggregator.
                        properties:
                          description:
                            description: The description of the aggregator.
                            type: string
                          identifier:
                            description: The identifier of the aggregator.
                            type: string
                          key:
                            description: The key of the aggregator object.
                            type: string
                          name:
                            description: The name of the aggregator.
                            type: string
                          type:
                            description: The type of the aggregator.
                            type: string
                        type: object
                      aggregatorKey:
                        description: The owning object key for this object.
                        type: string
                      countStatistics:
                        description: ConnectionPrimarySchemaMetadataCountStatistics
                          defines nested fields for Connection.PrimarySchema.Metadata.CountStatistics.
                        properties:
                          objectTypeCountList:
                            description: The array of statistics.
                            items:
                              description: ConnectionPrimarySchemaMetadataCountStatisticsObjectTypeCountList
                                defines nested fields for Connection.PrimarySchema.Metadata.CountStatistics.ObjectTypeCountList.
                              properties:
                                objectCount:
                                  description: The value for the count statistic object.
                                  format: int64
                                  type: integer
                                objectType:
                                  description: The type of object for the count statistic
                                    object.
                                  type: string
                              type: object
                            type: array
                        type: object
                      createdBy:
                        description: The user that created the object.
                        type: string
                      createdByName:
                        description: The user that created the object.
                        type: string
                      identifierPath:
                        description: The full path to identify this object.
                        type: string
                      infoFields:
                        additionalProperties:
                          type: string
                        description: Information property fields.
                        type: object
                      isFavorite:
                        description: Specifies whether this object is a favorite or
                          not.
                        type: boolean
                      labels:
                        description: Labels are keywords or tags that you can add
                          to data assets, dataflows and so on. You can define your
                          own labels and use them to categorize content.
                        items:
                          type: string
                        type: array
                      registryVersion:
                        description: The registry version of the object.
                        type: integer
                      timeCreated:
                        description: The date and time that the object was created.
                        type: string
                      timeUpdated:
                        description: The date and time that the object was updated.
                        type: string
                      updatedBy:
                        description: The user that updated the object.
                        type: string
                      updatedByName:
                        description: The user that updated the object.
                        type: string
                    type: object
                  modelType:
                    description: The object's type.
                    type: string
                  modelVersion:
                    description: The object's model version.
                    type: string
                  name:
                    description: Free form text without any restriction on permitted
                      characters. Name can have letters, numbers, and special characters.
                      The value is editable and is restricted to 1000 characters.
                    type: string
                  objectStatus:
                    description: The status of an object that can be set to value
                      1 for shallow references across objects, other values reserved.
                    type: integer
                  objectVersion:
                    description: The version of the object that is used to track changes
                      in the object instance.
                    type: integer
                  parentRef:
                    description: ConnectionPrimarySchemaParentRef defines nested fields
                      for Connection.PrimarySchema.ParentRef.
                    properties:
                      parent:
                        description: Key of the parent object.
                        type: string
                      rootDocId:
                        description: Key of the root document object.
                        type: string
                    type: object
                  resourceName:
                    description: A resource name can have letters, numbers, and special
                      characters. The value is editable and is restricted to 4000
                      characters.
                    type: string
                type: object
              realm:
                description: HDFS Realm name.
                type: string
              scope:
                description: Specifies the OAuth scopes that limit the permissions
                  granted by an access token.
                type: string
              secretKey:
                description: ConnectionSecretKey defines nested fields for Connection.SecretKey.
                properties:
                  secretConfig:
                    description: ConnectionSecretKeySecretConfig defines nested fields
                      for Connection.SecretKey.SecretConfig.
                    properties:
                      jsonData:
                        type: string
                      modelType:
                        type: string
                      secretId:
                        description: OCID of the OCI vault secret
                        type: string
                    type: object
                  value:
                    description: Attribute to provide sensitive content.
                    type: string
                type: object
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              tnsAlias:
                description: The Autonomous Data Warehouse instance service name.
                type: string
              tnsNames:
                description: Array of service names that are available for selection
                  in the tnsAlias property.
                items:
                  type: string
                type: array
              userId:
                description: The OCI user OCID for the user to connect to.
                type: string
              username:
                description: The user name for the connection.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
