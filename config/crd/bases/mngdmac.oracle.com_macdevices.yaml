---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: macdevices.mngdmac.oracle.com
spec:
  group: mngdmac.oracle.com
  names:
    kind: MacDevice
    listKind: MacDeviceList
    plural: macdevices
    singular: macdevice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the MacDevice
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the MacDevice
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: MacDevice is the Schema for the macdevices API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MacDeviceSpec defines the desired state of MacDevice.
            properties:
              macDeviceId:
                description: |-
                  The UUID of the existing MacDevice to bind and manage.
                  Required for direct get and terminate requests; delete confirmation also reuses it for exact list checks.
                type: string
              macOrderId:
                description: |-
                  The OCID of the MacOrder that owns the existing MacDevice.
                  Required for GetMacDevice, ListMacDevices, and TerminateMacDevice requests.
                type: string
            required:
            - macDeviceId
            - macOrderId
            type: object
          status:
            description: MacDeviceStatus defines the observed state of MacDevice.
            properties:
              compartmentId:
                description: OCID of the compartment to which the resource belongs
                  to.
                type: string
              id:
                description: The unique ID of the MacDevice.
                type: string
              ipAddress:
                description: The IP address assigned to the MacDevice.
                type: string
              isMarkedDecom:
                description: A flag that indicates if this MacDevice is decommissioned.
                type: boolean
              lifecycleState:
                description: The current status of the MacDevice.
                type: string
              macOrderId:
                description: The OCID of the resource.
                type: string
              serialNumber:
                description: The serial number of the MacDevice.
                type: string
              shape:
                description: The shape of the Mac.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: The time this resource was created. An RFC3339 formatted
                  datetime string.
                type: string
              timeDecom:
                description: An RFC3339-formatted datetime string containing the time
                  this MacDevice was decommissioned.
                type: string
              timeUpdated:
                description: The time this resource was last updated. An RFC3339 formatted
                  datetime string.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
