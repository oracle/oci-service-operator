---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: securitylists.core.oracle.com
spec:
  group: core.oracle.com
  names:
    kind: SecurityList
    listKind: SecurityListList
    plural: securitylists
    singular: securitylist
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the SecurityList
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the SecurityList
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: SecurityList is the Schema for the securitylists API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityListSpec defines the desired state of SecurityList.
            properties:
              compartmentId:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment to contain the security list.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              egressSecurityRules:
                description: Rules for allowing egress IP packets.
                items:
                  description: SecurityListEgressSecurityRule defines nested fields
                    for SecurityList.EgressSecurityRule.
                  properties:
                    description:
                      description: An optional description of your choice for the
                        rule.
                      type: string
                    destination:
                      description: |-
                        Conceptually, this is the range of IP addresses that a packet originating from the instance
                        can go to.
                        Allowed values:
                          * IP address range in CIDR notation. For example: `192.168.1.0/24` or `2001:0db8:0123:45::/56`
                            Note that IPv6 addressing is currently supported only in certain regions. See
                            IPv6 Addresses (https://docs.cloud.oracle.com/iaas/Content/Network/Concepts/ipv6.htm).
                          * The `cidrBlock` value for a Service, if you're
                            setting up a security list rule for traffic destined for a particular `Service` through
                            a service gateway. For example: `oci-phx-objectstorage`.
                      type: string
                    destinationType:
                      description: |-
                        Type of destination for the rule. The default is `CIDR_BLOCK`.
                        Allowed values:
                          * `CIDR_BLOCK`: If the rule's `destination` is an IP address range in CIDR notation.
                          * `SERVICE_CIDR_BLOCK`: If the rule's `destination` is the `cidrBlock` value for a
                            Service (the rule is for traffic destined for a
                            particular `Service` through a service gateway).
                      type: string
                    icmpOptions:
                      description: SecurityListEgressSecurityRuleIcmpOptions defines
                        nested fields for SecurityList.EgressSecurityRule.IcmpOptions.
                      properties:
                        code:
                          description: The ICMP code (optional).
                          type: integer
                        type:
                          description: The ICMP type.
                          type: integer
                      required:
                      - type
                      type: object
                    isStateless:
                      description: |-
                        A stateless rule allows traffic in one direction. Remember to add a corresponding
                        stateless rule in the other direction if you need to support bidirectional traffic. For
                        example, if egress traffic allows TCP destination port 80, there should be an ingress
                        rule to allow TCP source port 80. Defaults to false, which means the rule is stateful
                        and a corresponding rule is not necessary for bidirectional traffic.
                      type: boolean
                    protocol:
                      description: |-
                        The transport protocol. Specify either `all` or an IPv4 protocol number as
                        defined in
                        Protocol Numbers (http://www.iana.org/assignments/protocol-numbers/protocol-numbers.xhtml).
                        Options are supported only for ICMP ("1"), TCP ("6"), UDP ("17"), and ICMPv6 ("58").
                      type: string
                    tcpOptions:
                      description: SecurityListEgressSecurityRuleTcpOptions defines
                        nested fields for SecurityList.EgressSecurityRule.TcpOptions.
                      properties:
                        destinationPortRange:
                          description: SecurityListEgressSecurityRuleTcpOptionsDestinationPortRange
                            defines nested fields for SecurityList.EgressSecurityRule.TcpOptions.DestinationPortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                        sourcePortRange:
                          description: SecurityListEgressSecurityRuleTcpOptionsSourcePortRange
                            defines nested fields for SecurityList.EgressSecurityRule.TcpOptions.SourcePortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                      type: object
                    udpOptions:
                      description: SecurityListEgressSecurityRuleUdpOptions defines
                        nested fields for SecurityList.EgressSecurityRule.UdpOptions.
                      properties:
                        destinationPortRange:
                          description: SecurityListEgressSecurityRuleUdpOptionsDestinationPortRange
                            defines nested fields for SecurityList.EgressSecurityRule.UdpOptions.DestinationPortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                        sourcePortRange:
                          description: SecurityListEgressSecurityRuleUdpOptionsSourcePortRange
                            defines nested fields for SecurityList.EgressSecurityRule.UdpOptions.SourcePortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                      type: object
                  required:
                  - destination
                  - protocol
                  type: object
                type: array
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              ingressSecurityRules:
                description: Rules for allowing ingress IP packets.
                items:
                  description: SecurityListIngressSecurityRule defines nested fields
                    for SecurityList.IngressSecurityRule.
                  properties:
                    description:
                      description: An optional description of your choice for the
                        rule.
                      type: string
                    icmpOptions:
                      description: SecurityListIngressSecurityRuleIcmpOptions defines
                        nested fields for SecurityList.IngressSecurityRule.IcmpOptions.
                      properties:
                        code:
                          description: The ICMP code (optional).
                          type: integer
                        type:
                          description: The ICMP type.
                          type: integer
                      required:
                      - type
                      type: object
                    isStateless:
                      description: |-
                        A stateless rule allows traffic in one direction. Remember to add a corresponding
                        stateless rule in the other direction if you need to support bidirectional traffic. For
                        example, if ingress traffic allows TCP destination port 80, there should be an egress
                        rule to allow TCP source port 80. Defaults to false, which means the rule is stateful
                        and a corresponding rule is not necessary for bidirectional traffic.
                      type: boolean
                    protocol:
                      description: |-
                        The transport protocol. Specify either `all` or an IPv4 protocol number as
                        defined in
                        Protocol Numbers (http://www.iana.org/assignments/protocol-numbers/protocol-numbers.xhtml).
                        Options are supported only for ICMP ("1"), TCP ("6"), UDP ("17"), and ICMPv6 ("58").
                      type: string
                    source:
                      description: |-
                        Conceptually, this is the range of IP addresses that a packet coming into the instance
                        can come from.
                        Allowed values:
                          * IP address range in CIDR notation. For example: `192.168.1.0/24` or `2001:0db8:0123:45::/56`.
                            IPv6 addressing is supported for all commercial and government regions. See
                            IPv6 Addresses (https://docs.cloud.oracle.com/iaas/Content/Network/Concepts/ipv6.htm).
                          * The `cidrBlock` value for a Service, if you're
                            setting up a security list rule for traffic coming from a particular `Service` through
                            a service gateway. For example: `oci-phx-objectstorage`.
                      type: string
                    sourceType:
                      description: |-
                        Type of source for the rule. The default is `CIDR_BLOCK`.
                          * `CIDR_BLOCK`: If the rule's `source` is an IP address range in CIDR notation.
                          * `SERVICE_CIDR_BLOCK`: If the rule's `source` is the `cidrBlock` value for a
                            Service (the rule is for traffic coming from a
                            particular `Service` through a service gateway).
                      type: string
                    tcpOptions:
                      description: SecurityListIngressSecurityRuleTcpOptions defines
                        nested fields for SecurityList.IngressSecurityRule.TcpOptions.
                      properties:
                        destinationPortRange:
                          description: SecurityListIngressSecurityRuleTcpOptionsDestinationPortRange
                            defines nested fields for SecurityList.IngressSecurityRule.TcpOptions.DestinationPortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                        sourcePortRange:
                          description: SecurityListIngressSecurityRuleTcpOptionsSourcePortRange
                            defines nested fields for SecurityList.IngressSecurityRule.TcpOptions.SourcePortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                      type: object
                    udpOptions:
                      description: SecurityListIngressSecurityRuleUdpOptions defines
                        nested fields for SecurityList.IngressSecurityRule.UdpOptions.
                      properties:
                        destinationPortRange:
                          description: SecurityListIngressSecurityRuleUdpOptionsDestinationPortRange
                            defines nested fields for SecurityList.IngressSecurityRule.UdpOptions.DestinationPortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                        sourcePortRange:
                          description: SecurityListIngressSecurityRuleUdpOptionsSourcePortRange
                            defines nested fields for SecurityList.IngressSecurityRule.UdpOptions.SourcePortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                      type: object
                  required:
                  - protocol
                  - source
                  type: object
                type: array
              vcnId:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the VCN the security list belongs to.
                type: string
            required:
            - compartmentId
            - egressSecurityRules
            - ingressSecurityRules
            - vcnId
            type: object
          status:
            description: SecurityListStatus defines the observed state of SecurityList.
            properties:
              compartmentId:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment containing the security list.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              egressSecurityRules:
                description: Rules for allowing egress IP packets.
                items:
                  description: SecurityListEgressSecurityRuleObservedState defines
                    nested fields for SecurityList.EgressSecurityRule.
                  properties:
                    description:
                      description: An optional description of your choice for the
                        rule.
                      type: string
                    destination:
                      description: |-
                        Conceptually, this is the range of IP addresses that a packet originating from the instance
                        can go to.
                        Allowed values:
                          * IP address range in CIDR notation. For example: `192.168.1.0/24` or `2001:0db8:0123:45::/56`
                            Note that IPv6 addressing is currently supported only in certain regions. See
                            IPv6 Addresses (https://docs.cloud.oracle.com/iaas/Content/Network/Concepts/ipv6.htm).
                          * The `cidrBlock` value for a Service, if you're
                            setting up a security list rule for traffic destined for a particular `Service` through
                            a service gateway. For example: `oci-phx-objectstorage`.
                      type: string
                    destinationType:
                      description: |-
                        Type of destination for the rule. The default is `CIDR_BLOCK`.
                        Allowed values:
                          * `CIDR_BLOCK`: If the rule's `destination` is an IP address range in CIDR notation.
                          * `SERVICE_CIDR_BLOCK`: If the rule's `destination` is the `cidrBlock` value for a
                            Service (the rule is for traffic destined for a
                            particular `Service` through a service gateway).
                      type: string
                    icmpOptions:
                      description: SecurityListEgressSecurityRuleIcmpOptions defines
                        nested fields for SecurityList.EgressSecurityRule.IcmpOptions.
                      properties:
                        code:
                          description: The ICMP code (optional).
                          type: integer
                        type:
                          description: The ICMP type.
                          type: integer
                      required:
                      - type
                      type: object
                    isStateless:
                      description: |-
                        A stateless rule allows traffic in one direction. Remember to add a corresponding
                        stateless rule in the other direction if you need to support bidirectional traffic. For
                        example, if egress traffic allows TCP destination port 80, there should be an ingress
                        rule to allow TCP source port 80. Defaults to false, which means the rule is stateful
                        and a corresponding rule is not necessary for bidirectional traffic.
                      type: boolean
                    protocol:
                      description: |-
                        The transport protocol. Specify either `all` or an IPv4 protocol number as
                        defined in
                        Protocol Numbers (http://www.iana.org/assignments/protocol-numbers/protocol-numbers.xhtml).
                        Options are supported only for ICMP ("1"), TCP ("6"), UDP ("17"), and ICMPv6 ("58").
                      type: string
                    tcpOptions:
                      description: SecurityListEgressSecurityRuleTcpOptionsObservedState
                        defines nested fields for SecurityList.EgressSecurityRule.TcpOptions.
                      properties:
                        destinationPortRange:
                          description: SecurityListEgressSecurityRuleTcpOptionsDestinationPortRange
                            defines nested fields for SecurityList.EgressSecurityRule.TcpOptions.DestinationPortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                        sourcePortRange:
                          description: SecurityListEgressSecurityRuleTcpOptionsSourcePortRange
                            defines nested fields for SecurityList.EgressSecurityRule.TcpOptions.SourcePortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                      type: object
                    udpOptions:
                      description: SecurityListEgressSecurityRuleUdpOptionsObservedState
                        defines nested fields for SecurityList.EgressSecurityRule.UdpOptions.
                      properties:
                        destinationPortRange:
                          description: SecurityListEgressSecurityRuleUdpOptionsDestinationPortRange
                            defines nested fields for SecurityList.EgressSecurityRule.UdpOptions.DestinationPortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                        sourcePortRange:
                          description: SecurityListEgressSecurityRuleUdpOptionsSourcePortRange
                            defines nested fields for SecurityList.EgressSecurityRule.UdpOptions.SourcePortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                      type: object
                  required:
                  - destination
                  - protocol
                  type: object
                type: array
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The security list's Oracle Cloud ID (OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)).
                type: string
              ingressSecurityRules:
                description: Rules for allowing ingress IP packets.
                items:
                  description: SecurityListIngressSecurityRuleObservedState defines
                    nested fields for SecurityList.IngressSecurityRule.
                  properties:
                    description:
                      description: An optional description of your choice for the
                        rule.
                      type: string
                    icmpOptions:
                      description: SecurityListIngressSecurityRuleIcmpOptions defines
                        nested fields for SecurityList.IngressSecurityRule.IcmpOptions.
                      properties:
                        code:
                          description: The ICMP code (optional).
                          type: integer
                        type:
                          description: The ICMP type.
                          type: integer
                      required:
                      - type
                      type: object
                    isStateless:
                      description: |-
                        A stateless rule allows traffic in one direction. Remember to add a corresponding
                        stateless rule in the other direction if you need to support bidirectional traffic. For
                        example, if ingress traffic allows TCP destination port 80, there should be an egress
                        rule to allow TCP source port 80. Defaults to false, which means the rule is stateful
                        and a corresponding rule is not necessary for bidirectional traffic.
                      type: boolean
                    protocol:
                      description: |-
                        The transport protocol. Specify either `all` or an IPv4 protocol number as
                        defined in
                        Protocol Numbers (http://www.iana.org/assignments/protocol-numbers/protocol-numbers.xhtml).
                        Options are supported only for ICMP ("1"), TCP ("6"), UDP ("17"), and ICMPv6 ("58").
                      type: string
                    source:
                      description: |-
                        Conceptually, this is the range of IP addresses that a packet coming into the instance
                        can come from.
                        Allowed values:
                          * IP address range in CIDR notation. For example: `192.168.1.0/24` or `2001:0db8:0123:45::/56`.
                            IPv6 addressing is supported for all commercial and government regions. See
                            IPv6 Addresses (https://docs.cloud.oracle.com/iaas/Content/Network/Concepts/ipv6.htm).
                          * The `cidrBlock` value for a Service, if you're
                            setting up a security list rule for traffic coming from a particular `Service` through
                            a service gateway. For example: `oci-phx-objectstorage`.
                      type: string
                    sourceType:
                      description: |-
                        Type of source for the rule. The default is `CIDR_BLOCK`.
                          * `CIDR_BLOCK`: If the rule's `source` is an IP address range in CIDR notation.
                          * `SERVICE_CIDR_BLOCK`: If the rule's `source` is the `cidrBlock` value for a
                            Service (the rule is for traffic coming from a
                            particular `Service` through a service gateway).
                      type: string
                    tcpOptions:
                      description: SecurityListIngressSecurityRuleTcpOptionsObservedState
                        defines nested fields for SecurityList.IngressSecurityRule.TcpOptions.
                      properties:
                        destinationPortRange:
                          description: SecurityListIngressSecurityRuleTcpOptionsDestinationPortRange
                            defines nested fields for SecurityList.IngressSecurityRule.TcpOptions.DestinationPortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                        sourcePortRange:
                          description: SecurityListIngressSecurityRuleTcpOptionsSourcePortRange
                            defines nested fields for SecurityList.IngressSecurityRule.TcpOptions.SourcePortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                      type: object
                    udpOptions:
                      description: SecurityListIngressSecurityRuleUdpOptionsObservedState
                        defines nested fields for SecurityList.IngressSecurityRule.UdpOptions.
                      properties:
                        destinationPortRange:
                          description: SecurityListIngressSecurityRuleUdpOptionsDestinationPortRange
                            defines nested fields for SecurityList.IngressSecurityRule.UdpOptions.DestinationPortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                        sourcePortRange:
                          description: SecurityListIngressSecurityRuleUdpOptionsSourcePortRange
                            defines nested fields for SecurityList.IngressSecurityRule.UdpOptions.SourcePortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                      type: object
                  required:
                  - protocol
                  - source
                  type: object
                type: array
              lifecycleState:
                description: The security list's current state.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time the security list was created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              vcnId:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the VCN the security list belongs to.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
