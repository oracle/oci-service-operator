---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: volumegroups.core.oracle.com
spec:
  group: core.oracle.com
  names:
    kind: VolumeGroup
    listKind: VolumeGroupList
    plural: volumegroups
    singular: volumegroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the VolumeGroup
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the VolumeGroup
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: VolumeGroup is the Schema for the volumegroups API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VolumeGroupSpec defines the desired state of VolumeGroup.
            properties:
              availabilityDomain:
                description: The availability domain of the volume group.
                type: string
              backupPolicyId:
                description: |-
                  If provided, specifies the ID of the volume backup policy to assign to the newly
                  created volume group. If omitted, no policy will be assigned.
                type: string
              compartmentId:
                description: The OCID of the compartment that contains the volume
                  group.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              sourceDetails:
                description: VolumeGroupSourceDetails defines nested fields for VolumeGroup.SourceDetails.
                properties:
                  jsonData:
                    type: string
                  type:
                    type: string
                  volumeGroupBackupId:
                    description: The OCID of the volume group backup to restore from.
                    type: string
                  volumeGroupId:
                    description: The OCID of the volume group to clone from.
                    type: string
                  volumeGroupReplicaId:
                    description: The OCID of the volume group replica.
                    type: string
                  volumeIds:
                    description: OCIDs for the volumes in this volume group.
                    items:
                      type: string
                    type: array
                required:
                - volumeGroupBackupId
                - volumeGroupId
                - volumeGroupReplicaId
                - volumeIds
                type: object
              volumeGroupReplicas:
                description: |-
                  The list of volume group replicas that this volume group will be enabled to have
                  in the specified destination availability domains.
                items:
                  description: VolumeGroupReplicaFields defines nested fields for
                    VolumeGroup.VolumeGroupReplica.
                  properties:
                    availabilityDomain:
                      description: |-
                        The availability domain of the volume group replica.
                        Example: `Uocm:PHX-AD-1`
                      type: string
                    displayName:
                      description: |-
                        A user-friendly name. Does not have to be unique, and it's changeable.
                        Avoid entering confidential information.
                      type: string
                  required:
                  - availabilityDomain
                  type: object
                type: array
              volumeIds:
                description: OCIDs for the volumes in this volume group.
                items:
                  type: string
                type: array
            required:
            - availabilityDomain
            - compartmentId
            - sourceDetails
            type: object
          status:
            description: VolumeGroupStatus defines the observed state of VolumeGroup.
            properties:
              availabilityDomain:
                description: The availability domain of the volume group.
                type: string
              compartmentId:
                description: The OCID of the compartment that contains the volume
                  group.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The OCID for the volume group.
                type: string
              isHydrated:
                description: |-
                  Specifies whether the newly created cloned volume group's data has finished copying
                  from the source volume group or backup.
                type: boolean
              lifecycleState:
                description: The current state of a volume group.
                type: string
              sizeInGBs:
                description: The aggregate size of the volume group in GBs.
                format: int64
                type: integer
              sizeInMBs:
                description: The aggregate size of the volume group in MBs.
                format: int64
                type: integer
              sourceDetails:
                description: VolumeGroupSourceDetailsObservedState defines nested
                  fields for VolumeGroup.SourceDetails.
                properties:
                  jsonData:
                    type: string
                  type:
                    type: string
                  volumeGroupBackupId:
                    description: The OCID of the volume group backup to restore from.
                    type: string
                  volumeGroupId:
                    description: The OCID of the volume group to clone from.
                    type: string
                  volumeGroupReplicaId:
                    description: The OCID of the volume group replica.
                    type: string
                  volumeIds:
                    description: OCIDs for the volumes in this volume group.
                    items:
                      type: string
                    type: array
                required:
                - volumeGroupBackupId
                - volumeGroupId
                - volumeGroupReplicaId
                - volumeIds
                type: object
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: The date and time the volume group was created. Format
                  defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                type: string
              volumeGroupReplicas:
                description: The list of volume group replicas of this volume group.
                items:
                  description: VolumeGroupReplicaFields defines nested fields for
                    VolumeGroup.VolumeGroupReplica.
                  properties:
                    availabilityDomain:
                      description: |-
                        The availability domain of the volume group replica.
                        Example: `Uocm:PHX-AD-1`
                      type: string
                    displayName:
                      description: |-
                        A user-friendly name. Does not have to be unique, and it's changeable.
                        Avoid entering confidential information.
                      type: string
                  required:
                  - availabilityDomain
                  type: object
                type: array
              volumeIds:
                description: OCIDs for the volumes in this volume group.
                items:
                  type: string
                type: array
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
