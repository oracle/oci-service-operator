---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: inclusioncriteria.governancerulescontrolplane.oracle.com
spec:
  group: governancerulescontrolplane.oracle.com
  names:
    kind: InclusionCriterion
    listKind: InclusionCriterionList
    plural: inclusioncriteria
    singular: inclusioncriterion
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the InclusionCriterion
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the InclusionCriterion
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: InclusionCriterion is the Schema for the inclusioncriterions
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InclusionCriterionSpec defines the desired state of InclusionCriterion.
            properties:
              association:
                description: InclusionCriterionAssociation defines nested fields for
                  InclusionCriterion.Association.
                properties:
                  jsonData:
                    type: string
                  tenancyId:
                    description: The Oracle ID (OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm))
                      of the tenancy to which the governance rule will be applied
                      as part of this tenancy inclusion criterion.
                    type: string
                  type:
                    type: string
                type: object
              governanceRuleId:
                description: The Oracle ID (OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm))
                  of the governance rule. Every inclusion criterion is associated
                  with a governance rule.
                type: string
              type:
                description: Type of inclusion criterion - TENANCY, ALL or TAG. We
                  support TENANCY and ALL for now.
                type: string
            required:
            - governanceRuleId
            - type
            type: object
          status:
            description: InclusionCriterionStatus defines the observed state of InclusionCriterion.
            properties:
              association:
                description: InclusionCriterionAssociation defines nested fields for
                  InclusionCriterion.Association.
                properties:
                  jsonData:
                    type: string
                  tenancyId:
                    description: The Oracle ID (OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm))
                      of the tenancy to which the governance rule will be applied
                      as part of this tenancy inclusion criterion.
                    type: string
                  type:
                    type: string
                type: object
              governanceRuleId:
                description: The Oracle ID (OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm))
                  of the governance rule. Every inclusion criterion is associated
                  with a governance rule.
                type: string
              id:
                description: The Oracle ID (OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm))
                  of the inclusion criterion.
                type: string
              lifecycleState:
                description: The current state of the inclusion criterion.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  Date and time the inclusion criterion was created. An RFC3339 formatted datetime string.
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              timeUpdated:
                description: |-
                  Date and time the inclusion criterion was updated. An RFC3339 formatted datetime string.
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              type:
                description: Type of inclusion criterion - TENANCY, ALL or TAG. We
                  support TENANCY and ALL for now.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
