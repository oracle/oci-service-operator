---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: bootvolumes.core.oracle.com
spec:
  group: core.oracle.com
  names:
    kind: BootVolume
    listKind: BootVolumeList
    plural: bootvolumes
    singular: bootvolume
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the BootVolume
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the BootVolume
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: BootVolume is the Schema for the bootvolumes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BootVolumeSpec defines the desired state of BootVolume.
            properties:
              autotunePolicies:
                description: The list of autotune policies to be enabled for this
                  volume.
                items:
                  description: BootVolumeAutotunePolicy defines nested fields for
                    BootVolume.AutotunePolicy.
                  properties:
                    autotuneType:
                      type: string
                    jsonData:
                      type: string
                    maxVpusPerGB:
                      description: |-
                        This will be the maximum VPUs/GB performance level that the volume will be auto-tuned
                        temporarily based on performance monitoring.
                      format: int64
                      type: integer
                  type: object
                type: array
              availabilityDomain:
                description: |-
                  The availability domain of the volume. Omissible for cloning a volume. The new volume will be created in the availability domain of the source volume.
                  Example: `Uocm:PHX-AD-1`
                type: string
              backupPolicyId:
                description: |-
                  If provided, specifies the ID of the boot volume backup policy to assign to the newly
                  created boot volume. If omitted, no policy will be assigned.
                type: string
              bootVolumeReplicas:
                description: |-
                  The list of boot volume replicas to be enabled for this boot volume
                  in the specified destination availability domains.
                items:
                  description: BootVolumeReplicaFields defines nested fields for BootVolume.BootVolumeReplica.
                  properties:
                    availabilityDomain:
                      description: |-
                        The availability domain of the boot volume replica.
                        Example: `Uocm:PHX-AD-1`
                      type: string
                    displayName:
                      description: |-
                        A user-friendly name. Does not have to be unique, and it's changeable.
                        Avoid entering confidential information.
                      type: string
                  required:
                  - availabilityDomain
                  type: object
                type: array
              compartmentId:
                description: The OCID of the compartment that contains the boot volume.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              isAutoTuneEnabled:
                description: |-
                  Specifies whether the auto-tune performance is enabled for this boot volume. This field is deprecated.
                  Use the `DetachedVolumeAutotunePolicy` instead to enable the volume for detached autotune.
                type: boolean
              kmsKeyId:
                description: |-
                  The OCID of the Vault service key to assign as the master encryption key
                  for the boot volume.
                type: string
              sizeInGBs:
                description: The size of the volume in GBs.
                format: int64
                type: integer
              sourceDetails:
                description: BootVolumeSourceDetails defines nested fields for BootVolume.SourceDetails.
                properties:
                  id:
                    description: The OCID of the boot volume backup.
                    type: string
                  jsonData:
                    type: string
                  type:
                    type: string
                type: object
              vpusPerGB:
                description: |-
                  The number of volume performance units (VPUs) that will be applied to this volume per GB,
                  representing the Block Volume service's elastic performance options.
                  See Block Volume Performance Levels (https://docs.cloud.oracle.com/iaas/Content/Block/Concepts/blockvolumeperformance.htm#perf_levels) for more information.
                  Allowed values:
                    * `10`: Represents the Balanced option.
                    * `20`: Represents the Higher Performance option.
                    * `30`-`120`: Represents the Ultra High Performance option.
                  For performance autotune enabled volumes, it would be the Default(Minimum) VPUs/GB.
                format: int64
                type: integer
            required:
            - compartmentId
            - sourceDetails
            type: object
          status:
            description: BootVolumeStatus defines the observed state of BootVolume.
            properties:
              autoTunedVpusPerGB:
                description: The number of Volume Performance Units per GB that this
                  boot volume is effectively tuned to.
                format: int64
                type: integer
              autotunePolicies:
                description: The list of autotune policies enabled for this volume.
                items:
                  description: BootVolumeAutotunePolicy defines nested fields for
                    BootVolume.AutotunePolicy.
                  properties:
                    autotuneType:
                      type: string
                    jsonData:
                      type: string
                    maxVpusPerGB:
                      description: |-
                        This will be the maximum VPUs/GB performance level that the volume will be auto-tuned
                        temporarily based on performance monitoring.
                      format: int64
                      type: integer
                  type: object
                type: array
              availabilityDomain:
                description: |-
                  The availability domain of the boot volume.
                  Example: `Uocm:PHX-AD-1`
                type: string
              bootVolumeReplicas:
                description: The list of boot volume replicas of this boot volume
                items:
                  description: BootVolumeReplicaFields defines nested fields for BootVolume.BootVolumeReplica.
                  properties:
                    availabilityDomain:
                      description: |-
                        The availability domain of the boot volume replica.
                        Example: `Uocm:PHX-AD-1`
                      type: string
                    displayName:
                      description: |-
                        A user-friendly name. Does not have to be unique, and it's changeable.
                        Avoid entering confidential information.
                      type: string
                  required:
                  - availabilityDomain
                  type: object
                type: array
              compartmentId:
                description: The OCID of the compartment that contains the boot volume.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The boot volume's Oracle ID (OCID).
                type: string
              imageId:
                description: The image OCID used to create the boot volume.
                type: string
              isAutoTuneEnabled:
                description: |-
                  Specifies whether the auto-tune performance is enabled for this boot volume. This field is deprecated.
                  Use the `DetachedVolumeAutotunePolicy` instead to enable the volume for detached autotune.
                type: boolean
              isHydrated:
                description: |-
                  Specifies whether the boot volume's data has finished copying
                  from the source boot volume or boot volume backup.
                type: boolean
              kmsKeyId:
                description: The OCID of the Vault service master encryption key assigned
                  to the boot volume.
                type: string
              lifecycleState:
                description: The current state of a boot volume.
                type: string
              sizeInGBs:
                description: The size of the boot volume in GBs.
                format: int64
                type: integer
              sizeInMBs:
                description: |-
                  The size of the volume in MBs. The value must be a multiple of 1024.
                  This field is deprecated. Please use sizeInGBs.
                format: int64
                type: integer
              sourceDetails:
                description: BootVolumeSourceDetails defines nested fields for BootVolume.SourceDetails.
                properties:
                  id:
                    description: The OCID of the boot volume backup.
                    type: string
                  jsonData:
                    type: string
                  type:
                    type: string
                type: object
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              timeCreated:
                description: |-
                  The date and time the boot volume was created. Format defined
                  by RFC3339 (https://tools.ietf.org/html/rfc3339).
                type: string
              volumeGroupId:
                description: The OCID of the source volume group.
                type: string
              vpusPerGB:
                description: |-
                  The number of volume performance units (VPUs) that will be applied to this boot volume per GB,
                  representing the Block Volume service's elastic performance options.
                  See Block Volume Performance Levels (https://docs.cloud.oracle.com/iaas/Content/Block/Concepts/blockvolumeperformance.htm#perf_levels) for more information.
                  Allowed values:
                    * `10`: Represents Balanced option.
                    * `20`: Represents Higher Performance option.
                    * `30`-`120`: Represents the Ultra High Performance option.
                  For performance autotune enabled volumes, it would be the Default(Minimum) VPUs/GB.
                format: int64
                type: integer
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
