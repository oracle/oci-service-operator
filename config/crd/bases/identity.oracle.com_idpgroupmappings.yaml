---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: idpgroupmappings.identity.oracle.com
spec:
  group: identity.oracle.com
  names:
    kind: IdpGroupMapping
    listKind: IdpGroupMappingList
    plural: idpgroupmappings
    singular: idpgroupmapping
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the IdpGroupMapping
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the IdpGroupMapping
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: IdpGroupMapping is the Schema for the idpgroupmappings API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IdpGroupMappingSpec defines the desired state of IdpGroupMapping.
            properties:
              groupId:
                description: |-
                  The OCID of the IAM Service Group
                  you want to map to the IdP group.
                type: string
              idpGroupName:
                description: The name of the IdP group you want to map.
                type: string
            required:
            - groupId
            - idpGroupName
            type: object
          status:
            description: IdpGroupMappingStatus defines the observed state of IdpGroupMapping.
            properties:
              compartmentId:
                description: The OCID of the tenancy containing the `IdentityProvider`.
                type: string
              groupId:
                description: The OCID of the IAM Service group that is mapped to the
                  IdP group.
                type: string
              id:
                description: The OCID of the `IdpGroupMapping`.
                type: string
              idpGroupName:
                description: The name of the IdP group that is mapped to the IAM Service
                  group.
                type: string
              idpId:
                description: The OCID of the `IdentityProvider` this mapping belongs
                  to.
                type: string
              inactiveStatus:
                description: The detailed status of INACTIVE lifecycleState.
                format: int64
                type: integer
              lifecycleState:
                description: |-
                  The mapping's current state.  After creating a mapping object, make sure its `lifecycleState` changes
                  from CREATING to ACTIVE before using it.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  Date and time the mapping was created, in the format defined by RFC3339.
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
