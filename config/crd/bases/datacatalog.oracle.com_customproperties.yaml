---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: customproperties.datacatalog.oracle.com
spec:
  group: datacatalog.oracle.com
  names:
    kind: CustomProperty
    listKind: CustomPropertyList
    plural: customproperties
    singular: customproperty
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the CustomProperty
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the CustomProperty
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: CustomProperty is the Schema for the customproperties API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CustomPropertySpec defines the desired state of CustomProperty.
            properties:
              allowedValues:
                description: Allowed values for the custom property if any
                items:
                  type: string
                type: array
              dataType:
                description: The data type of the custom property
                type: string
              description:
                description: Detailed description of the custom property.
                type: string
              displayName:
                description: |-
                  A user-friendly display name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              isEditable:
                description: If this field is a editable field
                type: boolean
              isEventEnabled:
                description: If an OCI Event will be emitted when the custom property
                  is modified.
                type: boolean
              isFilterable:
                description: If this field allows to filter or create facets from
                  UI
                type: boolean
              isHidden:
                description: If this field is a hidden field
                type: boolean
              isHiddenInSearch:
                description: If this field is allowed to pop in search results
                type: boolean
              isMultiValued:
                description: If this field allows multiple values to be set
                type: boolean
              isShownInList:
                description: If this field is displayed in a list view of applicable
                  objects.
                type: boolean
              isSortable:
                description: If this field allows to sort from UI
                type: boolean
              properties:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  A map of maps that contains the properties which are specific to the data asset type. Each data asset type
                  definition defines it's set of required and optional properties. The map keys are category names and the
                  values are maps of property name to property value. Every property is contained inside of a category. Most
                  data assets have required properties within the "default" category. To determine the set of optional and
                  required properties for a data asset type, a query can be done on '/types?type=dataAsset' that returns a
                  collection of all data asset types. The appropriate data asset type, which includes definitions of all of
                  it's properties, can be identified from this collection.
                  Example: `{"properties": { "default": { "host": "host1", "port": "1521", "database": "orcl"}}}`
                type: object
            required:
            - displayName
            type: object
          status:
            description: CustomPropertyStatus defines the observed state of CustomProperty.
            properties:
              allowedValues:
                description: Allowed values for the custom property if any
                items:
                  type: string
                type: array
              createdById:
                description: OCID of the user who created the custom property.
                type: string
              dataType:
                description: Data type of the custom property
                type: string
              description:
                description: Description for the custom property
                type: string
              displayName:
                description: Display name of the custom property
                type: string
              events:
                description: Event configuration for this custom property, against
                  the desired subset of object types to which the property applies.
                items:
                  description: CustomPropertyEvent defines nested fields for CustomProperty.Event.
                  properties:
                    createdById:
                      description: OCID of the user who created the configuration.
                      type: string
                    eventConfigStatus:
                      description: Status of the configuration.
                      type: string
                    propertyId:
                      description: Unique property key identifier.
                      type: string
                    propertyName:
                      description: Name of the property.
                      type: string
                    timeCreated:
                      description: |-
                        The date and time the event was configured, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                        Example: `2019-03-25T21:10:29.600Z`
                      type: string
                    timeUpdated:
                      description: The last time that any change was made to the configuration.
                        An RFC3339 (https://tools.ietf.org/html/rfc3339) formatted
                        datetime string.
                      type: string
                    typeId:
                      description: Unique type key identifier.
                      type: string
                    typeName:
                      description: Name of the type.
                      type: string
                    updatedById:
                      description: OCID of the user who last modified the configuration.
                      type: string
                  type: object
                type: array
              isEditable:
                description: If this field is a editable field
                type: boolean
              isEventEnabled:
                description: If an OCI Event will be emitted when the custom property
                  is modified.
                type: boolean
              isFilterable:
                description: If this field allows to filter or create facets from
                  UI
                type: boolean
              isHidden:
                description: If this field is a hidden field
                type: boolean
              isHiddenInSearch:
                description: If this field is allowed to pop in search results
                type: boolean
              isListType:
                description: Is this property allowed to have list of values
                type: boolean
              isMultiValued:
                description: If this field allows multiple values to be set
                type: boolean
              isServiceDefined:
                description: If this field is defined by service or by a user
                type: boolean
              isShownInList:
                description: If this field is displayed in a list view of applicable
                  objects.
                type: boolean
              isSortable:
                description: If this field allows to sort from UI
                type: boolean
              key:
                description: Unique data asset key that is immutable.
                type: string
              lifecycleState:
                description: The current state of the custom property.
                type: string
              namespaceName:
                description: Namespace name of the custom property
                type: string
              properties:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  A map of maps that contains the properties which are specific to the asset type. Each data asset type
                  definition defines it's set of required and optional properties. The map keys are category names and the
                  values are maps of property name to property value. Every property is contained inside of a category. Most
                  data assets have required properties within the "default" category.
                  Example: `{"properties": { "default": { "host": "host1", "port": "1521", "database": "orcl"}}}`
                type: object
              scope:
                description: The set of object types to which the custom property
                  applies.
                items:
                  description: CustomPropertyScope defines nested fields for CustomProperty.Scope.
                  properties:
                    count:
                      description: Number of objects associated with this type
                      type: integer
                    isEventEnabled:
                      description: If an OCI Event will be emitted when the custom
                        property is modified.
                      type: boolean
                    typeId:
                      description: Unique type key identifier
                      type: string
                    typeName:
                      description: Name of the type associated with
                      type: string
                  type: object
                type: array
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time the custom property was created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2019-03-25T21:10:29.600Z`
                type: string
              timeUpdated:
                description: The last time that any change was made to the custom
                  property. An RFC3339 (https://tools.ietf.org/html/rfc3339) formatted
                  datetime string.
                type: string
              updatedById:
                description: OCID of the user who last modified the custom property.
                type: string
              usageCount:
                description: Total number of first class objects using this custom
                  property
                type: integer
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
