---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: computecapacityreservations.core.oracle.com
spec:
  group: core.oracle.com
  names:
    kind: ComputeCapacityReservation
    listKind: ComputeCapacityReservationList
    plural: computecapacityreservations
    singular: computecapacityreservation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the ComputeCapacityReservation
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ComputeCapacityReservation
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ComputeCapacityReservation is the Schema for the computecapacityreservations
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ComputeCapacityReservationSpec defines the desired state
              of ComputeCapacityReservation.
            properties:
              availabilityDomain:
                description: |-
                  The availability domain of this compute capacity reservation.
                  Example: `Uocm:PHX-AD-1`
                type: string
              compartmentId:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment containing the capacity reservation.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              instanceReservationConfigs:
                description: |-
                  The capacity configurations for the capacity reservation.
                  To use the reservation for the desired shape, specify the shape, count, and
                  optionally the fault domain where you want this configuration.
                items:
                  description: ComputeCapacityReservationInstanceReservationConfig
                    defines nested fields for ComputeCapacityReservation.InstanceReservationConfig.
                  properties:
                    clusterConfig:
                      description: ComputeCapacityReservationInstanceReservationConfigClusterConfig
                        defines nested fields for ComputeCapacityReservation.InstanceReservationConfig.ClusterConfig.
                      properties:
                        hpcIslandId:
                          description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                            of the HPC island.
                          type: string
                        networkBlockIds:
                          description: The list of OCIDs of the network blocks.
                          items:
                            type: string
                          type: array
                      required:
                      - hpcIslandId
                      type: object
                    faultDomain:
                      description: |-
                        The fault domain to use for instances created using this capacity configuration.
                        For more information, see Fault Domains (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/regions.htm#fault).
                        If you do not specify the fault domain, the capacity is available for an instance
                        that does not specify a fault domain. To change the fault domain for a reservation,
                        delete the reservation and create a new one in the preferred fault domain.
                        To retrieve a list of fault domains, use the `ListFaultDomains` operation in
                        the Identity and Access Management Service API (https://docs.cloud.oracle.com/iaas/api/#/en/identity/20160918/).
                        Example: `FAULT-DOMAIN-1`
                      type: string
                    instanceShape:
                      description: |-
                        The shape requested when launching instances using reserved capacity.
                        The shape determines the number of CPUs, amount of memory,
                        and other resources allocated to the instance.
                        You can list all available shapes by calling ListComputeCapacityReservationInstanceShapes.
                      type: string
                    instanceShapeConfig:
                      description: ComputeCapacityReservationInstanceReservationConfigInstanceShapeConfig
                        defines nested fields for ComputeCapacityReservation.InstanceReservationConfig.InstanceShapeConfig.
                      properties:
                        memoryInGBs:
                          description: The total amount of memory available to the
                            instance, in gigabytes.
                          type: number
                        ocpus:
                          description: The total number of OCPUs available to the
                            instance.
                          type: number
                      type: object
                    reservedCount:
                      description: The total number of instances that can be launched
                        from the capacity configuration.
                      format: int64
                      type: integer
                  required:
                  - instanceShape
                  - reservedCount
                  type: object
                type: array
              isDefaultReservation:
                description: |-
                  Whether this capacity reservation is the default.
                  For more information, see Capacity Reservations (https://docs.cloud.oracle.com/iaas/Content/Compute/Tasks/reserve-capacity.htm#default).
                type: boolean
            required:
            - availabilityDomain
            - compartmentId
            type: object
          status:
            description: ComputeCapacityReservationStatus defines the observed state
              of ComputeCapacityReservation.
            properties:
              availabilityDomain:
                description: |-
                  The availability domain of the compute capacity reservation.
                  Example: `Uocm:PHX-AD-1`
                type: string
              compartmentId:
                description: |-
                  The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the compartment
                  containing the compute capacity reservation.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compute capacity reservation.
                type: string
              instanceReservationConfigs:
                description: |-
                  The capacity configurations for the capacity reservation.
                  To use the reservation for the desired shape, specify the shape, count, and
                  optionally the fault domain where you want this configuration.
                items:
                  description: ComputeCapacityReservationInstanceReservationConfigObservedState
                    defines nested fields for ComputeCapacityReservation.InstanceReservationConfig.
                  properties:
                    clusterConfig:
                      description: ComputeCapacityReservationInstanceReservationConfigClusterConfig
                        defines nested fields for ComputeCapacityReservation.InstanceReservationConfig.ClusterConfig.
                      properties:
                        hpcIslandId:
                          description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                            of the HPC island.
                          type: string
                        networkBlockIds:
                          description: The list of OCIDs of the network blocks.
                          items:
                            type: string
                          type: array
                      required:
                      - hpcIslandId
                      type: object
                    faultDomain:
                      description: |-
                        The fault domain to use for instances created using this capacity configuration.
                        For more information, see Fault Domains (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/regions.htm#fault).
                        If you do not specify the fault domain, the capacity is available for an instance
                        that does not specify a fault domain. To change the fault domain for a reservation,
                        delete the reservation and create a new one in the preferred fault domain.
                        To retrieve a list of fault domains, use the `ListFaultDomains` operation in
                        the Identity and Access Management Service API (https://docs.cloud.oracle.com/iaas/api/#/en/identity/20160918/).
                        Example: `FAULT-DOMAIN-1`
                      type: string
                    instanceShape:
                      description: |-
                        The shape requested when launching instances using reserved capacity.
                        The shape determines the number of CPUs, amount of memory,
                        and other resources allocated to the instance.
                        You can list all available shapes by calling ListComputeCapacityReservationInstanceShapes.
                      type: string
                    instanceShapeConfig:
                      description: ComputeCapacityReservationInstanceReservationConfigInstanceShapeConfig
                        defines nested fields for ComputeCapacityReservation.InstanceReservationConfig.InstanceShapeConfig.
                      properties:
                        memoryInGBs:
                          description: The total amount of memory available to the
                            instance, in gigabytes.
                          type: number
                        ocpus:
                          description: The total number of OCPUs available to the
                            instance.
                          type: number
                      type: object
                    reservedCount:
                      description: The total number of instances that can be launched
                        from the capacity configuration.
                      format: int64
                      type: integer
                  required:
                  - instanceShape
                  - reservedCount
                  type: object
                type: array
              isDefaultReservation:
                description: |-
                  Whether this capacity reservation is the default.
                  For more information, see Capacity Reservations (https://docs.cloud.oracle.com/iaas/Content/Compute/Tasks/reserve-capacity.htm#default).
                type: boolean
              lifecycleState:
                description: The current state of the compute capacity reservation.
                type: string
              reservedInstanceCount:
                description: |-
                  The number of instances for which capacity will be held with this
                  compute capacity reservation. This number is the sum of the values of the `reservedCount` fields
                  for all of the instance capacity configurations under this reservation.
                  The purpose of this field is to calculate the percentage usage of the reservation.
                format: int64
                type: integer
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time the compute capacity reservation was created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              timeUpdated:
                description: |-
                  The date and time the compute capacity reservation was updated, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              usedInstanceCount:
                description: |-
                  The total number of instances currently consuming space in
                  this compute capacity reservation. This number is the sum of the values of the `usedCount` fields
                  for all of the instance capacity configurations under this reservation.
                  The purpose of this field is to calculate the percentage usage of the reservation.
                format: int64
                type: integer
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
