---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  name: vaults.keymanagement.oracle.com
spec:
  group: keymanagement.oracle.com
  names:
    kind: Vault
    listKind: VaultList
    plural: vaults
    singular: vault
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the Vault
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Vault
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Vault is the Schema for the vaults API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VaultSpec defines the desired state of Vault.
            properties:
              compartmentId:
                description: The OCID of the compartment where you want to create
                  this vault.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              deletionScheduleDays:
                description: |-
                  The number of days to retain the Vault after OCI deletion has been scheduled.
                  Set a value from 7 to 30 to control the retention window used when Vault deletion is scheduled.
                format: int32
                maximum: 30
                minimum: 7
                type: integer
              displayName:
                description: |-
                  A user-friendly name for the vault. It does not have to be unique, and it is changeable.
                  Avoid entering confidential information.
                type: string
              externalKeyManagerMetadata:
                description: VaultExternalKeyManagerMetadata defines nested fields
                  for Vault.ExternalKeyManagerMetadata.
                properties:
                  externalVaultEndpointUrl:
                    description: URI of the vault on external key manager.
                    type: string
                  oauthMetadata:
                    description: VaultExternalKeyManagerMetadataOauthMetadata defines
                      nested fields for Vault.ExternalKeyManagerMetadata.OauthMetadata.
                    properties:
                      clientAppId:
                        description: ID of the client app created in IDP.
                        type: string
                      clientAppSecret:
                        description: Secret of the client app created in IDP.
                        type: string
                      idcsAccountNameUrl:
                        description: Base URL of the IDCS account where confidential
                          client app is created.
                        type: string
                    required:
                    - clientAppId
                    - clientAppSecret
                    - idcsAccountNameUrl
                    type: object
                  privateEndpointId:
                    description: OCID of private endpoint created by customer.
                    type: string
                required:
                - externalVaultEndpointUrl
                - oauthMetadata
                - privateEndpointId
                type: object
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              vaultType:
                description: The type of vault to create. Each type of vault stores
                  the key with different degrees of isolation and has different options
                  and pricing.
                type: string
            required:
            - compartmentId
            - displayName
            - vaultType
            type: object
          status:
            description: VaultStatus defines the observed state of Vault.
            properties:
              compartmentId:
                description: The OCID of the compartment that contains this vault.
                type: string
              cryptoEndpoint:
                description: |-
                  The service endpoint to perform cryptographic operations against. Cryptographic operations include
                  Encrypt (https://docs.cloud.oracle.com/api/#/en/key/latest/EncryptedData/Encrypt), Decrypt (https://docs.cloud.oracle.com/api/#/en/key/latest/DecryptedData/Decrypt),
                  and GenerateDataEncryptionKey (https://docs.cloud.oracle.com/api/#/en/key/latest/GeneratedKey/GenerateDataEncryptionKey) operations.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name for the vault. It does not have to be unique, and it is changeable.
                  Avoid entering confidential information.
                type: string
              externalKeyManagerMetadataSummary:
                description: VaultExternalKeyManagerMetadataSummary defines nested
                  fields for Vault.ExternalKeyManagerMetadataSummary.
                properties:
                  externalVaultEndpointUrl:
                    description: URL of the vault on external key manager.
                    type: string
                  oauthMetadataSummary:
                    description: VaultExternalKeyManagerMetadataSummaryOauthMetadataSummary
                      defines nested fields for Vault.ExternalKeyManagerMetadataSummary.OauthMetadataSummary.
                    properties:
                      clientAppId:
                        description: ID of the client app created in IDP.
                        type: string
                      idcsAccountNameUrl:
                        description: Base URL of the IDCS account where confidential
                          client app is created.
                        type: string
                    type: object
                  privateEndpointId:
                    description: OCID of the private endpoint.
                    type: string
                  vendor:
                    description: Vendor of the external key manager.
                    type: string
                type: object
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The OCID of the vault.
                type: string
              isPrimary:
                description: A Boolean value that indicates whether the Vault is primary
                  Vault or replica Vault.
                type: boolean
              lifecycleState:
                description: |-
                  The vault's current lifecycle state.
                  Example: `DELETED`
                type: string
              managementEndpoint:
                description: The service endpoint to perform management operations
                  against. Management operations include "Create," "Update," "List,"
                  "Get," and "Delete" operations.
                type: string
              replicaDetails:
                description: VaultReplicaDetails defines nested fields for Vault.ReplicaDetails.
                properties:
                  replicationId:
                    description: ReplicationId associated with a vault operation
                    type: string
                type: object
              requestedDeletionScheduleDays:
                description: The retention period in days last applied when Vault
                  deletion was scheduled.
                format: int32
                type: integer
              restoredFromVaultId:
                description: |-
                  The OCID of the vault from which this vault was restored, if it was restored from a backup file.
                  If you restore a vault to the same region, the vault retains the same OCID that it had when you
                  backed up the vault.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time this vault was created, expressed in RFC 3339 (https://tools.ietf.org/html/rfc3339) timestamp format.
                  Example: `2018-04-03T21:10:29.600Z`
                type: string
              timeOfDeletion:
                description: |-
                  An optional property to indicate when to delete the vault, expressed in RFC 3339 (https://tools.ietf.org/html/rfc3339) timestamp format.
                  Example: `2018-04-03T21:10:29.600Z`
                type: string
              vaultType:
                description: |-
                  The type of vault. Each type of vault stores the key with different
                  degrees of isolation and has different options and pricing.
                type: string
              wrappingkeyId:
                description: The OCID of the vault's wrapping key.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
