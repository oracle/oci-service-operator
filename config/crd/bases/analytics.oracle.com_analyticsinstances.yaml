---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: analyticsinstances.analytics.oracle.com
spec:
  group: analytics.oracle.com
  names:
    kind: AnalyticsInstance
    listKind: AnalyticsInstanceList
    plural: analyticsinstances
    singular: analyticsinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      priority: 1
      type: string
    - description: status of the AnalyticsInstance
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the AnalyticsInstance
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: AnalyticsInstance is the Schema for the analyticsinstances API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AnalyticsInstanceSpec defines the desired state of AnalyticsInstance.
            properties:
              adminUser:
                description: user name of the authorized user.
                type: string
              capacity:
                description: AnalyticsInstanceCapacity defines nested fields for AnalyticsInstance.Capacity.
                properties:
                  capacityType:
                    description: |-
                      The capacity model to use.
                      Accepted values are:
                      OLPU_COUNT, USER_COUNT
                    type: string
                  capacityValue:
                    description: |-
                      The capacity value selected, either the number of OCPUs (OLPU_COUNT) or the number of users (USER_COUNT).
                      This parameter affects the number of OCPUs, amount of memory, and other resources allocated to the instance.
                    type: integer
                required:
                - capacityType
                - capacityValue
                type: object
              compartmentId:
                description: The OCID of the compartment.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              description:
                description: Optional description.
                type: string
              domainId:
                description: domain id for which the user is authorized.
                type: string
              emailNotification:
                description: Email address receiving notifications.
                type: string
              featureBundle:
                description: The feature set of an Analytics instance.
                type: string
              featureSet:
                description: Analytics feature set.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              idcsAccessToken:
                description: IDCS access token identifying a stripe and service administrator
                  user.
                type: string
              kmsKeyId:
                description: OCID of the OCI Vault Key encrypting the customer data
                  stored in this Analytics instance. A null value indicates Oracle
                  managed default encryption.
                type: string
              licenseType:
                description: The license used for the service.
                type: string
              name:
                description: The name of the Analytics instance. This name must be
                  unique in the tenancy and cannot be changed.
                type: string
              networkEndpointDetails:
                description: AnalyticsInstanceNetworkEndpointDetails defines nested
                  fields for AnalyticsInstance.NetworkEndpointDetails.
                properties:
                  jsonData:
                    type: string
                  networkEndpointType:
                    type: string
                  networkSecurityGroupIds:
                    description: Network Security Group OCIDs for an Analytics instance.
                    items:
                      type: string
                    type: array
                  subnetId:
                    description: The subnet OCID for the private endpoint.
                    type: string
                  vcnId:
                    description: The VCN OCID for the private endpoint.
                    type: string
                  whitelistedIps:
                    description: Source IP addresses or IP address ranges in ingress
                      rules.
                    items:
                      type: string
                    type: array
                  whitelistedServices:
                    description: Oracle Cloud Services that are allowed to access
                      this Analytics instance.
                    items:
                      type: string
                    type: array
                  whitelistedVcns:
                    description: Virtual Cloud Networks allowed to access this network
                      endpoint.
                    items:
                      description: AnalyticsInstanceNetworkEndpointDetailsWhitelistedVcn
                        defines nested fields for AnalyticsInstance.NetworkEndpointDetails.WhitelistedVcn.
                      properties:
                        id:
                          description: The Virtual Cloud Network OCID.
                          type: string
                        whitelistedIps:
                          description: Source IP addresses or IP address ranges in
                            ingress rules.
                          items:
                            type: string
                          type: array
                      required:
                      - id
                      type: object
                    type: array
                type: object
              updateChannel:
                description: Analytics instance update channel.
                type: string
            required:
            - capacity
            - compartmentId
            - featureSet
            - licenseType
            - name
            type: object
          status:
            description: AnalyticsInstanceStatus defines the observed state of AnalyticsInstance.
            properties:
              capacity:
                description: AnalyticsInstanceCapacity defines nested fields for AnalyticsInstance.Capacity.
                properties:
                  capacityType:
                    description: |-
                      The capacity model to use.
                      Accepted values are:
                      OLPU_COUNT, USER_COUNT
                    type: string
                  capacityValue:
                    description: |-
                      The capacity value selected, either the number of OCPUs (OLPU_COUNT) or the number of users (USER_COUNT).
                      This parameter affects the number of OCPUs, amount of memory, and other resources allocated to the instance.
                    type: integer
                required:
                - capacityType
                - capacityValue
                type: object
              compartmentId:
                description: The OCID of the compartment.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              description:
                description: Optional description.
                type: string
              domainId:
                description: Identity domain OCID.
                type: string
              emailNotification:
                description: Email address receiving notifications.
                type: string
              featureBundle:
                description: The feature set of an Analytics instance.
                type: string
              featureSet:
                description: Analytics feature set.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The resource OCID.
                type: string
              kmsKeyId:
                description: OCID of the OCI Vault Key encrypting the customer data
                  stored in this Analytics instance. A null value indicates Oracle
                  managed default encryption.
                type: string
              licenseType:
                description: The license used for the service.
                type: string
              lifecycleState:
                description: The current state of an instance.
                type: string
              name:
                description: The name of the Analytics instance. This name must be
                  unique in the tenancy and cannot be changed.
                type: string
              networkEndpointDetails:
                description: AnalyticsInstanceNetworkEndpointDetails defines nested
                  fields for AnalyticsInstance.NetworkEndpointDetails.
                properties:
                  jsonData:
                    type: string
                  networkEndpointType:
                    type: string
                  networkSecurityGroupIds:
                    description: Network Security Group OCIDs for an Analytics instance.
                    items:
                      type: string
                    type: array
                  subnetId:
                    description: The subnet OCID for the private endpoint.
                    type: string
                  vcnId:
                    description: The VCN OCID for the private endpoint.
                    type: string
                  whitelistedIps:
                    description: Source IP addresses or IP address ranges in ingress
                      rules.
                    items:
                      type: string
                    type: array
                  whitelistedServices:
                    description: Oracle Cloud Services that are allowed to access
                      this Analytics instance.
                    items:
                      type: string
                    type: array
                  whitelistedVcns:
                    description: Virtual Cloud Networks allowed to access this network
                      endpoint.
                    items:
                      description: AnalyticsInstanceNetworkEndpointDetailsWhitelistedVcn
                        defines nested fields for AnalyticsInstance.NetworkEndpointDetails.WhitelistedVcn.
                      properties:
                        id:
                          description: The Virtual Cloud Network OCID.
                          type: string
                        whitelistedIps:
                          description: Source IP addresses or IP address ranges in
                            ingress rules.
                          items:
                            type: string
                          type: array
                      required:
                      - id
                      type: object
                    type: array
                type: object
              privateAccessChannels:
                additionalProperties:
                  description: AnalyticsInstancePrivateAccessChannels defines nested
                    fields for AnalyticsInstance.PrivateAccessChannels.
                  properties:
                    displayName:
                      description: Display Name of the Private Access Channel.
                      type: string
                    egressSourceIpAddresses:
                      description: |-
                        The list of IP addresses from the customer subnet connected to private access channel, used as a source Ip by Private Access Channel
                        for network traffic from the AnalyticsInstance to Private Sources.
                      items:
                        type: string
                      type: array
                    ipAddress:
                      description: IP Address of the Private Access channel.
                      type: string
                    key:
                      description: Private Access Channel unique identifier key.
                      type: string
                    networkSecurityGroupIds:
                      description: Network Security Group OCIDs for an Analytics instance.
                      items:
                        type: string
                      type: array
                    privateSourceDnsZones:
                      description: |-
                        List of Private Source DNS zones registered with Private Access Channel,
                        where datasource hostnames from these dns zones / domains will be resolved in the peered VCN for access from Analytics Instance.
                        Min of 1 is required and Max of 30 Private Source DNS zones can be registered.
                      items:
                        description: AnalyticsInstancePrivateAccessChannelsPrivateSourceDnsZone
                          defines nested fields for AnalyticsInstance.PrivateAccessChannels.PrivateSourceDnsZone.
                        properties:
                          description:
                            description: Description of private source dns zone.
                            type: string
                          dnsZone:
                            description: 'Private Source DNS Zone. Ex: example-vcn.oraclevcn.com,
                              corp.example.com.'
                            type: string
                        type: object
                      type: array
                    privateSourceScanHosts:
                      description: List of Private Source DB SCAN hosts registered
                        with Private Access Channel for access from Analytics Instance.
                      items:
                        description: AnalyticsInstancePrivateAccessChannelsPrivateSourceScanHost
                          defines nested fields for AnalyticsInstance.PrivateAccessChannels.PrivateSourceScanHost.
                        properties:
                          description:
                            description: Description of private source scan host zone.
                            type: string
                          scanHostname:
                            description: 'Private Source Scan hostname. Ex: db01-scan.corp.example.com,
                              prd-db01-scan.mycompany.com.'
                            type: string
                          scanPort:
                            description: Private Source Scan host port. This is the
                              source port where SCAN protocol will get connected (e.g.
                              1521).
                            type: integer
                        type: object
                      type: array
                    subnetId:
                      description: OCID of the customer subnet connected to private
                        access channel.
                      type: string
                    vcnId:
                      description: OCID of the customer VCN peered with private access
                        channel.
                      type: string
                  type: object
                description: Map of PrivateAccessChannel unique identifier key as
                  KEY and PrivateAccessChannel Object as VALUE.
                type: object
              serviceUrl:
                description: URL of the Analytics service.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. These predefined keys are scoped to namespaces.
                  Example: `{"orcl-cloud": {"key": "value"}}`
                type: object
              timeCreated:
                description: |-
                  The date and time the instance was created, in the format defined by RFC3339.
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              timeUpdated:
                description: |-
                  The date and time the instance was last updated (in the format defined by RFC3339).
                  This timestamp represents updates made through this API. External events do not
                  influence it.
                type: string
              updateChannel:
                description: Analytics instance update channel.
                type: string
              vanityUrlDetails:
                additionalProperties:
                  description: AnalyticsInstanceVanityUrlDetails defines nested fields
                    for AnalyticsInstance.VanityUrlDetails.
                  properties:
                    description:
                      description: Description of the vanity url.
                      type: string
                    hosts:
                      description: List of fully qualified hostnames supported by
                        this vanity URL definition (max of 3).
                      items:
                        type: string
                      type: array
                    key:
                      description: The vanity url unique identifier key.
                      type: string
                    publicCertificate:
                      description: PEM certificate for HTTPS connections.
                      type: string
                    urls:
                      description: List of urls supported by this vanity URL definition
                        (max of 3).
                      items:
                        type: string
                      type: array
                  type: object
                description: Map of VanityUrl unique identifier key as KEY and VanityUrl
                  Object as VALUE.
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
