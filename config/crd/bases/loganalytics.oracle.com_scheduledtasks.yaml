---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: scheduledtasks.loganalytics.oracle.com
spec:
  group: loganalytics.oracle.com
  names:
    kind: ScheduledTask
    listKind: ScheduledTaskList
    plural: scheduledtasks
    singular: scheduledtask
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the ScheduledTask
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ScheduledTask
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ScheduledTask is the Schema for the scheduledtasks API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScheduledTaskSpec defines the desired state of ScheduledTask.
            properties:
              action:
                description: ScheduledTaskAction defines nested fields for ScheduledTask.Action.
                properties:
                  compartmentIdInSubtree:
                    description: if true, purge child compartments data
                    type: boolean
                  dataType:
                    description: the type of the log data to be purged
                    type: string
                  jsonData:
                    type: string
                  metricExtraction:
                    description: ScheduledTaskActionMetricExtraction defines nested
                      fields for ScheduledTask.Action.MetricExtraction.
                    properties:
                      compartmentId:
                        description: The compartment OCID (/iaas/Content/General/Concepts/identifiers.htm)
                          of the extracted metric.
                        type: string
                      metricCollections:
                        description: Details for the metrics to be collected.
                        items:
                          description: ScheduledTaskActionMetricExtractionMetricCollection
                            defines nested fields for ScheduledTask.Action.MetricExtraction.MetricCollection.
                          properties:
                            dimensions:
                              description: Selected dimension fields for the metric
                                collection.
                              items:
                                description: ScheduledTaskActionMetricExtractionMetricCollectionDimension
                                  defines nested fields for ScheduledTask.Action.MetricExtraction.MetricCollection.Dimension.
                                properties:
                                  dimensionName:
                                    description: Dimension name to be stored with
                                      the metric.
                                    type: string
                                  queryFieldName:
                                    description: Output field in the query to be used
                                      as the source for the metric dimension.
                                    type: string
                                required:
                                - queryFieldName
                                type: object
                              type: array
                            metricName:
                              description: |-
                                The metric name for this metric collection.
                                A valid value starts with an alphabetical character and includes only
                                alphanumeric characters, periods (.), underscores (_), hyphens (-), and dollar signs ($).
                              type: string
                            metricQueryFieldName:
                              description: Output field in the query to be used as
                                the metric value.
                              type: string
                            queryTableName:
                              description: Output table in the query.
                              type: string
                          required:
                          - dimensions
                          - metricName
                          - metricQueryFieldName
                          type: object
                        type: array
                      metricName:
                        description: |-
                          The metric name of the extracted metric.
                          A valid value starts with an alphabetical character and includes only
                          alphanumeric characters, periods (.), underscores (_), hyphens (-), and dollar signs ($).
                        type: string
                      namespace:
                        description: |-
                          The namespace of the extracted metric.
                          A valid value starts with an alphabetical character and includes only
                          alphanumeric characters and underscores (_).
                        type: string
                      resourceGroup:
                        description: |-
                          The resourceGroup of the extracted metric.
                          A valid value starts with an alphabetical character and includes only
                          alphanumeric characters, periods (.), underscores (_), hyphens (-), and dollar signs ($).
                        type: string
                    type: object
                  purgeCompartmentId:
                    description: the compartment OCID under which the data will be
                      purged
                    type: string
                  purgeDuration:
                    description: |-
                      The duration of data to be retained, which is used to
                      calculate the timeDataEnded when the task fires.
                      The value should be negative.
                      Purge duration in ISO 8601 extended format as described in
                      https://en.wikipedia.org/wiki/ISO_8601#Durations.
                      The largest supported unit is D, e.g. -P365D (not -P1Y) or -P14D (not -P2W).
                    type: string
                  queryString:
                    description: Purge query string.
                    type: string
                  savedSearchDuration:
                    description: |-
                      The duration of data to be searched for SAVED_SEARCH tasks,
                      used when the task fires to calculate the query time range.
                      Duration in ISO 8601 extended format as described in
                      https://en.wikipedia.org/wiki/ISO_8601#Durations.
                      The value should be positive.
                      The largest supported unit (as opposed to value) is D, e.g.  P14D (not P2W).
                      There are restrictions on the maximum duration value relative to the task schedule
                      value as specified in the following table.
                         Schedule Interval Range          | Maximum Duration
                    type: string
                  savedSearchId:
                    description: The ManagementSavedSearch id [OCID] utilized in the
                      action.
                    type: string
                  templateDetails:
                    description: ScheduledTaskActionTemplateDetails defines nested
                      fields for ScheduledTask.Action.TemplateDetails.
                    properties:
                      templateId:
                        description: The Config template Id of a particular template.
                        type: string
                      templateParams:
                        description: To store macro params.
                        items:
                          description: ScheduledTaskActionTemplateDetailsTemplateParam
                            defines nested fields for ScheduledTask.Action.TemplateDetails.TemplateParam.
                          properties:
                            keyField:
                              description: Contains macro parameter's names.
                              type: string
                            valueField:
                              description: Contains macro parameter's value.
                              type: string
                          required:
                          - keyField
                          - valueField
                          type: object
                        type: array
                    required:
                    - templateId
                    type: object
                  type:
                    type: string
                type: object
              compartmentId:
                description: Compartment Identifier OCID  (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm).
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              description:
                description: Description for this resource.
                type: string
              displayName:
                description: |-
                  A user-friendly name that is changeable and that does not have to be unique.
                  Format: a leading alphanumeric, followed by zero or more
                  alphanumerics, underscores, spaces, backslashes, or hyphens in any order).
                  No trailing spaces allowed.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              jsonData:
                type: string
              kind:
                type: string
              savedSearchId:
                description: The ManagementSavedSearch id [OCID] to be accelerated.
                type: string
              schedules:
                description: |-
                  Schedules, typically a single schedule.
                  Note there may only be a single schedule for SAVED_SEARCH and PURGE scheduled tasks.
                items:
                  description: ScheduledTaskSchedule defines nested fields for ScheduledTask.Schedule.
                  properties:
                    expression:
                      description: Value in cron format.
                      type: string
                    jsonData:
                      type: string
                    misfirePolicy:
                      description: Schedule misfire retry policy.
                      type: string
                    queryOffsetSecs:
                      description: Number of seconds to offset the query time window
                        by to accommodate capture late arriving data. For example,
                        a schedule run at 12:00 with a 10 minute interval and queryOffsetSecs=120
                        will use the query time window of 11:48-11:58 rather than
                        11:50-12:00 without queryOffsetSecs.
                      type: integer
                    recurringInterval:
                      description: |-
                        Recurring interval in ISO 8601 extended format as described in
                        https://en.wikipedia.org/wiki/ISO_8601#Durations.
                        The largest supported unit is D, e.g. P14D (not P2W).
                        The value must be at least 5 minutes (PT5M) and at most 3 weeks (P21D or PT30240M).
                      type: string
                    repeatCount:
                      description: |-
                        Number of times (0-based) to execute until auto-stop.
                        Default value -1 will execute indefinitely.
                        Value 0 will execute once.
                      type: integer
                    timeEnd:
                      description: End time for the schedule, even if the schedule
                        would otherwise have remaining executions.
                      type: string
                    timeOfFirstExecution:
                      description: |-
                        The date and time the scheduled task should execute first time after create or update;
                        thereafter the task will execute as specified in the schedule.
                      type: string
                    timeZone:
                      description: Time zone, by default UTC.
                      type: string
                    type:
                      type: string
                  type: object
                type: array
              taskType:
                description: Task type.
                type: string
            required:
            - compartmentId
            type: object
          status:
            description: ScheduledTaskStatus defines the observed state of ScheduledTask.
            properties:
              action:
                description: ScheduledTaskAction defines nested fields for ScheduledTask.Action.
                properties:
                  compartmentIdInSubtree:
                    description: if true, purge child compartments data
                    type: boolean
                  dataType:
                    description: the type of the log data to be purged
                    type: string
                  jsonData:
                    type: string
                  metricExtraction:
                    description: ScheduledTaskActionMetricExtraction defines nested
                      fields for ScheduledTask.Action.MetricExtraction.
                    properties:
                      compartmentId:
                        description: The compartment OCID (/iaas/Content/General/Concepts/identifiers.htm)
                          of the extracted metric.
                        type: string
                      metricCollections:
                        description: Details for the metrics to be collected.
                        items:
                          description: ScheduledTaskActionMetricExtractionMetricCollection
                            defines nested fields for ScheduledTask.Action.MetricExtraction.MetricCollection.
                          properties:
                            dimensions:
                              description: Selected dimension fields for the metric
                                collection.
                              items:
                                description: ScheduledTaskActionMetricExtractionMetricCollectionDimension
                                  defines nested fields for ScheduledTask.Action.MetricExtraction.MetricCollection.Dimension.
                                properties:
                                  dimensionName:
                                    description: Dimension name to be stored with
                                      the metric.
                                    type: string
                                  queryFieldName:
                                    description: Output field in the query to be used
                                      as the source for the metric dimension.
                                    type: string
                                required:
                                - queryFieldName
                                type: object
                              type: array
                            metricName:
                              description: |-
                                The metric name for this metric collection.
                                A valid value starts with an alphabetical character and includes only
                                alphanumeric characters, periods (.), underscores (_), hyphens (-), and dollar signs ($).
                              type: string
                            metricQueryFieldName:
                              description: Output field in the query to be used as
                                the metric value.
                              type: string
                            queryTableName:
                              description: Output table in the query.
                              type: string
                          required:
                          - dimensions
                          - metricName
                          - metricQueryFieldName
                          type: object
                        type: array
                      metricName:
                        description: |-
                          The metric name of the extracted metric.
                          A valid value starts with an alphabetical character and includes only
                          alphanumeric characters, periods (.), underscores (_), hyphens (-), and dollar signs ($).
                        type: string
                      namespace:
                        description: |-
                          The namespace of the extracted metric.
                          A valid value starts with an alphabetical character and includes only
                          alphanumeric characters and underscores (_).
                        type: string
                      resourceGroup:
                        description: |-
                          The resourceGroup of the extracted metric.
                          A valid value starts with an alphabetical character and includes only
                          alphanumeric characters, periods (.), underscores (_), hyphens (-), and dollar signs ($).
                        type: string
                    type: object
                  purgeCompartmentId:
                    description: the compartment OCID under which the data will be
                      purged
                    type: string
                  purgeDuration:
                    description: |-
                      The duration of data to be retained, which is used to
                      calculate the timeDataEnded when the task fires.
                      The value should be negative.
                      Purge duration in ISO 8601 extended format as described in
                      https://en.wikipedia.org/wiki/ISO_8601#Durations.
                      The largest supported unit is D, e.g. -P365D (not -P1Y) or -P14D (not -P2W).
                    type: string
                  queryString:
                    description: Purge query string.
                    type: string
                  savedSearchDuration:
                    description: |-
                      The duration of data to be searched for SAVED_SEARCH tasks,
                      used when the task fires to calculate the query time range.
                      Duration in ISO 8601 extended format as described in
                      https://en.wikipedia.org/wiki/ISO_8601#Durations.
                      The value should be positive.
                      The largest supported unit (as opposed to value) is D, e.g.  P14D (not P2W).
                      There are restrictions on the maximum duration value relative to the task schedule
                      value as specified in the following table.
                         Schedule Interval Range          | Maximum Duration
                    type: string
                  savedSearchId:
                    description: The ManagementSavedSearch id [OCID] utilized in the
                      action.
                    type: string
                  templateDetails:
                    description: ScheduledTaskActionTemplateDetails defines nested
                      fields for ScheduledTask.Action.TemplateDetails.
                    properties:
                      templateId:
                        description: The Config template Id of a particular template.
                        type: string
                      templateParams:
                        description: To store macro params.
                        items:
                          description: ScheduledTaskActionTemplateDetailsTemplateParam
                            defines nested fields for ScheduledTask.Action.TemplateDetails.TemplateParam.
                          properties:
                            keyField:
                              description: Contains macro parameter's names.
                              type: string
                            valueField:
                              description: Contains macro parameter's value.
                              type: string
                          required:
                          - keyField
                          - valueField
                          type: object
                        type: array
                    required:
                    - templateId
                    type: object
                  type:
                    type: string
                type: object
              compartmentId:
                description: Compartment Identifier OCID  (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm).
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              description:
                description: Description for this resource.
                type: string
              displayName:
                description: |-
                  A user-friendly name that is changeable and that does not have to be unique.
                  Format: a leading alphanumeric, followed by zero or more
                  alphanumerics, underscores, spaces, backslashes, or hyphens in any order).
                  No trailing spaces allowed.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the data plane resource.
                type: string
              jsonData:
                type: string
              kind:
                type: string
              lastExecutionStatus:
                description: The most recent task execution status.
                type: string
              lifecycleState:
                description: The current state of the scheduled task.
                type: string
              numOccurrences:
                description: Number of execution occurrences.
                format: int64
                type: integer
              pauseReason:
                description: reason for taskStatus PAUSED.
                type: string
              scheduleType:
                description: Type of the task schedule
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              targetService:
                description: Target Service
                type: string
              taskStatus:
                description: Status of the scheduled task.
                type: string
              taskType:
                description: Task type.
                type: string
              timeCreated:
                description: The date and time the scheduled task was created, in
                  the format defined by RFC3339.
                type: string
              timeLastExecuted:
                description: The date and time the scheduled task last executed, in
                  the format defined by RFC3339.
                type: string
              timeOfNextExecution:
                description: |-
                  The date and time the scheduled task will execute next,
                  in the format defined by RFC3339.
                type: string
              timeUpdated:
                description: The date and time the scheduled task was last updated,
                  in the format defined by RFC3339.
                type: string
              workRequestId:
                description: most recent Work Request Identifier OCID  (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  for the asynchronous request.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
