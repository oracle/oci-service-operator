---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: pbflistings.functions.oracle.com
spec:
  group: functions.oracle.com
  names:
    kind: PbfListing
    listKind: PbfListingList
    plural: pbflistings
    singular: pbflisting
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the PbfListing
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the PbfListing
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: PbfListing is the Schema for the pbflistings API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PbfListingSpec defines the desired state of PbfListing.
            type: object
          status:
            description: PbfListingStatus defines the observed state of PbfListing.
            properties:
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              description:
                description: 'A short overview of the PBF Listing: the purpose of
                  the PBF and and associated information.'
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: Unique identifier that is immutable on creation.
                type: string
              lifecycleState:
                description: The current state of the PBF resource.
                type: string
              name:
                description: |-
                  A brief descriptive name for the PBF listing. The PBF listing name must be unique, and not match and existing
                  PBF.
                type: string
              publisherDetails:
                description: PbfListingPublisherDetails defines nested fields for
                  PbfListing.PublisherDetails.
                properties:
                  name:
                    description: Name of the Publisher
                    type: string
                type: object
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: The time the PbfListing was created. An RFC3339 formatted
                  datetime string.
                type: string
              timeUpdated:
                description: The last time the PbfListing was updated. An RFC3339
                  formatted datetime string.
                type: string
              triggers:
                description: An array of Trigger. A list of triggers that may activate
                  the PBF.
                items:
                  description: PbfListingTrigger defines nested fields for PbfListing.Trigger.
                  properties:
                    name:
                      description: A brief descriptive name for the PBF trigger.
                      type: string
                  type: object
                type: array
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
