---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: listingrevisionattachments.marketplacepublisher.oracle.com
spec:
  group: marketplacepublisher.oracle.com
  names:
    kind: ListingRevisionAttachment
    listKind: ListingRevisionAttachmentList
    plural: listingrevisionattachments
    singular: listingrevisionattachment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the ListingRevisionAttachment
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ListingRevisionAttachment
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ListingRevisionAttachment is the Schema for the listingrevisionattachments
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ListingRevisionAttachmentSpec defines the desired state of
              ListingRevisionAttachment.
            properties:
              attachmentType:
                type: string
              customerName:
                description: Name of the customer
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              description:
                description: Description for this specified attachment.
                type: string
              displayName:
                description: The name for the listing revision attachment.
                type: string
              documentCategory:
                description: The document category of the listing revision attachment.
                type: string
              documentName:
                description: Name of the review support document
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              jsonData:
                type: string
              listingRevisionId:
                description: The OCID for the listing revision in Marketplace Publisher.
                type: string
              productCodes:
                description: List of product codes for success story
                items:
                  type: string
                type: array
              serviceName:
                description: Name of the service
                type: string
              templateCode:
                description: Type of review support document
                type: string
              type:
                description: Type of the service
                type: string
              url:
                description: Optional url to service information
                type: string
              videoAttachmentDetails:
                description: ListingRevisionAttachmentVideoAttachmentDetails defines
                  nested fields for ListingRevisionAttachment.VideoAttachmentDetails.
                properties:
                  contentUrl:
                    description: The URL of the video.
                    type: string
                required:
                - contentUrl
                type: object
            required:
            - listingRevisionId
            type: object
          status:
            description: ListingRevisionAttachmentStatus defines the observed state
              of ListingRevisionAttachment.
            properties:
              attachmentType:
                type: string
              compartmentId:
                description: The unique identifier for the compartment.
                type: string
              contentUrl:
                description: URL of the uploaded document.
                type: string
              customerName:
                description: Name of the customer
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              description:
                description: Description of the listing revision attachment.
                type: string
              displayName:
                description: Name of the listing revision attachment.
                type: string
              documentCategory:
                description: Possible lifecycle states.
                type: string
              documentName:
                description: Name of the review support document
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: Unique OCID identifier for the listing revision attachment.
                type: string
              jsonData:
                type: string
              lifecycleState:
                description: The current state of the attachment.
                type: string
              listingRevisionId:
                description: The unique identifier of the listing revision that the
                  specified attachment belongs to.
                type: string
              mimeType:
                description: The MIME type of the uploaded data.
                type: string
              productCodes:
                description: List of product codes for customer story
                items:
                  type: string
                type: array
              serviceName:
                description: Name of the service
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              templateCode:
                description: Template code for the uploaded document
                type: string
              timeCreated:
                description: The time the attachment was created. An RFC3339 formatted
                  datetime string.
                type: string
              timeUpdated:
                description: The time the attachment was updated. An RFC3339 formatted
                  datetime string.
                type: string
              type:
                description: Type of service
                type: string
              url:
                description: Optional url to service
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
