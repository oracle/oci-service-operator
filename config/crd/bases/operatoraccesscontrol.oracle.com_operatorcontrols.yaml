---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: operatorcontrols.operatoraccesscontrol.oracle.com
spec:
  group: operatoraccesscontrol.oracle.com
  names:
    kind: OperatorControl
    listKind: OperatorControlList
    plural: operatorcontrols
    singular: operatorcontrol
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the OperatorControl
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the OperatorControl
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: OperatorControl is the Schema for the operatorcontrols API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OperatorControlSpec defines the desired state of OperatorControl.
            properties:
              approverGroupsList:
                description: List of user groups who can approve an access request
                  associated with a resource governed by this operator control.
                items:
                  type: string
                type: array
              approversList:
                description: List of users who can approve an access request associated
                  with a resource governed by this operator control.
                items:
                  type: string
                type: array
              compartmentId:
                description: The OCID of the compartment that contains this operator
                  control.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: Defined tags for this resource. Each key is predefined
                  and scoped to a namespace.
                type: object
              description:
                description: Description of the operator control.
                type: string
              emailIdList:
                description: List of emailId.
                items:
                  type: string
                type: array
              freeformTags:
                additionalProperties:
                  type: string
                description: Simple key-value pair that is applied without any predefined
                  name, type or scope. Exists for cross-compatibility only.
                type: object
              isFullyPreApproved:
                description: |-
                  Whether all the operator actions have been pre-approved. If yes, all access requests associated with a resource governed by this operator control
                  will be auto-approved.
                type: boolean
              numberOfApprovers:
                description: Number of approvers required to approve an access request.
                type: integer
              operatorControlName:
                description: Name of the operator control.
                type: string
              preApprovedOpActionList:
                description: |-
                  List of pre-approved operator actions. Access requests associated with a resource governed by this operator control will be
                  auto-approved if the access request only contain operator actions in the pre-approved list.
                items:
                  type: string
                type: array
              resourceType:
                description: resourceType for which the OperatorControl is applicable
                type: string
              systemMessage:
                description: This is the message that will be displayed to the operator
                  users while accessing the system.
                type: string
            required:
            - approverGroupsList
            - compartmentId
            - isFullyPreApproved
            - operatorControlName
            - resourceType
            type: object
          status:
            description: OperatorControlStatus defines the observed state of OperatorControl.
            properties:
              approvalRequiredOpActionList:
                description: |-
                  List of operator actions that need explicit approval. Any operator action not in the pre-approved list will require explicit
                  approval. Access requests associated with a resource governed by this operator control will be
                  require explicit approval if the access request contains any operator action in this list.
                items:
                  type: string
                type: array
              approverGroupsList:
                description: List of user groups who can approve an access request
                  associated with a target resource under the governance of this operator
                  control.
                items:
                  type: string
                type: array
              approversList:
                description: List of users who can approve an access request associated
                  with a target resource under the governance of this operator control.
                items:
                  type: string
                type: array
              compartmentId:
                description: The OCID of the compartment that contains the operator
                  control.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: Defined tags for this resource. Each key is predefined
                  and scoped to a namespace.
                type: object
              description:
                description: Description of operator control.
                type: string
              emailIdList:
                description: List of emailId.
                items:
                  type: string
                type: array
              freeformTags:
                additionalProperties:
                  type: string
                description: Simple key-value pair that is applied without any predefined
                  name, type or scope. Exists for cross-compatibility only.
                type: object
              id:
                description: The OCID of the operator control.
                type: string
              isDefaultOperatorControl:
                description: Whether the operator control is a default Operator Control.
                type: boolean
              isFullyPreApproved:
                description: |-
                  Whether all the operator actions have been pre-approved. If yes, all access requests associated with a resource governed by this operator control
                  will be auto-approved.
                type: boolean
              lastModifiedInfo:
                description: Description associated with the latest modification of
                  the operator control.
                type: string
              lifecycleState:
                description: The current lifecycle state of the operator control.
                type: string
              numberOfApprovers:
                description: Number of approvers required to approve an access request.
                type: integer
              operatorControlName:
                description: Name of the operator control. The name must be unique.
                type: string
              preApprovedOpActionList:
                description: |-
                  List of pre-approved operator actions. Access requests associated with a resource governed by this operator control will be
                  automatically approved if the access request only contain operator actions in the pre-approved list.
                items:
                  type: string
                type: array
              resourceType:
                description: resourceType for which the OperatorControl is applicable
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemMessage:
                description: System message that would be displayed to the operator
                  users on accessing the target resource under the governance of this
                  operator control.
                type: string
              timeOfCreation:
                description: 'Time when the operator control was created expressed
                  in RFC 3339 (https://tools.ietf.org/html/rfc3339) timestamp format.
                  Example: ''2020-05-22T21:10:29.600Z'''
                type: string
              timeOfDeletion:
                description: |-
                  Time when deleted expressed in RFC 3339 (https://tools.ietf.org/html/rfc3339)timestamp format. Example: '2020-05-22T21:10:29.600Z'.
                  Note a deleted operator control still stays in the system, so that you can still audit operator actions associated with access requests
                  raised on target resources governed by the deleted operator control.
                type: string
              timeOfModification:
                description: 'Time when the operator control was last modified expressed
                  in RFC 3339 (https://tools.ietf.org/html/rfc3339) timestamp format.
                  Example: ''2020-05-22T21:10:29.600Z'''
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
