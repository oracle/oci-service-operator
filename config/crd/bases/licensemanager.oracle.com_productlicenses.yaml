---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: productlicenses.licensemanager.oracle.com
spec:
  group: licensemanager.oracle.com
  names:
    kind: ProductLicense
    listKind: ProductLicenseList
    plural: productlicenses
    singular: productlicense
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the ProductLicense
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ProductLicense
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ProductLicense is the Schema for the productlicenses API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProductLicenseSpec defines the desired state of ProductLicense.
            properties:
              compartmentId:
                description: The compartment OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  where product licenses are created.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              displayName:
                description: Name of the product license.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type, or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              images:
                description: The image details associated with the product license.
                items:
                  description: ProductLicenseImage defines nested fields for ProductLicense.Image.
                  properties:
                    listingId:
                      description: Marketplace image listing ID.
                      type: string
                    packageVersion:
                      description: Image package version.
                      type: string
                  required:
                  - listingId
                  - packageVersion
                  type: object
                type: array
              isVendorOracle:
                description: Specifies if the product license vendor is Oracle or
                  a third party.
                type: boolean
              licenseUnit:
                description: The product license unit.
                type: string
              vendorName:
                description: 'The product license vendor name, for example: Microsoft,
                  RHEL, and so on.'
                type: string
            required:
            - compartmentId
            - displayName
            - isVendorOracle
            - licenseUnit
            type: object
          status:
            description: ProductLicenseStatus defines the observed state of ProductLicense.
            properties:
              activeLicenseRecordCount:
                description: The number of active license records associated with
                  the product license.
                type: integer
              compartmentId:
                description: The compartment OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  where the product license is created.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              displayName:
                description: License record name
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type, or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: The product license OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm).
                type: string
              images:
                description: The images associated with the product license.
                items:
                  description: ProductLicenseImage defines nested fields for ProductLicense.Image.
                  properties:
                    listingId:
                      description: Marketplace image listing ID.
                      type: string
                    packageVersion:
                      description: Image package version.
                      type: string
                  required:
                  - listingId
                  - packageVersion
                  type: object
                type: array
              isOverSubscribed:
                description: Specifies whether or not the product license is oversubscribed.
                type: boolean
              isUnlimited:
                description: Specifies if the license unit count is unlimited.
                type: boolean
              isVendorOracle:
                description: Specifies whether the vendor is Oracle or a third party.
                type: boolean
              licenseUnit:
                description: The product license unit.
                type: string
              lifecycleState:
                description: The current product license state.
                type: string
              sdkStatus:
                description: |-
                  The current product license status.
                  This uses a distinct JSON name so it can coexist with the OSOK status envelope.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              statusDescription:
                description: Status description for the current product license status.
                type: string
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Usage of system tag keys. These predefined keys are scoped to namespaces.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: The time the product license was created. An RFC 3339
                  (https://tools.ietf.org/html/rfc3339)-formatted datetime string.
                type: string
              timeUpdated:
                description: The time the product license was updated. An RFC 3339
                  (https://tools.ietf.org/html/rfc3339)-formatted datetime string.
                type: string
              totalActiveLicenseUnitCount:
                description: The total number of licenses available for the product
                  license, calculated by adding up all the license counts for active
                  license records associated with the product license.
                type: integer
              totalLicenseRecordCount:
                description: The number of license records associated with the product
                  license.
                type: integer
              totalLicenseUnitsConsumed:
                description: The number of license units consumed. Updated after each
                  allocation run.
                type: number
              vendorName:
                description: The vendor of the ProductLicense
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
