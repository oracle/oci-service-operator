---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: artifacts.marketplacepublisher.oracle.com
spec:
  group: marketplacepublisher.oracle.com
  names:
    kind: Artifact
    listKind: ArtifactList
    plural: artifacts
    singular: artifact
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the Artifact
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Artifact
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Artifact is the Schema for the artifacts API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ArtifactSpec defines the desired state of Artifact.
            properties:
              artifactType:
                type: string
              compartmentId:
                description: The unique identifier for the compartment.
                type: string
              containerImage:
                description: ArtifactContainerImage defines nested fields for Artifact.ContainerImage.
                properties:
                  sourceRegistryId:
                    description: The source registry id of the container image.
                    type: string
                  sourceRegistryUrl:
                    description: The source registry url of the container image.
                    type: string
                required:
                - sourceRegistryId
                - sourceRegistryUrl
                type: object
              containerImageArtifactIds:
                description: List of container image artifact uniquie identifiers
                  included in the helm chart.
                items:
                  type: string
                type: array
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              displayName:
                description: The display name for the artifact.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              helmChart:
                description: ArtifactHelmChart defines nested fields for Artifact.HelmChart.
                properties:
                  sourceRegistryId:
                    description: The source registry OCID of the container image.
                    type: string
                  sourceRegistryUrl:
                    description: The source registry url of the helmchart image.
                    type: string
                  supportedKubernetesVersions:
                    description: The Supported Versions of Kubernetes
                    items:
                      type: string
                    type: array
                required:
                - sourceRegistryId
                - sourceRegistryUrl
                type: object
              jsonData:
                type: string
              machineImage:
                description: ArtifactMachineImage defines nested fields for Artifact.MachineImage.
                properties:
                  imageShapeCompatibilityEntries:
                    description: List of shape configurations compatible with the
                      image.
                    items:
                      description: ArtifactMachineImageImageShapeCompatibilityEntry
                        defines nested fields for Artifact.MachineImage.ImageShapeCompatibilityEntry.
                      properties:
                        memoryConstraints:
                          description: ArtifactMachineImageImageShapeCompatibilityEntryMemoryConstraints
                            defines nested fields for Artifact.MachineImage.ImageShapeCompatibilityEntry.MemoryConstraints.
                          properties:
                            maxInGBs:
                              description: The maximum amount of memory, in gigabytes.
                              type: integer
                            minInGBs:
                              description: The minimum amount of memory, in gigabytes.
                              type: integer
                          type: object
                        ocpuConstraints:
                          description: ArtifactMachineImageImageShapeCompatibilityEntryOcpuConstraints
                            defines nested fields for Artifact.MachineImage.ImageShapeCompatibilityEntry.OcpuConstraints.
                          properties:
                            max:
                              description: The maximum number of OCPUs supported for
                                this image and shape.
                              type: integer
                            min:
                              description: The minimum number of OCPUs supported for
                                this image and shape.
                              type: integer
                          type: object
                        shape:
                          description: Name of the shape.
                          type: string
                      required:
                      - shape
                      type: object
                    type: array
                  isSnapshotAllowed:
                    description: Indicates if the customer is allowed to take a snapshot
                      of running instance created from the machine image.
                    type: boolean
                  sourceImageId:
                    description: The OCID of source Machine Image.
                    type: string
                  username:
                    description: The username for machine image.
                    type: string
                required:
                - imageShapeCompatibilityEntries
                - isSnapshotAllowed
                - sourceImageId
                type: object
              stack:
                description: ArtifactStack defines nested fields for Artifact.Stack.
                properties:
                  imageListingIds:
                    description: Image listing OCIDs that are referenced in the Stack.
                    items:
                      type: string
                    type: array
                  sourceStackId:
                    description: The source stack OCID.
                    type: string
                required:
                - sourceStackId
                type: object
            required:
            - compartmentId
            type: object
          status:
            description: ArtifactStatus defines the observed state of Artifact.
            properties:
              artifactType:
                type: string
              compartmentId:
                description: The unique identifier for the compartment.
                type: string
              containerImage:
                description: ArtifactContainerImage defines nested fields for Artifact.ContainerImage.
                properties:
                  sourceRegistryId:
                    description: The source registry id of the container image.
                    type: string
                  sourceRegistryUrl:
                    description: The source registry url of the container image.
                    type: string
                required:
                - sourceRegistryId
                - sourceRegistryUrl
                type: object
              containerImageArtifactIds:
                description: List of container image artifact unique identifiers included
                  in the helm chart.
                items:
                  type: string
                type: array
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              displayName:
                description: A display name for the artifact.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              helmChart:
                description: ArtifactHelmChart defines nested fields for Artifact.HelmChart.
                properties:
                  sourceRegistryId:
                    description: The source registry OCID of the container image.
                    type: string
                  sourceRegistryUrl:
                    description: The source registry url of the helmchart image.
                    type: string
                  supportedKubernetesVersions:
                    description: The Supported Versions of Kubernetes
                    items:
                      type: string
                    type: array
                required:
                - sourceRegistryId
                - sourceRegistryUrl
                type: object
              id:
                description: Unique OCID identifier for the artifact.
                type: string
              jsonData:
                type: string
              lifecycleState:
                description: The current state for the Artifact.
                type: string
              machineImage:
                description: ArtifactMachineImage defines nested fields for Artifact.MachineImage.
                properties:
                  imageShapeCompatibilityEntries:
                    description: List of shape configurations compatible with the
                      image.
                    items:
                      description: ArtifactMachineImageImageShapeCompatibilityEntry
                        defines nested fields for Artifact.MachineImage.ImageShapeCompatibilityEntry.
                      properties:
                        memoryConstraints:
                          description: ArtifactMachineImageImageShapeCompatibilityEntryMemoryConstraints
                            defines nested fields for Artifact.MachineImage.ImageShapeCompatibilityEntry.MemoryConstraints.
                          properties:
                            maxInGBs:
                              description: The maximum amount of memory, in gigabytes.
                              type: integer
                            minInGBs:
                              description: The minimum amount of memory, in gigabytes.
                              type: integer
                          type: object
                        ocpuConstraints:
                          description: ArtifactMachineImageImageShapeCompatibilityEntryOcpuConstraints
                            defines nested fields for Artifact.MachineImage.ImageShapeCompatibilityEntry.OcpuConstraints.
                          properties:
                            max:
                              description: The maximum number of OCPUs supported for
                                this image and shape.
                              type: integer
                            min:
                              description: The minimum number of OCPUs supported for
                                this image and shape.
                              type: integer
                          type: object
                        shape:
                          description: Name of the shape.
                          type: string
                      required:
                      - shape
                      type: object
                    type: array
                  isSnapshotAllowed:
                    description: Indicates if the customer is allowed to take a snapshot
                      of running instance created from the machine image.
                    type: boolean
                  sourceImageId:
                    description: The OCID of source Machine Image.
                    type: string
                  username:
                    description: The username for machine image.
                    type: string
                required:
                - imageShapeCompatibilityEntries
                - isSnapshotAllowed
                - sourceImageId
                type: object
              publisherId:
                description: The unique identifier for the publisher.
                type: string
              sdkStatus:
                description: |-
                  The current status for the Artifact.
                  This uses a distinct JSON name so it can coexist with the OSOK status envelope.
                type: string
              stack:
                description: ArtifactStack defines nested fields for Artifact.Stack.
                properties:
                  imageListingIds:
                    description: Image listing OCIDs that are referenced in the Stack.
                    items:
                      type: string
                    type: array
                  sourceStackId:
                    description: The source stack OCID.
                    type: string
                required:
                - sourceStackId
                type: object
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              statusNotes:
                description: Status notes for the Artifact.
                type: string
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: |-
                  The date and time the artifact was created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2022-09-15T21:10:29.600Z`
                type: string
              timeUpdated:
                description: |-
                  The date and time the artifact was updated, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2022-09-15T21:10:29.600Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
