---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: pathanalyzertests.vnmonitoring.oracle.com
spec:
  group: vnmonitoring.oracle.com
  names:
    kind: PathAnalyzerTest
    listKind: PathAnalyzerTestList
    plural: pathanalyzertests
    singular: pathanalyzertest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the PathAnalyzerTest
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the PathAnalyzerTest
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: PathAnalyzerTest is the Schema for the pathanalyzertests API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PathAnalyzerTestSpec defines the desired state of PathAnalyzerTest.
            properties:
              compartmentId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  for the `PathAnalyzerTest` resource's compartment.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              destinationEndpoint:
                description: PathAnalyzerTestDestinationEndpoint defines nested fields
                  for PathAnalyzerTest.DestinationEndpoint.
                properties:
                  address:
                    description: The IPv4 address of the COMPUTE_INSTANCE-type `Endpoint`
                      object.
                    type: string
                  instanceId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the compute instance.
                    type: string
                  jsonData:
                    type: string
                  listenerId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the network load balancer listener.
                    type: string
                  loadBalancerId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the load balancer.
                    type: string
                  networkLoadBalancerId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the listener's network load balancer.
                    type: string
                  psaId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the PSA.
                    type: string
                  subnetId:
                    description: |-
                      The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the subnet containing the IP address.
                      This can be used to disambiguate which subnet is intended, in case the IP address
                      is used in more than one subnet (when there are subnets with overlapping IP ranges).
                    type: string
                  type:
                    type: string
                  vlanId:
                    description: |-
                      The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the VLAN containing the IP address.
                      This can be used to disambiguate which VLAN is queried, in case the endpoint IP
                      address belongs to more than one VLAN (when there are VLANs with overlapping IP ranges).
                    type: string
                  vnicId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the VNIC attached to the compute instance.
                    type: string
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              protocol:
                description: The IP protocol to use in the `PathAnalyzerTest` resource.
                type: integer
              protocolParameters:
                description: PathAnalyzerTestProtocolParameters defines nested fields
                  for PathAnalyzerTest.ProtocolParameters.
                properties:
                  destinationPort:
                    description: The destination port to use in a `PathAnalyzerTest`
                      resource.
                    type: integer
                  icmpCode:
                    description: The ICMP (https://www.iana.org/assignments/icmp-parameters/icmp-parameters.xhtml)
                      code.
                    type: integer
                  icmpType:
                    description: The ICMP (https://www.iana.org/assignments/icmp-parameters/icmp-parameters.xhtml)
                      type.
                    type: integer
                  jsonData:
                    type: string
                  sourcePort:
                    description: The source port to use in a `PathAnalyzerTest` resource.
                    type: integer
                  type:
                    type: string
                type: object
              queryOptions:
                description: PathAnalyzerTestQueryOptions defines nested fields for
                  PathAnalyzerTest.QueryOptions.
                properties:
                  isBiDirectionalAnalysis:
                    description: If true, a path analysis is done for both the forward
                      and reverse routes.
                    type: boolean
                type: object
              sourceEndpoint:
                description: PathAnalyzerTestSourceEndpoint defines nested fields
                  for PathAnalyzerTest.SourceEndpoint.
                properties:
                  address:
                    description: The IPv4 address of the COMPUTE_INSTANCE-type `Endpoint`
                      object.
                    type: string
                  instanceId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the compute instance.
                    type: string
                  jsonData:
                    type: string
                  listenerId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the network load balancer listener.
                    type: string
                  loadBalancerId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the load balancer.
                    type: string
                  networkLoadBalancerId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the listener's network load balancer.
                    type: string
                  psaId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the PSA.
                    type: string
                  subnetId:
                    description: |-
                      The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the subnet containing the IP address.
                      This can be used to disambiguate which subnet is intended, in case the IP address
                      is used in more than one subnet (when there are subnets with overlapping IP ranges).
                    type: string
                  type:
                    type: string
                  vlanId:
                    description: |-
                      The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the VLAN containing the IP address.
                      This can be used to disambiguate which VLAN is queried, in case the endpoint IP
                      address belongs to more than one VLAN (when there are VLANs with overlapping IP ranges).
                    type: string
                  vnicId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the VNIC attached to the compute instance.
                    type: string
                type: object
            required:
            - compartmentId
            - destinationEndpoint
            - protocol
            - sourceEndpoint
            type: object
          status:
            description: PathAnalyzerTestStatus defines the observed state of PathAnalyzerTest.
            properties:
              compartmentId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the `PathAnalyzerTest` resource's compartment.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              destinationEndpoint:
                description: PathAnalyzerTestDestinationEndpoint defines nested fields
                  for PathAnalyzerTest.DestinationEndpoint.
                properties:
                  address:
                    description: The IPv4 address of the COMPUTE_INSTANCE-type `Endpoint`
                      object.
                    type: string
                  instanceId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the compute instance.
                    type: string
                  jsonData:
                    type: string
                  listenerId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the network load balancer listener.
                    type: string
                  loadBalancerId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the load balancer.
                    type: string
                  networkLoadBalancerId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the listener's network load balancer.
                    type: string
                  psaId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the PSA.
                    type: string
                  subnetId:
                    description: |-
                      The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the subnet containing the IP address.
                      This can be used to disambiguate which subnet is intended, in case the IP address
                      is used in more than one subnet (when there are subnets with overlapping IP ranges).
                    type: string
                  type:
                    type: string
                  vlanId:
                    description: |-
                      The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the VLAN containing the IP address.
                      This can be used to disambiguate which VLAN is queried, in case the endpoint IP
                      address belongs to more than one VLAN (when there are VLANs with overlapping IP ranges).
                    type: string
                  vnicId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the VNIC attached to the compute instance.
                    type: string
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: A unique identifier established when the resource is
                  created. The identifier can't be changed later.
                type: string
              lifecycleState:
                description: The current state of the `PathAnalyzerTest` resource.
                type: string
              protocol:
                description: The IP protocol to use for the `PathAnalyzerTest` resource.
                type: integer
              protocolParameters:
                description: PathAnalyzerTestProtocolParameters defines nested fields
                  for PathAnalyzerTest.ProtocolParameters.
                properties:
                  destinationPort:
                    description: The destination port to use in a `PathAnalyzerTest`
                      resource.
                    type: integer
                  icmpCode:
                    description: The ICMP (https://www.iana.org/assignments/icmp-parameters/icmp-parameters.xhtml)
                      code.
                    type: integer
                  icmpType:
                    description: The ICMP (https://www.iana.org/assignments/icmp-parameters/icmp-parameters.xhtml)
                      type.
                    type: integer
                  jsonData:
                    type: string
                  sourcePort:
                    description: The source port to use in a `PathAnalyzerTest` resource.
                    type: integer
                  type:
                    type: string
                type: object
              queryOptions:
                description: PathAnalyzerTestQueryOptions defines nested fields for
                  PathAnalyzerTest.QueryOptions.
                properties:
                  isBiDirectionalAnalysis:
                    description: If true, a path analysis is done for both the forward
                      and reverse routes.
                    type: boolean
                type: object
              sourceEndpoint:
                description: PathAnalyzerTestSourceEndpoint defines nested fields
                  for PathAnalyzerTest.SourceEndpoint.
                properties:
                  address:
                    description: The IPv4 address of the COMPUTE_INSTANCE-type `Endpoint`
                      object.
                    type: string
                  instanceId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the compute instance.
                    type: string
                  jsonData:
                    type: string
                  listenerId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the network load balancer listener.
                    type: string
                  loadBalancerId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the load balancer.
                    type: string
                  networkLoadBalancerId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the listener's network load balancer.
                    type: string
                  psaId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the PSA.
                    type: string
                  subnetId:
                    description: |-
                      The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the subnet containing the IP address.
                      This can be used to disambiguate which subnet is intended, in case the IP address
                      is used in more than one subnet (when there are subnets with overlapping IP ranges).
                    type: string
                  type:
                    type: string
                  vlanId:
                    description: |-
                      The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the VLAN containing the IP address.
                      This can be used to disambiguate which VLAN is queried, in case the endpoint IP
                      address belongs to more than one VLAN (when there are VLANs with overlapping IP ranges).
                    type: string
                  vnicId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the VNIC attached to the compute instance.
                    type: string
                type: object
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Usage of system tag keys. These predefined keys are scoped to namespaces.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: The date and time the `PathAnalyzerTest` resource was
                  created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                type: string
              timeUpdated:
                description: The date and time the `PathAnalyzerTest` resource was
                  last updated, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
