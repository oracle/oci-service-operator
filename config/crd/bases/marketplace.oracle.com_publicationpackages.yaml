---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: publicationpackages.marketplace.oracle.com
spec:
  group: marketplace.oracle.com
  names:
    kind: PublicationPackage
    listKind: PublicationPackageList
    plural: publicationpackages
    singular: publicationpackage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the PublicationPackage
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the PublicationPackage
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: PublicationPackage is the Schema for the publicationpackages
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PublicationPackageSpec defines the desired state of PublicationPackage.
            type: object
          status:
            description: PublicationPackageStatus defines the observed state of PublicationPackage.
            properties:
              appCatalogListingId:
                description: The ID of the listing resource associated with this publication
                  package. For more information, see AppCatalogListing (https://docs.cloud.oracle.com/en-us/iaas/api/#/en/iaas/latest/AppCatalogListing/)
                  in the Core Services API.
                type: string
              appCatalogListingResourceVersion:
                description: The resource version of the listing resource associated
                  with this publication package.
                type: string
              description:
                description: A description of the package.
                type: string
              imageId:
                description: The ID of the image that corresponds to the package.
                type: string
              jsonData:
                type: string
              listingId:
                description: The ID of the listing that the specified package belongs
                  to.
                type: string
              operatingSystem:
                description: PublicationPackageOperatingSystem defines nested fields
                  for PublicationPackage.OperatingSystem.
                properties:
                  name:
                    description: The name of the operating system.
                    type: string
                type: object
              packageType:
                type: string
              packageVersion:
                description: The version of the specified package.
                type: string
              resourceId:
                description: The unique identifier for the package resource.
                type: string
              resourceLink:
                description: A link to the stack resource.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time the publication package was created, expressed in RFC 3339 (https://tools.ietf.org/html/rfc3339)
                  timestamp format.
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              variables:
                description: A list of variables for the stack resource.
                items:
                  description: PublicationPackageVariable defines nested fields for
                    PublicationPackage.Variable.
                  properties:
                    dataType:
                      description: The data type of the variable.
                      type: string
                    defaultValue:
                      description: The variable's default value.
                      type: string
                    description:
                      description: A description of the variable.
                      type: string
                    hintMessage:
                      description: A brief textual description that helps to explain
                        the variable.
                      type: string
                    isMandatory:
                      description: Whether the variable is mandatory.
                      type: boolean
                    name:
                      description: The name of the variable.
                      type: string
                  type: object
                type: array
              version:
                description: The package version.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
