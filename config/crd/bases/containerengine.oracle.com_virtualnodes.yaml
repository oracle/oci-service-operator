---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: virtualnodes.containerengine.oracle.com
spec:
  group: containerengine.oracle.com
  names:
    kind: VirtualNode
    listKind: VirtualNodeList
    plural: virtualnodes
    singular: virtualnode
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the VirtualNode
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the VirtualNode
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: VirtualNode is the Schema for the virtualnodes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VirtualNodeSpec defines the desired state of VirtualNode.
            type: object
          status:
            description: VirtualNodeStatus defines the observed state of VirtualNode.
            properties:
              availabilityDomain:
                description: The name of the availability domain in which this virtual
                  node is placed
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: The name of the virtual node.
                type: string
              faultDomain:
                description: The fault domain of this virtual node.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The ocid of the virtual node.
                type: string
              kubernetesVersion:
                description: The version of Kubernetes this virtual node is running.
                type: string
              lifecycleDetails:
                description: Details about the state of the Virtual Node.
                type: string
              lifecycleState:
                description: The state of the Virtual Node.
                type: string
              nsgIds:
                description: NSG Ids applied to virtual node vnic.
                items:
                  type: string
                type: array
              privateIp:
                description: The private IP address of this Virtual Node.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              subnetId:
                description: The OCID of the subnet in which this Virtual Node is
                  placed.
                type: string
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Usage of system tag keys. These predefined keys are scoped to namespaces.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: The time at which the virtual node was created.
                type: string
              virtualNodeError:
                description: An error that may be associated with the virtual node.
                type: string
              virtualNodePoolId:
                description: The ocid of the virtual node pool this virtual node belongs
                  to.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
