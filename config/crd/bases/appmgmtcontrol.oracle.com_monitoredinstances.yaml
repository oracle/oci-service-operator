---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: monitoredinstances.appmgmtcontrol.oracle.com
spec:
  group: appmgmtcontrol.oracle.com
  names:
    kind: MonitoredInstance
    listKind: MonitoredInstanceList
    plural: monitoredinstances
    singular: monitoredinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the MonitoredInstance
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the MonitoredInstance
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: MonitoredInstance is the Schema for the monitoredinstances API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MonitoredInstanceSpec defines the desired state of MonitoredInstance.
            properties:
              compartmentId:
                description: |-
                  The compartment containing the existing monitored instance when binding by list lookup.
                  Provide this together with `displayName` when `instanceId` is not set.
                type: string
              displayName:
                description: |-
                  The display name of the existing monitored instance when binding by list lookup.
                  Provide this together with `compartmentId` when `instanceId` is not set.
                type: string
              instanceId:
                description: |-
                  The OCI identifier of the existing monitored instance to bind directly.
                  The AppMgmt Control SDK addresses this value through the `monitoredInstanceId` request field.
                type: string
            type: object
          status:
            description: MonitoredInstanceStatus defines the observed state of MonitoredInstance.
            properties:
              compartmentId:
                description: Compartment Identifier OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                type: string
              displayName:
                description: |-
                  A user-friendly name of the monitored instance. It is binded to Compute Instance (https://docs.oracle.com/iaas/Content/Compute/Concepts/computeoverview.htm).
                  DisplayName is fetched from Core Service API (https://docs.oracle.com/iaas/api/#/en/iaas/20160918/Instance/).
                type: string
              instanceId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of monitored instance.
                type: string
              lifecycleDetails:
                description: A message describing the current state in more detail.
                  For example, can be used to provide actionable information for a
                  resource in Failed state.
                type: string
              lifecycleState:
                description: The current state of the monitored instance.
                type: string
              managementAgentId:
                description: |-
                  Management Agent Identifier OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm).
                  Used to invoke manage operations on Management Agent Cloud Service.
                type: string
              monitoringState:
                description: Monitoring status. Can be either enabled or disabled.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: The time the MonitoredInstance was created. An RFC3339
                  formatted datetime string
                type: string
              timeUpdated:
                description: The time the MonitoredInstance was updated. An RFC3339
                  formatted datetime string
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
