---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: supportedhostshapes.ocvp.oracle.com
spec:
  group: ocvp.oracle.com
  names:
    kind: SupportedHostShape
    listKind: SupportedHostShapeList
    plural: supportedhostshapes
    singular: supportedhostshape
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the SupportedHostShape
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the SupportedHostShape
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: SupportedHostShape is the Schema for the supportedhostshapes
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SupportedHostShapeSpec defines the desired state of SupportedHostShape.
            type: object
          status:
            description: SupportedHostShapeStatus defines the observed state of SupportedHostShape.
            properties:
              defaultOcpuCount:
                description: The default OCPU count of the shape.
                type: number
              description:
                description: Description of the shape.
                type: string
              isSingleHostSddcSupported:
                description: Indicates whether the shape supports single host SDDCs.
                type: boolean
              isSupportMonthlyCommitment:
                description: Whether the shape supports "MONTH" Commitment.
                type: boolean
              isSupportShieldedInstances:
                description: Indicates whether the shape supports shielded instances.
                type: boolean
              name:
                description: The name of the supported compute shape.
                type: string
              shapeFamily:
                description: The family of the shape. ESXi hosts of one SDDC must
                  have the same shape family.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              supportedOcpuCount:
                description: Support OCPU count of the shape.
                items:
                  type: number
                type: array
              supportedOperations:
                description: The operations where you can use the shape. The operations
                  can be CREATE_SDDC or CREATE_ESXI_HOST.
                items:
                  type: string
                type: array
              supportedVmwareSoftwareVersions:
                description: The VMware software versions supported by the shape.
                items:
                  type: string
                type: array
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
