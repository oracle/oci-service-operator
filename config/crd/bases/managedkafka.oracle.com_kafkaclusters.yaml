---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: kafkaclusters.managedkafka.oracle.com
spec:
  group: managedkafka.oracle.com
  names:
    kind: KafkaCluster
    listKind: KafkaClusterList
    plural: kafkaclusters
    singular: kafkacluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the KafkaCluster
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the KafkaCluster
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: KafkaCluster is the Schema for the kafkaclusters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KafkaClusterSpec defines the desired state of KafkaCluster.
            properties:
              accessSubnets:
                description: Subnets where broker/coordinator VNICs will be created.
                items:
                  description: KafkaClusterAccessSubnet defines nested fields for
                    KafkaCluster.AccessSubnet.
                  properties:
                    subnets:
                      description: Subnets OCIDs
                      items:
                        type: string
                      type: array
                  required:
                  - subnets
                  type: object
                type: array
              brokerShape:
                description: KafkaClusterBrokerShape defines nested fields for KafkaCluster.BrokerShape.
                properties:
                  nodeCount:
                    description: Number of Kafka broker nodes
                    type: integer
                  nodeShape:
                    description: Node shape for broker is passed as part of cluster
                      creation, similar to VM.Standard.A1.Flex
                    type: string
                  ocpuCount:
                    description: Number of OCPUs per nodes
                    type: integer
                  storageSizeInGbs:
                    description: Size of the storage per nodes.
                    type: integer
                required:
                - nodeCount
                - ocpuCount
                type: object
              clientCertificateBundle:
                description: CA certificate bundle for mTLS broker authentication.
                type: string
              clusterConfigId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of Kafka Cluster configuration object
                type: string
              clusterConfigVersion:
                description: The version of configuration object
                type: integer
              clusterType:
                description: |-
                  Type of the cluster to spin up.
                  DEVELOPMENT - setting that allows to sacrifice HA and spin up cluster on single node
                  PRODUCTION - Minimum allowed broker count is 3
                type: string
              compartmentId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment to create the KafkaCluster in.
                type: string
              coordinationType:
                description: Kafka coordination type. Set of available types depends
                  on Kafka version
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: A user-friendly name. Does not have to be unique, and
                  it's changeable. Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              kafkaVersion:
                description: Version of Kafka to use to spin up the cluster
                type: string
            required:
            - accessSubnets
            - brokerShape
            - clusterConfigId
            - clusterConfigVersion
            - clusterType
            - compartmentId
            - coordinationType
            - kafkaVersion
            type: object
          status:
            description: KafkaClusterStatus defines the observed state of KafkaCluster.
            properties:
              accessSubnets:
                description: Subnets where broker/coordinator VNICs will be created.
                items:
                  description: KafkaClusterAccessSubnet defines nested fields for
                    KafkaCluster.AccessSubnet.
                  properties:
                    subnets:
                      description: Subnets OCIDs
                      items:
                        type: string
                      type: array
                  required:
                  - subnets
                  type: object
                type: array
              brokerShape:
                description: KafkaClusterBrokerShape defines nested fields for KafkaCluster.BrokerShape.
                properties:
                  nodeCount:
                    description: Number of Kafka broker nodes
                    type: integer
                  nodeShape:
                    description: Node shape for broker is passed as part of cluster
                      creation, similar to VM.Standard.A1.Flex
                    type: string
                  ocpuCount:
                    description: Number of OCPUs per nodes
                    type: integer
                  storageSizeInGbs:
                    description: Size of the storage per nodes.
                    type: integer
                required:
                - nodeCount
                - ocpuCount
                type: object
              clientCertificateBundle:
                description: CA certificate bundle for mTLS broker authentication.
                type: string
              clusterConfigId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of Kafka Cluster configuration object
                type: string
              clusterConfigVersion:
                description: The version of configuration object
                type: integer
              clusterType:
                description: |-
                  Type of the cluster to spin up.
                  DEVELOPMENT - setting that allows to sacrifice HA and spin up cluster on a single node
                  PRODUCTION - Minimum allowed broker count is 3
                type: string
              compartmentId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment.
                type: string
              coordinationType:
                description: Kafka coordination type. Set of available types depends
                  on Kafka version
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: A user-friendly name. Does not have to be unique, and
                  it's changeable.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the KafkaCluster.
                type: string
              kafkaBootstrapUrls:
                description: Bootstrap URL that can be used to connect to Kafka
                items:
                  description: KafkaClusterKafkaBootstrapUrl defines nested fields
                    for KafkaCluster.KafkaBootstrapUrl.
                  properties:
                    name:
                      description: Name of the Kafka listener providing this bootstrap
                        URL
                      type: string
                    url:
                      description: Bootstrap URL
                      type: string
                  type: object
                type: array
              kafkaVersion:
                description: Version of Kafka to use to spin up the cluster
                type: string
              lifecycleDetails:
                description: |-
                  A message that describes the current state of the KafkaCluster in more detail. For example,
                  can be used to provide actionable information for a resource in the Failed state.
                type: string
              lifecycleState:
                description: The current state of the KafkaCluster.
                type: string
              secretId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the secret that contains superuser password.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: |-
                  The date and time the KafkaCluster was created, in the format defined by RFC 3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              timeUpdated:
                description: |-
                  The date and time the KafkaCluster was updated, in the format defined by RFC 3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
