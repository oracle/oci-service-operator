---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: operatorcontrolassignments.operatoraccesscontrol.oracle.com
spec:
  group: operatoraccesscontrol.oracle.com
  names:
    kind: OperatorControlAssignment
    listKind: OperatorControlAssignmentList
    plural: operatorcontrolassignments
    singular: operatorcontrolassignment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the OperatorControlAssignment
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the OperatorControlAssignment
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: OperatorControlAssignment is the Schema for the operatorcontrolassignments
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OperatorControlAssignmentSpec defines the desired state of
              OperatorControlAssignment.
            properties:
              comment:
                description: Comment about the assignment of the operator control
                  to this target resource.
                type: string
              compartmentId:
                description: The OCID of the compartment that contains the operator
                  control assignment.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: Defined tags for this resource. Each key is predefined
                  and scoped to a namespace.
                type: object
              freeformTags:
                additionalProperties:
                  type: string
                description: Simple key-value pair that is applied without any predefined
                  name, type or scope. Exists for cross-compatibility only.
                type: object
              isAutoApproveDuringMaintenance:
                description: The boolean if true would autoApprove during maintenance.
                type: boolean
              isEnforcedAlways:
                description: If set, then the target resource is always governed by
                  the operator control.
                type: boolean
              isHypervisorLogForwarded:
                description: If set, then the hypervisor audit logs will be forwarded
                  to the relevant remote syslog server
                type: boolean
              isLogForwarded:
                description: If set, then the audit logs will be forwarded to the
                  relevant remote logging server
                type: boolean
              operatorControlId:
                description: The OCID of the operator control that is being assigned
                  to a target resource.
                type: string
              remoteSyslogServerAddress:
                description: The address of the remote syslog server where the audit
                  logs will be forwarded to. Address in host or IP format.
                type: string
              remoteSyslogServerCACert:
                description: The CA certificate of the remote syslog server. Identity
                  of the remote syslog server will be asserted based on this certificate.
                type: string
              remoteSyslogServerPort:
                description: The listening port of the remote syslog server. The port
                  range is 0 - 65535. Only TCP supported.
                type: integer
              resourceCompartmentId:
                description: The OCID of the compartment that contains the target
                  resource.
                type: string
              resourceId:
                description: The OCID of the target resource being brought under the
                  governance of the operator control.
                type: string
              resourceName:
                description: Name of the target resource.
                type: string
              resourceType:
                description: Type of the target resource.
                type: string
              timeAssignmentFrom:
                description: 'The time at which the target resource will be brought
                  under the governance of the operator control in RFC 3339 (https://tools.ietf.org/html/rfc3339)
                  timestamp format. Example: ''2020-05-22T21:10:29.600Z'''
                type: string
              timeAssignmentTo:
                description: 'The time at which the target resource will leave the
                  governance of the operator control in RFC 3339 (https://tools.ietf.org/html/rfc3339)timestamp
                  format.Example: ''2020-05-22T21:10:29.600Z'''
                type: string
            required:
            - compartmentId
            - isEnforcedAlways
            - operatorControlId
            - resourceCompartmentId
            - resourceId
            - resourceName
            - resourceType
            type: object
          status:
            description: OperatorControlAssignmentStatus defines the observed state
              of OperatorControlAssignment.
            properties:
              assignerId:
                description: The OCID of the user who created this operator control
                  assignment.
                type: string
              comment:
                description: Comment about the assignment of the operator control
                  to this target resource.
                type: string
              compartmentId:
                description: The OCID of the comparment that contains the operator
                  control assignment.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: Defined tags for this resource. Each key is predefined
                  and scoped to a namespace.
                type: object
              detachmentDescription:
                description: description containing reason for releasing of OperatorControl.
                type: string
              errorCode:
                description: The code identifying the error occurred during Assignment
                  operation.
                type: integer
              errorMessage:
                description: The message describing the error occurred during Assignment
                  operation.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: Simple key-value pair that is applied without any predefined
                  name, type or scope. Exists for cross-compatibility only.
                type: object
              id:
                description: The OCID of the operator control assignment.
                type: string
              isAutoApproveDuringMaintenance:
                description: The boolean if true would autoApprove during maintenance.
                type: boolean
              isDefaultAssignment:
                description: Whether the assignment is a default assignment.
                type: boolean
              isEnforcedAlways:
                description: If set, then the target resource is always governed by
                  the operator control.
                type: boolean
              isHypervisorLogForwarded:
                description: If set, then the hypervisor audit logs will be forwarded
                  to the relevant remote syslog server
                type: boolean
              isLogForwarded:
                description: If set indicates that the audit logs are being forwarded
                  to the relevant remote logging server
                type: boolean
              lifecycleDetails:
                description: More in detail about the lifeCycleState.
                type: string
              lifecycleState:
                description: The current lifcycle state of the OperatorControl.
                type: string
              opControlName:
                description: Name of the operator control name associated.
                type: string
              operatorControlId:
                description: The OCID of the operator control.
                type: string
              remoteSyslogServerAddress:
                description: The address of the remote syslog server where the audit
                  logs are being forwarded to. Address in host or IP format.
                type: string
              remoteSyslogServerCACert:
                description: The CA certificate of the remote syslog server.
                type: string
              remoteSyslogServerPort:
                description: The listening port of the remote syslog server. The port
                  range is 0 - 65535. Only TCP supported.
                type: integer
              resourceCompartmentId:
                description: The OCID of the compartment that contains the target
                  resource.
                type: string
              resourceId:
                description: The OCID of the target resource.
                type: string
              resourceName:
                description: Name of the target resource.
                type: string
              resourceType:
                description: resourceType for which the OperatorControlAssignment
                  is applicable
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeAssignmentFrom:
                description: |-
                  The time at which the target resource will be brought under the governance of the operator control expressed in RFC 3339 (https://tools.ietf.org/html/rfc3339) timestamp format.
                  Example: '2020-05-22T21:10:29.600Z'
                type: string
              timeAssignmentTo:
                description: |-
                  The time at which the target resource will leave the governance of the operator control expressed in RFC 3339 (https://tools.ietf.org/html/rfc3339) timestamp format.
                  Example: '2020-05-22T21:10:29.600Z'
                type: string
              timeOfAssignment:
                description: 'Time when the operator control assignment is created
                  in RFC 3339 (https://tools.ietf.org/html/rfc3339) timestamp format.
                  Example: ''2020-05-22T21:10:29.600Z'''
                type: string
              timeOfDeletion:
                description: 'Time on which the operator control assignment was deleted
                  in RFC 3339 (https://tools.ietf.org/html/rfc3339)timestamp format.Example:
                  ''2020-05-22T21:10:29.600Z'''
                type: string
              unassignerId:
                description: User id who released the operatorControl.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
