---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: usagecarbonemissionsqueries.usageapi.oracle.com
spec:
  group: usageapi.oracle.com
  names:
    kind: UsageCarbonEmissionsQuery
    listKind: UsageCarbonEmissionsQueryList
    plural: usagecarbonemissionsqueries
    singular: usagecarbonemissionsquery
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the UsageCarbonEmissionsQuery
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the UsageCarbonEmissionsQuery
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: UsageCarbonEmissionsQuery is the Schema for the usagecarbonemissionsqueries
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UsageCarbonEmissionsQuerySpec defines the desired state of
              UsageCarbonEmissionsQuery.
            properties:
              compartmentId:
                description: The compartment OCID.
                type: string
              queryDefinition:
                description: UsageCarbonEmissionsQueryQueryDefinition defines nested
                  fields for UsageCarbonEmissionsQuery.QueryDefinition.
                properties:
                  costAnalysisUI:
                    description: UsageCarbonEmissionsQueryQueryDefinitionCostAnalysisUI
                      defines nested fields for UsageCarbonEmissionsQuery.QueryDefinition.CostAnalysisUI.
                    properties:
                      graph:
                        description: The graph type.
                        type: string
                      isCumulativeGraph:
                        description: A cumulative graph.
                        type: boolean
                    type: object
                  displayName:
                    description: The query display name. Avoid entering confidential
                      information.
                    type: string
                  reportQuery:
                    description: UsageCarbonEmissionsQueryQueryDefinitionReportQuery
                      defines nested fields for UsageCarbonEmissionsQuery.QueryDefinition.ReportQuery.
                    properties:
                      compartmentDepth:
                        description: The compartment depth level.
                        type: integer
                      dateRangeName:
                        description: The user interface date range, for example, LAST_THREE_MONTHS.
                          Overrides the timeUsageStarted and timeUsageEnded properties.
                        type: string
                      emissionCalculationMethod:
                        description: Specifies the method used for emission calculation,
                          such as POWER_BASED or SPEND_BASED
                        type: string
                      emissionType:
                        description: Specifies the type of emission, such as MARKET_BASED
                          or LOCATION_BASED.
                        type: string
                      filter:
                        description: UsageCarbonEmissionsQueryQueryDefinitionReportQueryFilter
                          defines nested fields for UsageCarbonEmissionsQuery.QueryDefinition.ReportQuery.Filter.
                        properties:
                          dimensions:
                            description: The dimensions to filter on.
                            items:
                              description: UsageCarbonEmissionsQueryQueryDefinitionReportQueryFilterDimension
                                defines nested fields for UsageCarbonEmissionsQuery.QueryDefinition.ReportQuery.Filter.Dimension.
                              properties:
                                key:
                                  description: The dimension key.
                                  type: string
                                value:
                                  description: The dimension value.
                                  type: string
                              required:
                              - key
                              - value
                              type: object
                            type: array
                          operator:
                            description: 'The filter operator. Example: ''AND'', ''OR'',
                              ''NOT''.'
                            type: string
                          tags:
                            description: The tags to filter on.
                            items:
                              description: UsageCarbonEmissionsQueryQueryDefinitionReportQueryFilterTag
                                defines nested fields for UsageCarbonEmissionsQuery.QueryDefinition.ReportQuery.Filter.Tag.
                              properties:
                                key:
                                  description: The tag key.
                                  type: string
                                namespace:
                                  description: The tag namespace.
                                  type: string
                                value:
                                  description: The tag value.
                                  type: string
                              type: object
                            type: array
                        type: object
                      granularity:
                        description: The carbon emission granularity. DAILY - Daily
                          data aggregation. MONTHLY - Monthly data aggregation.
                        type: string
                      groupBy:
                        description: |-
                          Specifies what to aggregate the result by.
                          For example:
                            `["tagNamespace", "tagKey", "tagValue", "service", "skuName", "skuPartNumber", "unit",
                              "compartmentName", "compartmentPath", "compartmentId", "platform", "region", "logicalAd",
                              "resourceId", "tenantId", "tenantName"]`
                        items:
                          type: string
                        type: array
                      groupByTag:
                        description: |-
                          GroupBy a specific tagKey. Provide the tagNamespace and tagKey in the tag object. Only supports one tag in the list.
                          For example:
                            `[{"namespace":"oracle", "key":"createdBy"]`
                        items:
                          description: UsageCarbonEmissionsQueryQueryDefinitionReportQueryGroupByTag
                            defines nested fields for UsageCarbonEmissionsQuery.QueryDefinition.ReportQuery.GroupByTag.
                          properties:
                            key:
                              description: The tag key.
                              type: string
                            namespace:
                              description: The tag namespace.
                              type: string
                            value:
                              description: The tag value.
                              type: string
                          type: object
                        type: array
                      isAggregateByTime:
                        description: Specifies whether aggregated by time. If isAggregateByTime
                          is true, all usage or costs over the query time period are
                          summed.
                        type: boolean
                      tenantId:
                        description: Tenant ID.
                        type: string
                      timeUsageEnded:
                        description: The usage end time.
                        type: string
                      timeUsageStarted:
                        description: The usage start time.
                        type: string
                    required:
                    - tenantId
                    type: object
                  version:
                    description: The saved query version.
                    type: integer
                required:
                - costAnalysisUI
                - displayName
                - reportQuery
                - version
                type: object
            required:
            - compartmentId
            - queryDefinition
            type: object
          status:
            description: UsageCarbonEmissionsQueryStatus defines the observed state
              of UsageCarbonEmissionsQuery.
            properties:
              compartmentId:
                description: The compartment OCID.
                type: string
              id:
                description: The query OCID.
                type: string
              queryDefinition:
                description: UsageCarbonEmissionsQueryQueryDefinition defines nested
                  fields for UsageCarbonEmissionsQuery.QueryDefinition.
                properties:
                  costAnalysisUI:
                    description: UsageCarbonEmissionsQueryQueryDefinitionCostAnalysisUI
                      defines nested fields for UsageCarbonEmissionsQuery.QueryDefinition.CostAnalysisUI.
                    properties:
                      graph:
                        description: The graph type.
                        type: string
                      isCumulativeGraph:
                        description: A cumulative graph.
                        type: boolean
                    type: object
                  displayName:
                    description: The query display name. Avoid entering confidential
                      information.
                    type: string
                  reportQuery:
                    description: UsageCarbonEmissionsQueryQueryDefinitionReportQuery
                      defines nested fields for UsageCarbonEmissionsQuery.QueryDefinition.ReportQuery.
                    properties:
                      compartmentDepth:
                        description: The compartment depth level.
                        type: integer
                      dateRangeName:
                        description: The user interface date range, for example, LAST_THREE_MONTHS.
                          Overrides the timeUsageStarted and timeUsageEnded properties.
                        type: string
                      emissionCalculationMethod:
                        description: Specifies the method used for emission calculation,
                          such as POWER_BASED or SPEND_BASED
                        type: string
                      emissionType:
                        description: Specifies the type of emission, such as MARKET_BASED
                          or LOCATION_BASED.
                        type: string
                      filter:
                        description: UsageCarbonEmissionsQueryQueryDefinitionReportQueryFilter
                          defines nested fields for UsageCarbonEmissionsQuery.QueryDefinition.ReportQuery.Filter.
                        properties:
                          dimensions:
                            description: The dimensions to filter on.
                            items:
                              description: UsageCarbonEmissionsQueryQueryDefinitionReportQueryFilterDimension
                                defines nested fields for UsageCarbonEmissionsQuery.QueryDefinition.ReportQuery.Filter.Dimension.
                              properties:
                                key:
                                  description: The dimension key.
                                  type: string
                                value:
                                  description: The dimension value.
                                  type: string
                              required:
                              - key
                              - value
                              type: object
                            type: array
                          operator:
                            description: 'The filter operator. Example: ''AND'', ''OR'',
                              ''NOT''.'
                            type: string
                          tags:
                            description: The tags to filter on.
                            items:
                              description: UsageCarbonEmissionsQueryQueryDefinitionReportQueryFilterTag
                                defines nested fields for UsageCarbonEmissionsQuery.QueryDefinition.ReportQuery.Filter.Tag.
                              properties:
                                key:
                                  description: The tag key.
                                  type: string
                                namespace:
                                  description: The tag namespace.
                                  type: string
                                value:
                                  description: The tag value.
                                  type: string
                              type: object
                            type: array
                        type: object
                      granularity:
                        description: The carbon emission granularity. DAILY - Daily
                          data aggregation. MONTHLY - Monthly data aggregation.
                        type: string
                      groupBy:
                        description: |-
                          Specifies what to aggregate the result by.
                          For example:
                            `["tagNamespace", "tagKey", "tagValue", "service", "skuName", "skuPartNumber", "unit",
                              "compartmentName", "compartmentPath", "compartmentId", "platform", "region", "logicalAd",
                              "resourceId", "tenantId", "tenantName"]`
                        items:
                          type: string
                        type: array
                      groupByTag:
                        description: |-
                          GroupBy a specific tagKey. Provide the tagNamespace and tagKey in the tag object. Only supports one tag in the list.
                          For example:
                            `[{"namespace":"oracle", "key":"createdBy"]`
                        items:
                          description: UsageCarbonEmissionsQueryQueryDefinitionReportQueryGroupByTag
                            defines nested fields for UsageCarbonEmissionsQuery.QueryDefinition.ReportQuery.GroupByTag.
                          properties:
                            key:
                              description: The tag key.
                              type: string
                            namespace:
                              description: The tag namespace.
                              type: string
                            value:
                              description: The tag value.
                              type: string
                          type: object
                        type: array
                      isAggregateByTime:
                        description: Specifies whether aggregated by time. If isAggregateByTime
                          is true, all usage or costs over the query time period are
                          summed.
                        type: boolean
                      tenantId:
                        description: Tenant ID.
                        type: string
                      timeUsageEnded:
                        description: The usage end time.
                        type: string
                      timeUsageStarted:
                        description: The usage start time.
                        type: string
                    required:
                    - tenantId
                    type: object
                  version:
                    description: The saved query version.
                    type: integer
                required:
                - costAnalysisUI
                - displayName
                - reportQuery
                - version
                type: object
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
