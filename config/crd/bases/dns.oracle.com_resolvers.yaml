---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: resolvers.dns.oracle.com
spec:
  group: dns.oracle.com
  names:
    kind: Resolver
    listKind: ResolverList
    plural: resolvers
    singular: resolver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the Resolver
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Resolver
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Resolver is the Schema for the resolvers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ResolverSpec defines the desired state of Resolver.
            properties:
              attachedViews:
                description: The attached views. Views are evaluated in order.
                items:
                  description: ResolverAttachedView defines nested fields for Resolver.AttachedView.
                  properties:
                    viewId:
                      description: The OCID of the view.
                      type: string
                  required:
                  - viewId
                  type: object
                type: array
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).

                  **Example:** `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: The display name of the resolver.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).

                  **Example:** `{"Department": "Finance"}`
                type: object
              rules:
                description: Rules for the resolver. Rules are evaluated in order.
                items:
                  description: ResolverRule defines nested fields for Resolver.Rule.
                  properties:
                    action:
                      type: string
                    clientAddressConditions:
                      description: |-
                        A list of CIDR blocks. The query must come from a client within one of the blocks in order for the rule action
                        to apply.
                      items:
                        type: string
                      type: array
                    destinationAddresses:
                      description: IP addresses to which queries should be forwarded.
                        Currently limited to a single address.
                      items:
                        type: string
                      type: array
                    qnameCoverConditions:
                      description: A list of domain names. The query must be covered
                        by one of the domains in order for the rule action to apply.
                      items:
                        type: string
                      type: array
                    sourceEndpointName:
                      description: |-
                        Case-insensitive name of an endpoint, that is a sub-resource of the resolver, to use as the forwarding
                        interface. The endpoint must have isForwarding set to true.
                      type: string
                  required:
                  - destinationAddresses
                  - sourceEndpointName
                  type: object
                type: array
            type: object
          status:
            description: ResolverStatus defines the observed state of Resolver.
            properties:
              attachedVcnId:
                description: The OCID of the attached VCN.
                type: string
              attachedViews:
                description: The attached views. Views are evaluated in order.
                items:
                  description: ResolverAttachedView defines nested fields for Resolver.AttachedView.
                  properties:
                    viewId:
                      description: The OCID of the view.
                      type: string
                  required:
                  - viewId
                  type: object
                type: array
              compartmentId:
                description: The OCID of the owning compartment.
                type: string
              defaultViewId:
                description: The OCID of the default view.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).

                  **Example:** `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: The display name of the resolver.
                type: string
              endpoints:
                description: Read-only array of endpoints for the resolver.
                items:
                  description: ResolverEndpointFields defines nested fields for Resolver.Endpoint.
                  properties:
                    compartmentId:
                      description: |-
                        The OCID of the owning compartment. This will match the resolver that the resolver endpoint is under
                        and will be updated if the resolver's compartment is changed.
                      type: string
                    endpointType:
                      type: string
                    forwardingAddress:
                      description: |-
                        An IP address from which forwarded queries may be sent. For VNIC endpoints, this IP address must be part
                        of the subnet and will be assigned by the system if unspecified when isForwarding is true.
                      type: string
                    isForwarding:
                      description: A Boolean flag indicating whether or not the resolver
                        endpoint is for forwarding.
                      type: boolean
                    isListening:
                      description: A Boolean flag indicating whether or not the resolver
                        endpoint is for listening.
                      type: boolean
                    lifecycleState:
                      description: The current state of the resource.
                      type: string
                    listeningAddress:
                      description: |-
                        An IP address to listen to queries on. For VNIC endpoints this IP address must be part of the
                        subnet and will be assigned by the system if unspecified when isListening is true.
                      type: string
                    name:
                      description: The name of the resolver endpoint. Must be unique,
                        case-insensitive, within the resolver.
                      type: string
                    self:
                      description: The canonical absolute URL of the resource.
                      type: string
                    subnetId:
                      description: The OCID of a subnet. Must be part of the VCN that
                        the resolver is attached to.
                      type: string
                    timeCreated:
                      description: |-
                        The date and time the resource was created in "YYYY-MM-ddThh:mm:ssZ" format
                        with a Z offset, as defined by RFC 3339.
                        **Example:** `2016-07-22T17:23:59:60Z`
                      type: string
                    timeUpdated:
                      description: |-
                        The date and time the resource was last updated in "YYYY-MM-ddThh:mm:ssZ"
                        format with a Z offset, as defined by RFC 3339.
                        **Example:** `2016-07-22T17:23:59:60Z`
                      type: string
                  type: object
                type: array
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).

                  **Example:** `{"Department": "Finance"}`
                type: object
              id:
                description: The OCID of the resolver.
                type: string
              isProtected:
                description: A Boolean flag indicating whether or not parts of the
                  resource are unable to be explicitly managed.
                type: boolean
              lifecycleState:
                description: The current state of the resource.
                type: string
              rules:
                description: Rules for the resolver. Rules are evaluated in order.
                items:
                  description: ResolverRule defines nested fields for Resolver.Rule.
                  properties:
                    action:
                      type: string
                    clientAddressConditions:
                      description: |-
                        A list of CIDR blocks. The query must come from a client within one of the blocks in order for the rule action
                        to apply.
                      items:
                        type: string
                      type: array
                    destinationAddresses:
                      description: IP addresses to which queries should be forwarded.
                        Currently limited to a single address.
                      items:
                        type: string
                      type: array
                    qnameCoverConditions:
                      description: A list of domain names. The query must be covered
                        by one of the domains in order for the rule action to apply.
                      items:
                        type: string
                      type: array
                    sourceEndpointName:
                      description: |-
                        Case-insensitive name of an endpoint, that is a sub-resource of the resolver, to use as the forwarding
                        interface. The endpoint must have isForwarding set to true.
                      type: string
                  required:
                  - destinationAddresses
                  - sourceEndpointName
                  type: object
                type: array
              self:
                description: The canonical absolute URL of the resource.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time the resource was created in "YYYY-MM-ddThh:mm:ssZ" format
                  with a Z offset, as defined by RFC 3339.
                  **Example:** `2016-07-22T17:23:59:60Z`
                type: string
              timeUpdated:
                description: |-
                  The date and time the resource was last updated in "YYYY-MM-ddThh:mm:ssZ"
                  format with a Z offset, as defined by RFC 3339.
                  **Example:** `2016-07-22T17:23:59:60Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
