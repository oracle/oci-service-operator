---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: publishers.marketplace.oracle.com
spec:
  group: marketplace.oracle.com
  names:
    kind: Publisher
    listKind: PublisherList
    plural: publishers
    singular: publisher
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the Publisher
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Publisher
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Publisher is the Schema for the publishers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PublisherSpec defines the desired state of Publisher.
            type: object
          status:
            description: PublisherStatus defines the observed state of Publisher.
            properties:
              contactEmail:
                description: The email address of the publisher.
                type: string
              contactPhone:
                description: The phone number of the publisher.
                type: string
              description:
                description: A description of the publisher.
                type: string
              hqAddress:
                description: The address of the publisher's headquarters.
                type: string
              id:
                description: Unique identifier for the publisher.
                type: string
              links:
                description: Reference links.
                items:
                  description: PublisherLink defines nested fields for Publisher.Link.
                  properties:
                    href:
                      description: The anchor tag.
                      type: string
                    rel:
                      description: Reference links to the previous page, next page,
                        and other pages.
                      type: string
                  type: object
                type: array
              logo:
                description: PublisherLogo defines nested fields for Publisher.Logo.
                properties:
                  contentUrl:
                    description: The content URL of the upload data.
                    type: string
                  fileExtension:
                    description: The file extension of the upload data.
                    type: string
                  mimeType:
                    description: The MIME type of the upload data.
                    type: string
                  name:
                    description: The name used to refer to the upload data.
                    type: string
                type: object
              name:
                description: The name of the publisher.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              websiteUrl:
                description: The publisher's website.
                type: string
              yearFounded:
                description: The year the publisher's company or organization was
                  founded.
                format: int64
                type: integer
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
