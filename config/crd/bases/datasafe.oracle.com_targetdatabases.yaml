---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: targetdatabases.datasafe.oracle.com
spec:
  group: datasafe.oracle.com
  names:
    kind: TargetDatabase
    listKind: TargetDatabaseList
    plural: targetdatabases
    singular: targetdatabase
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the TargetDatabase
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the TargetDatabase
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: TargetDatabase is the Schema for the targetdatabases API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TargetDatabaseSpec defines the desired state of TargetDatabase.
            properties:
              compartmentId:
                description: The OCID of the compartment in which to create the Data
                  Safe target database.
                type: string
              connectionOption:
                description: TargetDatabaseConnectionOption defines nested fields
                  for TargetDatabase.ConnectionOption.
                properties:
                  connectionType:
                    type: string
                  datasafePrivateEndpointId:
                    description: The OCID of the Data Safe private endpoint.
                    type: string
                  jsonData:
                    type: string
                  onPremConnectorId:
                    description: The OCID of the on-premises connector.
                    type: string
                type: object
              credentials:
                description: TargetDatabaseCredentials defines nested fields for TargetDatabase.Credentials.
                properties:
                  password:
                    description: The password of the database user.
                    type: string
                  userName:
                    description: The database user name.
                    type: string
                required:
                - password
                - userName
                type: object
              databaseDetails:
                description: TargetDatabaseDatabaseDetails defines nested fields for
                  TargetDatabase.DatabaseDetails.
                properties:
                  autonomousDatabaseId:
                    description: The OCID of the Autonomous Database registered as
                      a target database in Data Safe.
                    type: string
                  databaseType:
                    type: string
                  dbSystemId:
                    description: The OCID of the cloud database registered as a target
                      database in Data Safe.
                    type: string
                  infrastructureType:
                    description: The infrastructure type the database is running on.
                    type: string
                  instanceId:
                    description: The OCID of the compute instance on which the database
                      is running.
                    type: string
                  ipAddresses:
                    description: The list of database host IP Addresses. Fully qualified
                      domain names can be used if connectionType is 'ONPREM_CONNECTOR'.
                    items:
                      type: string
                    type: array
                  jsonData:
                    type: string
                  listenerPort:
                    description: The port number of the database listener.
                    type: integer
                  pluggableDatabaseId:
                    description: The OCID of the pluggable database registered as
                      a target database in Data Safe.
                    type: string
                  serviceName:
                    description: The service name of the database registered as target
                      database.
                    type: string
                  vmClusterId:
                    description: The OCID of the VM cluster in which the database
                      is running.
                    type: string
                required:
                - infrastructureType
                type: object
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm)
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              description:
                description: The description of the target database in Data Safe.
                type: string
              displayName:
                description: The display name of the target database in Data Safe.
                  The name is modifiable and does not need to be unique.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm)
                  Example: `{"Department": "Finance"}`
                type: object
              peerTargetDatabaseDetails:
                description: The details of the database to be registered as a peer
                  target database.
                items:
                  description: TargetDatabasePeerTargetDatabaseDetail defines nested
                    fields for TargetDatabase.PeerTargetDatabaseDetail.
                  properties:
                    databaseDetails:
                      description: TargetDatabasePeerTargetDatabaseDetailDatabaseDetails
                        defines nested fields for TargetDatabase.PeerTargetDatabaseDetail.DatabaseDetails.
                      properties:
                        autonomousDatabaseId:
                          description: The OCID of the Autonomous Database registered
                            as a target database in Data Safe.
                          type: string
                        databaseType:
                          type: string
                        dbSystemId:
                          description: The OCID of the cloud database registered as
                            a target database in Data Safe.
                          type: string
                        infrastructureType:
                          description: The infrastructure type the database is running
                            on.
                          type: string
                        instanceId:
                          description: The OCID of the compute instance on which the
                            database is running.
                          type: string
                        ipAddresses:
                          description: The list of database host IP Addresses. Fully
                            qualified domain names can be used if connectionType is
                            'ONPREM_CONNECTOR'.
                          items:
                            type: string
                          type: array
                        jsonData:
                          type: string
                        listenerPort:
                          description: The port number of the database listener.
                          type: integer
                        pluggableDatabaseId:
                          description: The OCID of the pluggable database registered
                            as a target database in Data Safe.
                          type: string
                        serviceName:
                          description: The service name of the database registered
                            as target database.
                          type: string
                        vmClusterId:
                          description: The OCID of the VM cluster in which the database
                            is running.
                          type: string
                      required:
                      - infrastructureType
                      type: object
                    dataguardAssociationId:
                      description: The OCID of the Data Guard Association resource
                        in which the database being registered is considered as peer
                        database to the primary database.
                      type: string
                    description:
                      description: The description of the peer target database in
                        Data Safe.
                      type: string
                    displayName:
                      description: The display name of the peer target database in
                        Data Safe. The name is modifiable and does not need to be
                        unique.
                      type: string
                    tlsConfig:
                      description: TargetDatabasePeerTargetDatabaseDetailTlsConfig
                        defines nested fields for TargetDatabase.PeerTargetDatabaseDetail.TlsConfig.
                      properties:
                        certificateStoreType:
                          description: The format of the certificate store.
                          type: string
                        keyStoreContent:
                          description: Base64 encoded string of key store file content.
                          type: string
                        status:
                          description: Status to represent whether the database connection
                            is TLS enabled or not.
                          type: string
                        storePassword:
                          description: The password to read the trust store and key
                            store files, if they are password protected.
                          type: string
                        trustStoreContent:
                          description: Base64 encoded string of trust store file content.
                          type: string
                      required:
                      - status
                      type: object
                  required:
                  - databaseDetails
                  type: object
                type: array
              tlsConfig:
                description: TargetDatabaseTlsConfig defines nested fields for TargetDatabase.TlsConfig.
                properties:
                  certificateStoreType:
                    description: The format of the certificate store.
                    type: string
                  keyStoreContent:
                    description: Base64 encoded string of key store file content.
                    type: string
                  status:
                    description: Status to represent whether the database connection
                      is TLS enabled or not.
                    type: string
                  storePassword:
                    description: The password to read the trust store and key store
                      files, if they are password protected.
                    type: string
                  trustStoreContent:
                    description: Base64 encoded string of trust store file content.
                    type: string
                required:
                - status
                type: object
            required:
            - compartmentId
            - databaseDetails
            type: object
          status:
            description: TargetDatabaseStatus defines the observed state of TargetDatabase.
            properties:
              associatedResourceIds:
                description: The OCIDs of associated resources like database, Data
                  Safe private endpoint etc.
                items:
                  type: string
                type: array
              compartmentId:
                description: The OCID of the compartment which contains the Data Safe
                  target database.
                type: string
              connectionOption:
                description: TargetDatabaseConnectionOption defines nested fields
                  for TargetDatabase.ConnectionOption.
                properties:
                  connectionType:
                    type: string
                  datasafePrivateEndpointId:
                    description: The OCID of the Data Safe private endpoint.
                    type: string
                  jsonData:
                    type: string
                  onPremConnectorId:
                    description: The OCID of the on-premises connector.
                    type: string
                type: object
              credentials:
                description: TargetDatabaseCredentialsObservedState defines nested
                  fields for TargetDatabase.Credentials.
                properties:
                  userName:
                    description: The database user name.
                    type: string
                type: object
              databaseDetails:
                description: TargetDatabaseDatabaseDetails defines nested fields for
                  TargetDatabase.DatabaseDetails.
                properties:
                  autonomousDatabaseId:
                    description: The OCID of the Autonomous Database registered as
                      a target database in Data Safe.
                    type: string
                  databaseType:
                    type: string
                  dbSystemId:
                    description: The OCID of the cloud database registered as a target
                      database in Data Safe.
                    type: string
                  infrastructureType:
                    description: The infrastructure type the database is running on.
                    type: string
                  instanceId:
                    description: The OCID of the compute instance on which the database
                      is running.
                    type: string
                  ipAddresses:
                    description: The list of database host IP Addresses. Fully qualified
                      domain names can be used if connectionType is 'ONPREM_CONNECTOR'.
                    items:
                      type: string
                    type: array
                  jsonData:
                    type: string
                  listenerPort:
                    description: The port number of the database listener.
                    type: integer
                  pluggableDatabaseId:
                    description: The OCID of the pluggable database registered as
                      a target database in Data Safe.
                    type: string
                  serviceName:
                    description: The service name of the database registered as target
                      database.
                    type: string
                  vmClusterId:
                    description: The OCID of the VM cluster in which the database
                      is running.
                    type: string
                required:
                - infrastructureType
                type: object
              databaseType:
                description: The database type.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm)
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              description:
                description: The description of the target database in Data Safe.
                type: string
              displayName:
                description: The display name of the target database in Data Safe.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm)
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The OCID of the Data Safe target database.
                type: string
              infrastructureType:
                description: The infrastructure type the database is running on.
                type: string
              lifecycleDetails:
                description: Details about the current state of the target database
                  in Data Safe.
                type: string
              lifecycleState:
                description: The current state of the target database in Data Safe.
                type: string
              peerTargetDatabases:
                description: The OCIDs of associated resources like database, Data
                  Safe private endpoint, etc.
                items:
                  description: TargetDatabasePeerTargetDatabase defines nested fields
                    for TargetDatabase.PeerTargetDatabase.
                  properties:
                    databaseDetails:
                      description: TargetDatabasePeerTargetDatabaseDatabaseDetails
                        defines nested fields for TargetDatabase.PeerTargetDatabase.DatabaseDetails.
                      properties:
                        autonomousDatabaseId:
                          description: The OCID of the Autonomous Database registered
                            as a target database in Data Safe.
                          type: string
                        databaseType:
                          type: string
                        dbSystemId:
                          description: The OCID of the cloud database registered as
                            a target database in Data Safe.
                          type: string
                        infrastructureType:
                          description: The infrastructure type the database is running
                            on.
                          type: string
                        instanceId:
                          description: The OCID of the compute instance on which the
                            database is running.
                          type: string
                        ipAddresses:
                          description: The list of database host IP Addresses. Fully
                            qualified domain names can be used if connectionType is
                            'ONPREM_CONNECTOR'.
                          items:
                            type: string
                          type: array
                        jsonData:
                          type: string
                        listenerPort:
                          description: The port number of the database listener.
                          type: integer
                        pluggableDatabaseId:
                          description: The OCID of the pluggable database registered
                            as a target database in Data Safe.
                          type: string
                        serviceName:
                          description: The service name of the database registered
                            as target database.
                          type: string
                        vmClusterId:
                          description: The OCID of the VM cluster in which the database
                            is running.
                          type: string
                      type: object
                    databaseUniqueName:
                      description: Unique name of the database associated to the peer
                        target database.
                      type: string
                    dataguardAssociationId:
                      description: The OCID of the Data Guard Association resource
                        in which the database associated to the peer target database
                        is considered as peer database to the primary database.
                      type: string
                    description:
                      description: The description of the peer target database in
                        Data Safe.
                      type: string
                    displayName:
                      description: The display name of the peer target database in
                        Data Safe.
                      type: string
                    key:
                      description: The secondary key assigned for the peer target
                        database in Data Safe.
                      type: integer
                    lifecycleDetails:
                      description: Details about the current state of the peer target
                        database in Data Safe.
                      type: string
                    lifecycleState:
                      description: The current state of the peer target database in
                        Data Safe.
                      type: string
                    role:
                      description: Role of the database associated to the peer target
                        database.
                      type: string
                    timeCreated:
                      description: The date and time of the peer target database registration
                        in Data Safe.
                      type: string
                    tlsConfig:
                      description: TargetDatabasePeerTargetDatabaseTlsConfig defines
                        nested fields for TargetDatabase.PeerTargetDatabase.TlsConfig.
                      properties:
                        certificateStoreType:
                          description: The format of the certificate store.
                          type: string
                        keyStoreContent:
                          description: Base64 encoded string of key store file content.
                          type: string
                        status:
                          description: Status to represent whether the database connection
                            is TLS enabled or not.
                          type: string
                        storePassword:
                          description: The password to read the trust store and key
                            store files, if they are password protected.
                          type: string
                        trustStoreContent:
                          description: Base64 encoded string of trust store file content.
                          type: string
                      type: object
                  type: object
                type: array
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace. For more information, see Resource Tags.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: The date and time of the target database registration
                  and creation in Data Safe.
                type: string
              timeUpdated:
                description: The date and time of the target database update in Data
                  Safe.
                type: string
              tlsConfig:
                description: TargetDatabaseTlsConfig defines nested fields for TargetDatabase.TlsConfig.
                properties:
                  certificateStoreType:
                    description: The format of the certificate store.
                    type: string
                  keyStoreContent:
                    description: Base64 encoded string of key store file content.
                    type: string
                  status:
                    description: Status to represent whether the database connection
                      is TLS enabled or not.
                    type: string
                  storePassword:
                    description: The password to read the trust store and key store
                      files, if they are password protected.
                    type: string
                  trustStoreContent:
                    description: Base64 encoded string of trust store file content.
                    type: string
                required:
                - status
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
