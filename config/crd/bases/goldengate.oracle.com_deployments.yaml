---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: deployments.goldengate.oracle.com
spec:
  group: goldengate.oracle.com
  names:
    kind: Deployment
    listKind: DeploymentList
    plural: deployments
    singular: deployment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the Deployment
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Deployment
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Deployment is the Schema for the deployments API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DeploymentSpec defines the desired state of Deployment.
            properties:
              availabilityDomain:
                description: The availability domain of a placement.
                type: string
              backupSchedule:
                description: DeploymentBackupSchedule defines nested fields for Deployment.BackupSchedule.
                properties:
                  bucketName:
                    description: Name of the bucket where the object is to be uploaded
                      in the object storage
                    type: string
                  compartmentId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the compartment being referenced.
                    type: string
                  frequencyBackupScheduled:
                    description: The frequency of the deployment backup schedule.
                      Frequency can be DAILY, WEEKLY or MONTHLY.
                    type: string
                  isMetadataOnly:
                    description: Parameter to allow users to create backup without
                      trails
                    type: boolean
                  namespaceName:
                    description: Name of namespace that serves as a container for
                      all of your buckets
                    type: string
                  timeBackupScheduled:
                    description: |-
                      The start timestamp for the deployment backup schedule. The format is defined by
                      RFC3339 (https://tools.ietf.org/html/rfc3339), such as `2024-10-25T18:19:29.600Z`.
                    type: string
                required:
                - bucketName
                - compartmentId
                - frequencyBackupScheduled
                - isMetadataOnly
                - namespaceName
                - timeBackupScheduled
                type: object
              byolCpuCoreCountLimit:
                description: |-
                  The maximum number of CPUs allowed with a 'Bring Your Own License' (BYOL) license type.
                  Any CPU usage above this limit is considered as License Included and billed.
                type: integer
              clusterPlacementGroupId:
                description: |-
                  The OCID(/Content/General/Concepts/identifiers.htm) of the cluster placement group for the resource.
                  Only applicable for multicloud subscriptions. The cluster placement group id must be provided when a multicloud
                  subscription id is provided. Otherwise the cluster placement group must not be provided.
                type: string
              compartmentId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment being referenced.
                type: string
              cpuCoreCount:
                description: The Minimum number of OCPUs to be made available for
                  this Deployment.
                type: integer
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Tags defined for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              deploymentBackupId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the backup being referenced.
                type: string
              deploymentType:
                description: |-
                  The type of deployment, which can be any one of the Allowed values.
                  NOTE: Use of the value 'OGG' is maintained for backward compatibility purposes.
                      Its use is discouraged in favor of 'DATABASE_ORACLE'.
                type: string
              description:
                description: Metadata about this specific object.
                type: string
              displayName:
                description: An object's Display Name.
                type: string
              environmentType:
                description: Specifies whether the deployment is used in a production
                  or development/testing environment.
                type: string
              faultDomain:
                description: The fault domain of a placement.
                type: string
              fqdn:
                description: A three-label Fully Qualified Domain Name (FQDN) for
                  a resource.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  A simple key-value pair that is applied without any predefined name, type, or scope. Exists
                  for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              isAutoScalingEnabled:
                description: Indicates if auto scaling is enabled for the Deployment's
                  CPU core count.
                type: boolean
              isByolCpuCoreCountLimitEnabled:
                description: |-
                  Flag to allow to configure the 'Bring Your Own License' (BYOL) license type CPU limit.
                  If enabled, the exact number of CPUs must be provided via byolCpuCoreCountLimit.
                type: boolean
              isPublic:
                description: True if this object is publicly available.
                type: boolean
              licenseModel:
                description: The Oracle license model that applies to a Deployment.
                type: string
              loadBalancerSubnetId:
                description: |-
                  The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of a public subnet in the customer tenancy.
                  Can be provided only for public deployments. If provided, the loadbalancer will be created in this subnet instead of the service tenancy.
                  For backward compatibility, this is an optional property. It will become mandatory for public deployments after October 1, 2024.
                type: string
              locks:
                description: Locks associated with this resource.
                items:
                  description: DeploymentLock defines nested fields for Deployment.Lock.
                  properties:
                    message:
                      description: |-
                        A message added by the creator of the lock. This is typically used to give an
                        indication of why the resource is locked.
                      type: string
                    type:
                      description: Type of the lock.
                      type: string
                  required:
                  - type
                  type: object
                type: array
              maintenanceConfiguration:
                description: DeploymentMaintenanceConfiguration defines nested fields
                  for Deployment.MaintenanceConfiguration.
                properties:
                  bundleReleaseUpgradePeriodInDays:
                    description: |-
                      Defines auto upgrade period for bundle releases. Manually configured period cannot be longer than service defined period for bundle releases.
                      This period must be shorter or equal to major release upgrade period. Not passing this field during create will equate to using the service default.
                    type: integer
                  interimReleaseUpgradePeriodInDays:
                    description: Defines auto upgrade period for interim releases.
                      This period must be shorter or equal to bundle release upgrade
                      period.
                    type: integer
                  isInterimReleaseAutoUpgradeEnabled:
                    description: |-
                      By default auto upgrade for interim releases are not enabled. If auto-upgrade is enabled for interim release,
                      you have to specify interimReleaseUpgradePeriodInDays too.
                    type: boolean
                  majorReleaseUpgradePeriodInDays:
                    description: |-
                      Defines auto upgrade period for major releases. Manually configured period cannot be longer than service defined period for major releases.
                      Not passing this field during create will equate to using the service default.
                    type: integer
                  securityPatchUpgradePeriodInDays:
                    description: |-
                      Defines auto upgrade period for releases with security fix. Manually configured period cannot be longer than service defined period for security releases.
                      Not passing this field during create will equate to using the service default.
                    type: integer
                type: object
              maintenanceWindow:
                description: DeploymentMaintenanceWindow defines nested fields for
                  Deployment.MaintenanceWindow.
                properties:
                  day:
                    description: Days of the week.
                    type: string
                  startHour:
                    description: Start hour for maintenance period. Hour is in UTC.
                    type: integer
                required:
                - day
                - startHour
                type: object
              nsgIds:
                description: An array of Network Security Group OCIDs used to define
                  network access for either Deployments or Connections.
                items:
                  type: string
                type: array
              oggData:
                description: DeploymentOggData defines nested fields for Deployment.OggData.
                properties:
                  adminPassword:
                    description: |-
                      The password associated with the GoldenGate deployment console username.
                      The password must be 8 to 30 characters long and must contain at least 1 uppercase, 1 lowercase, 1 numeric,
                      and 1 special character. Special characters such as '$', '^', or '?' are not allowed.
                      This field will be deprecated and replaced by "passwordSecretId".
                    type: string
                  adminUsername:
                    description: The GoldenGate deployment console username.
                    type: string
                  certificate:
                    description: The base64 encoded content of the PEM file containing
                      the SSL certificate.
                    type: string
                  credentialStore:
                    description: The type of credential store for OGG.
                    type: string
                  deploymentName:
                    description: |-
                      The name given to the GoldenGate service deployment.
                      The name must be 1 to 32 characters long, must contain only alphanumeric characters and must start with a letter.
                    type: string
                  groupToRolesMapping:
                    description: DeploymentOggDataGroupToRolesMapping defines nested
                      fields for Deployment.OggData.GroupToRolesMapping.
                    properties:
                      administratorGroupId:
                        description: |-
                          The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the IDP group which will be mapped to goldengate role administratorGroup.
                          It grants full access to the user, including the ability to alter general, non-security related operational parameters
                          and profiles of the server.
                        type: string
                      operatorGroupId:
                        description: |-
                          The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the IDP group which will be mapped to goldengate role operatorGroup.
                          It allows users to perform only operational actions, like starting and stopping resources.
                          Operators cannot alter the operational parameters or profiles of the MA server.
                        type: string
                      securityGroupId:
                        description: |-
                          The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the IDP group which will be mapped to goldengate role securityGroup.
                          It grants administration of security related objects and invoke security related service requests. This role has full privileges.
                        type: string
                      userGroupId:
                        description: |-
                          The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the IDP group which will be mapped to goldengate role userGroup.
                          It allows information-only service requests, which do not alter or affect the operation of either the MA.
                          Examples of query and read-only information include performance metric information and resource status and monitoring information
                        type: string
                    required:
                    - securityGroupId
                    type: object
                  identityDomainId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the Identity Domain when IAM credential store is used.
                    type: string
                  key:
                    description: The base64 encoded content of the PEM file containing
                      the private key.
                    type: string
                  oggVersion:
                    description: Version of OGG
                    type: string
                  passwordSecretId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the Secret where the deployment password is stored.
                    type: string
                required:
                - deploymentName
                type: object
              placements:
                description: An array of local peers of deployment
                items:
                  description: DeploymentPlacement defines nested fields for Deployment.Placement.
                  properties:
                    availabilityDomain:
                      description: The availability domain of a placement.
                      type: string
                    faultDomain:
                      description: The fault domain of a placement.
                      type: string
                  type: object
                type: array
              securityAttributes:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Security attributes for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Oracle-ZPR": {"MaxEgressCount": {"value": "42", "mode": "enforce"}}}`
                type: object
              sourceDeploymentId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the deployment being referenced.
                type: string
              subnetId:
                description: |-
                  The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the subnet of the deployment's private endpoint.
                  The subnet must be a private subnet. For backward compatibility, public subnets are allowed until May 31 2025,
                  after which the private subnet will be enforced.
                type: string
              subscriptionId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the subscription with which resource needs to be associated with.
                type: string
            required:
            - compartmentId
            - displayName
            - subnetId
            type: object
          status:
            description: DeploymentStatus defines the observed state of Deployment.
            properties:
              availabilityDomain:
                description: The availability domain of a placement.
                type: string
              backupSchedule:
                description: DeploymentBackupSchedule defines nested fields for Deployment.BackupSchedule.
                properties:
                  bucketName:
                    description: Name of the bucket where the object is to be uploaded
                      in the object storage
                    type: string
                  compartmentId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the compartment being referenced.
                    type: string
                  frequencyBackupScheduled:
                    description: The frequency of the deployment backup schedule.
                      Frequency can be DAILY, WEEKLY or MONTHLY.
                    type: string
                  isMetadataOnly:
                    description: Parameter to allow users to create backup without
                      trails
                    type: boolean
                  namespaceName:
                    description: Name of namespace that serves as a container for
                      all of your buckets
                    type: string
                  timeBackupScheduled:
                    description: |-
                      The start timestamp for the deployment backup schedule. The format is defined by
                      RFC3339 (https://tools.ietf.org/html/rfc3339), such as `2024-10-25T18:19:29.600Z`.
                    type: string
                required:
                - bucketName
                - compartmentId
                - frequencyBackupScheduled
                - isMetadataOnly
                - namespaceName
                - timeBackupScheduled
                type: object
              byolCpuCoreCountLimit:
                description: |-
                  The maximum number of CPUs allowed with a 'Bring Your Own License' (BYOL) license type.
                  Any CPU usage above this limit is considered as License Included and billed.
                type: integer
              category:
                description: |-
                  The deployment category defines the broad separation of the deployment type into three categories.
                  Currently the separation is 'DATA_REPLICATION', 'STREAM_ANALYTICS' and 'DATA_TRANSFORMS'.
                type: string
              clusterPlacementGroupId:
                description: |-
                  The OCID(/Content/General/Concepts/identifiers.htm) of the cluster placement group for the resource.
                  Only applicable for multicloud subscriptions. The cluster placement group id must be provided when a multicloud
                  subscription id is provided. Otherwise the cluster placement group must not be provided.
                type: string
              compartmentId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment being referenced.
                type: string
              cpuCoreCount:
                description: The Minimum number of OCPUs to be made available for
                  this Deployment.
                type: integer
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Tags defined for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              deploymentBackupId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the backup being referenced.
                type: string
              deploymentDiagnosticData:
                description: DeploymentDiagnosticData defines nested fields for Deployment.DeploymentDiagnosticData.
                properties:
                  bucketName:
                    description: Name of the bucket where the object is to be uploaded
                      in the object storage
                    type: string
                  diagnosticState:
                    description: The state of the deployment diagnostic collection.
                    type: string
                  namespaceName:
                    description: Name of namespace that serves as a container for
                      all of your buckets
                    type: string
                  objectName:
                    description: Name of the diagnostic collected and uploaded to
                      object storage
                    type: string
                  timeDiagnosticEnd:
                    description: The time until which the diagnostic collection should
                      collect the logs. The format is defined by RFC3339 (https://tools.ietf.org/html/rfc3339),
                      such as `2016-08-25T21:10:29.600Z`.
                    type: string
                  timeDiagnosticStart:
                    description: The time from which the diagnostic collection should
                      collect the logs. The format is defined by RFC3339 (https://tools.ietf.org/html/rfc3339),
                      such as `2016-08-25T21:10:29.600Z`.
                    type: string
                type: object
              deploymentRole:
                description: The type of the deployment role.
                type: string
              deploymentType:
                description: |-
                  The type of deployment, which can be any one of the Allowed values.
                  NOTE: Use of the value 'OGG' is maintained for backward compatibility purposes.
                      Its use is discouraged in favor of 'DATABASE_ORACLE'.
                type: string
              deploymentUrl:
                description: The URL of a resource.
                type: string
              description:
                description: Metadata about this specific object.
                type: string
              displayName:
                description: An object's Display Name.
                type: string
              environmentType:
                description: Specifies whether the deployment is used in a production
                  or development/testing environment.
                type: string
              faultDomain:
                description: The fault domain of a placement.
                type: string
              fqdn:
                description: A three-label Fully Qualified Domain Name (FQDN) for
                  a resource.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  A simple key-value pair that is applied without any predefined name, type, or scope. Exists
                  for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the deployment being referenced.
                type: string
              ingressIps:
                description: |-
                  List of ingress IP addresses from where the GoldenGate deployment connects to this connection's privateIp.
                  Customers may optionally set up ingress security rules to restrict traffic from these IP addresses.
                items:
                  description: DeploymentIngressIp defines nested fields for Deployment.IngressIp.
                  properties:
                    ingressIp:
                      description: A Private Endpoint IPv4 or IPv6 Address created
                        in the customer's subnet.
                      type: string
                  type: object
                type: array
              isAutoScalingEnabled:
                description: Indicates if auto scaling is enabled for the Deployment's
                  CPU core count.
                type: boolean
              isByolCpuCoreCountLimitEnabled:
                description: |-
                  Flag to allow to configure the 'Bring Your Own License' (BYOL) license type CPU limit.
                  If enabled, the exact number of CPUs must be provided via byolCpuCoreCountLimit.
                type: boolean
              isHealthy:
                description: True if all of the aggregate resources are working correctly.
                type: boolean
              isLatestVersion:
                description: Indicates if the resource is the the latest available
                  version.
                type: boolean
              isPublic:
                description: True if this object is publicly available.
                type: boolean
              isStorageUtilizationLimitExceeded:
                description: |-
                  Deprecated: This field is not updated and will be removed in future versions. If storage utilization exceeds the limit, the respective warning message will appear in deployment messages, which can be accessed through /messages?deploymentId=.
                  Indicator will be true if the amount of storage being utilized exceeds the allowable storage utilization limit.  Exceeding the limit may be an indication of a misconfiguration of the deployment's GoldenGate service.
                type: boolean
              licenseModel:
                description: The Oracle license model that applies to a Deployment.
                type: string
              lifecycleDetails:
                description: |-
                  Describes the object's current state in detail. For example, it can be used to provide
                  actionable information for a resource in a Failed state.
                type: string
              lifecycleState:
                description: Possible lifecycle states.
                type: string
              lifecycleSubState:
                description: Possible GGS lifecycle sub-states.
                type: string
              loadBalancerId:
                description: |-
                  The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the loadbalancer in the customer's subnet.
                  The loadbalancer of the public deployment created in the customer subnet.
                type: string
              loadBalancerSubnetId:
                description: |-
                  The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of a public subnet in the customer tenancy.
                  Can be provided only for public deployments. If provided, the loadbalancer will be created in this subnet instead of the service tenancy.
                  For backward compatibility, this is an optional property. It will become mandatory for public deployments after October 1, 2024.
                type: string
              locks:
                description: Locks associated with this resource.
                items:
                  description: DeploymentLock defines nested fields for Deployment.Lock.
                  properties:
                    message:
                      description: |-
                        A message added by the creator of the lock. This is typically used to give an
                        indication of why the resource is locked.
                      type: string
                    type:
                      description: Type of the lock.
                      type: string
                  required:
                  - type
                  type: object
                type: array
              maintenanceConfiguration:
                description: DeploymentMaintenanceConfiguration defines nested fields
                  for Deployment.MaintenanceConfiguration.
                properties:
                  bundleReleaseUpgradePeriodInDays:
                    description: |-
                      Defines auto upgrade period for bundle releases. Manually configured period cannot be longer than service defined period for bundle releases.
                      This period must be shorter or equal to major release upgrade period. Not passing this field during create will equate to using the service default.
                    type: integer
                  interimReleaseUpgradePeriodInDays:
                    description: Defines auto upgrade period for interim releases.
                      This period must be shorter or equal to bundle release upgrade
                      period.
                    type: integer
                  isInterimReleaseAutoUpgradeEnabled:
                    description: |-
                      By default auto upgrade for interim releases are not enabled. If auto-upgrade is enabled for interim release,
                      you have to specify interimReleaseUpgradePeriodInDays too.
                    type: boolean
                  majorReleaseUpgradePeriodInDays:
                    description: |-
                      Defines auto upgrade period for major releases. Manually configured period cannot be longer than service defined period for major releases.
                      Not passing this field during create will equate to using the service default.
                    type: integer
                  securityPatchUpgradePeriodInDays:
                    description: |-
                      Defines auto upgrade period for releases with security fix. Manually configured period cannot be longer than service defined period for security releases.
                      Not passing this field during create will equate to using the service default.
                    type: integer
                type: object
              maintenanceWindow:
                description: DeploymentMaintenanceWindow defines nested fields for
                  Deployment.MaintenanceWindow.
                properties:
                  day:
                    description: Days of the week.
                    type: string
                  startHour:
                    description: Start hour for maintenance period. Hour is in UTC.
                    type: integer
                required:
                - day
                - startHour
                type: object
              nextMaintenanceActionType:
                description: Type of the next maintenance.
                type: string
              nextMaintenanceDescription:
                description: Description of the next maintenance.
                type: string
              nsgIds:
                description: An array of Network Security Group OCIDs used to define
                  network access for either Deployments or Connections.
                items:
                  type: string
                type: array
              oggData:
                description: DeploymentOggData defines nested fields for Deployment.OggData.
                properties:
                  adminPassword:
                    description: |-
                      The password associated with the GoldenGate deployment console username.
                      The password must be 8 to 30 characters long and must contain at least 1 uppercase, 1 lowercase, 1 numeric,
                      and 1 special character. Special characters such as '$', '^', or '?' are not allowed.
                      This field will be deprecated and replaced by "passwordSecretId".
                    type: string
                  adminUsername:
                    description: The GoldenGate deployment console username.
                    type: string
                  certificate:
                    description: The base64 encoded content of the PEM file containing
                      the SSL certificate.
                    type: string
                  credentialStore:
                    description: The type of credential store for OGG.
                    type: string
                  deploymentName:
                    description: |-
                      The name given to the GoldenGate service deployment.
                      The name must be 1 to 32 characters long, must contain only alphanumeric characters and must start with a letter.
                    type: string
                  groupToRolesMapping:
                    description: DeploymentOggDataGroupToRolesMapping defines nested
                      fields for Deployment.OggData.GroupToRolesMapping.
                    properties:
                      administratorGroupId:
                        description: |-
                          The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the IDP group which will be mapped to goldengate role administratorGroup.
                          It grants full access to the user, including the ability to alter general, non-security related operational parameters
                          and profiles of the server.
                        type: string
                      operatorGroupId:
                        description: |-
                          The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the IDP group which will be mapped to goldengate role operatorGroup.
                          It allows users to perform only operational actions, like starting and stopping resources.
                          Operators cannot alter the operational parameters or profiles of the MA server.
                        type: string
                      securityGroupId:
                        description: |-
                          The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the IDP group which will be mapped to goldengate role securityGroup.
                          It grants administration of security related objects and invoke security related service requests. This role has full privileges.
                        type: string
                      userGroupId:
                        description: |-
                          The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the IDP group which will be mapped to goldengate role userGroup.
                          It allows information-only service requests, which do not alter or affect the operation of either the MA.
                          Examples of query and read-only information include performance metric information and resource status and monitoring information
                        type: string
                    required:
                    - securityGroupId
                    type: object
                  identityDomainId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the Identity Domain when IAM credential store is used.
                    type: string
                  key:
                    description: The base64 encoded content of the PEM file containing
                      the private key.
                    type: string
                  oggVersion:
                    description: Version of OGG
                    type: string
                  passwordSecretId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the Secret where the deployment password is stored.
                    type: string
                required:
                - deploymentName
                type: object
              placements:
                description: An array of local peers of deployment
                items:
                  description: DeploymentPlacement defines nested fields for Deployment.Placement.
                  properties:
                    availabilityDomain:
                      description: The availability domain of a placement.
                      type: string
                    faultDomain:
                      description: The fault domain of a placement.
                      type: string
                  type: object
                type: array
              privateIpAddress:
                description: |-
                  The private IP address in the customer's VCN representing the access point for the
                  associated endpoint service in the GoldenGate service VCN.
                type: string
              publicIpAddress:
                description: The public IP address representing the access point for
                  the Deployment.
                type: string
              securityAttributes:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Security attributes for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Oracle-ZPR": {"MaxEgressCount": {"value": "42", "mode": "enforce"}}}`
                type: object
              sourceDeploymentId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the deployment being referenced.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              storageUtilizationInBytes:
                description: The amount of storage being utilized (in bytes)
                format: int64
                type: integer
              subnetId:
                description: |-
                  The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the subnet of the deployment's private endpoint.
                  The subnet must be a private subnet. For backward compatibility, public subnets are allowed until May 31 2025,
                  after which the private subnet will be enforced.
                type: string
              subscriptionId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the subscription with which resource needs to be associated with.
                type: string
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  The system tags associated with this resource, if any. The system tags are set by Oracle
                  Cloud Infrastructure services. Each key is predefined and scoped to namespaces.  For more
                  information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{orcl-cloud: {free-tier-retain: true}}`
                type: object
              timeCreated:
                description: |-
                  The time the resource was created. The format is defined by
                  RFC3339 (https://tools.ietf.org/html/rfc3339), such as `2016-08-25T21:10:29.600Z`.
                type: string
              timeLastBackupScheduled:
                description: |-
                  The timestamp of last deployment backup scheduled. The format is defined by
                  RFC3339 (https://tools.ietf.org/html/rfc3339), such as `2024-10-25T18:19:29.600Z`.
                type: string
              timeNextBackupScheduled:
                description: |-
                  The timestamp of next deployment backup scheduled. The format is defined by
                  RFC3339 (https://tools.ietf.org/html/rfc3339), such as `2024-10-26T20:19:29.600Z`.
                type: string
              timeOfNextMaintenance:
                description: |-
                  The time of next maintenance schedule. The format is defined by
                  RFC3339 (https://tools.ietf.org/html/rfc3339), such as `2016-08-25T21:10:29.600Z`.
                type: string
              timeOggVersionSupportedUntil:
                description: |-
                  The time until OGG version is supported. After this date has passed OGG version will not be available anymore. The format is defined by
                  RFC3339 (https://tools.ietf.org/html/rfc3339), such as `2016-08-25T21:10:29.600Z`.
                type: string
              timeRoleChanged:
                description: |-
                  The time of the last role change. The format is defined by
                  RFC3339 (https://tools.ietf.org/html/rfc3339), such as `2016-08-25T21:10:29.600Z`.
                type: string
              timeUpdated:
                description: |-
                  The time the resource was last updated. The format is defined by
                  RFC3339 (https://tools.ietf.org/html/rfc3339), such as `2016-08-25T21:10:29.600Z`.
                type: string
              timeUpgradeRequired:
                description: |-
                  Note: Deprecated: Use timeOfNextMaintenance instead, or related upgrade records
                  to check, when deployment will be forced to upgrade to a newer version.
                  Old description:
                  The date the existing version in use will no longer be considered as usable
                  and an upgrade will be required.  This date is typically 6 months after the
                  version was released for use by GGS.  The format is defined by
                  RFC3339 (https://tools.ietf.org/html/rfc3339), such as `2016-08-25T21:10:29.600Z`.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
