---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: organizationsubscriptions.osuborganizationsubscription.oracle.com
spec:
  group: osuborganizationsubscription.oracle.com
  names:
    kind: OrganizationSubscription
    listKind: OrganizationSubscriptionList
    plural: organizationsubscriptions
    singular: organizationsubscription
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the OrganizationSubscription
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the OrganizationSubscription
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: OrganizationSubscription is the Schema for the organizationsubscriptions
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OrganizationSubscriptionSpec defines the desired state of
              OrganizationSubscription.
            type: object
          status:
            description: OrganizationSubscriptionStatus defines the observed state
              of OrganizationSubscription.
            properties:
              currency:
                description: OrganizationSubscriptionCurrency defines nested fields
                  for OrganizationSubscription.Currency.
                properties:
                  isoCode:
                    description: Currency Code
                    type: string
                  name:
                    description: Currency name
                    type: string
                  stdPrecision:
                    description: Standard Precision of the Currency
                    format: int64
                    type: integer
                type: object
              id:
                description: SPM internal Subscription ID
                type: string
              sdkStatus:
                description: |-
                  Status of the plan
                  This uses a distinct JSON name so it can coexist with the OSOK status envelope.
                type: string
              serviceName:
                description: Customer friendly service name provided by PRG
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeEnd:
                description: Represents the date when the last service of the subscription
                  ends
                type: string
              timeStart:
                description: Represents the date when the first service of the subscription
                  was activated
                type: string
              totalValue:
                description: Total aggregate TCLV of all lines for the subscription
                  including expired, active, and signed
                type: string
              type:
                description: Subscription Type i.e. IAAS,SAAS,PAAS
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
