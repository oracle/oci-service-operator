---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: orresetuipasswords.identity.oracle.com
spec:
  group: identity.oracle.com
  names:
    kind: OrResetUIPassword
    listKind: OrResetUIPasswordList
    plural: orresetuipasswords
    singular: orresetuipassword
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the OrResetUIPassword
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the OrResetUIPassword
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: OrResetUIPassword is the Schema for the orresetuipasswords API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OrResetUIPasswordSpec defines the desired state of OrResetUIPassword.
            type: object
          status:
            description: OrResetUIPasswordStatus defines the observed state of OrResetUIPassword.
            properties:
              inactiveStatus:
                description: The detailed status of INACTIVE lifecycleState.
                format: int64
                type: integer
              lifecycleState:
                description: |-
                  The password's current state. After creating a password, make sure its `lifecycleState` changes from
                  CREATING to ACTIVE before using it.
                type: string
              password:
                description: The user's password for the Console.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  Date and time the password was created, in the format defined by RFC3339.
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              userId:
                description: The OCID of the user.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
