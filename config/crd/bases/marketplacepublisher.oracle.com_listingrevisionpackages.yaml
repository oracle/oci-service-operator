---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: listingrevisionpackages.marketplacepublisher.oracle.com
spec:
  group: marketplacepublisher.oracle.com
  names:
    kind: ListingRevisionPackage
    listKind: ListingRevisionPackageList
    plural: listingrevisionpackages
    singular: listingrevisionpackage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the ListingRevisionPackage
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ListingRevisionPackage
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ListingRevisionPackage is the Schema for the listingrevisionpackages
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ListingRevisionPackageSpec defines the desired state of ListingRevisionPackage.
            properties:
              areSecurityUpgradesProvided:
                description: Identifies whether security upgrades will be provided
                  for this package.
                type: boolean
              artifactId:
                description: The unique identifier for the artifact.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              description:
                description: Description for this package.
                type: string
              displayName:
                description: The name for the listing revision package.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              isDefault:
                description: Identifies that this will be default package for the
                  listing revision.
                type: boolean
              listingRevisionId:
                description: The OCID for the listing revision in Marketplace Publisher.
                type: string
              packageVersion:
                description: The version for the package.
                type: string
              termId:
                description: The unique identifier for the term.
                type: string
            required:
            - areSecurityUpgradesProvided
            - artifactId
            - listingRevisionId
            - packageVersion
            - termId
            type: object
          status:
            description: ListingRevisionPackageStatus defines the observed state of
              ListingRevisionPackage.
            properties:
              areSecurityUpgradesProvided:
                description: Identifies whether security upgrades will be provided
                  for this package.
                type: boolean
              artifactId:
                description: The unique identifier for the artifact.
                type: string
              compartmentId:
                description: The unique identifier for the compartment.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              description:
                description: The description of this package.
                type: string
              displayName:
                description: The name of the listing revision package.
                type: string
              extendedMetadata:
                additionalProperties:
                  type: string
                description: |-
                  Additional metadata key/value pairs for the listing revision package summary.
                  For example:
                  `{"partnerListingRevisionPackageStatus": "Published","parentListingRevisionPackageId": "1" }`
                type: object
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: The OCID for the listing revision package in Marketplace
                  Publisher.
                type: string
              isDefault:
                description: Identifies that this will be default package for the
                  listing revision.
                type: boolean
              jsonData:
                type: string
              lifecycleState:
                description: The current state for the listing revision package.
                type: string
              listingRevisionId:
                description: The unique identifier for the listing revision.
                type: string
              machineImageDetails:
                description: ListingRevisionPackageMachineImageDetails defines nested
                  fields for ListingRevisionPackage.MachineImageDetails.
                properties:
                  computeAppCatalogId:
                    description: Replicated Images App Catalog Listing OCID.
                    type: string
                  computeImageId:
                    description: Replicated Image OCID.
                    type: string
                  computePackageVersion:
                    description: Replicated Images Package version.
                    type: string
                type: object
              packageType:
                type: string
              packageVersion:
                description: The version for the package.
                type: string
              sdkStatus:
                description: |-
                  The current status for the listing revision package.
                  This uses a distinct JSON name so it can coexist with the OSOK status envelope.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              termId:
                description: The unique identifier for the term.
                type: string
              timeCreated:
                description: |-
                  The date and time this listing revision package was created, expressed in RFC 3339 (https://tools.ietf.org/html/rfc3339)
                  timestamp format.
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              timeUpdated:
                description: |-
                  The date and time this listing revision package was updated, expressed in RFC 3339 (https://tools.ietf.org/html/rfc3339)
                  timestamp format.
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
