---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: knowledgebases.generativeaiagent.oracle.com
spec:
  group: generativeaiagent.oracle.com
  names:
    kind: KnowledgeBase
    listKind: KnowledgeBaseList
    plural: knowledgebases
    singular: knowledgebase
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the KnowledgeBase
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the KnowledgeBase
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: KnowledgeBase is the Schema for the knowledgebases API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KnowledgeBaseSpec defines the desired state of KnowledgeBase.
            properties:
              compartmentId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment to create the knowledge base in.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              description:
                description: A user-friendly description of the knowledge base.
                type: string
              displayName:
                description: A user-friendly name. Does not have to be unique, and
                  it's changeable. Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              indexConfig:
                description: KnowledgeBaseIndexConfig defines nested fields for KnowledgeBase.IndexConfig.
                properties:
                  clusterId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the OpenSearch Cluster.
                    type: string
                  databaseConnection:
                    description: KnowledgeBaseIndexConfigDatabaseConnection defines
                      nested fields for KnowledgeBase.IndexConfig.DatabaseConnection.
                    properties:
                      connectionId:
                        description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                          of the Database Tools Connection.
                        type: string
                      connectionType:
                        type: string
                      jsonData:
                        type: string
                    type: object
                  databaseFunctions:
                    description: Array of Database functions to be used.
                    items:
                      description: KnowledgeBaseIndexConfigDatabaseFunction defines
                        nested fields for KnowledgeBase.IndexConfig.DatabaseFunction.
                      properties:
                        name:
                          description: The name of the Database function.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  indexConfigType:
                    type: string
                  indexes:
                    description: Index configuration for open search.
                    items:
                      description: KnowledgeBaseIndexConfigIndex defines nested fields
                        for KnowledgeBase.IndexConfig.Index.
                      properties:
                        name:
                          description: The index name in opensearch.
                          type: string
                        schema:
                          description: KnowledgeBaseIndexConfigIndexSchema defines
                            nested fields for KnowledgeBase.IndexConfig.Index.Schema.
                          properties:
                            bodyKey:
                              description: Body key name.
                              type: string
                            embeddingBodyKey:
                              description: Field within customer managed OCI OpenSearch
                                document containing the vector embedding for queries.
                              type: string
                            titleKey:
                              description: Title key that stores the Title of a document,
                                if available.
                              type: string
                            urlKey:
                              description: URL key that stores the URL of a document,
                                if available.
                              type: string
                          required:
                          - bodyKey
                          type: object
                      required:
                      - name
                      - schema
                      type: object
                    type: array
                  jsonData:
                    type: string
                  secretDetail:
                    description: KnowledgeBaseIndexConfigSecretDetail defines nested
                      fields for KnowledgeBase.IndexConfig.SecretDetail.
                    properties:
                      clientId:
                        description: The IDCS Connect clientId.
                        type: string
                      idcsUrl:
                        description: The URL represent authentication url of the IDCS.
                        type: string
                      jsonData:
                        type: string
                      scopeUrl:
                        description: Fully qualified scope url
                        type: string
                      type:
                        type: string
                      vaultSecretId:
                        description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                          of the secret for client secret.
                        type: string
                    type: object
                  shouldEnableHybridSearch:
                    description: Whether to enable Hybrid search in service managed
                      OpenSearch.
                    type: boolean
                type: object
            required:
            - compartmentId
            - indexConfig
            type: object
          status:
            description: KnowledgeBaseStatus defines the observed state of KnowledgeBase.
            properties:
              compartmentId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              description:
                description: A description of the knowledge base.
                type: string
              displayName:
                description: A user-friendly name. Does not have to be unique, and
                  it's changeable.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the knowledge base.
                type: string
              indexConfig:
                description: KnowledgeBaseIndexConfig defines nested fields for KnowledgeBase.IndexConfig.
                properties:
                  clusterId:
                    description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                      of the OpenSearch Cluster.
                    type: string
                  databaseConnection:
                    description: KnowledgeBaseIndexConfigDatabaseConnection defines
                      nested fields for KnowledgeBase.IndexConfig.DatabaseConnection.
                    properties:
                      connectionId:
                        description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                          of the Database Tools Connection.
                        type: string
                      connectionType:
                        type: string
                      jsonData:
                        type: string
                    type: object
                  databaseFunctions:
                    description: Array of Database functions to be used.
                    items:
                      description: KnowledgeBaseIndexConfigDatabaseFunction defines
                        nested fields for KnowledgeBase.IndexConfig.DatabaseFunction.
                      properties:
                        name:
                          description: The name of the Database function.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  indexConfigType:
                    type: string
                  indexes:
                    description: Index configuration for open search.
                    items:
                      description: KnowledgeBaseIndexConfigIndex defines nested fields
                        for KnowledgeBase.IndexConfig.Index.
                      properties:
                        name:
                          description: The index name in opensearch.
                          type: string
                        schema:
                          description: KnowledgeBaseIndexConfigIndexSchema defines
                            nested fields for KnowledgeBase.IndexConfig.Index.Schema.
                          properties:
                            bodyKey:
                              description: Body key name.
                              type: string
                            embeddingBodyKey:
                              description: Field within customer managed OCI OpenSearch
                                document containing the vector embedding for queries.
                              type: string
                            titleKey:
                              description: Title key that stores the Title of a document,
                                if available.
                              type: string
                            urlKey:
                              description: URL key that stores the URL of a document,
                                if available.
                              type: string
                          required:
                          - bodyKey
                          type: object
                      required:
                      - name
                      - schema
                      type: object
                    type: array
                  jsonData:
                    type: string
                  secretDetail:
                    description: KnowledgeBaseIndexConfigSecretDetail defines nested
                      fields for KnowledgeBase.IndexConfig.SecretDetail.
                    properties:
                      clientId:
                        description: The IDCS Connect clientId.
                        type: string
                      idcsUrl:
                        description: The URL represent authentication url of the IDCS.
                        type: string
                      jsonData:
                        type: string
                      scopeUrl:
                        description: Fully qualified scope url
                        type: string
                      type:
                        type: string
                      vaultSecretId:
                        description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                          of the secret for client secret.
                        type: string
                    type: object
                  shouldEnableHybridSearch:
                    description: Whether to enable Hybrid search in service managed
                      OpenSearch.
                    type: boolean
                type: object
              knowledgeBaseStatistics:
                description: KnowledgeBaseStatistics defines nested fields for KnowledgeBase.KnowledgeBaseStatistics.
                properties:
                  sizeInBytes:
                    description: Knowledge Base size in bytes.
                    format: int64
                    type: integer
                  totalIngestedFiles:
                    description: Total number of ingested files in Knowledge Base.
                    format: int64
                    type: integer
                type: object
              lifecycleDetails:
                description: |-
                  A message that describes the current state of the knowledge base in more detail. For example,
                  can be used to provide actionable information for a resource in the Failed state.
                type: string
              lifecycleState:
                description: The current state of the knowledge base.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: |-
                  The date and time the knowledge base was created, in the format defined by RFC 3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              timeUpdated:
                description: |-
                  The date and time the knowledge base was updated, in the format defined by RFC 3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
