---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: gdppipelines.gdp.oracle.com
spec:
  group: gdp.oracle.com
  names:
    kind: GdpPipeline
    listKind: GdpPipelineList
    plural: gdppipelines
    singular: gdppipeline
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the GdpPipeline
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the GdpPipeline
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: GdpPipeline is the Schema for the gdppipelines API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GdpPipelineSpec defines the desired state of GdpPipeline.
            properties:
              approvalKeyVaultId:
                description: The KMS vault OCID for the key to be used for file transfer
                  approvals.
                type: string
              authorizationDetails:
                description: Authorization information about the pipeline being configured.
                type: string
              bucketDetails:
                description: Information about the bucket in the pipeline being configured.
                items:
                  description: GdpPipelineBucketDetail defines nested fields for GdpPipeline.BucketDetail.
                  properties:
                    bucketType:
                      description: Type of bucket. SENDER pipelines can be SOURCE,
                        TRANSFER, REJECT, or FAILED. RECEIVER pipelines have a DESTINATION
                        bucket.
                      type: string
                    id:
                      description: OCID of the bucket.
                      type: string
                    name:
                      description: Name of the bucket.
                      type: string
                    namespace:
                      description: Namespace of the bucket.
                      type: string
                  required:
                  - bucketType
                  - id
                  - name
                  - namespace
                  type: object
                type: array
              compartmentId:
                description: The OCID of the compartment.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              description:
                description: Short field input by customer for a description of the
                  data pipeline use-case.
                type: string
              displayName:
                description: Pipeline short name.
                type: string
              fileTypes:
                description: List of file types allowed to be transferred in the pipeline
                  according to the authorization details (e.g. .pdf, .xml, .doc).
                items:
                  type: string
                type: array
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              isApprovalNeeded:
                description: Determines whether file transfers need to go through
                  an approval workflow.
                type: boolean
              isChunkingEnabled:
                description: Determines whether file must be chunked during the transfer.
                  This is only a property of SENDER pipelines.
                type: boolean
              isFileOverrideInDestinationEnabled:
                description: Enable file override feature in destination bucket
                type: boolean
              isScanningEnabled:
                description: Determines whether GDP Scanning should be enabled for
                  the pipeline.
                type: boolean
              peeringRegion:
                description: Public region name where the peered pipeline exists.
                type: string
              pipelineType:
                description: Type of pipeline. Can be SENDER or RECEIVER.
                type: string
              serviceLogGroupId:
                description: the OCID of the service log group.
                type: string
            required:
            - bucketDetails
            - compartmentId
            - displayName
            - peeringRegion
            - pipelineType
            type: object
          status:
            description: GdpPipelineStatus defines the observed state of GdpPipeline.
            properties:
              approvalKeyVaultId:
                description: The KMS vault OCID for the key to be used for file transfer
                  approvals.
                type: string
              authorizationDetails:
                description: Authorization information about the pipeline being configured.
                type: string
              bucketDetails:
                description: Information about the bucket in the pipeline being configured.
                items:
                  description: GdpPipelineBucketDetail defines nested fields for GdpPipeline.BucketDetail.
                  properties:
                    bucketType:
                      description: Type of bucket. SENDER pipelines can be SOURCE,
                        TRANSFER, REJECT, or FAILED. RECEIVER pipelines have a DESTINATION
                        bucket.
                      type: string
                    id:
                      description: OCID of the bucket.
                      type: string
                    name:
                      description: Name of the bucket.
                      type: string
                    namespace:
                      description: Namespace of the bucket.
                      type: string
                  required:
                  - bucketType
                  - id
                  - name
                  - namespace
                  type: object
                type: array
              compartmentId:
                description: The OCID of the compartment.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              description:
                description: Short field input by customer for a description of the
                  data pipeline use-case.
                type: string
              displayName:
                description: Pipeline short name.
                type: string
              fileTypes:
                description: List of file types allowed to be transferred in the pipeline
                  according to the authorization details (e.g. .pdf, .xml, .doc).
                items:
                  type: string
                type: array
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: The OCID of the pipeline.
                type: string
              isApprovalNeeded:
                description: Determines whether file transfers need to go through
                  an approval workflow.
                type: boolean
              isChunkingEnabled:
                description: Determines whether file must be chunked during the transfer.
                  This is only a property of SENDER pipelines.
                type: boolean
              isFileOverrideInDestinationEnabled:
                description: Enable file override feature in destination bucket
                type: boolean
              isScanningEnabled:
                description: Determines whether GDP Scanning should be enabled for
                  the pipeline.
                type: boolean
              lifecycleDetails:
                description: Additional details about the current state of the pipeline.
                type: string
              lifecycleState:
                description: The current state of the pipeline.
                type: string
              peeredGdpPipelineId:
                description: OCID of the peered pipeline.
                type: string
              peeringRegion:
                description: Public region name where the peered pipeline exists.
                type: string
              pipelineType:
                description: Type of pipeline. Can be SENDER or RECEIVER.
                type: string
              serviceLogGroupId:
                description: the OCID of the service log group.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: The time the the pipeline was created. An RFC3339 formatted
                  datetime string.
                type: string
              timeUpdated:
                description: The time the pipeline was updated. An RFC3339 formatted
                  datetime string.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
