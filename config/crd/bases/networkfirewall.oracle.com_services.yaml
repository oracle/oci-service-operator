---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: services.networkfirewall.oracle.com
spec:
  group: networkfirewall.oracle.com
  names:
    kind: Service
    listKind: ServiceList
    plural: services
    singular: service
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      priority: 1
      type: string
    - description: status of the Service
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Service
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Service is the Schema for the services API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceSpec defines the desired state of Service.
            properties:
              description:
                description: The description of the service. This field can be used
                  to add additional info.
                type: string
              jsonData:
                type: string
              name:
                description: Name of the service
                type: string
              portRanges:
                description: List of port-ranges to be used.
                items:
                  description: ServicePortRange defines nested fields for Service.PortRange.
                  properties:
                    maximumPort:
                      description: The maximum port in the range (inclusive), which
                        may be absent for a single-port range.
                      type: integer
                    minimumPort:
                      description: The minimum port in the range (inclusive), or the
                        sole port of a single-port range.
                      type: integer
                  required:
                  - minimumPort
                  type: object
                type: array
              type:
                type: string
            required:
            - name
            type: object
          status:
            description: ServiceStatus defines the observed state of Service.
            properties:
              description:
                description: The description of the service. This field can be used
                  to add additional info.
                type: string
              jsonData:
                type: string
              name:
                description: Name of the service.
                type: string
              parentResourceId:
                description: OCID of the Network Firewall Policy this service belongs
                  to.
                type: string
              portRanges:
                description: List of port-ranges used.
                items:
                  description: ServicePortRange defines nested fields for Service.PortRange.
                  properties:
                    maximumPort:
                      description: The maximum port in the range (inclusive), which
                        may be absent for a single-port range.
                      type: integer
                    minimumPort:
                      description: The minimum port in the range (inclusive), or the
                        sole port of a single-port range.
                      type: integer
                  required:
                  - minimumPort
                  type: object
                type: array
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              type:
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
