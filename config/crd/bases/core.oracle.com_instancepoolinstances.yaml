---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: instancepoolinstances.core.oracle.com
spec:
  group: core.oracle.com
  names:
    kind: InstancePoolInstance
    listKind: InstancePoolInstanceList
    plural: instancepoolinstances
    singular: instancepoolinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the InstancePoolInstance
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the InstancePoolInstance
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: InstancePoolInstance is the Schema for the instancepoolinstances
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InstancePoolInstanceSpec defines the desired state of InstancePoolInstance.
            type: object
          status:
            description: InstancePoolInstanceStatus defines the observed state of
              InstancePoolInstance.
            properties:
              availabilityDomain:
                description: The availability domain the instance is running in.
                type: string
              compartmentId:
                description: |-
                  The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the compartment that contains the
                  instance.
                type: string
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              faultDomain:
                description: The fault domain the instance is running in.
                type: string
              id:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the instance.
                type: string
              instanceConfigurationId:
                description: |-
                  The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the instance configuration
                  used to create the instance.
                type: string
              instancePoolId:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the instance pool.
                type: string
              lifecycleState:
                description: The attachment state of the instance in relation to the
                  instance pool.
                type: string
              loadBalancerBackends:
                description: The load balancer backends that are configured for the
                  instance.
                items:
                  description: InstancePoolInstanceLoadBalancerBackend defines nested
                    fields for InstancePoolInstance.LoadBalancerBackend.
                  properties:
                    backendHealthStatus:
                      description: The health of the backend as observed by the load
                        balancer.
                      type: string
                    backendName:
                      description: The name of the backend in the backend set.
                      type: string
                    backendSetName:
                      description: The name of the backend set on the load balancer.
                      type: string
                    loadBalancerId:
                      description: The OCID of the load balancer attached to the instance
                        pool.
                      type: string
                  type: object
                type: array
              region:
                description: The region that contains the availability domain the
                  instance is running in.
                type: string
              shape:
                description: |-
                  The shape of the instance. The shape determines the number of CPUs, amount of memory,
                  and other resources allocated to the instance.
                type: string
              state:
                description: The lifecycle state of the instance. Refer to `lifecycleState`
                  in the Instance resource.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time the instance pool instance was created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
