---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: customtables.usageapi.oracle.com
spec:
  group: usageapi.oracle.com
  names:
    kind: CustomTable
    listKind: CustomTableList
    plural: customtables
    singular: customtable
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the CustomTable
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the CustomTable
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: CustomTable is the Schema for the customtables API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CustomTableSpec defines the desired state of CustomTable.
            properties:
              compartmentId:
                description: The compartment OCID.
                type: string
              savedCustomTable:
                description: CustomTableSavedCustomTable defines nested fields for
                  CustomTable.SavedCustomTable.
                properties:
                  columnGroupBy:
                    description: |-
                      The column groupBy key list.
                      For example:
                        `["tagNamespace", "tagKey", "tagValue", "service", "skuName", "skuPartNumber", "unit",
                          "compartmentName", "compartmentPath", "compartmentId", "platform", "region", "logicalAd",
                          "resourceId", "tenantId", "tenantName"]`
                    items:
                      type: string
                    type: array
                  compartmentDepth:
                    description: The compartment depth level.
                    type: number
                  displayName:
                    description: The name of the custom table.
                    type: string
                  groupByTag:
                    description: |-
                      GroupBy a specific tagKey. Provide the tagNamespace and tagKey in the tag object. Only one tag in the list is supported.
                      For example:
                        `[{"namespace":"oracle", "key":"createdBy"]`
                    items:
                      description: CustomTableSavedCustomTableGroupByTag defines nested
                        fields for CustomTable.SavedCustomTable.GroupByTag.
                      properties:
                        key:
                          description: The tag key.
                          type: string
                        namespace:
                          description: The tag namespace.
                          type: string
                        value:
                          description: The tag value.
                          type: string
                      type: object
                    type: array
                  rowGroupBy:
                    description: |-
                      The row groupBy key list.
                      For example:
                        `["tagNamespace", "tagKey", "tagValue", "service", "skuName", "skuPartNumber", "unit",
                          "compartmentName", "compartmentPath", "compartmentId", "platform", "region", "logicalAd",
                          "resourceId", "tenantId", "tenantName"]`
                    items:
                      type: string
                    type: array
                  version:
                    description: The version of the custom table.
                    type: number
                required:
                - displayName
                type: object
              savedReportId:
                description: The associated saved report OCID.
                type: string
            required:
            - compartmentId
            - savedCustomTable
            - savedReportId
            type: object
          status:
            description: CustomTableStatus defines the observed state of CustomTable.
            properties:
              compartmentId:
                description: The custom table compartment OCID.
                type: string
              id:
                description: The custom table OCID.
                type: string
              savedCustomTable:
                description: CustomTableSavedCustomTable defines nested fields for
                  CustomTable.SavedCustomTable.
                properties:
                  columnGroupBy:
                    description: |-
                      The column groupBy key list.
                      For example:
                        `["tagNamespace", "tagKey", "tagValue", "service", "skuName", "skuPartNumber", "unit",
                          "compartmentName", "compartmentPath", "compartmentId", "platform", "region", "logicalAd",
                          "resourceId", "tenantId", "tenantName"]`
                    items:
                      type: string
                    type: array
                  compartmentDepth:
                    description: The compartment depth level.
                    type: number
                  displayName:
                    description: The name of the custom table.
                    type: string
                  groupByTag:
                    description: |-
                      GroupBy a specific tagKey. Provide the tagNamespace and tagKey in the tag object. Only one tag in the list is supported.
                      For example:
                        `[{"namespace":"oracle", "key":"createdBy"]`
                    items:
                      description: CustomTableSavedCustomTableGroupByTag defines nested
                        fields for CustomTable.SavedCustomTable.GroupByTag.
                      properties:
                        key:
                          description: The tag key.
                          type: string
                        namespace:
                          description: The tag namespace.
                          type: string
                        value:
                          description: The tag value.
                          type: string
                      type: object
                    type: array
                  rowGroupBy:
                    description: |-
                      The row groupBy key list.
                      For example:
                        `["tagNamespace", "tagKey", "tagValue", "service", "skuName", "skuPartNumber", "unit",
                          "compartmentName", "compartmentPath", "compartmentId", "platform", "region", "logicalAd",
                          "resourceId", "tenantId", "tenantName"]`
                    items:
                      type: string
                    type: array
                  version:
                    description: The version of the custom table.
                    type: number
                required:
                - displayName
                type: object
              savedReportId:
                description: The custom table associated saved report OCID.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
