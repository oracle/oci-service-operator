---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: jobdefinitions.datacatalog.oracle.com
spec:
  group: datacatalog.oracle.com
  names:
    kind: JobDefinition
    listKind: JobDefinitionList
    plural: jobdefinitions
    singular: jobdefinition
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the JobDefinition
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the JobDefinition
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: JobDefinition is the Schema for the jobdefinitions API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: JobDefinitionSpec defines the desired state of JobDefinition.
            properties:
              connectionKey:
                description: The key of the connection resource to be used for the
                  job.
                type: string
              dataAssetKey:
                description: The key of the data asset for which the job is defined.
                type: string
              description:
                description: Detailed description of the job definition.
                type: string
              displayName:
                description: |-
                  A user-friendly display name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              glossaryKey:
                description: Unique key of the glossary to which this job applies.
                type: string
              isIncremental:
                description: Specifies if the job definition is incremental or full.
                type: boolean
              isSampleDataExtracted:
                description: Specify if sample data to be extracted as part of this
                  harvest.
                type: boolean
              jobType:
                description: Type of the job definition.
                type: string
              properties:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  A map of maps that contains the properties which are specific to the job type. Each job type
                  definition may define it's set of required and optional properties. The map keys are category names and the
                  values are maps of property name to property value. Every property is contained inside of a category. Most
                  job definitions have required properties within the "default" category.
                  Example: `{"properties": { "default": { "host": "host1", "port": "1521", "database": "orcl"}}}`
                type: object
              sampleDataSizeInMBs:
                description: Specify the sample data size in MB, specified as number
                  of rows, for this metadata harvest.
                type: integer
            required:
            - displayName
            - jobType
            type: object
          status:
            description: JobDefinitionStatus defines the observed state of JobDefinition.
            properties:
              catalogId:
                description: The data catalog's OCID.
                type: string
              connectionKey:
                description: |-
                  The key of the default connection resource to be used for harvest, sampling, profiling jobs.
                  This may be overridden in each job instance.
                type: string
              createdById:
                description: OCID of the user who created this job definition.
                type: string
              dataAssetKey:
                description: The key of the data asset for which the job is defined.
                type: string
              description:
                description: Detailed description of the job definition.
                type: string
              displayName:
                description: |-
                  A user-friendly display name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              glossaryKey:
                description: Unique key of the glossary to which this job applies.
                type: string
              internalVersion:
                description: Version of the job definition object. Used internally
                  but can be visible to users.
                type: string
              isIncremental:
                description: Specifies if the job definition is incremental or full.
                type: boolean
              isSampleDataExtracted:
                description: Specify if sample data to be extracted as part of this
                  harvest.
                type: boolean
              jobExecutionState:
                description: Status of the latest job execution, such as running,
                  paused, or completed.
                type: string
              jobType:
                description: Type of the job definition.
                type: string
              key:
                description: Unique key of the job definition resource that is immutable.
                type: string
              lifecycleState:
                description: Lifecycle state of the job definition.
                type: string
              properties:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  A map of maps that contains the properties which are specific to the job type. Each job type
                  definition may define it's set of required and optional properties. The map keys are category names and the
                  values are maps of property name to property value. Every property is contained inside of a category. Most
                  job definitions have required properties within the "default" category.
                  Example: `{"properties": { "default": { "host": "host1", "port": "1521", "database": "orcl"}}}`
                type: object
              sampleDataSizeInMBs:
                description: Specify the sample data size in MB, specified as number
                  of rows, for this metadata harvest.
                type: integer
              scheduleType:
                description: Type of job schedule for the latest job executed.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time the job definition was created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2019-03-25T21:10:29.600Z`
                type: string
              timeLatestExecutionEnded:
                description: |-
                  Time that the latest job execution ended or null if it hasn't yet completed.
                  An RFC3339 (https://tools.ietf.org/html/rfc3339) formatted datetime string.
                type: string
              timeLatestExecutionStarted:
                description: Time that the latest job execution started. An RFC3339
                  (https://tools.ietf.org/html/rfc3339) formatted datetime string.
                type: string
              timeUpdated:
                description: The last time that any change was made to the data asset.
                  An RFC3339 (https://tools.ietf.org/html/rfc3339) formatted datetime
                  string.
                type: string
              updatedById:
                description: OCID of the user who updated this job definition.
                type: string
              uri:
                description: URI to the job definition instance in the API.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
