---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: wlsdomains.wlms.oracle.com
spec:
  group: wlms.oracle.com
  names:
    kind: WlsDomain
    listKind: WlsDomainList
    plural: wlsdomains
    singular: wlsdomain
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the WlsDomain
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the WlsDomain
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: WlsDomain is the Schema for the wlsdomains API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WlsDomainSpec defines the desired state of WlsDomain.
            properties:
              compartmentId:
                description: |-
                  The compartment containing the existing WebLogic domain when binding by list lookup.
                  Provide this together with `displayName` when `id` is not set.
                type: string
              configuration:
                additionalProperties:
                  description: JSONValue preserves arbitrary JSON values inside generated
                    CRD fields.
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: |-
                  Desired in-place WLMS configuration for an existing bound WebLogic domain.
                  Use the WLMS update field names such as `isPatchEnabled`, `isRollbackOnFailure`, `serversShutdownTimeout`, and the `*ScriptPath` keys.
                  Omit this field to leave the current OCI configuration unchanged.
                type: object
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  The display name of the existing WebLogic domain when binding by list lookup.
                  Provide this together with `compartmentId` when `id` is not set.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The OCI identifier of the existing WebLogic domain to
                  bind directly.
                type: string
              middlewareType:
                description: |-
                  Optional exact-match filter used to narrow list-based binding to an existing domain.
                  Supported values are `WLS` and `FMW`.
                type: string
              weblogicVersion:
                description: |-
                  Optional exact-match filter used to narrow list-based binding to an existing domain.
                  Supported values include `v12.2.1.4`, `v14.1.1.0`, and `v14.1.2.0`.
                type: string
            type: object
          status:
            description: WlsDomainStatus defines the observed state of WlsDomain.
            properties:
              compartmentId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment.
                type: string
              configuration:
                description: WlsDomainConfiguration defines nested fields for WlsDomain.Configuration.
                properties:
                  adminServerControlMode:
                    description: Whether to manage the admin server using Node Manager
                      or scripts.
                    type: string
                  adminServerStartScriptPath:
                    description: Path to admin server start script.
                    type: string
                  adminServerStopScriptPath:
                    description: Path to admin server stop script.
                    type: string
                  isPatchEnabled:
                    description: Whether or not the WebLogic domain is enabled for
                      patching.
                    type: boolean
                  isRollbackOnFailure:
                    description: Whether or not to rollback on failure during patching
                      of WebLogic domain.
                    type: boolean
                  managedServerControlMode:
                    description: Whether to manage the managed server using Node Manager
                      or scripts.
                    type: string
                  managedServerStartScriptPath:
                    description: Path to managed server start script.
                    type: string
                  managedServerStopScriptPath:
                    description: Path to managed server stop script.
                    type: string
                  serversShutdownTimeout:
                    description: Servers shutdown timeout in seconds. If set to 0
                      seconds, it means there is no timeout.
                    type: integer
                type: object
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: A user-friendly name that does not have to be unique
                  and is changeable.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the WebLogic domain.
                type: string
              isAcceptedTermsAndConditions:
                description: Whether or not the terms of use agreement has been accepted
                  for the WebLogic domain.
                type: boolean
              lifecycleDetails:
                description: |-
                  A message that describes the current state of the WebLogic domain in more detail. For example,
                  it can be used to provide actionable information for a resource in the Failed state.
                type: string
              lifecycleState:
                description: The current state of the WebLogic service domain.
                type: string
              middlewareType:
                description: The middleware type on the administration server of the
                  WebLogic domain.
                type: string
              patchReadinessStatus:
                description: The patch readiness status of the WebLogic domain.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: |-
                  The date and time the WebLogic domain was created (in RFC 3339 (https://tools.ietf.org/rfc/rfc3339) format).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              timeUpdated:
                description: |-
                  The date and time the WebLogic domain was updated (in RFC 3339 (https://tools.ietf.org/rfc/rfc3339) format).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              weblogicVersion:
                description: The version of the WebLogic domain.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
