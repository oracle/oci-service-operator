---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: fsuactions.fleetsoftwareupdate.oracle.com
spec:
  group: fleetsoftwareupdate.oracle.com
  names:
    kind: FsuAction
    listKind: FsuActionList
    plural: fsuactions
    singular: fsuaction
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the FsuAction
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the FsuAction
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: FsuAction is the Schema for the fsuactions API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FsuActionSpec defines the desired state of FsuAction.
            properties:
              compartmentId:
                description: Compartment Identifier.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              details:
                description: FsuActionDetails defines nested fields for FsuAction.Details.
                properties:
                  jsonData:
                    type: string
                  strategy:
                    type: string
                  targets:
                    description: OCIDs of targets to rollback.
                    items:
                      type: string
                    type: array
                type: object
              displayName:
                description: Exadata Fleet Update Action display name.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              fsuCycleId:
                description: OCID identifier for the Exadata Fleet Update Cycle the
                  Action will be part of.
                type: string
              jsonData:
                type: string
              scheduleDetails:
                description: FsuActionScheduleDetails defines nested fields for FsuAction.ScheduleDetails.
                properties:
                  jsonData:
                    type: string
                  timeToStart:
                    description: |-
                      The date and time the Exadata Fleet Update Action is expected to start.
                      RFC 3339 (https://tools.ietf.org/rfc/rfc3339), section 14.29.
                    type: string
                  type:
                    type: string
                type: object
              type:
                type: string
            required:
            - compartmentId
            type: object
          status:
            description: FsuActionStatus defines the observed state of FsuAction.
            properties:
              compartmentId:
                description: Compartment Identifier.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              details:
                description: FsuActionDetails defines nested fields for FsuAction.Details.
                properties:
                  jsonData:
                    type: string
                  strategy:
                    type: string
                  targets:
                    description: OCIDs of targets to rollback.
                    items:
                      type: string
                    type: array
                type: object
              displayName:
                description: Exadata Fleet Update Action display name.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              fsuCycleId:
                description: OCID identifier for the Exadata Fleet Update Cycle the
                  Action will be part of.
                type: string
              id:
                description: OCID identifier for the Exadata Fleet Update Action.
                type: string
              jsonData:
                type: string
              lifecycleDetails:
                description: |-
                  A message describing the current state in more detail.
                  For example, can be used to provide actionable information for a resource in Failed state.
                type: string
              lifecycleState:
                description: The current state of the Exadata Fleet Update Action.
                type: string
              progress:
                description: FsuActionProgress defines nested fields for FsuAction.Progress.
                properties:
                  completedTargets:
                    description: Number of targets with completed jobs.
                    type: integer
                  failedTargets:
                    description: Number of targets with failed jobs.
                    type: integer
                  inProgressTargets:
                    description: Number of targets with jobs in progress.
                    type: integer
                  totalTargets:
                    description: Total number of targets impacted by Exadata Fleet
                      Update Action.
                    type: integer
                  waitingTargets:
                    description: Number of targets with jobs waiting for batch to
                      execute or for user to resume.
                    type: integer
                type: object
              relatedFsuActionId:
                description: OCID identifier for the Exadata Fleet Update Action.
                type: string
              scheduleDetails:
                description: FsuActionScheduleDetails defines nested fields for FsuAction.ScheduleDetails.
                properties:
                  jsonData:
                    type: string
                  timeToStart:
                    description: |-
                      The date and time the Exadata Fleet Update Action is expected to start.
                      RFC 3339 (https://tools.ietf.org/rfc/rfc3339), section 14.29.
                    type: string
                  type:
                    type: string
                type: object
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Usage of system tag keys. These predefined keys are scoped to namespaces.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: |-
                  The date and time the Action was created, as described in
                  RFC 3339 (https://tools.ietf.org/rfc/rfc3339), section 14.29.
                type: string
              timeFinished:
                description: |-
                  The date and time the Action was finished,
                  as described in RFC 3339 (https://tools.ietf.org/rfc/rfc3339).
                type: string
              timeStarted:
                description: |-
                  The date and time the Action was started,
                  as described in RFC 3339 (https://tools.ietf.org/rfc/rfc3339),
                  section 14.29.
                type: string
              timeUpdated:
                description: |-
                  The date and time the Action was last updated, as described in
                  RFC 3339 (https://tools.ietf.org/rfc/rfc3339), section 14.29.
                type: string
              type:
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
