---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: folders.datacatalog.oracle.com
spec:
  group: datacatalog.oracle.com
  names:
    kind: Folder
    listKind: FolderList
    plural: folders
    singular: folder
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the Folder
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Folder
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Folder is the Schema for the folders API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FolderSpec defines the desired state of Folder.
            properties:
              businessName:
                description: Optional user friendly business name of the folder. If
                  set, this supplements the harvested display name of the object.
                type: string
              customPropertyMembers:
                description: The list of customized properties along with the values
                  for this object
                items:
                  description: FolderCustomPropertyMember defines nested fields for
                    Folder.CustomPropertyMember.
                  properties:
                    displayName:
                      description: Name of the custom property
                      type: string
                    key:
                      description: Unique Identifier of the attribute which is ID
                      type: string
                    namespaceName:
                      description: Namespace name of the custom property
                      type: string
                    value:
                      description: The custom property value
                      type: string
                  type: object
                type: array
              description:
                description: Detailed description of a folder.
                type: string
              displayName:
                description: |-
                  A user-friendly display name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              harvestStatus:
                description: Folder harvesting status.
                type: string
              lastJobKey:
                description: The job key of the harvest process that updated the folder
                  definition from the source system.
                type: string
              parentFolderKey:
                description: The key of the containing folder or null if there isn't
                  a parent folder.
                type: string
              properties:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  A map of maps that contains the properties which are specific to the folder type. Each folder type
                  definition defines it's set of required and optional properties. The map keys are category names and the
                  values are maps of property name to property value. Every property is contained inside of a category. Most
                  folders have required properties within the "default" category. To determine the set of optional and
                  required properties for a folder type, a query can be done on '/types?type=folder' that returns a
                  collection of all folder types. The appropriate folder type, which includes definitions of all of
                  it's properties, can be identified from this collection.
                  Example: `{"properties": { "default": { "key1": "value1"}}}`
                type: object
              timeExternal:
                description: Last modified timestamp of this object in the external
                  system.
                type: string
              typeKey:
                description: Type key of the object. Type keys can be found via the
                  '/types' endpoint.
                type: string
            required:
            - displayName
            - timeExternal
            type: object
          status:
            description: FolderStatus defines the observed state of Folder.
            properties:
              businessName:
                description: Optional user friendly business name of the folder. If
                  set, this supplements the harvested display name of the object.
                type: string
              createdById:
                description: OCID of the user who created the folder.
                type: string
              customPropertyMembers:
                description: The list of customized properties along with the values
                  for this object
                items:
                  description: FolderCustomPropertyMember defines nested fields for
                    Folder.CustomPropertyMember.
                  properties:
                    displayName:
                      description: Name of the custom property
                      type: string
                    key:
                      description: Unique Identifier of the attribute which is ID
                      type: string
                    namespaceName:
                      description: Namespace name of the custom property
                      type: string
                    value:
                      description: The custom property value
                      type: string
                  type: object
                type: array
              dataAssetKey:
                description: The key of the associated data asset.
                type: string
              description:
                description: Detailed description of a folder.
                type: string
              displayName:
                description: |-
                  A user-friendly display name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              externalKey:
                description: Unique external key of this object in the source system.
                type: string
              harvestStatus:
                description: Status of the object as updated by the harvest process.
                type: string
              key:
                description: Unique folder key that is immutable.
                type: string
              lastJobKey:
                description: The key of the last harvest process to update the metadata
                  of this object.
                type: string
              lifecycleDetails:
                description: |-
                  A message describing the current state in more detail. An object not in ACTIVE state may have functional limitations,
                  see service documentation for details.
                type: string
              lifecycleState:
                description: The current state of the folder.
                type: string
              objectRelationships:
                description: List of objects and their relationships to this folder.
                items:
                  description: FolderObjectRelationship defines nested fields for
                    Folder.ObjectRelationship.
                  properties:
                    key:
                      description: Unique id of the object.
                      type: string
                    name:
                      description: Name of the object.
                      type: string
                    parentKey:
                      description: Key of the parent object for the resource.
                      type: string
                    parentPath:
                      description: Full path of the parent object.
                      type: string
                    path:
                      description: Full path of the object.
                      type: string
                    relationshipType:
                      description: Type of relationship with the referencing object.
                      type: string
                    timeCreated:
                      description: |-
                        The date and time the relationship was created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                        Example: `2019-03-25T21:10:29.600Z`
                      type: string
                    timeUpdated:
                      description: The last time a change was made to this reference.
                        An RFC3339 (https://tools.ietf.org/html/rfc3339) formatted
                        datetime string.
                      type: string
                    typeKey:
                      description: Type key of the object. Type keys can be found
                        via the '/types' endpoint.
                      type: string
                    typeName:
                      description: Type name of the object. Type names can be found
                        via the '/types' endpoint.
                      type: string
                  type: object
                type: array
              objectStorageUrl:
                description: URL of the folder in the object store.
                type: string
              parentFolderKey:
                description: The unique key of the containing folder or null if there
                  is no parent folder.
                type: string
              path:
                description: Full path of the folder.
                type: string
              properties:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  A map of maps that contains the properties which are specific to the folder type. Each folder type
                  definition defines it's set of required and optional properties. The map keys are category names and the
                  values are maps of property name to property value. Every property is contained inside of a category. Most
                  folders have required properties within the "default" category.
                  Example: `{"properties": { "default": { "key1": "value1"}}}`
                type: object
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time the folder was created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2019-03-25T21:10:29.600Z`
                type: string
              timeExternal:
                description: Last modified timestamp of this object in the external
                  system.
                type: string
              timeHarvested:
                description: The date and time the folder was harvested, in the format
                  defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                type: string
              timeUpdated:
                description: The last time that any change was made to the folder.
                  An RFC3339 (https://tools.ietf.org/html/rfc3339) formatted datetime
                  string.
                type: string
              typeKey:
                description: The type of folder object. Type keys can be found via
                  the '/types' endpoint.
                type: string
              updatedById:
                description: OCID of the user who modified the folder.
                type: string
              uri:
                description: URI to the folder instance in the API.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
