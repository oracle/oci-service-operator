---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  name: tables.nosql.oracle.com
spec:
  group: nosql.oracle.com
  names:
    kind: Table
    listKind: TableList
    plural: tables
    singular: table
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      priority: 1
      type: string
    - description: status of the Table
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Table
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Table is the Schema for the tables API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TableSpec defines the desired state of Table.
            properties:
              compartmentId:
                description: Compartment Identifier.
                type: string
              ddlStatement:
                description: Complete CREATE TABLE DDL statement.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and
                  scoped to a namespace.  Example: `{"foo-namespace":
                  {"bar-key": "value"}}`
                type: object
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined
                  name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              isAutoReclaimable:
                description: True if table can be reclaimed after an idle period.
                type: boolean
              name:
                description: Table name.
                type: string
              tableLimits:
                description: TableLimits defines nested fields for Table.TableLimits.
                properties:
                  capacityMode:
                    description: |-
                      The capacity mode of the table.  If capacityMode = ON_DEMAND,
                      maxReadUnits and maxWriteUnits are not used, and both will have
                      the value of zero.
                    type: string
                  maxReadUnits:
                    description: Maximum sustained read throughput limit for the table.
                    type: integer
                  maxStorageInGBs:
                    description: Maximum size of storage used by the table.
                    type: integer
                  maxWriteUnits:
                    description: Maximum sustained write throughput limit for the
                      table.
                    type: integer
                required:
                - maxReadUnits
                - maxStorageInGBs
                - maxWriteUnits
                type: object
            required:
            - compartmentId
            - ddlStatement
            - name
            type: object
          status:
            description: TableStatus defines the observed state of Table.
            properties:
              compartmentId:
                description: Compartment Identifier.
                type: string
              ddlStatement:
                description: A DDL statement representing the schema.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and
                  scoped to a namespace.  Example: `{"foo-namespace":
                  {"bar-key": "value"}}`
                type: object
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined
                  name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: Unique identifier that is immutable.
                type: string
              isAutoReclaimable:
                description: True if this table can be reclaimed after an idle period.
                type: boolean
              isMultiRegion:
                description: True if this table is currently a member of a replication
                  set.
                type: boolean
              lifecycleDetails:
                description: A message describing the current state in more detail.
                type: string
              lifecycleState:
                description: The state of a table.
                type: string
              localReplicaInitializationInPercent:
                description: |-
                  If this table is in a replication set, this value represents
                  the progress of the initialization of the replica's data.  A
                  value of 100 indicates that initialization has completed.
                type: integer
              name:
                description: Human-friendly table name, immutable.
                type: string
              replicas:
                description: An array of Replica listing this table's replicas, if
                  any
                items:
                  description: TableReplica defines nested fields for Table.Replica.
                  properties:
                    capacityMode:
                      description: The capacity mode of the replica.
                      type: string
                    lifecycleDetails:
                      description: A message describing the current state in more
                        detail.
                      type: string
                    lifecycleState:
                      description: The state of the replica.
                      type: string
                    maxWriteUnits:
                      description: Maximum sustained write throughput limit of the
                        replica table.
                      type: integer
                    region:
                      description: A customer-facing region identifier
                      type: string
                    tableId:
                      description: The OCID of the replica table
                      type: string
                  type: object
                type: array
              schema:
                description: TableSchema defines nested fields for Table.Schema.
                properties:
                  columns:
                    description: The columns of a table.
                    items:
                      description: TableSchemaColumn defines nested fields for Table.Schema.Column.
                      properties:
                        defaultValue:
                          description: The column default value.
                          type: string
                        isAsUuid:
                          description: True if the STRING column was declared AS UUID.
                          type: boolean
                        isGenerated:
                          description: True if the STRING AS UUID column is also GENERATED
                            BY DEFAULT.
                          type: boolean
                        isNullable:
                          description: The column nullable flag.
                          type: boolean
                        name:
                          description: The column name.
                          type: string
                        type:
                          description: The column type.
                          type: string
                      type: object
                    type: array
                  identity:
                    description: TableSchemaIdentity defines nested fields for Table.Schema.Identity.
                    properties:
                      columnName:
                        description: The name of the identity column.
                        type: string
                      isAlways:
                        description: True if the identity value is GENERATED ALWAYS.
                        type: boolean
                      isNull:
                        description: True if the identity value is GENERATED BY DEFAULT
                          ON NULL.
                        type: boolean
                    type: object
                  primaryKey:
                    description: A list of column names that make up a key.
                    items:
                      type: string
                    type: array
                  shardKey:
                    description: A list of column names that make up a key.
                    items:
                      type: string
                    type: array
                  ttl:
                    description: The default Time-to-Live for the table, in days.
                    type: integer
                type: object
              schemaState:
                description: |-
                  The current state of this table's schema. Available states are
                  MUTABLE - The schema can be changed. The table is not eligible for replication.
                  FROZEN - The schema is immutable. The table is eligible for replication.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Read-only system tag. These predefined keys are scoped to
                  namespaces.  At present the only supported namespace is
                  `"orcl-cloud"`; and the only key in that namespace is
                  `"free-tier-retained"`.
                  Example: `{"orcl-cloud"": {"free-tier-retained": "true"}}`
                type: object
              tableLimits:
                description: TableLimits defines nested fields for Table.TableLimits.
                properties:
                  capacityMode:
                    description: |-
                      The capacity mode of the table.  If capacityMode = ON_DEMAND,
                      maxReadUnits and maxWriteUnits are not used, and both will have
                      the value of zero.
                    type: string
                  maxReadUnits:
                    description: Maximum sustained read throughput limit for the table.
                    type: integer
                  maxStorageInGBs:
                    description: Maximum size of storage used by the table.
                    type: integer
                  maxWriteUnits:
                    description: Maximum sustained write throughput limit for the
                      table.
                    type: integer
                required:
                - maxReadUnits
                - maxStorageInGBs
                - maxWriteUnits
                type: object
              timeCreated:
                description: |-
                  The time the the table was created. An RFC3339 formatted
                  datetime string.
                type: string
              timeOfExpiration:
                description: |-
                  If lifecycleState is INACTIVE, indicates when
                  this table will be automatically removed.
                  An RFC3339 formatted datetime string.
                type: string
              timeUpdated:
                description: |-
                  The time the the table's metadata was last updated. An
                  RFC3339 formatted datetime string.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
