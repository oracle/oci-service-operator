---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: healthcheckers.networkloadbalancer.oracle.com
spec:
  group: networkloadbalancer.oracle.com
  names:
    kind: HealthChecker
    listKind: HealthCheckerList
    plural: healthcheckers
    singular: healthchecker
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the HealthChecker
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the HealthChecker
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: HealthChecker is the Schema for the healthcheckers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HealthCheckerSpec defines the desired state of HealthChecker.
            properties:
              intervalInMillis:
                description: |-
                  The interval between health checks, in milliseconds.
                  Example: `10000`
                type: integer
              port:
                description: |-
                  The backend server port against which to run the health check.
                  Example: `8080`
                type: integer
              protocol:
                description: |-
                  The protocol that the health check must use; either HTTP, UDP, or TCP.
                  Example: `HTTP`
                type: string
              requestData:
                description: Base64 encoded pattern to be sent as UDP or TCP health
                  check probe.
                type: string
              responseBodyRegex:
                description: |-
                  A regular expression for parsing the response body from the backend server.
                  Example: `^((?!false).|\s)*$`
                type: string
              responseData:
                description: Base64 encoded pattern to be validated as UDP or TCP
                  health check probe response.
                type: string
              retries:
                description: |-
                  The number of retries to attempt before a backend server is considered "unhealthy". This number also applies
                  when recovering a server to the "healthy" state.
                  Example: `3`
                type: integer
              returnCode:
                description: |-
                  The status code a healthy backend server should return. If you configure the health check policy to use the HTTP protocol,
                  then you can use common HTTP status codes such as "200".
                  Example: `200`
                type: integer
              timeoutInMillis:
                description: |-
                  The maximum time, in milliseconds, to wait for a reply to a health check. A health check is successful only if a reply
                  returns within this timeout period.
                  Example: `3000`
                type: integer
              urlPath:
                description: |-
                  The path against which to run the health check.
                  Example: `/healthcheck`
                type: string
            type: object
          status:
            description: HealthCheckerStatus defines the observed state of HealthChecker.
            properties:
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
