---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: pbflistingversions.functions.oracle.com
spec:
  group: functions.oracle.com
  names:
    kind: PbfListingVersion
    listKind: PbfListingVersionList
    plural: pbflistingversions
    singular: pbflistingversion
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the PbfListingVersion
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the PbfListingVersion
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: PbfListingVersion is the Schema for the pbflistingversions API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PbfListingVersionSpec defines the desired state of PbfListingVersion.
            type: object
          status:
            description: PbfListingVersionStatus defines the observed state of PbfListingVersion.
            properties:
              changeSummary:
                description: Details changes are included in this version.
                type: string
              config:
                description: Details about the required and optional Function configurations
                  needed for proper performance of the PBF.
                items:
                  description: PbfListingVersionConfig defines nested fields for PbfListingVersion.Config.
                  properties:
                    description:
                      description: Details about why this config is required and what
                        it will be used for.
                      type: string
                    isOptional:
                      description: Is this a required config or an optional one. Requests
                        with required config params missing will be rejected.
                      type: boolean
                    key:
                      description: The key name of the config param.
                      type: string
                  type: object
                type: array
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: Unique identifier that is immutable on creation
                type: string
              lifecycleState:
                description: The current state of the PBF resource.
                type: string
              name:
                description: Semantic version
                type: string
              pbfListingId:
                description: The OCID of the PbfListing this resource version belongs
                  to.
                type: string
              requirements:
                description: PbfListingVersionRequirements defines nested fields for
                  PbfListingVersion.Requirements.
                properties:
                  minMemoryRequiredInMBs:
                    description: |-
                      Minimum memory required by this PBF. The user should use memory greater than or equal to
                      this value while configuring the Function.
                    format: int64
                    type: integer
                  policies:
                    description: List of policies required for this PBF execution.
                    items:
                      description: PbfListingVersionRequirementsPolicy defines nested
                        fields for PbfListingVersion.Requirements.Policy.
                      properties:
                        description:
                          description: Details about why this policy is required and
                            what it will be used for.
                          type: string
                        policy:
                          description: Policy required for PBF execution
                          type: string
                      type: object
                    type: array
                type: object
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: The time the PbfListingVersion was created. An RFC3339
                  formatted datetime string.
                type: string
              timeUpdated:
                description: The last time the PbfListingVersion was updated. An RFC3339
                  formatted datetime string.
                type: string
              triggers:
                description: An array of Trigger. A list of triggers that may activate
                  the PBF.
                items:
                  description: PbfListingVersionTrigger defines nested fields for
                    PbfListingVersion.Trigger.
                  properties:
                    name:
                      description: A brief descriptive name for the PBF trigger.
                      type: string
                  type: object
                type: array
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
