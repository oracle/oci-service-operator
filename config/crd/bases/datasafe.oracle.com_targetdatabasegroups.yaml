---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: targetdatabasegroups.datasafe.oracle.com
spec:
  group: datasafe.oracle.com
  names:
    kind: TargetDatabaseGroup
    listKind: TargetDatabaseGroupList
    plural: targetdatabasegroups
    singular: targetdatabasegroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the TargetDatabaseGroup
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the TargetDatabaseGroup
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: TargetDatabaseGroup is the Schema for the targetdatabasegroups
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TargetDatabaseGroupSpec defines the desired state of TargetDatabaseGroup.
            properties:
              compartmentId:
                description: The OCID of the compartment to create the target database
                  group.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm)
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              description:
                description: Description of the target database group (optional).
                type: string
              displayName:
                description: The name of the target database group.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm)
                  Example: `{"Department": "Finance"}`
                type: object
              matchingCriteria:
                description: TargetDatabaseGroupMatchingCriteria defines nested fields
                  for TargetDatabaseGroup.MatchingCriteria.
                properties:
                  exclude:
                    description: TargetDatabaseGroupMatchingCriteriaExclude defines
                      nested fields for TargetDatabaseGroup.MatchingCriteria.Exclude.
                    properties:
                      targetDatabaseIds:
                        description: The list of target database OCIDS, that should
                          be excluded from the target database group (even if they
                          match some of the other criteria).
                        items:
                          type: string
                        type: array
                    required:
                    - targetDatabaseIds
                    type: object
                  include:
                    description: TargetDatabaseGroupMatchingCriteriaInclude defines
                      nested fields for TargetDatabaseGroup.MatchingCriteria.Include.
                    properties:
                      compartments:
                        description: List of compartment objects, each containing
                          the OCID of the compartment and a boolean value that indicates
                          whether the target databases in the compartments and sub-compartments
                          should also be included in the target database group.
                        items:
                          description: TargetDatabaseGroupMatchingCriteriaIncludeCompartment
                            defines nested fields for TargetDatabaseGroup.MatchingCriteria.Include.Compartment.
                          properties:
                            id:
                              description: The OCID of the compartment for including
                                target databases to the target database group. All
                                target databases in the compartment will be members
                                of the target database group.
                              type: string
                            isIncludeSubtree:
                              description: This indicates whether the target databases
                                of sub-compartments should also be included in the
                                target database group. By default, this parameter
                                is set to false.
                              type: boolean
                          required:
                          - id
                          type: object
                        type: array
                      definedTags:
                        additionalProperties:
                          additionalProperties:
                            type: string
                          type: object
                        description: |-
                          Defined tags for this resource. Each key is predefined and scoped to a namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm)
                          Example: `{"Operations": {"CostCenter": "42"}}`
                        type: object
                      freeformTags:
                        additionalProperties:
                          type: string
                        description: |-
                          Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm)
                          Example: `{"Department": "Finance"}`
                        type: object
                      targetDatabaseIds:
                        description: The list of target database OCIDs to be included
                          in the target database group.
                        items:
                          type: string
                        type: array
                    type: object
                required:
                - include
                type: object
            required:
            - compartmentId
            - displayName
            - matchingCriteria
            type: object
          status:
            description: TargetDatabaseGroupStatus defines the observed state of TargetDatabaseGroup.
            properties:
              compartmentId:
                description: The OCID for the compartment containing the target database
                  group.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm)
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              description:
                description: Description of the target database group.
                type: string
              displayName:
                description: The name of the target database group.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm)
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The OCID of the specified target database group.
                type: string
              lifecycleDetails:
                description: Details for the lifecycle status of the target database
                  group.
                type: string
              lifecycleState:
                description: The lifecycle status of the target database group.
                type: string
              matchingCriteria:
                description: TargetDatabaseGroupMatchingCriteria defines nested fields
                  for TargetDatabaseGroup.MatchingCriteria.
                properties:
                  exclude:
                    description: TargetDatabaseGroupMatchingCriteriaExclude defines
                      nested fields for TargetDatabaseGroup.MatchingCriteria.Exclude.
                    properties:
                      targetDatabaseIds:
                        description: The list of target database OCIDS, that should
                          be excluded from the target database group (even if they
                          match some of the other criteria).
                        items:
                          type: string
                        type: array
                    required:
                    - targetDatabaseIds
                    type: object
                  include:
                    description: TargetDatabaseGroupMatchingCriteriaInclude defines
                      nested fields for TargetDatabaseGroup.MatchingCriteria.Include.
                    properties:
                      compartments:
                        description: List of compartment objects, each containing
                          the OCID of the compartment and a boolean value that indicates
                          whether the target databases in the compartments and sub-compartments
                          should also be included in the target database group.
                        items:
                          description: TargetDatabaseGroupMatchingCriteriaIncludeCompartment
                            defines nested fields for TargetDatabaseGroup.MatchingCriteria.Include.Compartment.
                          properties:
                            id:
                              description: The OCID of the compartment for including
                                target databases to the target database group. All
                                target databases in the compartment will be members
                                of the target database group.
                              type: string
                            isIncludeSubtree:
                              description: This indicates whether the target databases
                                of sub-compartments should also be included in the
                                target database group. By default, this parameter
                                is set to false.
                              type: boolean
                          required:
                          - id
                          type: object
                        type: array
                      definedTags:
                        additionalProperties:
                          additionalProperties:
                            type: string
                          type: object
                        description: |-
                          Defined tags for this resource. Each key is predefined and scoped to a namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm)
                          Example: `{"Operations": {"CostCenter": "42"}}`
                        type: object
                      freeformTags:
                        additionalProperties:
                          type: string
                        description: |-
                          Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace. For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm)
                          Example: `{"Department": "Finance"}`
                        type: object
                      targetDatabaseIds:
                        description: The list of target database OCIDs to be included
                          in the target database group.
                        items:
                          type: string
                        type: array
                    type: object
                required:
                - include
                type: object
              membershipCount:
                description: The number of target databases in the specified target
                  database group.
                type: integer
              membershipUpdateTime:
                description: Time when the members of the target database group were
                  last changed, i.e. the list was refreshed, a target database was
                  added or removed.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace. For more information, see Resource Tags.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: Time when the target database group was created.
                type: string
              timeUpdated:
                description: Time when the target database group was last updated.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
