---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: instanceagentplugins.computeinstanceagent.oracle.com
spec:
  group: computeinstanceagent.oracle.com
  names:
    kind: InstanceAgentPlugin
    listKind: InstanceAgentPluginList
    plural: instanceagentplugins
    singular: instanceagentplugin
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the InstanceAgentPlugin
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the InstanceAgentPlugin
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: InstanceAgentPlugin is the Schema for the instanceagentplugins
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InstanceAgentPluginSpec defines the desired state of InstanceAgentPlugin.
            properties:
              compartmentId:
                description: |-
                  The OCID of the compartment that contains the compute instance.
                  Required for direct bind and reread operations.
                type: string
              instanceagentId:
                description: |-
                  The OCID of the compute instance whose Oracle Cloud Agent plugin should be observed.
                  Required for GetInstanceAgentPlugin and ListInstanceAgentPlugins requests.
                type: string
              pluginName:
                description: |-
                  The Oracle Cloud Agent plugin name to bind and observe on the target compute instance.
                  Required for direct get and exact list confirmation reads.
                type: string
            required:
            - compartmentId
            - instanceagentId
            - pluginName
            type: object
          status:
            description: InstanceAgentPluginStatus defines the observed state of InstanceAgentPlugin.
            properties:
              compartmentId:
                description: The compartment OCID recorded for exact rereads and replacement-only
                  drift checks.
                type: string
              instanceagentId:
                description: The compute instance OCID recorded for exact rereads
                  and replacement-only drift checks.
                type: string
              message:
                description: An optional message from the plugin.
                type: string
              name:
                description: The plugin name.
                type: string
              sdkStatus:
                description: The raw plugin status returned by Compute Instance Agent.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeLastUpdatedUtc:
                description: The last updated time of the plugin, in UTC.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
