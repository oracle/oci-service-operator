---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: instancepools.core.oracle.com
spec:
  group: core.oracle.com
  names:
    kind: InstancePool
    listKind: InstancePoolList
    plural: instancepools
    singular: instancepool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the InstancePool
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the InstancePool
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: InstancePool is the Schema for the instancepools API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InstancePoolSpec defines the desired state of InstancePool.
            properties:
              compartmentId:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment containing the instance pool.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              instanceConfigurationId:
                description: |-
                  The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the instance configuration associated
                  with the instance pool.
                type: string
              instanceDisplayNameFormatter:
                description: |-
                  A user-friendly formatter for the instance pool's instances. Instance displaynames follow the format.
                  The formatter does not retroactively change instance's displaynames, only instance displaynames in the future follow the format
                type: string
              instanceHostnameFormatter:
                description: |-
                  A user-friendly formatter for the instance pool's instances. Instance hostnames follow the format.
                  The formatter does not retroactively change instance's hostnames, only instance hostnames in the future follow the format
                type: string
              loadBalancers:
                description: The load balancers to attach to the instance pool.
                items:
                  description: InstancePoolLoadBalancer defines nested fields for
                    InstancePool.LoadBalancer.
                  properties:
                    backendSetName:
                      description: The name of the backend set on the load balancer
                        to add instances to.
                      type: string
                    loadBalancerId:
                      description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                        of the load balancer to attach to the instance pool.
                      type: string
                    port:
                      description: The port value to use when creating the backend
                        set.
                      type: integer
                    vnicSelection:
                      description: |-
                        Indicates which VNIC on each instance in the pool should be used to associate with the load balancer.
                        Possible values are "PrimaryVnic" or the displayName of one of the secondary VNICs on the instance configuration
                        that is associated with the instance pool.
                      type: string
                  required:
                  - backendSetName
                  - loadBalancerId
                  - port
                  - vnicSelection
                  type: object
                type: array
              placementConfigurations:
                description: |-
                  The placement configurations for the instance pool. Provide one placement configuration for
                  each availability domain.
                  To use the instance pool with a regional subnet, provide a placement configuration for
                  each availability domain, and include the regional subnet in each placement
                  configuration.
                items:
                  description: InstancePoolPlacementConfiguration defines nested fields
                    for InstancePool.PlacementConfiguration.
                  properties:
                    availabilityDomain:
                      description: |-
                        The availability domain to place instances.
                        Example: `Uocm:PHX-AD-1`
                      type: string
                    faultDomains:
                      description: |-
                        The fault domains to place instances.
                        If you don't provide any values, the system makes a best effort to distribute
                        instances across all fault domains based on capacity.
                        To distribute the instances evenly across selected fault domains, provide a
                        set of fault domains. For example, you might want instances to be evenly
                        distributed if your applications require high availability.
                        To get a list of fault domains, use the
                        ListFaultDomains operation
                        in the Identity and Access Management Service API.
                        Example: `[FAULT-DOMAIN-1, FAULT-DOMAIN-2, FAULT-DOMAIN-3]`
                      items:
                        type: string
                      type: array
                    primarySubnetId:
                      description: |-
                        The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the primary subnet in which to place instances. This field is deprecated.
                        Use `primaryVnicSubnets` instead to set VNIC data for instances in the pool.
                      type: string
                    primaryVnicSubnets:
                      description: InstancePoolPlacementConfigurationPrimaryVnicSubnets
                        defines nested fields for InstancePool.PlacementConfiguration.PrimaryVnicSubnets.
                      properties:
                        ipv6AddressIpv6SubnetCidrPairDetails:
                          description: |-
                            A list of IPv6 prefix ranges from which the VNIC should be assigned an IPv6 address.
                            You can provide only the prefix ranges and OCI will select an available
                            address from the range. You can optionally choose to leave the prefix range empty
                            and instead provide the specific IPv6 address that should be used from within that range.
                          items:
                            description: InstancePoolPlacementConfigurationPrimaryVnicSubnetsIpv6AddressIpv6SubnetCidrPairDetail
                              defines nested fields for InstancePool.PlacementConfiguration.PrimaryVnicSubnets.Ipv6AddressIpv6SubnetCidrPairDetail.
                            properties:
                              ipv6SubnetCidr:
                                description: Optional. Used to disambiguate which
                                  subnet prefix should be used to create an IPv6 allocation.
                                type: string
                            type: object
                          type: array
                        isAssignIpv6Ip:
                          description: |-
                            Whether to allocate an IPv6 address at instance and VNIC creation from an IPv6 enabled
                            subnet. Default: False. When provided you may optionally provide an IPv6 prefix
                            (`ipv6SubnetCidr`) of your choice to assign the IPv6 address from. If `ipv6SubnetCidr`
                            is not provided then an IPv6 prefix is chosen
                            for you.
                          type: boolean
                        subnetId:
                          description: The subnet OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                            for the secondary VNIC.
                          type: string
                      required:
                      - subnetId
                      type: object
                    secondaryVnicSubnets:
                      description: The set of secondary VNIC data for instances in
                        the pool.
                      items:
                        description: InstancePoolPlacementConfigurationSecondaryVnicSubnet
                          defines nested fields for InstancePool.PlacementConfiguration.SecondaryVnicSubnet.
                        properties:
                          displayName:
                            description: |-
                              The display name of the VNIC. This is also used to match against the instance configuration defined
                              secondary VNIC.
                            type: string
                          ipv6AddressIpv6SubnetCidrPairDetails:
                            description: |-
                              A list of IPv6 prefix ranges from which the VNIC should be assigned an IPv6 address.
                              You can provide only the prefix ranges and OCI will select an available
                              address from the range. You can optionally choose to leave the prefix range empty
                              and instead provide the specific IPv6 address that should be used from within that range.
                            items:
                              description: InstancePoolPlacementConfigurationSecondaryVnicSubnetIpv6AddressIpv6SubnetCidrPairDetail
                                defines nested fields for InstancePool.PlacementConfiguration.SecondaryVnicSubnet.Ipv6AddressIpv6SubnetCidrPairDetail.
                              properties:
                                ipv6SubnetCidr:
                                  description: Optional. Used to disambiguate which
                                    subnet prefix should be used to create an IPv6
                                    allocation.
                                  type: string
                              type: object
                            type: array
                          isAssignIpv6Ip:
                            description: |-
                              Whether to allocate an IPv6 address at instance and VNIC creation from an IPv6 enabled
                              subnet. Default: False. When provided you may optionally provide an IPv6 prefix
                              (`ipv6SubnetCidr`) of your choice to assign the IPv6 address from. If `ipv6SubnetCidr`
                              is not provided then an IPv6 prefix is chosen
                              for you.
                            type: boolean
                          subnetId:
                            description: The subnet OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                              for the secondary VNIC.
                            type: string
                        required:
                        - subnetId
                        type: object
                      type: array
                  required:
                  - availabilityDomain
                  type: object
                type: array
              size:
                description: The number of instances that should be in the instance
                  pool.
                type: integer
            required:
            - compartmentId
            - instanceConfigurationId
            - placementConfigurations
            - size
            type: object
          status:
            description: InstancePoolStatus defines the observed state of InstancePool.
            properties:
              availabilityDomains:
                description: The availability domains for the instance pool.
                items:
                  type: string
                type: array
              compartmentId:
                description: |-
                  The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the compartment containing the instance
                  pool.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the instance pool.
                type: string
              instanceConfigurationId:
                description: |-
                  The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the instance configuration associated
                  with the instance pool.
                type: string
              instanceDisplayNameFormatter:
                description: |-
                  A user-friendly formatter for the instance pool's instances. Instance displaynames follow the format.
                  The formatter does not retroactively change instance's displaynames, only instance displaynames in the future follow the format
                type: string
              instanceHostnameFormatter:
                description: |-
                  A user-friendly formatter for the instance pool's instances. Instance hostnames follow the format.
                  The formatter does not retroactively change instance's hostnames, only instance hostnames in the future follow the format
                type: string
              lifecycleState:
                description: The current state of the instance pool.
                type: string
              loadBalancers:
                description: The load balancers attached to the instance pool.
                items:
                  description: InstancePoolLoadBalancer defines nested fields for
                    InstancePool.LoadBalancer.
                  properties:
                    backendSetName:
                      description: The name of the backend set on the load balancer
                        to add instances to.
                      type: string
                    loadBalancerId:
                      description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                        of the load balancer to attach to the instance pool.
                      type: string
                    port:
                      description: The port value to use when creating the backend
                        set.
                      type: integer
                    vnicSelection:
                      description: |-
                        Indicates which VNIC on each instance in the pool should be used to associate with the load balancer.
                        Possible values are "PrimaryVnic" or the displayName of one of the secondary VNICs on the instance configuration
                        that is associated with the instance pool.
                      type: string
                  required:
                  - backendSetName
                  - loadBalancerId
                  - port
                  - vnicSelection
                  type: object
                type: array
              placementConfigurations:
                description: The placement configurations for the instance pool.
                items:
                  description: InstancePoolPlacementConfigurationObservedState defines
                    nested fields for InstancePool.PlacementConfiguration.
                  properties:
                    availabilityDomain:
                      description: |-
                        The availability domain to place instances.
                        Example: `Uocm:PHX-AD-1`
                      type: string
                    faultDomains:
                      description: |-
                        The fault domains to place instances.
                        If you don't provide any values, the system makes a best effort to distribute
                        instances across all fault domains based on capacity.
                        To distribute the instances evenly across selected fault domains, provide a
                        set of fault domains. For example, you might want instances to be evenly
                        distributed if your applications require high availability.
                        To get a list of fault domains, use the
                        ListFaultDomains operation
                        in the Identity and Access Management Service API.
                        Example: `[FAULT-DOMAIN-1, FAULT-DOMAIN-2, FAULT-DOMAIN-3]`
                      items:
                        type: string
                      type: array
                    primarySubnetId:
                      description: |-
                        The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of the primary subnet in which to place instances. This field is deprecated.
                        Use `primaryVnicSubnets` instead to set VNIC data for instances in the pool.
                      type: string
                    primaryVnicSubnets:
                      description: InstancePoolPlacementConfigurationPrimaryVnicSubnets
                        defines nested fields for InstancePool.PlacementConfiguration.PrimaryVnicSubnets.
                      properties:
                        ipv6AddressIpv6SubnetCidrPairDetails:
                          description: |-
                            A list of IPv6 prefix ranges from which the VNIC should be assigned an IPv6 address.
                            You can provide only the prefix ranges and OCI will select an available
                            address from the range. You can optionally choose to leave the prefix range empty
                            and instead provide the specific IPv6 address that should be used from within that range.
                          items:
                            description: InstancePoolPlacementConfigurationPrimaryVnicSubnetsIpv6AddressIpv6SubnetCidrPairDetail
                              defines nested fields for InstancePool.PlacementConfiguration.PrimaryVnicSubnets.Ipv6AddressIpv6SubnetCidrPairDetail.
                            properties:
                              ipv6SubnetCidr:
                                description: Optional. Used to disambiguate which
                                  subnet prefix should be used to create an IPv6 allocation.
                                type: string
                            type: object
                          type: array
                        isAssignIpv6Ip:
                          description: |-
                            Whether to allocate an IPv6 address at instance and VNIC creation from an IPv6 enabled
                            subnet. Default: False. When provided you may optionally provide an IPv6 prefix
                            (`ipv6SubnetCidr`) of your choice to assign the IPv6 address from. If `ipv6SubnetCidr`
                            is not provided then an IPv6 prefix is chosen
                            for you.
                          type: boolean
                        subnetId:
                          description: The subnet OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                            for the secondary VNIC.
                          type: string
                      required:
                      - subnetId
                      type: object
                    secondaryVnicSubnets:
                      description: The set of secondary VNIC data for instances in
                        the pool.
                      items:
                        description: InstancePoolPlacementConfigurationSecondaryVnicSubnet
                          defines nested fields for InstancePool.PlacementConfiguration.SecondaryVnicSubnet.
                        properties:
                          displayName:
                            description: |-
                              The display name of the VNIC. This is also used to match against the instance configuration defined
                              secondary VNIC.
                            type: string
                          ipv6AddressIpv6SubnetCidrPairDetails:
                            description: |-
                              A list of IPv6 prefix ranges from which the VNIC should be assigned an IPv6 address.
                              You can provide only the prefix ranges and OCI will select an available
                              address from the range. You can optionally choose to leave the prefix range empty
                              and instead provide the specific IPv6 address that should be used from within that range.
                            items:
                              description: InstancePoolPlacementConfigurationSecondaryVnicSubnetIpv6AddressIpv6SubnetCidrPairDetail
                                defines nested fields for InstancePool.PlacementConfiguration.SecondaryVnicSubnet.Ipv6AddressIpv6SubnetCidrPairDetail.
                              properties:
                                ipv6SubnetCidr:
                                  description: Optional. Used to disambiguate which
                                    subnet prefix should be used to create an IPv6
                                    allocation.
                                  type: string
                              type: object
                            type: array
                          isAssignIpv6Ip:
                            description: |-
                              Whether to allocate an IPv6 address at instance and VNIC creation from an IPv6 enabled
                              subnet. Default: False. When provided you may optionally provide an IPv6 prefix
                              (`ipv6SubnetCidr`) of your choice to assign the IPv6 address from. If `ipv6SubnetCidr`
                              is not provided then an IPv6 prefix is chosen
                              for you.
                            type: boolean
                          subnetId:
                            description: The subnet OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                              for the secondary VNIC.
                            type: string
                        required:
                        - subnetId
                        type: object
                      type: array
                  required:
                  - availabilityDomain
                  type: object
                type: array
              size:
                description: The number of instances that should be in the instance
                  pool.
                type: integer
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time the instance pool was created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
