#
# Replace the admin password before running e2e if you do not want to use the
# checked-in sample value.
# This Secret must stay in the same namespace as the DbSystem custom resource.
#

apiVersion: v1
kind: Secret
metadata:
  name: admin-secret
type: Opaque
stringData:
  username: adminuser
  password: <replace-with-strong-password>
