---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: mfatotpdevices.identity.oracle.com
spec:
  group: identity.oracle.com
  names:
    kind: MfaTotpDevice
    listKind: MfaTotpDeviceList
    plural: mfatotpdevices
    singular: mfatotpdevice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the MfaTotpDevice
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the MfaTotpDevice
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: MfaTotpDevice is the Schema for the mfatotpdevices API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MfaTotpDeviceSpec defines the desired state of MfaTotpDevice.
            type: object
          status:
            description: MfaTotpDeviceStatus defines the observed state of MfaTotpDevice.
            properties:
              id:
                description: The OCID of the MFA TOTP device.
                type: string
              inactiveStatus:
                description: |-
                  The detailed status of INACTIVE lifecycleState.
                  Allowed values are:
                   - 1 - SUSPENDED
                   - 2 - DISABLED
                   - 4 - BLOCKED
                   - 8 - LOCKED
                format: int64
                type: integer
              isActivated:
                description: Flag to indicate if the MFA TOTP device has been activated.
                type: boolean
              lifecycleState:
                description: |-
                  The MFA TOTP device's current state. After creating the MFA TOTP device, make sure its `lifecycleState` changes from
                  CREATING to ACTIVE before using it.
                type: string
              seed:
                description: The seed for the MFA TOTP device (Base32 encoded).
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  Date and time the `MfaTotpDevice` object was created, in the format defined by RFC3339.
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              timeExpires:
                description: |-
                  Date and time when this MFA TOTP device will expire, in the format defined by RFC3339.
                  Null if it never expires.
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              userId:
                description: The OCID of the user the MFA TOTP device belongs to.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
