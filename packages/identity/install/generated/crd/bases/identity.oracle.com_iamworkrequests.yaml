---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: iamworkrequests.identity.oracle.com
spec:
  group: identity.oracle.com
  names:
    kind: IamWorkRequest
    listKind: IamWorkRequestList
    plural: iamworkrequests
    singular: iamworkrequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the IamWorkRequest
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the IamWorkRequest
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: IamWorkRequest is the Schema for the iamworkrequests API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IamWorkRequestSpec defines the desired state of IamWorkRequest.
            type: object
          status:
            description: IamWorkRequestStatus defines the observed state of IamWorkRequest.
            properties:
              compartmentId:
                description: The OCID of the compartment containing this IAM work
                  request.
                type: string
              id:
                description: The OCID of the work request.
                type: string
              operationType:
                description: The asynchronous operation tracked by this IAM work request.
                type: string
              percentComplete:
                description: How much progress the operation has made.
                type: number
              resources:
                description: The resources this work request affects.
                items:
                  description: IamWorkRequestResource defines nested fields for IamWorkRequest.Resource.
                  properties:
                    actionType:
                      description: |-
                        The way in which this resource is affected by the work tracked in the work request.
                        A resource being created, updated, or deleted will remain in the IN_PROGRESS state until
                        work is complete for that resource at which point it will transition to CREATED, UPDATED,
                        or DELETED, respectively.
                      type: string
                    entityType:
                      description: The resource type the work request is affects.
                      type: string
                    entityUri:
                      description: The URI path that the user can do a GET on to access
                        the resource metadata.
                      type: string
                    identifier:
                      description: An OCID of the resource that the work request affects.
                      type: string
                  type: object
                type: array
              sdkStatus:
                description: |-
                  The status of the work request.
                  This uses a distinct JSON name so it can coexist with the OSOK status envelope.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeAccepted:
                description: |-
                  Date and time the work was accepted, in the format defined by RFC3339.
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              timeFinished:
                description: |-
                  Date and time the work completed, in the format defined by RFC3339.
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              timeStarted:
                description: |-
                  Date and time the work started, in the format defined by RFC3339.
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
