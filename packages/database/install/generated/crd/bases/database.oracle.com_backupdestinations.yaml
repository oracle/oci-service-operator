---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: backupdestinations.database.oracle.com
spec:
  group: database.oracle.com
  names:
    kind: BackupDestination
    listKind: BackupDestinationList
    plural: backupdestinations
    singular: backupdestination
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the BackupDestination
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the BackupDestination
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: BackupDestination is the Schema for the backupdestinations API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BackupDestinationSpec defines the desired state of BackupDestination.
            properties:
              compartmentId:
                description: The OCID (https://docs.cloud.oracle.com/Content/General/Concepts/identifiers.htm)
                  of the compartment.
                type: string
              connectionString:
                description: The connection string for connecting to the Recovery
                  Appliance.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).
                type: object
              displayName:
                description: The user-provided name of the backup destination.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              jsonData:
                type: string
              localMountPointPath:
                description: |-
                  **Deprecated.** The local directory path on each VM cluster node where the NFS server location is mounted. The local directory path and the NFS server location must each be the same across all of the VM cluster nodes. Ensure that the NFS mount is maintained continuously on all of the VM cluster nodes.
                  This field is deprecated. Use the mountTypeDetails field instead to specify the mount type for NFS.
                type: string
              mountTypeDetails:
                description: BackupDestinationMountTypeDetails defines nested fields
                  for BackupDestination.MountTypeDetails.
                properties:
                  jsonData:
                    type: string
                  localMountPointPath:
                    description: The local directory path on each VM cluster node
                      where the NFS server location is mounted. The local directory
                      path and the NFS server location must each be the same across
                      all of the VM cluster nodes. Ensure that the NFS mount is maintained
                      continuously on all of the VM cluster nodes.
                    type: string
                  mountType:
                    type: string
                  nfsServer:
                    description: IP addresses for NFS Auto mount.
                    items:
                      type: string
                    type: array
                  nfsServerExport:
                    description: Specifies the directory on which to mount the file
                      system
                    type: string
                type: object
              nfsMountType:
                description: NFS Mount type for backup destination.
                type: string
              nfsServer:
                description: IP addresses for NFS Auto mount.
                items:
                  type: string
                type: array
              nfsServerExport:
                description: Specifies the directory on which to mount the file system
                type: string
              type:
                type: string
              vpcUsers:
                description: The Virtual Private Catalog (VPC) users that are used
                  to access the Recovery Appliance.
                items:
                  type: string
                type: array
            required:
            - compartmentId
            - displayName
            type: object
          status:
            description: BackupDestinationStatus defines the observed state of BackupDestination.
            properties:
              associatedDatabases:
                description: List of databases associated with the backup destination.
                items:
                  description: BackupDestinationAssociatedDatabase defines nested
                    fields for BackupDestination.AssociatedDatabase.
                  properties:
                    dbName:
                      description: The display name of the database that is associated
                        with the backup destination.
                      type: string
                    id:
                      description: The database OCID (https://docs.cloud.oracle.com/Content/General/Concepts/identifiers.htm).
                      type: string
                  type: object
                type: array
              compartmentId:
                description: The OCID (https://docs.cloud.oracle.com/Content/General/Concepts/identifiers.htm)
                  of the compartment.
                type: string
              connectionString:
                description: For a RECOVERY_APPLIANCE backup destination, the connection
                  string for connecting to the Recovery Appliance.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).
                type: object
              displayName:
                description: The user-provided name of the backup destination.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.cloud.oracle.com/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: The OCID (https://docs.cloud.oracle.com/Content/General/Concepts/identifiers.htm)
                  of the backup destination.
                type: string
              lifecycleDetails:
                description: |-
                  A descriptive text associated with the lifecycleState.
                  Typically contains additional displayable text
                type: string
              lifecycleState:
                description: The current lifecycle state of the backup destination.
                type: string
              localMountPointPath:
                description: The local directory path on each VM cluster node where
                  the NFS server location is mounted. The local directory path and
                  the NFS server location must each be the same across all of the
                  VM cluster nodes. Ensure that the NFS mount is maintained continuously
                  on all of the VM cluster nodes.
                type: string
              nfsMountType:
                description: NFS Mount type for backup destination.
                type: string
              nfsServer:
                description: Host names or IP addresses for NFS Auto mount.
                items:
                  type: string
                type: array
              nfsServerExport:
                description: Specifies the directory on which to mount the file system
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: The date and time the backup destination was created.
                type: string
              type:
                description: Type of the backup destination.
                type: string
              vpcUsers:
                description: For a RECOVERY_APPLIANCE backup destination, the Virtual
                  Private Catalog (VPC) users that are used to access the Recovery
                  Appliance.
                items:
                  type: string
                type: array
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
