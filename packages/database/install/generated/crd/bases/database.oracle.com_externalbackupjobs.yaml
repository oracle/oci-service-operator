---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: externalbackupjobs.database.oracle.com
spec:
  group: database.oracle.com
  names:
    kind: ExternalBackupJob
    listKind: ExternalBackupJobList
    plural: externalbackupjobs
    singular: externalbackupjob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the ExternalBackupJob
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ExternalBackupJob
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ExternalBackupJob is the Schema for the externalbackupjobs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ExternalBackupJobSpec defines the desired state of ExternalBackupJob.
            properties:
              availabilityDomain:
                description: The targeted availability domain for the backup.
                type: string
              characterSet:
                description: The character set for the database.
                type: string
              compartmentId:
                description: The OCID (https://docs.cloud.oracle.com/Content/General/Concepts/identifiers.htm)
                  of the compartment where this backup should be created.
                type: string
              databaseEdition:
                description: |-
                  The Oracle Database edition to use for creating a database from this standalone backup.
                  Note that 2-node RAC DB systems require Enterprise Edition - Extreme Performance.
                type: string
              databaseMode:
                description: The mode (single instance or RAC) of the database being
                  backed up.
                type: string
              dbName:
                description: The name of the database from which the backup is being
                  taken.
                type: string
              dbUniqueName:
                description: The `DB_UNIQUE_NAME` of the Oracle Database being backed
                  up.
                type: string
              dbVersion:
                description: A valid Oracle Database version.
                type: string
              displayName:
                description: A user-friendly name for the backup. This name does not
                  have to be unique.
                type: string
              externalDatabaseIdentifier:
                description: The `DBID` of the Oracle Database being backed up.
                format: int64
                type: integer
              ncharacterSet:
                description: The national character set for the database.
                type: string
              pdbName:
                description: The pluggable database name.
                type: string
            required:
            - availabilityDomain
            - characterSet
            - compartmentId
            - databaseEdition
            - databaseMode
            - dbName
            - dbVersion
            - displayName
            - externalDatabaseIdentifier
            - ncharacterSet
            type: object
          status:
            description: ExternalBackupJobStatus defines the observed state of ExternalBackupJob.
            properties:
              backupId:
                description: The OCID (https://docs.cloud.oracle.com/Content/General/Concepts/identifiers.htm)
                  of the associated backup resource.
                type: string
              bucketName:
                description: The name of the Swift compartment bucket where the backup
                  should be stored.
                type: string
              provisioning:
                description: An indicator for the provisioning state of the resource.
                  If `TRUE`, the resource is still being provisioned.
                type: boolean
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              swiftPath:
                description: The Swift path to use as a destination for the standalone
                  backup.
                type: string
              tag:
                description: The tag for RMAN to apply to the backup.
                type: string
              userName:
                description: The Swift user name to use for transferring the standalone
                  backup to the designated Swift compartment bucket.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
