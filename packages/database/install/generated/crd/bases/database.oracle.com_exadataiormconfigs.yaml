---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: exadataiormconfigs.database.oracle.com
spec:
  group: database.oracle.com
  names:
    kind: ExadataIormConfig
    listKind: ExadataIormConfigList
    plural: exadataiormconfigs
    singular: exadataiormconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the ExadataIormConfig
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ExadataIormConfig
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ExadataIormConfig is the Schema for the exadataiormconfigs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ExadataIormConfigSpec defines the desired state of ExadataIormConfig.
            properties:
              dbPlans:
                description: |-
                  Array of IORM Setting for all the database in
                  this Exadata DB System
                items:
                  description: ExadataIormConfigDbPlan defines nested fields for ExadataIormConfig.DbPlan.
                  properties:
                    dbName:
                      description: The database name. For the default `DbPlan`, the
                        `dbName` is `default`.
                      type: string
                    share:
                      description: The relative priority of this database.
                      type: integer
                  type: object
                type: array
              objective:
                description: |-
                  Value for the IORM objective
                  Default is "Auto"
                type: string
            type: object
          status:
            description: ExadataIormConfigStatus defines the observed state of ExadataIormConfig.
            properties:
              dbPlans:
                description: |-
                  An array of IORM settings for all the database in
                  the Exadata DB system.
                items:
                  description: ExadataIormConfigDbPlan defines nested fields for ExadataIormConfig.DbPlan.
                  properties:
                    dbName:
                      description: The database name. For the default `DbPlan`, the
                        `dbName` is `default`.
                      type: string
                    share:
                      description: The relative priority of this database.
                      type: integer
                  type: object
                type: array
              lifecycleDetails:
                description: Additional information about the current `lifecycleState`.
                type: string
              lifecycleState:
                description: The current state of IORM configuration for the Exadata
                  DB system.
                type: string
              objective:
                description: |-
                  The current value for the IORM objective.
                  The default is `AUTO`.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
