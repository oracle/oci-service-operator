---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: schedules.dataintegration.oracle.com
spec:
  group: dataintegration.oracle.com
  names:
    kind: Schedule
    listKind: ScheduleList
    plural: schedules
    singular: schedule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      priority: 1
      type: string
    - description: status of the Schedule
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Schedule
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Schedule is the Schema for the schedules API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScheduleSpec defines the desired state of Schedule.
            properties:
              description:
                description: Detailed description for the object.
                type: string
              frequencyDetails:
                description: ScheduleFrequencyDetails defines nested fields for Schedule.FrequencyDetails.
                properties:
                  customExpression:
                    description: This holds the complete cron expression for this
                      schedule, for example, 10 0/5 * * * ? that fires every 5 minutes,
                      at 10 seconds after the minute (i.e. 10:00:10 am, 10:05:10 am,
                      etc.)
                    type: string
                  dayOfWeek:
                    description: This holds the day of the week on which the schedule
                      should be triggered.
                    type: string
                  days:
                    description: A list of days of the month to be scheduled. i.e.
                      excute every 2nd,3rd, 10th of the month.
                    items:
                      type: integer
                    type: array
                  frequency:
                    description: the frequency of the schedule.
                    type: string
                  interval:
                    description: This hold the repeatability aspect of a schedule.
                      i.e. in a monhtly frequency, a task can be scheduled for every
                      month, once in two months, once in tree months etc.
                    type: integer
                  jsonData:
                    type: string
                  modelType:
                    type: string
                  time:
                    description: ScheduleFrequencyDetailsTime defines nested fields
                      for Schedule.FrequencyDetails.Time.
                    properties:
                      hour:
                        description: The hour value.
                        type: integer
                      minute:
                        description: The minute value.
                        type: integer
                      second:
                        description: The second value.
                        type: integer
                    type: object
                  weekOfMonth:
                    description: This holds the week of the month in which the schedule
                      should be triggered.
                    type: string
                type: object
              identifier:
                description: Value can only contain upper case letters, underscore,
                  and numbers. It should begin with upper case letter or underscore.
                  The value can be modified.
                type: string
              isDaylightAdjustmentEnabled:
                description: A flag to indicate whether daylight adjustment should
                  be considered or not.
                type: boolean
              key:
                description: Generated key that can be used in API calls to identify
                  schedule. On scenarios where reference to the schedule is needed,
                  a value can be passed in create.
                type: string
              modelType:
                description: The type of the object.
                type: string
              modelVersion:
                description: This is a version number that is used by the service
                  to upgrade objects if needed through releases of the service.
                type: string
              name:
                description: Free form text without any restriction on permitted characters.
                  Name can have letters, numbers, and special characters. The value
                  is editable and is restricted to 1000 characters.
                type: string
              objectStatus:
                description: The status of an object that can be set to value 1 for
                  shallow references across objects, other values reserved.
                type: integer
              objectVersion:
                description: This is used by the service for optimistic locking of
                  the object, to prevent multiple users from simultaneously updating
                  the object.
                type: integer
              parentRef:
                description: ScheduleParentRef defines nested fields for Schedule.ParentRef.
                properties:
                  parent:
                    description: Key of the parent object.
                    type: string
                  rootDocId:
                    description: Key of the root document object.
                    type: string
                type: object
              registryMetadata:
                description: ScheduleRegistryMetadata defines nested fields for Schedule.RegistryMetadata.
                properties:
                  aggregatorKey:
                    description: The owning object's key for this object.
                    type: string
                  isFavorite:
                    description: Specifies whether this object is a favorite or not.
                    type: boolean
                  key:
                    description: The identifying key for the object.
                    type: string
                  labels:
                    description: Labels are keywords or labels that you can add to
                      data assets, dataflows etc. You can define your own labels and
                      use them to categorize content.
                    items:
                      type: string
                    type: array
                  registryVersion:
                    description: The registry version.
                    type: integer
                type: object
              timezone:
                description: The timezone for the schedule.
                type: string
            required:
            - identifier
            - name
            type: object
          status:
            description: ScheduleStatus defines the observed state of Schedule.
            properties:
              description:
                description: Detailed description for the object.
                type: string
              frequencyDetails:
                description: ScheduleFrequencyDetails defines nested fields for Schedule.FrequencyDetails.
                properties:
                  customExpression:
                    description: This holds the complete cron expression for this
                      schedule, for example, 10 0/5 * * * ? that fires every 5 minutes,
                      at 10 seconds after the minute (i.e. 10:00:10 am, 10:05:10 am,
                      etc.)
                    type: string
                  dayOfWeek:
                    description: This holds the day of the week on which the schedule
                      should be triggered.
                    type: string
                  days:
                    description: A list of days of the month to be scheduled. i.e.
                      excute every 2nd,3rd, 10th of the month.
                    items:
                      type: integer
                    type: array
                  frequency:
                    description: the frequency of the schedule.
                    type: string
                  interval:
                    description: This hold the repeatability aspect of a schedule.
                      i.e. in a monhtly frequency, a task can be scheduled for every
                      month, once in two months, once in tree months etc.
                    type: integer
                  jsonData:
                    type: string
                  modelType:
                    type: string
                  time:
                    description: ScheduleFrequencyDetailsTime defines nested fields
                      for Schedule.FrequencyDetails.Time.
                    properties:
                      hour:
                        description: The hour value.
                        type: integer
                      minute:
                        description: The minute value.
                        type: integer
                      second:
                        description: The second value.
                        type: integer
                    type: object
                  weekOfMonth:
                    description: This holds the week of the month in which the schedule
                      should be triggered.
                    type: string
                type: object
              identifier:
                description: Value can only contain upper case letters, underscore,
                  and numbers. It should begin with upper case letter or underscore.
                  The value can be modified.
                type: string
              isDaylightAdjustmentEnabled:
                description: A flag to indicate daylight saving.
                type: boolean
              key:
                description: Generated key that can be used in API calls to identify
                  schedule. On scenarios where reference to the schedule is needed,
                  a value can be passed in create.
                type: string
              metadata:
                description: ScheduleMetadata defines nested fields for Schedule.Metadata.
                properties:
                  aggregator:
                    description: ScheduleMetadataAggregator defines nested fields
                      for Schedule.Metadata.Aggregator.
                    properties:
                      description:
                        description: The description of the aggregator.
                        type: string
                      identifier:
                        description: The identifier of the aggregator.
                        type: string
                      key:
                        description: The key of the aggregator object.
                        type: string
                      name:
                        description: The name of the aggregator.
                        type: string
                      type:
                        description: The type of the aggregator.
                        type: string
                    type: object
                  aggregatorKey:
                    description: The owning object key for this object.
                    type: string
                  countStatistics:
                    description: ScheduleMetadataCountStatistics defines nested fields
                      for Schedule.Metadata.CountStatistics.
                    properties:
                      objectTypeCountList:
                        description: The array of statistics.
                        items:
                          description: ScheduleMetadataCountStatisticsObjectTypeCountList
                            defines nested fields for Schedule.Metadata.CountStatistics.ObjectTypeCountList.
                          properties:
                            objectCount:
                              description: The value for the count statistic object.
                              format: int64
                              type: integer
                            objectType:
                              description: The type of object for the count statistic
                                object.
                              type: string
                          type: object
                        type: array
                    type: object
                  createdBy:
                    description: The user that created the object.
                    type: string
                  createdByName:
                    description: The user that created the object.
                    type: string
                  identifierPath:
                    description: The full path to identify this object.
                    type: string
                  infoFields:
                    additionalProperties:
                      type: string
                    description: Information property fields.
                    type: object
                  isFavorite:
                    description: Specifies whether this object is a favorite or not.
                    type: boolean
                  labels:
                    description: Labels are keywords or tags that you can add to data
                      assets, dataflows and so on. You can define your own labels
                      and use them to categorize content.
                    items:
                      type: string
                    type: array
                  registryVersion:
                    description: The registry version of the object.
                    type: integer
                  timeCreated:
                    description: The date and time that the object was created.
                    type: string
                  timeUpdated:
                    description: The date and time that the object was updated.
                    type: string
                  updatedBy:
                    description: The user that updated the object.
                    type: string
                  updatedByName:
                    description: The user that updated the object.
                    type: string
                type: object
              modelType:
                description: The type of the object.
                type: string
              modelVersion:
                description: This is a version number that is used by the service
                  to upgrade objects if needed through releases of the service.
                type: string
              name:
                description: Free form text without any restriction on permitted characters.
                  Name can have letters, numbers, and special characters. The value
                  is editable and is restricted to 1000 characters.
                type: string
              objectStatus:
                description: The status of an object that can be set to value 1 for
                  shallow references across objects, other values reserved.
                type: integer
              objectVersion:
                description: This is used by the service for optimistic locking of
                  the object, to prevent multiple users from simultaneously updating
                  the object.
                type: integer
              parentRef:
                description: ScheduleParentRef defines nested fields for Schedule.ParentRef.
                properties:
                  parent:
                    description: Key of the parent object.
                    type: string
                  rootDocId:
                    description: Key of the root document object.
                    type: string
                type: object
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timezone:
                description: The timezone for the schedule.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
