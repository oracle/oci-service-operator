---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: domains.tenantmanagercontrolplane.oracle.com
spec:
  group: tenantmanagercontrolplane.oracle.com
  names:
    kind: Domain
    listKind: DomainList
    plural: domains
    singular: domain
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the Domain
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Domain
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Domain is the Schema for the domains API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DomainSpec defines the desired state of Domain.
            properties:
              compartmentId:
                description: OCID of the tenancy.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              domainName:
                description: The domain name.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              isGovernanceEnabled:
                description: Indicates whether governance should be enabled for this
                  domain. Defaults to false.
                type: boolean
              lifecycleState:
                description: |-
                  Optional exact-match filter used to narrow list-based binding and delete confirmation reads.
                  Supported values include `CREATING`, `ACTIVE`, `INACTIVE`, `UPDATING`, `FAILED`, and `TERMINATED`.
                type: string
              status:
                description: |-
                  Optional exact-match filter used to narrow list-based binding and delete confirmation reads.
                  Supported values include `PENDING`, `RELEASING`, `RELEASED`, `EXPIRING`, `REVOKING`, `REVOKED`, `ACTIVE`, and `FAILED`.
                type: string
              subscriptionEmail:
                description: Email address to be used to notify the user, and that
                  the ONS subscription will be created with.
                type: string
            required:
            - compartmentId
            - domainName
            type: object
          status:
            description: DomainStatus defines the observed state of Domain.
            properties:
              compartmentId:
                description: The tenancy OCID recorded for list identity matching
                  and replacement-only drift checks.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              domainName:
                description: The domain name.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                type: object
              id:
                description: The OCID of the domain.
                type: string
              isGovernanceEnabled:
                description: Whether governance was requested for the bound domain;
                  tracked from the spec because the SDK does not echo it on reads.
                type: boolean
              lifecycleState:
                description: Lifecycle state of the domain.
                type: string
              ownerId:
                description: The OCID of the tenancy that has started the registration
                  process for this domain.
                type: string
              sdkStatus:
                description: The raw domain status returned by Tenant Manager Control
                  Plane.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              subscriptionEmail:
                description: The accepted subscription email tracked from the spec
                  because the SDK does not echo it on reads.
                type: string
              timeCreated:
                description: Date-time when this domain was created. An RFC 3339-formatted
                  date and time string.
                type: string
              timeUpdated:
                description: Date-time when this domain was last updated. An RFC 3339-formatted
                  date and time string.
                type: string
              txtRecord:
                description: The code that the owner of the domain will need to add
                  as a TXT record to their domain.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
