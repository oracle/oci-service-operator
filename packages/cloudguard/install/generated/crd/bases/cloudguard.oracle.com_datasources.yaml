---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: datasources.cloudguard.oracle.com
spec:
  group: cloudguard.oracle.com
  names:
    kind: DataSource
    listKind: DataSourceList
    plural: datasources
    singular: datasource
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the DataSource
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the DataSource
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: DataSource is the Schema for the datasources API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSourceSpec defines the desired state of DataSource.
            properties:
              compartmentId:
                description: Compartment OCID of the data source
                type: string
              dataSourceDetails:
                description: DataSourceDetails defines nested fields for DataSource.DataSourceDetails.
                properties:
                  additionalEntitiesCount:
                    description: The additional entities count used for data source
                      query
                    type: integer
                  dataSourceFeedProvider:
                    type: string
                  description:
                    description: Description text for the query
                    type: string
                  intervalInMinutes:
                    description: Interval in minutes that query is run periodically.
                    type: integer
                  intervalInSeconds:
                    description: Interval in minutes which query is run periodically.
                    type: integer
                  jsonData:
                    type: string
                  loggingQueryDetails:
                    description: DataSourceDetailsLoggingQueryDetails defines nested
                      fields for DataSource.DataSourceDetails.LoggingQueryDetails.
                    properties:
                      jsonData:
                        type: string
                      keyEntitiesCount:
                        description: The key entities count used for data source query
                        type: integer
                      loggingQueryType:
                        type: string
                    type: object
                  loggingQueryType:
                    description: Type of logging query for data source (Sighting/Insight)
                    type: string
                  operator:
                    description: Operator used in data source
                    type: string
                  query:
                    description: The continuous query expression that is run periodically.
                    type: string
                  queryStartTime:
                    description: DataSourceDetailsQueryStartTime defines nested fields
                      for DataSource.DataSourceDetails.QueryStartTime.
                    properties:
                      jsonData:
                        type: string
                      queryStartTime:
                        description: Time when the query can start. If not specified
                          it can start immediately
                        type: string
                      startPolicyType:
                        type: string
                    type: object
                  regions:
                    description: List of logging query regions
                    items:
                      type: string
                    type: array
                  scheduledQueryScopeDetails:
                    description: Target information in which scheduled query will
                      be run
                    items:
                      description: DataSourceDetailsScheduledQueryScopeDetail defines
                        nested fields for DataSource.DataSourceDetails.ScheduledQueryScopeDetail.
                      properties:
                        region:
                          description: region on which scheduled query needs to be
                            run
                          type: string
                        resourceIds:
                          description: List of OCIDs on scheduled query needs to run
                          items:
                            type: string
                          type: array
                        resourceType:
                          description: Type of resource
                          type: string
                      type: object
                    type: array
                  threshold:
                    description: The integer value that must be exceeded, fall below
                      or equal to (depending on the operator), for the query result
                      to trigger an event
                    type: integer
                type: object
              dataSourceFeedProvider:
                description: Type of data source feed provider (LoggingQuery)
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              displayName:
                description: Data source display name
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                  Avoid entering confidential information.
                type: object
              status:
                description: Enablement status of data source.
                type: string
            required:
            - compartmentId
            - dataSourceFeedProvider
            - displayName
            type: object
          status:
            description: DataSourceStatus defines the observed state of DataSource.
            properties:
              compartmentId:
                description: Compartment OCID of data source
                type: string
              dataSourceDetails:
                description: DataSourceDetails defines nested fields for DataSource.DataSourceDetails.
                properties:
                  additionalEntitiesCount:
                    description: The additional entities count used for data source
                      query
                    type: integer
                  dataSourceFeedProvider:
                    type: string
                  description:
                    description: Description text for the query
                    type: string
                  intervalInMinutes:
                    description: Interval in minutes that query is run periodically.
                    type: integer
                  intervalInSeconds:
                    description: Interval in minutes which query is run periodically.
                    type: integer
                  jsonData:
                    type: string
                  loggingQueryDetails:
                    description: DataSourceDetailsLoggingQueryDetails defines nested
                      fields for DataSource.DataSourceDetails.LoggingQueryDetails.
                    properties:
                      jsonData:
                        type: string
                      keyEntitiesCount:
                        description: The key entities count used for data source query
                        type: integer
                      loggingQueryType:
                        type: string
                    type: object
                  loggingQueryType:
                    description: Type of logging query for data source (Sighting/Insight)
                    type: string
                  operator:
                    description: Operator used in data source
                    type: string
                  query:
                    description: The continuous query expression that is run periodically.
                    type: string
                  queryStartTime:
                    description: DataSourceDetailsQueryStartTime defines nested fields
                      for DataSource.DataSourceDetails.QueryStartTime.
                    properties:
                      jsonData:
                        type: string
                      queryStartTime:
                        description: Time when the query can start. If not specified
                          it can start immediately
                        type: string
                      startPolicyType:
                        type: string
                    type: object
                  regions:
                    description: List of logging query regions
                    items:
                      type: string
                    type: array
                  scheduledQueryScopeDetails:
                    description: Target information in which scheduled query will
                      be run
                    items:
                      description: DataSourceDetailsScheduledQueryScopeDetail defines
                        nested fields for DataSource.DataSourceDetails.ScheduledQueryScopeDetail.
                      properties:
                        region:
                          description: region on which scheduled query needs to be
                            run
                          type: string
                        resourceIds:
                          description: List of OCIDs on scheduled query needs to run
                          items:
                            type: string
                          type: array
                        resourceType:
                          description: Type of resource
                          type: string
                      type: object
                    type: array
                  threshold:
                    description: The integer value that must be exceeded, fall below
                      or equal to (depending on the operator), for the query result
                      to trigger an event
                    type: integer
                type: object
              dataSourceDetectorMappingInfo:
                description: Information about the detector recipe and rule attached
                items:
                  description: DataSourceDetectorMappingInfo defines nested fields
                    for DataSource.DataSourceDetectorMappingInfo.
                  properties:
                    detectorRecipeId:
                      description: ID of the detector recipe attached to the data
                        source
                      type: string
                    detectorRuleId:
                      description: ID of the detector rule attached to the data source
                      type: string
                  type: object
                type: array
              dataSourceFeedProvider:
                description: Possible type of dataSourceFeed Provider(LoggingQuery)
                type: string
              dataSourceSummaryDetails:
                description: DataSourceSummaryDetails defines nested fields for DataSource.DataSourceSummaryDetails.
                properties:
                  dataSourceDetectorMappingInfo:
                    description: Data source mapping with detector recipe and detector
                      rule
                    items:
                      description: DataSourceSummaryDetailsDataSourceDetectorMappingInfo
                        defines nested fields for DataSource.DataSourceSummaryDetails.DataSourceDetectorMappingInfo.
                      properties:
                        detectorRecipeId:
                          description: ID of the detector recipe attached to the data
                            source
                          type: string
                        detectorRuleId:
                          description: ID of the detector rule attached to the data
                            source
                          type: string
                      type: object
                    type: array
                  dataSourceFeedProvider:
                    type: string
                  description:
                    description: Description for the scheduled query
                    type: string
                  intervalInSeconds:
                    description: Interval in minutes in which the query is run periodically.
                    type: integer
                  jsonData:
                    type: string
                  regionStatusDetail:
                    description: DataSource query metadata replication region and
                      status.
                    items:
                      description: DataSourceSummaryDetailsRegionStatusDetail defines
                        nested fields for DataSource.DataSourceSummaryDetails.RegionStatusDetail.
                      properties:
                        region:
                          description: Data source replication region
                          type: string
                        status:
                          description: Data source replication region status
                          type: string
                      type: object
                    type: array
                  regions:
                    description: Data source customer-specified regions
                    items:
                      type: string
                    type: array
                  scheduledQueryScopeDetails:
                    description: Target information in which scheduled query will
                      be run
                    items:
                      description: DataSourceSummaryDetailsScheduledQueryScopeDetail
                        defines nested fields for DataSource.DataSourceSummaryDetails.ScheduledQueryScopeDetail.
                      properties:
                        region:
                          description: region on which scheduled query needs to be
                            run
                          type: string
                        resourceIds:
                          description: List of OCIDs on scheduled query needs to run
                          items:
                            type: string
                          type: array
                        resourceType:
                          description: Type of resource
                          type: string
                      type: object
                    type: array
                type: object
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"foo-namespace": {"bar-key": "value"}}`
                type: object
              displayName:
                description: Display name of the data source
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"bar-key": "value"}`
                  Avoid entering confidential information.
                type: object
              id:
                description: OCID for the data source
                type: string
              lifecycleDetails:
                description: A message describing the current state in more detail.
                  For example, this can be used to provide actionable information
                  for a zone in the `Failed` state.
                type: string
              lifecycleState:
                description: The current lifecycle state of the resource.
                type: string
              locks:
                description: Locks associated with this resource.
                items:
                  description: DataSourceLock defines nested fields for DataSource.Lock.
                  properties:
                    message:
                      description: |-
                        A message added by the creator of the lock. This is typically used to give an
                        indication of why the resource is locked.
                      type: string
                    relatedResourceId:
                      description: The ID of the resource that is locking this resource.
                        Indicates that deleting this resource will remove the lock.
                      type: string
                    timeCreated:
                      description: When the lock was created, in the format defined
                        by RFC 3339.
                      type: string
                    type:
                      description: Type of the lock.
                      type: string
                  type: object
                type: array
              loggingQueryDetails:
                description: DataSourceLoggingQueryDetails defines nested fields for
                  DataSource.LoggingQueryDetails.
                properties:
                  jsonData:
                    type: string
                  keyEntitiesCount:
                    description: The key entities count used for data source query
                    type: integer
                  loggingQueryType:
                    type: string
                type: object
              regionStatusDetail:
                description: Information about the region and status of query replication
                items:
                  description: DataSourceRegionStatusDetail defines nested fields
                    for DataSource.RegionStatusDetail.
                  properties:
                    region:
                      description: Data source replication region
                      type: string
                    status:
                      description: Data source replication region status
                      type: string
                  type: object
                type: array
              sdkStatus:
                description: |-
                  Enablement status of the data source
                  This uses a distinct JSON name so it can coexist with the OSOK status envelope.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  System tags can be viewed by users, but can only be created by the system.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: The date and time the Data source was created. Format
                  defined by RFC3339.
                type: string
              timeUpdated:
                description: The date and time the data source was updated. Format
                  defined by RFC3339.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
