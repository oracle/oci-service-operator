---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: zonerecords.dns.oracle.com
spec:
  group: dns.oracle.com
  names:
    kind: ZoneRecord
    listKind: ZoneRecordList
    plural: zonerecords
    singular: zonerecord
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the ZoneRecord
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ZoneRecord
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ZoneRecord is the Schema for the zonerecords API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ZoneRecordSpec defines the desired state of ZoneRecord.
            properties:
              items:
                items:
                  description: ZoneRecordItem defines nested fields for ZoneRecord.Item.
                  properties:
                    domain:
                      description: The fully qualified domain name where the record
                        can be located.
                      type: string
                    isProtected:
                      description: |-
                        A Boolean flag indicating whether or not parts of the record
                        are unable to be explicitly managed.
                      type: boolean
                    rdata:
                      description: |-
                        The record's data, as whitespace-delimited tokens in
                        type-specific presentation format. All RDATA is normalized and the
                        returned presentation of your RDATA may differ from its initial input.
                        For more information about RDATA, see Supported DNS Resource Record Types (https://docs.cloud.oracle.com/iaas/Content/DNS/Reference/supporteddnsresource.htm)
                      type: string
                    recordHash:
                      description: A unique identifier for the record within its zone.
                      type: string
                    rrsetVersion:
                      description: |-
                        The latest version of the record's zone in which its RRSet differs
                        from the preceding version.
                      type: string
                    rtype:
                      description: The type of DNS record, such as A or CNAME. For
                        more information, see Resource Record (RR) TYPEs (https://www.iana.org/assignments/dns-parameters/dns-parameters.xhtml#dns-parameters-4).
                      type: string
                    ttl:
                      description: The Time To Live for the record, in seconds. Using
                        a TTL lower than 30 seconds is not recommended.
                      type: integer
                  required:
                  - domain
                  - rdata
                  - rtype
                  - ttl
                  type: object
                type: array
            type: object
          status:
            description: ZoneRecordStatus defines the observed state of ZoneRecord.
            properties:
              domain:
                description: The fully qualified domain name where the record can
                  be located.
                type: string
              isProtected:
                description: |-
                  A Boolean flag indicating whether or not parts of the record
                  are unable to be explicitly managed.
                type: boolean
              rdata:
                description: |-
                  The record's data, as whitespace-delimited tokens in
                  type-specific presentation format. All RDATA is normalized and the
                  returned presentation of your RDATA may differ from its initial input.
                  For more information about RDATA, see Supported DNS Resource Record Types (https://docs.cloud.oracle.com/iaas/Content/DNS/Reference/supporteddnsresource.htm)
                type: string
              recordHash:
                description: A unique identifier for the record within its zone.
                type: string
              rrsetVersion:
                description: |-
                  The latest version of the record's zone in which its RRSet differs
                  from the preceding version.
                type: string
              rtype:
                description: The type of DNS record, such as A or CNAME. For more
                  information, see Resource Record (RR) TYPEs (https://www.iana.org/assignments/dns-parameters/dns-parameters.xhtml#dns-parameters-4).
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              ttl:
                description: The Time To Live for the record, in seconds. Using a
                  TTL lower than 30 seconds is not recommended.
                type: integer
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
