---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: dataassets.datacatalog.oracle.com
spec:
  group: datacatalog.oracle.com
  names:
    kind: DataAsset
    listKind: DataAssetList
    plural: dataassets
    singular: dataasset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the DataAsset
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the DataAsset
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: DataAsset is the Schema for the dataassets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataAssetSpec defines the desired state of DataAsset.
            properties:
              customPropertyMembers:
                description: The list of customized properties along with the values
                  for this object
                items:
                  description: DataAssetCustomPropertyMember defines nested fields
                    for DataAsset.CustomPropertyMember.
                  properties:
                    displayName:
                      description: Name of the custom property
                      type: string
                    key:
                      description: Unique Identifier of the attribute which is ID
                      type: string
                    namespaceName:
                      description: Namespace name of the custom property
                      type: string
                    value:
                      description: The custom property value
                      type: string
                  type: object
                type: array
              description:
                description: Detailed description of the data asset.
                type: string
              displayName:
                description: |-
                  A user-friendly display name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              properties:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  A map of maps that contains the properties which are specific to the data asset type. Each data asset type
                  definition defines it's set of required and optional properties. The map keys are category names and the
                  values are maps of property name to property value. Every property is contained inside of a category. Most
                  data assets have required properties within the "default" category. To determine the set of optional and
                  required properties for a data asset type, a query can be done on '/types?type=dataAsset' that returns a
                  collection of all data asset types. The appropriate data asset type, which includes definitions of all of
                  it's properties, can be identified from this collection.
                  Example: `{"properties": { "default": { "host": "host1", "port": "1521", "database": "orcl"}}}`
                type: object
              typeKey:
                description: The key of the data asset type. This can be obtained
                  via the '/types' endpoint.
                type: string
            required:
            - displayName
            - typeKey
            type: object
          status:
            description: DataAssetStatus defines the observed state of DataAsset.
            properties:
              catalogId:
                description: The data catalog's OCID.
                type: string
              createdById:
                description: OCID of the user who created the data asset.
                type: string
              customPropertyMembers:
                description: The list of customized properties along with the values
                  for this object
                items:
                  description: DataAssetCustomPropertyMember defines nested fields
                    for DataAsset.CustomPropertyMember.
                  properties:
                    displayName:
                      description: Name of the custom property
                      type: string
                    key:
                      description: Unique Identifier of the attribute which is ID
                      type: string
                    namespaceName:
                      description: Namespace name of the custom property
                      type: string
                    value:
                      description: The custom property value
                      type: string
                  type: object
                type: array
              dataSelectorPatterns:
                description: The list of data selector patterns used in the harvest
                  for this data asset to derive logical entities.
                items:
                  description: DataAssetDataSelectorPattern defines nested fields
                    for DataAsset.DataSelectorPattern.
                  properties:
                    catalogId:
                      description: The data catalog's OCID.
                      type: string
                    description:
                      description: Detailed description of the pattern.
                      type: string
                    displayName:
                      description: |-
                        A user-friendly display name. Does not have to be unique, and it's changeable.
                        Avoid entering confidential information.
                      type: string
                    expression:
                      description: |-
                        Input string which drives the selection process, allowing for fine-grained control using qualifiers.
                        Refer to the user documentation for details of the format and examples. A pattern cannot include both
                        a prefix and an expression.
                      type: string
                    filePathPrefix:
                      description: |-
                        Input string which drives the selection process.
                        Refer to the user documentation for details of the format and examples. A pattern cannot include both
                        a prefix and an expression.
                      type: string
                    key:
                      description: Unique pattern key that is immutable.
                      type: string
                    lifecycleState:
                      description: State of the pattern.
                      type: string
                    timeCreated:
                      description: |-
                        The date and time the pattern was created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                        Example: `2019-03-25T21:10:29.600Z`
                      type: string
                  type: object
                type: array
              description:
                description: Detailed description of the data asset.
                type: string
              displayName:
                description: |-
                  A user-friendly display name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              externalKey:
                description: External URI that can be used to reference the object.
                  Format will differ based on the type of object.
                type: string
              key:
                description: Unique data asset key that is immutable.
                type: string
              lifecycleDetails:
                description: |-
                  A message describing the current state in more detail. An object not in ACTIVE state may have functional limitations,
                  see service documentation for details.
                type: string
              lifecycleState:
                description: The current state of the data asset.
                type: string
              properties:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  A map of maps that contains the properties which are specific to the asset type. Each data asset type
                  definition defines it's set of required and optional properties. The map keys are category names and the
                  values are maps of property name to property value. Every property is contained inside of a category. Most
                  data assets have required properties within the "default" category.
                  Example: `{"properties": { "default": { "host": "host1", "port": "1521", "database": "orcl"}}}`
                type: object
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time the data asset was created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2019-03-25T21:10:29.600Z`
                type: string
              timeHarvested:
                description: The last time that a harvest was performed on the data
                  asset. An RFC3339 (https://tools.ietf.org/html/rfc3339) formatted
                  datetime string.
                type: string
              timeUpdated:
                description: The last time that any change was made to the data asset.
                  An RFC3339 (https://tools.ietf.org/html/rfc3339) formatted datetime
                  string.
                type: string
              typeKey:
                description: The key of the object type. Type key's can be found via
                  the '/types' endpoint.
                type: string
              updatedById:
                description: OCID of the user who last modified the data asset.
                type: string
              uri:
                description: URI to the data asset instance in the API.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
