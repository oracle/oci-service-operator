---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: entities.datacatalog.oracle.com
spec:
  group: datacatalog.oracle.com
  names:
    kind: Entity
    listKind: EntityList
    plural: entities
    singular: entity
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the Entity
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Entity
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Entity is the Schema for the entities API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EntitySpec defines the desired state of Entity.
            properties:
              businessName:
                description: Optional user friendly business name of the data entity.
                  If set, this supplements the harvested display name of the object.
                type: string
              customPropertyMembers:
                description: The list of customized properties along with the values
                  for this object
                items:
                  description: EntityCustomPropertyMember defines nested fields for
                    Entity.CustomPropertyMember.
                  properties:
                    displayName:
                      description: Name of the custom property
                      type: string
                    key:
                      description: Unique Identifier of the attribute which is ID
                      type: string
                    namespaceName:
                      description: Namespace name of the custom property
                      type: string
                    value:
                      description: The custom property value
                      type: string
                  type: object
                type: array
              description:
                description: Detailed description of a data entity.
                type: string
              displayName:
                description: |-
                  A user-friendly display name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              folderKey:
                description: Key of the associated folder.
                type: string
              harvestStatus:
                description: |-
                  Status of the object as updated by the harvest process. When an entity object is created , it's harvest status
                  will indicate if the entity's metadata has been fully harvested or not. The harvest process can perform
                  shallow harvesting to allow users to browse the metadata and can on-demand deep harvest on any object
                  This requires a harvest status indicator for catalog objects.
                type: string
              isLogical:
                description: Property to indicate if the object is a physical materialized
                  object or virtual. For example, View.
                type: boolean
              isPartition:
                description: Property to indicate if the object is a sub object of
                  a parent physical object.
                type: boolean
              lastJobKey:
                description: Key of the last harvest process to update this object.
                type: string
              patternKey:
                description: Key of the associated pattern if this is a logical entity.
                type: string
              properties:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  A map of maps that contains the properties which are specific to the entity type. Each entity type
                  definition defines it's set of required and optional properties. The map keys are category names and the
                  values are maps of property name to property value. Every property is contained inside of a category. Most
                  entities have required properties within the "default" category. To determine the set of required and
                  optional properties for an entity type, a query can be done on '/types?type=dataEntity' that returns a
                  collection of all entity types. The appropriate entity type, which includes definitions of all of
                  it's properties, can be identified from this collection.
                  Example: `{"properties": { "default": { "key1": "value1"}}}`
                type: object
              realizedExpression:
                description: The expression realized after resolving qualifiers .
                  Used in deriving this logical entity
                type: string
              timeExternal:
                description: Last modified timestamp of the object in the external
                  system.
                type: string
              typeKey:
                description: The type of data entity object. Type key's can be found
                  via the '/types' endpoint.
                type: string
            required:
            - displayName
            - timeExternal
            type: object
          status:
            description: EntityStatus defines the observed state of Entity.
            properties:
              businessName:
                description: Optional user friendly business name of the data entity.
                  If set, this supplements the harvested display name of the object.
                type: string
              createdById:
                description: OCID of the user who created this object in the data
                  catalog.
                type: string
              customPropertyMembers:
                description: The list of customized properties along with the values
                  for this object
                items:
                  description: EntityCustomPropertyMember defines nested fields for
                    Entity.CustomPropertyMember.
                  properties:
                    displayName:
                      description: Name of the custom property
                      type: string
                    key:
                      description: Unique Identifier of the attribute which is ID
                      type: string
                    namespaceName:
                      description: Namespace name of the custom property
                      type: string
                    value:
                      description: The custom property value
                      type: string
                  type: object
                type: array
              dataAssetKey:
                description: Unique key of the parent data asset.
                type: string
              description:
                description: Detailed description of a data entity.
                type: string
              displayName:
                description: |-
                  A user-friendly display name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              externalKey:
                description: Unique external key of this object in the source system.
                type: string
              folderKey:
                description: Key of the associated folder.
                type: string
              folderName:
                description: Name of the associated folder. This name is harvested
                  from the source data asset when the parent folder for the entiy
                  is harvested.
                type: string
              harvestStatus:
                description: Status of the object as updated by the harvest process.
                type: string
              isLogical:
                description: |-
                  Property that identifies if the object is a physical object (materialized) or virtual/logical object
                  defined on other objects.
                type: boolean
              isPartition:
                description: Property that identifies if an object is a sub object
                  of a physical or materialized parent object.
                type: boolean
              key:
                description: Unique data entity key that is immutable.
                type: string
              lastJobKey:
                description: Key of the last harvest process to update this object.
                type: string
              lifecycleDetails:
                description: |-
                  A message describing the current state in more detail. An object not in ACTIVE state may have functional limitations,
                  see service documentation for details.
                type: string
              lifecycleState:
                description: The current state of the data entity.
                type: string
              objectRelationships:
                description: List of objects and their relationships to this entity.
                items:
                  description: EntityObjectRelationship defines nested fields for
                    Entity.ObjectRelationship.
                  properties:
                    key:
                      description: Unique id of the object.
                      type: string
                    name:
                      description: Name of the object.
                      type: string
                    parentKey:
                      description: Key of the parent object for the resource.
                      type: string
                    parentPath:
                      description: Full path of the parent object.
                      type: string
                    path:
                      description: Full path of the object.
                      type: string
                    relationshipType:
                      description: Type of relationship with the referencing object.
                      type: string
                    timeCreated:
                      description: |-
                        The date and time the relationship was created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                        Example: `2019-03-25T21:10:29.600Z`
                      type: string
                    timeUpdated:
                      description: The last time a change was made to this reference.
                        An RFC3339 (https://tools.ietf.org/html/rfc3339) formatted
                        datetime string.
                      type: string
                    typeKey:
                      description: Type key of the object. Type keys can be found
                        via the '/types' endpoint.
                      type: string
                    typeName:
                      description: Type name of the object. Type names can be found
                        via the '/types' endpoint.
                      type: string
                  type: object
                type: array
              objectStorageUrl:
                description: URL of the data entity in the object store.
                type: string
              path:
                description: Full path of the data entity.
                type: string
              patternKey:
                description: Key of the associated pattern if this is a logical entity.
                type: string
              properties:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  A map of maps that contains the properties which are specific to the entity type. Each entity type
                  definition defines it's set of required and optional properties. The map keys are category names and the
                  values are maps of property name to property value. Every property is contained inside of a category. Most
                  data entities have required properties within the "default" category.
                  Example: `{"properties": { "default": { "key1": "value1"}}}`
                type: object
              realizedExpression:
                description: The expression realized after resolving qualifiers .
                  Used in deriving this logical entity
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time the data entity was created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2019-03-25T21:10:29.600Z`
                type: string
              timeExternal:
                description: Last modified timestamp of this object in the external
                  system.
                type: string
              timeHarvested:
                description: The date and time the entity was harvested, in the format
                  defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                type: string
              timeStatusUpdated:
                description: Time that the data entities status was last updated.
                  An RFC3339 (https://tools.ietf.org/html/rfc3339) formatted datetime
                  string.
                type: string
              timeUpdated:
                description: The last time that any change was made to the data entity.
                  An RFC3339 (https://tools.ietf.org/html/rfc3339) formatted datetime
                  string.
                type: string
              typeKey:
                description: The type of data entity object. Type key's can be found
                  via the '/types' endpoint.
                type: string
              updatedById:
                description: OCID of the user who updated this object in the data
                  catalog.
                type: string
              uri:
                description: URI to the data entity instance in the API.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
