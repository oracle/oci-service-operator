---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: capturefilters.core.oracle.com
spec:
  group: core.oracle.com
  names:
    kind: CaptureFilter
    listKind: CaptureFilterList
    plural: capturefilters
    singular: capturefilter
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the CaptureFilter
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the CaptureFilter
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: CaptureFilter is the Schema for the capturefilters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CaptureFilterSpec defines the desired state of CaptureFilter.
            properties:
              compartmentId:
                description: The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment containing the capture filter.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              filterType:
                description: Indicates which service will use this capture filter
                type: string
              flowLogCaptureFilterRules:
                description: The set of rules governing what traffic the VCN flow
                  log collects.
                items:
                  description: CaptureFilterFlowLogCaptureFilterRule defines nested
                    fields for CaptureFilter.FlowLogCaptureFilterRule.
                  properties:
                    destinationCidr:
                      description: Traffic to this CIDR will be captured in the VCN
                        flow log.
                      type: string
                    flowLogType:
                      description: |-
                        Type or types of VCN flow logs to store. `ALL` includes records for both accepted traffic and
                        rejected traffic.
                      type: string
                    icmpOptions:
                      description: CaptureFilterFlowLogCaptureFilterRuleIcmpOptions
                        defines nested fields for CaptureFilter.FlowLogCaptureFilterRule.IcmpOptions.
                      properties:
                        code:
                          description: The ICMP code (optional).
                          type: integer
                        type:
                          description: The ICMP type.
                          type: integer
                      required:
                      - type
                      type: object
                    isEnabled:
                      description: Indicates whether a VCN flow log capture filter
                        rule is enabled.
                      type: boolean
                    priority:
                      description: A lower number indicates a higher priority, range
                        0-9. Each rule must have a distinct priority.
                      type: integer
                    protocol:
                      description: The transport protocol the filter uses.
                      type: string
                    ruleAction:
                      description: Include or exclude a `ruleAction` object.
                      type: string
                    samplingRate:
                      description: Sampling interval as `1` of `X`, where `X` is an
                        integer not greater than `100000`.
                      type: integer
                    sourceCidr:
                      description: Traffic from this CIDR will be captured in the
                        VCN flow log.
                      type: string
                    tcpOptions:
                      description: CaptureFilterFlowLogCaptureFilterRuleTcpOptions
                        defines nested fields for CaptureFilter.FlowLogCaptureFilterRule.TcpOptions.
                      properties:
                        destinationPortRange:
                          description: CaptureFilterFlowLogCaptureFilterRuleTcpOptionsDestinationPortRange
                            defines nested fields for CaptureFilter.FlowLogCaptureFilterRule.TcpOptions.DestinationPortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                        sourcePortRange:
                          description: CaptureFilterFlowLogCaptureFilterRuleTcpOptionsSourcePortRange
                            defines nested fields for CaptureFilter.FlowLogCaptureFilterRule.TcpOptions.SourcePortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                      type: object
                    udpOptions:
                      description: CaptureFilterFlowLogCaptureFilterRuleUdpOptions
                        defines nested fields for CaptureFilter.FlowLogCaptureFilterRule.UdpOptions.
                      properties:
                        destinationPortRange:
                          description: CaptureFilterFlowLogCaptureFilterRuleUdpOptionsDestinationPortRange
                            defines nested fields for CaptureFilter.FlowLogCaptureFilterRule.UdpOptions.DestinationPortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                        sourcePortRange:
                          description: CaptureFilterFlowLogCaptureFilterRuleUdpOptionsSourcePortRange
                            defines nested fields for CaptureFilter.FlowLogCaptureFilterRule.UdpOptions.SourcePortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                      type: object
                  type: object
                type: array
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              vtapCaptureFilterRules:
                description: The set of rules governing what traffic a VTAP mirrors.
                items:
                  description: CaptureFilterVtapCaptureFilterRule defines nested fields
                    for CaptureFilter.VtapCaptureFilterRule.
                  properties:
                    destinationCidr:
                      description: Traffic sent to this CIDR block through the VTAP
                        source will be mirrored to the VTAP target.
                      type: string
                    icmpOptions:
                      description: CaptureFilterVtapCaptureFilterRuleIcmpOptions defines
                        nested fields for CaptureFilter.VtapCaptureFilterRule.IcmpOptions.
                      properties:
                        code:
                          description: The ICMP code (optional).
                          type: integer
                        type:
                          description: The ICMP type.
                          type: integer
                      required:
                      - type
                      type: object
                    protocol:
                      description: |-
                        The transport protocol used in the filter. If do not choose a protocol, all protocols will be used in the filter.
                        Supported options are:
                          * 1 = ICMP
                          * 6 = TCP
                          * 17 = UDP
                      type: string
                    ruleAction:
                      description: Include or exclude packets meeting this definition
                        from mirrored traffic.
                      type: string
                    sourceCidr:
                      description: Traffic from this CIDR block to the VTAP source
                        will be mirrored to the VTAP target.
                      type: string
                    tcpOptions:
                      description: CaptureFilterVtapCaptureFilterRuleTcpOptions defines
                        nested fields for CaptureFilter.VtapCaptureFilterRule.TcpOptions.
                      properties:
                        destinationPortRange:
                          description: CaptureFilterVtapCaptureFilterRuleTcpOptionsDestinationPortRange
                            defines nested fields for CaptureFilter.VtapCaptureFilterRule.TcpOptions.DestinationPortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                        sourcePortRange:
                          description: CaptureFilterVtapCaptureFilterRuleTcpOptionsSourcePortRange
                            defines nested fields for CaptureFilter.VtapCaptureFilterRule.TcpOptions.SourcePortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                      type: object
                    trafficDirection:
                      description: The traffic direction the VTAP is configured to
                        mirror.
                      type: string
                    udpOptions:
                      description: CaptureFilterVtapCaptureFilterRuleUdpOptions defines
                        nested fields for CaptureFilter.VtapCaptureFilterRule.UdpOptions.
                      properties:
                        destinationPortRange:
                          description: CaptureFilterVtapCaptureFilterRuleUdpOptionsDestinationPortRange
                            defines nested fields for CaptureFilter.VtapCaptureFilterRule.UdpOptions.DestinationPortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                        sourcePortRange:
                          description: CaptureFilterVtapCaptureFilterRuleUdpOptionsSourcePortRange
                            defines nested fields for CaptureFilter.VtapCaptureFilterRule.UdpOptions.SourcePortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                      type: object
                  required:
                  - trafficDirection
                  type: object
                type: array
            required:
            - compartmentId
            - filterType
            type: object
          status:
            description: CaptureFilterStatus defines the observed state of CaptureFilter.
            properties:
              id:
                description: The capture filter's Oracle ID (OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm)).
                type: string
              lifecycleState:
                description: The capture filter's current administrative state.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time the capture filter was created, in the format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2021-08-25T21:10:29.600Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
