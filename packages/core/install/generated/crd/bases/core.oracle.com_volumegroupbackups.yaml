---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: volumegroupbackups.core.oracle.com
spec:
  group: core.oracle.com
  names:
    kind: VolumeGroupBackup
    listKind: VolumeGroupBackupList
    plural: volumegroupbackups
    singular: volumegroupbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the VolumeGroupBackup
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the VolumeGroupBackup
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: VolumeGroupBackup is the Schema for the volumegroupbackups API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VolumeGroupBackupSpec defines the desired state of VolumeGroupBackup.
            properties:
              compartmentId:
                description: |-
                  The OCID of the compartment that will contain the volume group
                  backup. This parameter is optional, by default backup will be created in
                  the same compartment and source volume group.
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a
                  namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  A user-friendly name. Does not have to be unique, and it's changeable.
                  Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no
                  predefined name, type, or namespace. For more information, see Resource Tags (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              type:
                description: The type of backup to create. If omitted, defaults to
                  incremental.
                type: string
              volumeGroupId:
                description: The OCID of the volume group that needs to be backed
                  up.
                type: string
            required:
            - volumeGroupId
            type: object
          status:
            description: VolumeGroupBackupStatus defines the observed state of VolumeGroupBackup.
            properties:
              expirationTime:
                description: |-
                  The date and time the volume group backup will expire and be automatically deleted.
                  Format defined by RFC3339 (https://tools.ietf.org/html/rfc3339). This parameter will always be present for volume group
                  backups that were created automatically by a scheduled-backup policy. For manually
                  created volume group backups, it will be absent, signifying that there is no expiration
                  time and the backup will last forever until manually deleted.
                type: string
              id:
                description: The OCID of the volume group backup.
                type: string
              lifecycleState:
                description: The current state of a volume group backup.
                type: string
              sizeInGBs:
                description: The aggregate size of the volume group backup, in GBs.
                format: int64
                type: integer
              sizeInMBs:
                description: The aggregate size of the volume group backup, in MBs.
                format: int64
                type: integer
              sourceType:
                description: |-
                  Specifies whether the volume group backup was created manually, or via scheduled
                  backup policy.
                type: string
              sourceVolumeGroupBackupId:
                description: The OCID of the source volume group backup.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeCreated:
                description: |-
                  The date and time the volume group backup was created. This is the time the actual point-in-time image
                  of the volume group data was taken. Format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                type: string
              timeRequestReceived:
                description: The date and time the request to create the volume group
                  backup was received. Format defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                type: string
              uniqueSizeInGbs:
                description: |-
                  The aggregate size used by the volume group backup, in GBs.
                  It is typically smaller than sizeInGBs, depending on the spaceconsumed
                  on the volume group and whether the volume backup is full or incremental.
                format: int64
                type: integer
              uniqueSizeInMbs:
                description: |-
                  The aggregate size used by the volume group backup, in MBs.
                  It is typically smaller than sizeInMBs, depending on the spaceconsumed
                  on the volume group and whether the volume backup is full or incremental.
                format: int64
                type: integer
              volumeBackupIds:
                description: OCIDs for the volume backups in this volume group backup.
                items:
                  type: string
                type: array
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
