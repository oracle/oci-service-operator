---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: networksecuritygroupsecurityrules.core.oracle.com
spec:
  group: core.oracle.com
  names:
    kind: NetworkSecurityGroupSecurityRule
    listKind: NetworkSecurityGroupSecurityRuleList
    plural: networksecuritygroupsecurityrules
    singular: networksecuritygroupsecurityrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the NetworkSecurityGroupSecurityRule
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the NetworkSecurityGroupSecurityRule
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: NetworkSecurityGroupSecurityRule is the Schema for the networksecuritygroupsecurityrules
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NetworkSecurityGroupSecurityRuleSpec defines the desired
              state of NetworkSecurityGroupSecurityRule.
            properties:
              securityRules:
                description: The NSG security rules to update.
                items:
                  description: NetworkSecurityGroupSecurityRuleSecurityRule defines
                    nested fields for NetworkSecurityGroupSecurityRule.SecurityRule.
                  properties:
                    description:
                      description: An optional description of your choice for the
                        rule. Avoid entering confidential information.
                      type: string
                    destination:
                      description: |-
                        Conceptually, this is the range of IP addresses that a packet originating from the instance
                        can go to.
                        Allowed values:
                          * An IP address range in CIDR notation. For example: `192.168.1.0/24` or `2001:0db8:0123:45::/56`
                            IPv6 addressing is supported for all commercial and government regions. See
                            IPv6 Addresses (https://docs.cloud.oracle.com/iaas/Content/Network/Concepts/ipv6.htm).
                          * The `cidrBlock` value for a Service, if you're
                            setting up a security rule for traffic destined for a particular `Service` through
                            a service gateway. For example: `oci-phx-objectstorage`.
                          * The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of a NetworkSecurityGroup in the same
                            VCN. The value can be the NSG that the rule belongs to if the rule's intent is to control
                            traffic between VNICs in the same NSG.
                      type: string
                    destinationType:
                      description: |-
                        Type of destination for the rule. Required if `direction` = `EGRESS`.
                        Allowed values:
                          * `CIDR_BLOCK`: If the rule's `destination` is an IP address range in CIDR notation.
                          * `SERVICE_CIDR_BLOCK`: If the rule's `destination` is the `cidrBlock` value for a
                            Service (the rule is for traffic destined for a
                            particular `Service` through a service gateway).
                          * `NETWORK_SECURITY_GROUP`: If the rule's `destination` is the OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of a
                            NetworkSecurityGroup.
                      type: string
                    direction:
                      description: |-
                        Direction of the security rule. Set to `EGRESS` for rules to allow outbound IP packets,
                        or `INGRESS` for rules to allow inbound IP packets.
                      type: string
                    icmpOptions:
                      description: NetworkSecurityGroupSecurityRuleSecurityRuleIcmpOptions
                        defines nested fields for NetworkSecurityGroupSecurityRule.SecurityRule.IcmpOptions.
                      properties:
                        code:
                          description: The ICMP code (optional).
                          type: integer
                        type:
                          description: The ICMP type.
                          type: integer
                      required:
                      - type
                      type: object
                    id:
                      description: |-
                        The Oracle-assigned ID of the security rule that you want to update. You can't change this value.
                        Example: `04ABEC`
                      type: string
                    isStateless:
                      description: |-
                        A stateless rule allows traffic in one direction. Remember to add a corresponding
                        stateless rule in the other direction if you need to support bidirectional traffic. For
                        example, if egress traffic allows TCP destination port 80, there should be an ingress
                        rule to allow TCP source port 80. Defaults to false, which means the rule is stateful
                        and a corresponding rule is not necessary for bidirectional traffic.
                      type: boolean
                    protocol:
                      description: |-
                        The transport protocol. Specify either `all` or an IPv4 protocol number as
                        defined in
                        Protocol Numbers (http://www.iana.org/assignments/protocol-numbers/protocol-numbers.xhtml).
                        Options are supported only for ICMP ("1"), TCP ("6"), UDP ("17"), and ICMPv6 ("58").
                      type: string
                    source:
                      description: |-
                        Conceptually, this is the range of IP addresses that a packet coming into the instance
                        can come from.
                        Allowed values:
                          * An IP address range in CIDR notation. For example: `192.168.1.0/24` or `2001:0db8:0123:45::/56`
                            IPv6 addressing is supported for all commercial and government regions. See
                            IPv6 Addresses (https://docs.cloud.oracle.com/iaas/Content/Network/Concepts/ipv6.htm).
                          * The `cidrBlock` value for a Service, if you're
                            setting up a security rule for traffic coming from a particular `Service` through
                            a service gateway. For example: `oci-phx-objectstorage`.
                          * The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of a NetworkSecurityGroup in the same
                            VCN. The value can be the NSG that the rule belongs to if the rule's intent is to control
                            traffic between VNICs in the same NSG.
                      type: string
                    sourceType:
                      description: |-
                        Type of source for the rule. Required if `direction` = `INGRESS`.
                          * `CIDR_BLOCK`: If the rule's `source` is an IP address range in CIDR notation.
                          * `SERVICE_CIDR_BLOCK`: If the rule's `source` is the `cidrBlock` value for a
                            Service (the rule is for traffic coming from a
                            particular `Service` through a service gateway).
                          * `NETWORK_SECURITY_GROUP`: If the rule's `source` is the OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of a
                            NetworkSecurityGroup.
                      type: string
                    tcpOptions:
                      description: NetworkSecurityGroupSecurityRuleSecurityRuleTcpOptions
                        defines nested fields for NetworkSecurityGroupSecurityRule.SecurityRule.TcpOptions.
                      properties:
                        destinationPortRange:
                          description: NetworkSecurityGroupSecurityRuleSecurityRuleTcpOptionsDestinationPortRange
                            defines nested fields for NetworkSecurityGroupSecurityRule.SecurityRule.TcpOptions.DestinationPortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                        sourcePortRange:
                          description: NetworkSecurityGroupSecurityRuleSecurityRuleTcpOptionsSourcePortRange
                            defines nested fields for NetworkSecurityGroupSecurityRule.SecurityRule.TcpOptions.SourcePortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                      type: object
                    udpOptions:
                      description: NetworkSecurityGroupSecurityRuleSecurityRuleUdpOptions
                        defines nested fields for NetworkSecurityGroupSecurityRule.SecurityRule.UdpOptions.
                      properties:
                        destinationPortRange:
                          description: NetworkSecurityGroupSecurityRuleSecurityRuleUdpOptionsDestinationPortRange
                            defines nested fields for NetworkSecurityGroupSecurityRule.SecurityRule.UdpOptions.DestinationPortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                        sourcePortRange:
                          description: NetworkSecurityGroupSecurityRuleSecurityRuleUdpOptionsSourcePortRange
                            defines nested fields for NetworkSecurityGroupSecurityRule.SecurityRule.UdpOptions.SourcePortRange.
                          properties:
                            max:
                              description: |-
                                The maximum port number, which must not be less than the minimum port number. To specify
                                a single port number, set both the min and max to the same value.
                              type: integer
                            min:
                              description: The minimum port number, which must not
                                be greater than the maximum port number.
                              type: integer
                          required:
                          - max
                          - min
                          type: object
                      type: object
                  required:
                  - direction
                  - id
                  - protocol
                  type: object
                type: array
            type: object
          status:
            description: NetworkSecurityGroupSecurityRuleStatus defines the observed
              state of NetworkSecurityGroupSecurityRule.
            properties:
              description:
                description: An optional description of your choice for the rule.
                type: string
              destination:
                description: |-
                  Conceptually, this is the range of IP addresses that a packet originating from the instance
                  can go to.
                  Allowed values:
                    * An IP address range in CIDR notation. For example: `192.168.1.0/24` or `2001:0db8:0123:45::/56`
                      IPv6 addressing is supported for all commercial and government regions.
                      See IPv6 Addresses (https://docs.cloud.oracle.com/iaas/Content/Network/Concepts/ipv6.htm).
                    * The `cidrBlock` value for a Service, if you're
                      setting up a security rule for traffic destined for a particular `Service` through
                      a service gateway. For example: `oci-phx-objectstorage`.
                    * The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of a NetworkSecurityGroup in the same
                      VCN. The value can be the NSG that the rule belongs to if the rule's intent is to control
                      traffic between VNICs in the same NSG.
                type: string
              destinationType:
                description: |-
                  Type of destination for the rule. Required if `direction` = `EGRESS`.
                  Allowed values:
                    * `CIDR_BLOCK`: If the rule's `destination` is an IP address range in CIDR notation.
                    * `SERVICE_CIDR_BLOCK`: If the rule's `destination` is the `cidrBlock` value for a
                      Service (the rule is for traffic destined for a
                      particular `Service` through a service gateway).
                    * `NETWORK_SECURITY_GROUP`: If the rule's `destination` is the OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of a
                      NetworkSecurityGroup.
                type: string
              direction:
                description: |-
                  Direction of the security rule. Set to `EGRESS` for rules to allow outbound IP packets,
                  or `INGRESS` for rules to allow inbound IP packets.
                type: string
              icmpOptions:
                description: NetworkSecurityGroupSecurityRuleIcmpOptions defines nested
                  fields for NetworkSecurityGroupSecurityRule.IcmpOptions.
                properties:
                  code:
                    description: The ICMP code (optional).
                    type: integer
                  type:
                    description: The ICMP type.
                    type: integer
                type: object
              id:
                description: |-
                  An Oracle-assigned identifier for the security rule. You specify this ID when you want to
                  update or delete the rule.
                  Example: `04ABEC`
                type: string
              isStateless:
                description: |-
                  A stateless rule allows traffic in one direction. Remember to add a corresponding
                  stateless rule in the other direction if you need to support bidirectional traffic. For
                  example, if egress traffic allows TCP destination port 80, there should be an ingress
                  rule to allow TCP source port 80. Defaults to false, which means the rule is stateful
                  and a corresponding rule is not necessary for bidirectional traffic.
                type: boolean
              isValid:
                description: |-
                  Whether the rule is valid. The value is `True` when the rule is first created. If
                  the rule's `source` or `destination` is a network security group, the value changes to
                  `False` if that network security group is deleted.
                type: boolean
              protocol:
                description: |-
                  The transport protocol. Specify either `all` or an IPv4 protocol number as
                  defined in
                  Protocol Numbers (http://www.iana.org/assignments/protocol-numbers/protocol-numbers.xhtml).
                  Options are supported only for ICMP ("1"), TCP ("6"), UDP ("17"), and ICMPv6 ("58").
                type: string
              source:
                description: |-
                  Conceptually, this is the range of IP addresses that a packet coming into the instance
                  can come from.
                  Allowed values:
                    * An IP address range in CIDR notation. For example: `192.168.1.0/24` or `2001:0db8:0123:45::/56`
                      IPv6 addressing is supported for all commercial and government regions.
                      See IPv6 Addresses (https://docs.cloud.oracle.com/iaas/Content/Network/Concepts/ipv6.htm).
                    * The `cidrBlock` value for a Service, if you're
                      setting up a security rule for traffic coming from a particular `Service` through
                      a service gateway. For example: `oci-phx-objectstorage`.
                    * The OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of a NetworkSecurityGroup in the same
                      VCN. The value can be the NSG that the rule belongs to if the rule's intent is to control
                      traffic between VNICs in the same NSG.
                type: string
              sourceType:
                description: |-
                  Type of source for the rule. Required if `direction` = `INGRESS`.
                    * `CIDR_BLOCK`: If the rule's `source` is an IP address range in CIDR notation.
                    * `SERVICE_CIDR_BLOCK`: If the rule's `source` is the `cidrBlock` value for a
                      Service (the rule is for traffic coming from a
                      particular `Service` through a service gateway).
                    * `NETWORK_SECURITY_GROUP`: If the rule's `source` is the OCID (https://docs.cloud.oracle.com/iaas/Content/General/Concepts/identifiers.htm) of a
                      NetworkSecurityGroup.
                type: string
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              tcpOptions:
                description: NetworkSecurityGroupSecurityRuleTcpOptions defines nested
                  fields for NetworkSecurityGroupSecurityRule.TcpOptions.
                properties:
                  destinationPortRange:
                    description: NetworkSecurityGroupSecurityRuleTcpOptionsDestinationPortRange
                      defines nested fields for NetworkSecurityGroupSecurityRule.TcpOptions.DestinationPortRange.
                    properties:
                      max:
                        description: |-
                          The maximum port number, which must not be less than the minimum port number. To specify
                          a single port number, set both the min and max to the same value.
                        type: integer
                      min:
                        description: The minimum port number, which must not be greater
                          than the maximum port number.
                        type: integer
                    type: object
                  sourcePortRange:
                    description: NetworkSecurityGroupSecurityRuleTcpOptionsSourcePortRange
                      defines nested fields for NetworkSecurityGroupSecurityRule.TcpOptions.SourcePortRange.
                    properties:
                      max:
                        description: |-
                          The maximum port number, which must not be less than the minimum port number. To specify
                          a single port number, set both the min and max to the same value.
                        type: integer
                      min:
                        description: The minimum port number, which must not be greater
                          than the maximum port number.
                        type: integer
                    type: object
                type: object
              timeCreated:
                description: The date and time the security rule was created. Format
                  defined by RFC3339 (https://tools.ietf.org/html/rfc3339).
                type: string
              udpOptions:
                description: NetworkSecurityGroupSecurityRuleUdpOptions defines nested
                  fields for NetworkSecurityGroupSecurityRule.UdpOptions.
                properties:
                  destinationPortRange:
                    description: NetworkSecurityGroupSecurityRuleUdpOptionsDestinationPortRange
                      defines nested fields for NetworkSecurityGroupSecurityRule.UdpOptions.DestinationPortRange.
                    properties:
                      max:
                        description: |-
                          The maximum port number, which must not be less than the minimum port number. To specify
                          a single port number, set both the min and max to the same value.
                        type: integer
                      min:
                        description: The minimum port number, which must not be greater
                          than the maximum port number.
                        type: integer
                    type: object
                  sourcePortRange:
                    description: NetworkSecurityGroupSecurityRuleUdpOptionsSourcePortRange
                      defines nested fields for NetworkSecurityGroupSecurityRule.UdpOptions.SourcePortRange.
                    properties:
                      max:
                        description: |-
                          The maximum port number, which must not be less than the minimum port number. To specify
                          a single port number, set both the min and max to the same value.
                        type: integer
                      min:
                        description: The minimum port number, which must not be greater
                          than the maximum port number.
                        type: integer
                    type: object
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
