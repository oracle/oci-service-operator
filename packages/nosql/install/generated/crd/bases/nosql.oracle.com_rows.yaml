---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: rows.nosql.oracle.com
spec:
  group: nosql.oracle.com
  names:
    kind: Row
    listKind: RowList
    plural: rows
    singular: row
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the Row
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the Row
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Row is the Schema for the rows API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RowSpec defines the desired state of Row.
            properties:
              compartmentId:
                description: |-
                  The OCID of the table's compartment.  Required
                  if the tableNameOrId path parameter is a table name.
                  Optional if tableNameOrId is an OCID.  If tableNameOrId
                  is an OCID, and compartmentId is supplied, the latter
                  must match the identified table's compartmentId.
                type: string
              identityCacheSize:
                description: |-
                  Sets the number of generated identity values that are
                  requested from the server during a put. If present and greater than 0,
                  this value takes precedence over a default value for the table.
                type: integer
              isExactMatch:
                description: |-
                  If present and true, the presented row value must exactly
                  match the table's schema.  Otherwise, rows with missing
                  non-key fields or extra fields can be written successfully.
                type: boolean
              isGetReturnRow:
                description: |-
                  If true, and the put fails due to an option setting, then
                  the existing row will be returned.
                type: boolean
              isTtlUseTableDefault:
                description: If true, set time-to-live for this row to the table's
                  default.
                type: boolean
              option:
                description: Specifies a condition for the put operation.
                type: string
              timeoutInMs:
                description: Timeout setting for the put.
                type: integer
              ttl:
                description: Time-to-live for the row, in days.
                type: integer
            type: object
          status:
            description: RowStatus defines the observed state of Row.
            properties:
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              timeOfExpiration:
                description: |-
                  The expiration time of the row. A zero value indicates that
                  the row does not expire. An RFC3339 formatted datetime
                  string.
                type: string
              usage:
                description: RowUsage defines nested fields for Row.Usage.
                properties:
                  readUnitsConsumed:
                    description: Read Units consumed by this operation.
                    type: integer
                  writeUnitsConsumed:
                    description: Write Units consumed by this operation.
                    type: integer
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
